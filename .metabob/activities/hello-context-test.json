{
  "activity_id": "hello-context-test",
  "name": "Hello Context Test",
  "description": "Test activity with contextRequirements after fix",
  "category": "infrastructure",
  "task_steps": [
    {
      "task_id": "write-hello",
      "subagent": "general",
      "description": "Write hello world message with context to file",
      "dependencies": [],
      "prompt": {
        "template": "Write a hello world message to /tmp/hello-context-{{testId}}.txt\n\nThe message should include:\n1. The greeting from context: {{greeting}}\n2. A message from: {{name}}\n\nUse the write tool to create the file.",
        "variables": [
          {
            "name": "testId",
            "type": "string",
            "required": true,
            "description": "Unique test identifier"
          },
          {
            "name": "name",
            "type": "string",
            "required": true,
            "description": "Name to include in message"
          }
        ]
      },
      "context_rules": {
        "max_tokens": 2000,
        "compression_strategy": "filter"
      },
      "validation": {
        "required_files": [
          "/tmp/hello-context-{{testId}}.txt"
        ],
        "required_patterns": [],
        "forbidden_patterns": [],
        "commands": []
      },
      "retry": {
        "max_attempts": 2,
        "strategy": "simple"
      }
    }
  ],
  "tasks": [
    {
      "task_id": "write-hello",
      "subagent": "general",
      "description": "Write hello world message with context to file",
      "dependencies": [],
      "prompt": {
        "template": "Write a hello world message to /tmp/hello-context-{{testId}}.txt\n\nThe message should include:\n1. The greeting from context: {{greeting}}\n2. A message from: {{name}}\n\nUse the write tool to create the file.",
        "variables": [
          {
            "name": "testId",
            "type": "string",
            "required": true,
            "description": "Unique test identifier"
          },
          {
            "name": "name",
            "type": "string",
            "required": true,
            "description": "Name to include in message"
          }
        ]
      },
      "context_rules": {
        "max_tokens": 2000,
        "compression_strategy": "filter"
      },
      "validation": {
        "required_files": [
          "/tmp/hello-context-{{testId}}.txt"
        ],
        "required_patterns": [],
        "forbidden_patterns": [],
        "commands": []
      },
      "retry": {
        "max_attempts": 2,
        "strategy": "simple"
      }
    }
  ],
  "estimated_metrics": {
    "execution_count": 0,
    "success_rate": 0,
    "avg_duration_ms": 0,
    "avg_cost": 0
  },
  "author": null,
  "created_at": "2026-02-19T23:47:54.629Z",
  "metadata": {}
}