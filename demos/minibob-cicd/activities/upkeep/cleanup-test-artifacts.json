{
  "id": "cleanup-test-artifacts",
  "name": "Cleanup Test Artifacts",
  "description": "Safely identifies and removes temporary test artifacts while preventing accidental deletion of important files, with full transparency through logging",
  "category": "upkeep",
  "variables": {
    "dry_run": {
      "type": "boolean",
      "description": "Run in dry-run mode without actually deleting files",
      "default": true
    },
    "max_age_days": {
      "type": "number",
      "description": "Only delete files older than this many days",
      "default": 1
    },
    "extra_patterns": {
      "type": "array",
      "description": "Additional file patterns to include in cleanup",
      "default": []
    }
  },
  "steps": [
    {
      "tool": "bash",
      "params": {
        "command": "mkdir -p logs"
      },
      "description": "Ensure logs directory exists"
    },
    {
      "tool": "bash",
      "params": {
        "command": "chmod +x scripts/cleanup.ts"
      },
      "description": "Make cleanup script executable"
    },
    {
      "tool": "bash",
      "params": {
        "command": "cd {{working_dir}} && bun run scripts/cleanup.ts {{#if dry_run}}--dry-run{{/if}}"
      },
      "description": "Execute the cleanup script"
    },
    {
      "tool": "bash",
      "params": {
        "command": "ls -la logs/cleanup-report-*.json | tail -1"
      },
      "description": "Show the latest cleanup report"
    }
  ],
  "expected_outputs": [
    {
      "name": "cleanup_log",
      "path": "logs/cleanup.log",
      "description": "Detailed log of cleanup operations"
    },
    {
      "name": "cleanup_report",
      "path": "logs/cleanup-report-*.json",
      "description": "JSON report with cleanup summary and details"
    }
  ],
  "success_criteria": [
    "Cleanup script executes without errors",
    "Log file is created with detailed operations",
    "Report file is generated with summary statistics",
    "No protected files are accidentally deleted"
  ],
  "safety_checks": [
    "Protected paths are respected",
    "File age checks prevent deletion of recent files",
    "Dry-run mode available for safe testing",
    "Comprehensive logging for audit trail"
  ]
}