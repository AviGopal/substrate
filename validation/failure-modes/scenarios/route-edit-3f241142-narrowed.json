{
  "id": "route-edit-3f241142-narrowed",
  "routing_class": "recombination",
  "target_template_id": "development-vessel:draft-gap-closing-activity",
  "mode_class": "edit_intent_route",
  "stage": "detection",
  "outcome_class": "gap",
  "title": "[narrowed from route-edit-3f241142] In repos/development-vessel/src/resolvers/feature-compose.ts, report each drafting m",
  "description": "[narrowed from route-edit-3f241142] In repos/development-vessel/src/resolvers/feature-compose.ts, report each drafting model's outcome to the grading endpoint. THREE edits in this one file; all are required and together they are complete. A previous attempt was correctly refused for using globalThis to carry state, so this version uses two ordinary module-level variables instead.\n\nEdit 1 declares them. Replace this line, which occurs exactly once:\nconst PER_CALL_TIMEOUT_MS = 600_000;\n\nwith exactly:\nlet lastDraftModel = \"\";\nlet lastDraftEndpoint = \"\";\nconst PER_CALL_TIMEOUT_MS = 600000;\n\nEdit 2 records the model. Replace this line, which occurs exactly once:\n  console.log(\"[fc-draft-model] selected=\" + String((j as { model_selection?: { selected?: unknown } })?.model_selection?.selected ?? \"unknown\") + \" task=feature_compose\");\n\nwith exactly:\n  lastDraftModel = String((j as { model_selection?: { selected?: unknown } })?.model_selection?.selected ?? \"\");\n  lastDraftEndpoint = endpoint;\n  console.log(\"[fc-draft-model] pick=\" + lastDraftModel + \" task=feature_compose\");\n\nEdit 3 reports the outcome. Replace these two consecutive lines, which occur exactly once:\n    writeFileSync(\n      reportPath,\n\nwith exactly:\n    if (lastDraftModel && lastDraftEndpoint) {\n      void fetch(lastDraftEndpoint, {\n        method: \"POST\",\n        headers: { \"Content-Type\": \"application/json\", Authorization: \"ApiKey \" + METABOB_API_KEY },\n        body: JSON.stringify({\n          type: \"llmArmOutcome_write\",\n          model: lastDraftModel,\n          reached: verdict === \"FAVORABLE\",\n          task_type: \"feature_compose\",\n        }),\n      }).catch(() => {});\n    }\n    writeFileSync(\n      (reportPath),\n\nIMPORTANT, because a previous attempt failed typecheck with 'TS2300: Duplicate identifier endpoint': the ONLY new declarations in this whole change are lastDraftModel and lastDraftEndpoint, both in edit 1. Do NOT redeclare endpoint, verdict, METABOB_API_KEY or PER_CALL_TIMEOUT_MS anywhere; endpoint is already a parameter of llmCall and the others already exist in scope. Edit 1 also changes the numeric literal 600_000 to 600000, which is the same value.\n\nThe receiving endpoint already exists and is verified live: llm-resolver-vessel serves llmArmOutcome_write, landed in commit fec22cbc, and an operator probe returned resolved true. Its handler forwards to recordArmOutcome, which already maintains the per-task task_alpha and task_beta posteriors. Drafting is the only work the model selector collects no evidence about: feature_compose and patch_with_tools carry zero learned posterior while seven other task types are graded. The fetch is fire-and-forget with a catch and cannot affect any verdict.\n\nWHY PREVIOUS ATTEMPTS ON THIS GAP FAILED (most recent last):\n- semantic_reject: adversarial refuters agreed 2/2 (conf 0.90, 1.00): The patch does not genuinely close the gap because the fetch operation in Edit 3 is entirely dependent on the execution of `lastDraftModel` and `last\n- semantic_reject: The patch fails to guarantee reporting each drafting model's outcome as module-level variables get overwritten on subsequent calls, potentially losing intermediate outcomes\n- anchor_not_found: {\"error\":\"old_string not found in file. The closest real text is lines 217-222 of 6555, VERBATIM:\\n217:   lastDraftModel = (j as { fallback_from?: unknown })?.fallback_from\\n218:     ? \\\"\\\"\\n219:     \n\nDo not repeat these failures. Address the specific cause named above.",
  "goal_text": "[narrowed from route-edit-3f241142] In repos/development-vessel/src/resolvers/feature-compose.ts, report each drafting model's outcome to the grading endpoint. THREE edits in this one file; all are required and together they are complete. A previous attempt was correctly refused for using globalThis to carry state, so this version uses two ordinary module-level variables instead.\n\nEdit 1 declares them. Replace this line, which occurs exactly once:\nconst PER_CALL_TIMEOUT_MS = 600_000;\n\nwith exactly:\nlet lastDraftModel = \"\";\nlet lastDraftEndpoint = \"\";\nconst PER_CALL_TIMEOUT_MS = 600000;\n\nEdit 2 records the model. Replace this line, which occurs exactly once:\n  console.log(\"[fc-draft-model] selected=\" + String((j as { model_selection?: { selected?: unknown } })?.model_selection?.selected ?? \"unknown\") + \" task=feature_compose\");\n\nwith exactly:\n  lastDraftModel = String((j as { model_selection?: { selected?: unknown } })?.model_selection?.selected ?? \"\");\n  lastDraftEndpoint = endpoint;\n  console.log(\"[fc-draft-model] pick=\" + lastDraftModel + \" task=feature_compose\");\n\nEdit 3 reports the outcome. Replace these two consecutive lines, which occur exactly once:\n    writeFileSync(\n      reportPath,\n\nwith exactly:\n    if (lastDraftModel && lastDraftEndpoint) {\n      void fetch(lastDraftEndpoint, {\n        method: \"POST\",\n        headers: { \"Content-Type\": \"application/json\", Authorization: \"ApiKey \" + METABOB_API_KEY },\n        body: JSON.stringify({\n          type: \"llmArmOutcome_write\",\n          model: lastDraftModel,\n          reached: verdict === \"FAVORABLE\",\n          task_type: \"feature_compose\",\n        }),\n      }).catch(() => {});\n    }\n    writeFileSync(\n      (reportPath),\n\nIMPORTANT, because a previous attempt failed typecheck with 'TS2300: Duplicate identifier endpoint': the ONLY new declarations in this whole change are lastDraftModel and lastDraftEndpoint, both in edit 1. Do NOT redeclare endpoint, verdict, METABOB_API_KEY or PER_CALL_TIMEOUT_MS anywhere; endpoint is already a parameter of llmCall and the others already exist in scope. Edit 1 also changes the numeric literal 600_000 to 600000, which is the same value.\n\nThe receiving endpoint already exists and is verified live: llm-resolver-vessel serves llmArmOutcome_write, landed in commit fec22cbc, and an operator probe returned resolved true. Its handler forwards to recordArmOutcome, which already maintains the per-task task_alpha and task_beta posteriors. Drafting is the only work the model selector collects no evidence about: feature_compose and patch_with_tools carry zero learned posterior while seven other task types are graded. The fetch is fire-and-forget with a catch and cannot affect any verdict.\n\nWHY PREVIOUS ATTEMPTS ON THIS GAP FAILED (most recent last):\n- semantic_reject: adversarial refuters agreed 2/2 (conf 0.90, 1.00): The patch does not genuinely close the gap because the fetch operation in Edit 3 is entirely dependent on the execution of `lastDraftModel` and `last\n- semantic_reject: The patch fails to guarantee reporting each drafting model's outcome as module-level variables get overwritten on subsequent calls, potentially losing intermediate outcomes\n- anchor_not_found: {\"error\":\"old_string not found in file. The closest real text is lines 217-222 of 6555, VERBATIM:\\n217:   lastDraftModel = (j as { fallback_from?: unknown })?.fallback_from\\n218:     ? \\\"\\\"\\n219:     \n\nDo not repeat these failures. Address the specific cause named above.",
  "expected_input_shapes": [],
  "expected_output_shapes": [],
  "cite_principle": null,
  "target_file_paths": [],
  "operator_seed": false,
  "bridge_source": "gap_to_scenario_bridge",
  "source_gap_id": "route-edit-3f241142-narrowed",
  "source_gap_source": "substrate_detected"
}