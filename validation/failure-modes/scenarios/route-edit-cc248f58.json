{
  "id": "route-edit-cc248f58",
  "routing_class": "recombination",
  "target_template_id": "development-vessel:draft-gap-closing-activity",
  "mode_class": "edit_intent_route",
  "stage": "detection",
  "outcome_class": "gap",
  "title": "In repos/development-vessel/src/resolvers/gap-to-feature.ts, in the region \"A slice sequence cut short by a capacity ref",
  "description": "In repos/development-vessel/src/resolvers/gap-to-feature.ts, in the region \"A slice sequence cut short by a capacity refusal never got its attempt either\", deliver a reach verdict after the second direct compose call so its execution row can be graded.\n\nREGION. Ground on the comment beginning \"A slice sequence cut short by a capacity refusal never got its attempt either\", which occurs exactly once in this file. Below it is a second invocation of resolveFeatureCompose, followed by the line assigning its body to cb. That assignment is the edit site.\n\nTHE DEFECT, measured. This file invokes resolveFeatureCompose at three call sites and verdict delivery exists at only one. Over the thirty eight compose executions since delivery landed, every one logged a successful trace capture so every report carried the execution identifier, yet only thirty four percent of rows received a reach tag; the rest passed through the two undelivered call sites and stay permanently ungraded. Where delivery does run the effect is measured and monotone across eleven readings: the arm's beta rose from nineteen point four eight to thirty four point four eight while alpha stayed frozen at fifty four point three nine, and its implied success rate fell from seventy three point six percent to sixty one point two percent against a counter derived rate near twelve percent. The undelivered majority is the only remaining constraint on that convergence.\n\nWHAT MAKES THIS SAFE. Both halves are already in scope at the insertion point: the report carries the trace store's execution identifier on its execution_id field, and the verdict is the report's ok field. This mirrors the delivery already present and working at the first call site. It is fire and forget with a bounded timeout and a swallowing catch, so a trace-store problem cannot affect the compose result, and the function's return value is untouched.\n\nPerform exactly one replacement. Find the sole occurrence of this line:\n  const cb = compose.body as Record<string, unknown>;\n\nReplace it with these lines:\n  const cb = compose.body as Record<string, unknown>;\n  try {\n    const reachId2 = typeof cb[\"execution_id\"] === \"string\" ? (cb[\"execution_id\"] as string) : \"\";\n    if (reachId2.length > 0) {\n      const reachEndpoint2 = process.env[\"METABOB_ENDPOINT\"] ?? \"http://127.0.0.1:8080\";\n      const reachKey2 = process.env[\"METABOB_API_KEY\"] ?? \"\";\n      void fetch(`${reachEndpoint2}/v2/activities/execution-traces/reach`, {\n        method: \"POST\",\n        headers: { \"Content-Type\": \"application/json\", ...(reachKey2 ? { Authorization: `ApiKey ${reachKey2}` } : {}) },\n        body: JSON.stringify({ execution_id: reachId2, reached: cb[\"ok\"] === true }),\n        signal: AbortSignal.timeout(5000),\n      }).catch(() => { /* grading must never affect the compose result */ });\n    }\n  } catch { /* verdict delivery is best effort */ }\n\nThe first line is reproduced verbatim so every downstream use of cb is unchanged. Distinct local names are used so this cannot collide with the delivery block at the earlier call site. No imports are added: fetch is global and the API key is already read from the environment elsewhere in this file.\n\nOne replacement, one file, inside the named region. Change nothing else.",
  "goal_text": "In repos/development-vessel/src/resolvers/gap-to-feature.ts, in the region \"A slice sequence cut short by a capacity refusal never got its attempt either\", deliver a reach verdict after the second direct compose call so its execution row can be graded.\n\nREGION. Ground on the comment beginning \"A slice sequence cut short by a capacity refusal never got its attempt either\", which occurs exactly once in this file. Below it is a second invocation of resolveFeatureCompose, followed by the line assigning its body to cb. That assignment is the edit site.\n\nTHE DEFECT, measured. This file invokes resolveFeatureCompose at three call sites and verdict delivery exists at only one. Over the thirty eight compose executions since delivery landed, every one logged a successful trace capture so every report carried the execution identifier, yet only thirty four percent of rows received a reach tag; the rest passed through the two undelivered call sites and stay permanently ungraded. Where delivery does run the effect is measured and monotone across eleven readings: the arm's beta rose from nineteen point four eight to thirty four point four eight while alpha stayed frozen at fifty four point three nine, and its implied success rate fell from seventy three point six percent to sixty one point two percent against a counter derived rate near twelve percent. The undelivered majority is the only remaining constraint on that convergence.\n\nWHAT MAKES THIS SAFE. Both halves are already in scope at the insertion point: the report carries the trace store's execution identifier on its execution_id field, and the verdict is the report's ok field. This mirrors the delivery already present and working at the first call site. It is fire and forget with a bounded timeout and a swallowing catch, so a trace-store problem cannot affect the compose result, and the function's return value is untouched.\n\nPerform exactly one replacement. Find the sole occurrence of this line:\n  const cb = compose.body as Record<string, unknown>;\n\nReplace it with these lines:\n  const cb = compose.body as Record<string, unknown>;\n  try {\n    const reachId2 = typeof cb[\"execution_id\"] === \"string\" ? (cb[\"execution_id\"] as string) : \"\";\n    if (reachId2.length > 0) {\n      const reachEndpoint2 = process.env[\"METABOB_ENDPOINT\"] ?? \"http://127.0.0.1:8080\";\n      const reachKey2 = process.env[\"METABOB_API_KEY\"] ?? \"\";\n      void fetch(`${reachEndpoint2}/v2/activities/execution-traces/reach`, {\n        method: \"POST\",\n        headers: { \"Content-Type\": \"application/json\", ...(reachKey2 ? { Authorization: `ApiKey ${reachKey2}` } : {}) },\n        body: JSON.stringify({ execution_id: reachId2, reached: cb[\"ok\"] === true }),\n        signal: AbortSignal.timeout(5000),\n      }).catch(() => { /* grading must never affect the compose result */ });\n    }\n  } catch { /* verdict delivery is best effort */ }\n\nThe first line is reproduced verbatim so every downstream use of cb is unchanged. Distinct local names are used so this cannot collide with the delivery block at the earlier call site. No imports are added: fetch is global and the API key is already read from the environment elsewhere in this file.\n\nOne replacement, one file, inside the named region. Change nothing else.",
  "expected_input_shapes": [],
  "expected_output_shapes": [],
  "cite_principle": null,
  "target_file_paths": [],
  "operator_seed": false,
  "bridge_source": "gap_to_scenario_bridge",
  "source_gap_id": "route-edit-cc248f58",
  "source_gap_source": "substrate_detected"
}