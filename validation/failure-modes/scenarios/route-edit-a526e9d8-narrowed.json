{
  "id": "route-edit-a526e9d8-narrowed",
  "routing_class": "recombination",
  "target_template_id": "development-vessel:draft-gap-closing-activity",
  "mode_class": "edit_intent_route",
  "stage": "detection",
  "outcome_class": "gap",
  "title": "[narrowed from route-edit-a526e9d8] investigate and decompose gap route-edit-3edba860: In repos/human-surface-vessel/src",
  "description": "[narrowed from route-edit-a526e9d8] investigate and decompose gap route-edit-3edba860: In repos/human-surface-vessel/src/store.ts, immediately above the line `export function recentFeedback(limit = 50): Feedback[] {`, add a function `hydrateFeedbackFromLog()` and invoke it once at module scope so it runs at import time. It must read the newline-delimited JSON file at `${process.env.WORKSPACE_ROOT ?? \"/workspace\"}/interactor-log/uiFeedback_write.jsonl` with readFileSync, parse each l\n\nWHY PREVIOUS ATTEMPTS ON THIS GAP FAILED (most recent last):\n- compose_execution_failure: replace_lines DRIFT-REFUSED for repos/human-surface-vessel/src/store.ts 166-195: expected first/last [\"const hydratedFeedbackIds = new Set<string>(); // Keep track of hydrated feedback IDs\"/\"    }\"] b\n- wrong_location: The patch removes the hydrateFeedbackFromLog function definition instead of adding it, leaving a module-scope call to an undefined function that would crash at import time.\n- semantic_reject: zero behaviour delta — added lines are comment/whitespace-only — zero behaviour delta\n\nDo not repeat these failures. Address the specific cause named above.",
  "goal_text": "[narrowed from route-edit-a526e9d8] investigate and decompose gap route-edit-3edba860: In repos/human-surface-vessel/src/store.ts, immediately above the line `export function recentFeedback(limit = 50): Feedback[] {`, add a function `hydrateFeedbackFromLog()` and invoke it once at module scope so it runs at import time. It must read the newline-delimited JSON file at `${process.env.WORKSPACE_ROOT ?? \"/workspace\"}/interactor-log/uiFeedback_write.jsonl` with readFileSync, parse each l\n\nWHY PREVIOUS ATTEMPTS ON THIS GAP FAILED (most recent last):\n- compose_execution_failure: replace_lines DRIFT-REFUSED for repos/human-surface-vessel/src/store.ts 166-195: expected first/last [\"const hydratedFeedbackIds = new Set<string>(); // Keep track of hydrated feedback IDs\"/\"    }\"] b\n- wrong_location: The patch removes the hydrateFeedbackFromLog function definition instead of adding it, leaving a module-scope call to an undefined function that would crash at import time.\n- semantic_reject: zero behaviour delta — added lines are comment/whitespace-only — zero behaviour delta\n\nDo not repeat these failures. Address the specific cause named above.",
  "expected_input_shapes": [],
  "expected_output_shapes": [],
  "cite_principle": null,
  "target_file_paths": [],
  "operator_seed": false,
  "bridge_source": "gap_to_scenario_bridge",
  "source_gap_id": "route-edit-a526e9d8-narrowed",
  "source_gap_source": "substrate_detected"
}