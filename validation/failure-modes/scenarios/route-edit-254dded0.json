{
  "id": "route-edit-254dded0",
  "routing_class": "recombination",
  "target_template_id": "development-vessel:draft-gap-closing-activity",
  "mode_class": "edit_intent_route",
  "stage": "detection",
  "outcome_class": "gap",
  "title": "investigate and decompose gap route-edit-dac2f850: Edit repos/discovery-vessel/src/index.ts so the GET /bootstrap handle",
  "description": "investigate and decompose gap route-edit-dac2f850: Edit repos/discovery-vessel/src/index.ts so the GET /bootstrap handler stops returning a loopback identity_endpoint.\n\nThe relevant code is the const identity_endpoint assignment inside the app.get(\"/bootstrap\", ...) handler. Today its final fallback is process.env.IDENTITY_VESSEL_URL, which on this deployment is http://127.0.0.1:8101 — so a remote client that fetches /bootstrap is handed an",
  "goal_text": "investigate and decompose gap route-edit-dac2f850: Edit repos/discovery-vessel/src/index.ts so the GET /bootstrap handler stops returning a loopback identity_endpoint.\n\nThe relevant code is the const identity_endpoint assignment inside the app.get(\"/bootstrap\", ...) handler. Today its final fallback is process.env.IDENTITY_VESSEL_URL, which on this deployment is http://127.0.0.1:8101 — so a remote client that fetches /bootstrap is handed an",
  "expected_input_shapes": [],
  "expected_output_shapes": [],
  "cite_principle": null,
  "target_file_paths": [],
  "operator_seed": false,
  "bridge_source": "gap_to_scenario_bridge",
  "source_gap_id": "route-edit-254dded0",
  "source_gap_source": "substrate_detected"
}