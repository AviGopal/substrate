{
  "id": "route-edit-1422f960",
  "routing_class": "recombination",
  "target_template_id": "development-vessel:draft-gap-closing-activity",
  "mode_class": "edit_intent_route",
  "stage": "detection",
  "outcome_class": "gap",
  "title": "In repos/development-vessel/src/resolvers/gap-to-feature.ts, add a fourth exclusion block to admitActionableGaps that re",
  "description": "In repos/development-vessel/src/resolvers/gap-to-feature.ts, add a fourth exclusion block to admitActionableGaps that retires a gap whose every recorded attempt failed for the same reason.\n\nInsert the new block immediately BEFORE this existing line, which occurs exactly once in the file:\n    // (E3) PHANTOM ANCHOR — retire a gap whose quoted anchor is already gone from the file\n\nInsert exactly this code, followed by one blank line:\n\n    // (E4) IDENTICAL REPEATED FAILURE — a gap whose every recorded attempt failed for the\n    // same normalized reason will fail that way again, so each retry burns a scarce LLM\n    // completion for a guaranteed loss. Measured 2026-09-12: 20 of 1462 open gaps qualify,\n    // representing 71 wasted attempts. Reasons beginning \"== install\" are EXCLUDED because\n    // that is the undistilled build-log dump the lesson writer stores instead of the error:\n    // those are identical for every failure, so identity there is an artifact, not evidence.\n    // FAIL-OPEN by construction: any parse or shape problem admits exactly as before.\n    try {\n      if (\n        ((meta.failure_lessons as Array<Record<string, unknown>> | undefined) ?? [])\n          .map((l) => String(l?.[\"reason\"] ?? \"\").toLowerCase().replace(/[0-9a-f]{8,}/g, \"H\").replace(/[0-9]+/g, \"N\").slice(0, 120))\n          .filter((s) => s.length > 20 && !s.startsWith(\"== install\")).length >= 3 &&\n        new Set(\n          ((meta.failure_lessons as Array<Record<string, unknown>> | undefined) ?? [])\n            .map((l) => String(l?.[\"reason\"] ?? \"\").toLowerCase().replace(/[0-9a-f]{8,}/g, \"H\").replace(/[0-9]+/g, \"N\").slice(0, 120))\n            .filter((s) => s.length > 20 && !s.startsWith(\"== install\")),\n        ).size === 1\n      ) {\n        excluded.push({ id, reason: \"identical_repeated_failure\" });\n        continue;\n      }\n    } catch { /* fail-open: admit as before */ }\n\nThe block deliberately declares NO new named variables: it reuses meta, excluded and id, which already exist in this function. Do not introduce any new const or let. Do not change any other line in the file. Do not reformat surrounding code.\n\nPOST-CONDITION: after this change the file contains the exact string identical_repeated_failure, the new block sits immediately above the (E3) PHANTOM ANCHOR comment, and the file typechecks clean.",
  "goal_text": "In repos/development-vessel/src/resolvers/gap-to-feature.ts, add a fourth exclusion block to admitActionableGaps that retires a gap whose every recorded attempt failed for the same reason.\n\nInsert the new block immediately BEFORE this existing line, which occurs exactly once in the file:\n    // (E3) PHANTOM ANCHOR — retire a gap whose quoted anchor is already gone from the file\n\nInsert exactly this code, followed by one blank line:\n\n    // (E4) IDENTICAL REPEATED FAILURE — a gap whose every recorded attempt failed for the\n    // same normalized reason will fail that way again, so each retry burns a scarce LLM\n    // completion for a guaranteed loss. Measured 2026-09-12: 20 of 1462 open gaps qualify,\n    // representing 71 wasted attempts. Reasons beginning \"== install\" are EXCLUDED because\n    // that is the undistilled build-log dump the lesson writer stores instead of the error:\n    // those are identical for every failure, so identity there is an artifact, not evidence.\n    // FAIL-OPEN by construction: any parse or shape problem admits exactly as before.\n    try {\n      if (\n        ((meta.failure_lessons as Array<Record<string, unknown>> | undefined) ?? [])\n          .map((l) => String(l?.[\"reason\"] ?? \"\").toLowerCase().replace(/[0-9a-f]{8,}/g, \"H\").replace(/[0-9]+/g, \"N\").slice(0, 120))\n          .filter((s) => s.length > 20 && !s.startsWith(\"== install\")).length >= 3 &&\n        new Set(\n          ((meta.failure_lessons as Array<Record<string, unknown>> | undefined) ?? [])\n            .map((l) => String(l?.[\"reason\"] ?? \"\").toLowerCase().replace(/[0-9a-f]{8,}/g, \"H\").replace(/[0-9]+/g, \"N\").slice(0, 120))\n            .filter((s) => s.length > 20 && !s.startsWith(\"== install\")),\n        ).size === 1\n      ) {\n        excluded.push({ id, reason: \"identical_repeated_failure\" });\n        continue;\n      }\n    } catch { /* fail-open: admit as before */ }\n\nThe block deliberately declares NO new named variables: it reuses meta, excluded and id, which already exist in this function. Do not introduce any new const or let. Do not change any other line in the file. Do not reformat surrounding code.\n\nPOST-CONDITION: after this change the file contains the exact string identical_repeated_failure, the new block sits immediately above the (E3) PHANTOM ANCHOR comment, and the file typechecks clean.",
  "expected_input_shapes": [],
  "expected_output_shapes": [],
  "cite_principle": null,
  "target_file_paths": [],
  "operator_seed": false,
  "bridge_source": "gap_to_scenario_bridge",
  "source_gap_id": "route-edit-1422f960",
  "source_gap_source": "substrate_detected"
}