{
  "id": "route-edit-3edba860",
  "routing_class": "recombination",
  "target_template_id": "development-vessel:draft-gap-closing-activity",
  "mode_class": "edit_intent_route",
  "stage": "detection",
  "outcome_class": "gap",
  "title": "In repos/human-surface-vessel/src/store.ts, immediately above the line `export function recentFeedback(limit = 50): Feed",
  "description": "In repos/human-surface-vessel/src/store.ts, immediately above the line `export function recentFeedback(limit = 50): Feedback[] {`, add a function `hydrateFeedbackFromLog()` and invoke it once at module scope so it runs at import time. It must read the newline-delimited JSON file at `${process.env.WORKSPACE_ROOT ?? \"/workspace\"}/interactor-log/uiFeedback_write.jsonl` with readFileSync, parse each line, and push a Feedback entry for each record into the existing in-memory `feedback` array, wrapped in try/catch so a missing file is ignored. Place all new code outside any comment block.",
  "goal_text": "In repos/human-surface-vessel/src/store.ts, immediately above the line `export function recentFeedback(limit = 50): Feedback[] {`, add a function `hydrateFeedbackFromLog()` and invoke it once at module scope so it runs at import time. It must read the newline-delimited JSON file at `${process.env.WORKSPACE_ROOT ?? \"/workspace\"}/interactor-log/uiFeedback_write.jsonl` with readFileSync, parse each line, and push a Feedback entry for each record into the existing in-memory `feedback` array, wrapped in try/catch so a missing file is ignored. Place all new code outside any comment block.",
  "expected_input_shapes": [],
  "expected_output_shapes": [],
  "cite_principle": null,
  "target_file_paths": [],
  "operator_seed": false,
  "bridge_source": "gap_to_scenario_bridge",
  "source_gap_id": "route-edit-3edba860",
  "source_gap_source": "substrate_detected"
}