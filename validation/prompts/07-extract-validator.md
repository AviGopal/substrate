Extract a `validate(a, b)` helper into a new file (`src/validate.ts` for TypeScript projects, `src/validate.py` for Python projects) that checks both arguments are finite numbers and throws/raises a `TypeError` with message `"arguments must be finite numbers"` if not. Update all arithmetic functions (`add`, `subtract`, and any others present) to call `validate(a, b)` at the top. Add tests for the validation behaviour. All existing tests must still pass.
