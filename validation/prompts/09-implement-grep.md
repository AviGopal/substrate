Implement the `grep(pattern, flags, files)` function so all tests pass. The function searches each file for lines matching `pattern` and returns the matching lines concatenated as a string. Supported flags (space-separated in the `flags` argument): `-n` (prepend line number), `-i` (case-insensitive), `-v` (invert match), `-x` (match entire line), `-l` (print only file names). When searching multiple files, prepend `filename:` to each result line (unless `-l`). All 24 tests must pass.
