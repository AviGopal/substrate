The test suite in `src/math.test.ts` is failing because `multiply` has a bug.

Look at `src/math.ts`. The `multiply(a, b)` function returns the wrong result — for example `multiply(3, 4)` returns `16` instead of `12`.

Fix the bug in `src/math.ts` so all `multiply` tests pass. Do not modify `src/math.test.ts`. Run the tests to confirm they pass.
