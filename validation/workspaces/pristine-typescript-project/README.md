# pristine-typescript-project

A tiny TypeScript workspace used as a benchmark seed. Run `bun test` to
exercise the math helpers in `src/math.ts`. The seed ships with a known
bug; benchmark prompts ask agents to find or fix it.
