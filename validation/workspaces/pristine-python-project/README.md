# pristine-python-project

A tiny Python workspace used as a benchmark seed. Run `pytest` to exercise
the math helpers in `src/math_utils.py`. The seed ships with a known bug;
benchmark prompts ask agents to find or fix it.
