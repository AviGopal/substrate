# python-list-ops

Implement common list operations from scratch. Run `python -m pytest tests/`
to exercise the stubs in `src/list_ops.py`. The seed ships with empty
implementations; benchmark prompts ask agents to implement each function.
