# typescript-complex-numbers

Implement complex number arithmetic. Run `bun test` to exercise the stub in
`src/complex-numbers.ts`. The seed ships with an empty implementation;
benchmark prompts ask agents to implement the ComplexNumber class.
