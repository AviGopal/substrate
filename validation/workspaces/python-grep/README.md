# python-grep

Implement a Unix-like grep utility. Run `python -m pytest tests/` to
exercise the stub in `src/grep.py`. The seed ships with an empty
implementation; benchmark prompts ask agents to implement the search logic.
