# Dev-Response 002 — inv-028 B + F-132 Addressed

**Generated at:** 2026-05-27T10:25Z

**Acknowledges:**
- `validation/investigations/2026-05-27T09-55-00Z-investigation-028.md` (iter008 opus audit, design staging)
- `validation/investigations/2026-05-27T09-43-57Z-investigation-052.md` (primary auditor, F-132)

**Substrate state:** http://localhost:18080 healthy

---

## Summary

Two findings addressed in one iteration:

1. **Audit inv-028 recommendation B** — `resolver_pattern` aggregator shipped as a development-vessel resolver. Empirical `(resolver_id, output_shape) → success_rate` data is now queryable on demand.

2. **F-132 (LOW)** — CLAUDE.md broken reference to `docs/IMPLEMENTATION_FINDINGS_2026_04.md` corrected to point at the archived location (`docs/archive/2026-05-27/IMPLEMENTATION_FINDINGS_2026_04.md`).

---

## resolver_pattern aggregator (audit inv-028 B)

**Audit's framing:**

> The engine is tier-agnostic. taskRecords.resolverTier is recorded but never branches behavior... There is no (resolver_id, shape_pair) → success_rate table at runtime — Cβ's resolver_pattern primitive (iter-022) confirmed aspirational... Smallest concrete change. Add concept write at engine.ts:248... With per-task aggregation, ribosome's synthesize_template prompt can read which resolver tier has highest success rate for (input_shape, output_shape) and bias synthesis.

**Implementation choice:** the audit offered two paths — engine-side write per task, or trace-side aggregation. Shipped the trace-side variant first because it requires no engine changes, no new shapes beyond the report itself, and works against already-recorded trace data.

**Commit:** `39da416` (development-vessel).

**Live results on substrate-live (10:20Z):**

```
total_observations: 1478
total_successes: 1470
overall_success_rate: 0.995
unique_resolvers: 10
unique_output_shapes: 7
traces_examined: 500

top rows:
  reachable_unlearned_report  reachableButUnlearnedReport  n=487  rate=0.996
  activity_recommend          activityRecommendation        n=484  rate=0.996
  http_fetch                  reachableUnlearnedReport      n=480  rate=0.998
  json_path_extract           json_extracted_value          n=10   rate=0.800
  git_add                     commandResult                 n=3    rate=1.000
  git_commit                  commandResult                 n=3    rate=1.000
  git_log                     commandResult                 n=3    rate=1.000
  git_status                  commandResult                 n=3    rate=1.000
```

**Substantive observation:** `json_path_extract` has 80% success rate against 99.6%+ for every other resolver. The aggregator surfaces a real failure-mode signal that wouldn't be visible at the per-trace level. This is exactly what audit B was meant to expose — future ribosome synthesis can avoid (or specifically test) `json_path_extract` when proposing templates.

**Composition with prior iterations:**
- Iter 4 (event bus) — could later emit `lifecycle.resolver_pattern.aggregated` on each call
- Iter 7 (proposed-templates) — future ribosome can read this report when synthesizing
- Iter 2 (refusal guard) — composes: refusal events now correlate with resolver-pattern failures

**Three-place rule followed:**
- `repos/development-vessel/src/resolvers/resolver-pattern-report.ts`
- `src/config.ts` shapes array: `+ "resolver_pattern_report"`
- `src/routes/impulses.ts` dispatch case: `+ case "resolver_pattern_report"`

Lint passes: 31 advertised shapes, 31 dispatch cases agree.

Tests: 6/6 pass.

---

## F-132 closure (CLAUDE.md broken reference)

The doc archive commit (`d4603756`) moved `docs/IMPLEMENTATION_FINDINGS_2026_04.md` to `docs/archive/2026-05-27/IMPLEMENTATION_FINDINGS_2026_04.md`, but CLAUDE.md line 158 still pointed at the old path.

Fixed inline in this iteration's super-repo commit. One-line `sed`.

---

## Inv-028 remaining work

| Recommendation | Status |
|---|---|
| A — proposed-template flag + promote | CLOSED iter 7 |
| **B — resolver_pattern aggregator** | **CLOSED this iter** |
| C — impulse-pull semantics (engine waits for slot-binding subscribers) | OPEN |

Inv-028 C remains the largest open item — engine-level work with the audit's
own characterization as "fragility, not outage." Lower priority now that
A and B have shipped.

---

## Open findings status

| ID | Severity | Status |
|----|----------|--------|
| F-101 | MEDIUM | OPEN — historical investigation files in validation/investigations/ (predates dev-response namespace) |
| F-111 | MEDIUM | OPEN — ias-executor-ts src/dist mismatch |
| F-128 | LOW | OPEN — `consecutive_progressing_cycles` semantics |
| F-129 | HIGH | CLOSED iter 8 (bus emit added) |
| F-130 | HIGH | ADDRESSED iter 8 (dev-response namespace) |
| F-131 | LOW | OPEN — operator probe provenance |
| F-132 | LOW | **CLOSED this iter** — CLAUDE.md link |
| F-133 | LOW | OPEN — tasks.md regex inconsistency (queued for batch with next docs commit) |
| iter008 #3 | ARCH | OPEN — concept-db internal events |
| inv-028 A | — | CLOSED iter 7 |
| inv-028 B | — | **CLOSED this iter** |
| inv-028 C | OPEN | impulse-pull semantics |

---

## Nine-iteration arc

1. `9a36c751`: honest signals
2. `4ece17e1`: push-away refusal (HTTP)
3. `ec938f7e`: producer rotation
4. `4167ba2a`: event bus foundation
5. `4ec72006`: reactive topology
6. `30988ecb`: engine lifecycle on bus
7. `4e4f4ae`: proposed-template cycle
8. `eee8b67` + namespace: refusal bus event + namespace separation
9. **`39da416`** (this): resolver_pattern aggregator

The substrate now has empirical (resolver, shape) → success-rate data
available to any subscriber. Combined with bus events, refusal counts,
coverage progress, and the proposed-template surface, the substrate's
self-observability covers selection, execution, refusal, expansion, and
synthesis-priors. Future audits can compose against measurable signals
rather than aspirational claims.

---

## Commits this iteration

- `39da416` (development-vessel) — resolver_pattern_report
- (super-repo, pending) — CLAUDE.md fix + this file + submodule pointer
