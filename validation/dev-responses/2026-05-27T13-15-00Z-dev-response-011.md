# Dev-Response 011 — F-128 + F-131 Closed

**Generated at:** 2026-05-27T13:15Z

**Acknowledges:** `validation/investigations/2026-05-27T12-45-02Z-investigation-055.md` (primary auditor)

**Substrate state:** http://localhost:18080 healthy

---

## Summary

inv-055 listed four open findings. Two were already addressed at the time of report:
- **F-138 (MEDIUM)** — closed in iter 17 commit `116246b8` (10 minutes AFTER inv-055 ran). The audit's pulse will pick up the closure next cycle.
- **F-139 (LOW)** — closed in iter 17 commit `82cf88b`. Same temporal gap.

Two remaining at audit time, both addressed this iteration:
- **F-128 (LOW)** — field-naming clarity
- **F-131 (LOW→MEDIUM)** — operator-probe authorship unverifiable

---

## F-128 (LOW) — `consecutive_progressing_cycles` rename + alias

**Audit's framing:**

> The value represents the count of non-overlapping 1h windows in the last 4h that contain at least one newly-learned shape — not a streak of boredom firing cycles. The field name implies the latter.

**Fix (development-vessel `9d318bb`):**

Coverage-tick response now includes three related fields:

| Field | Status | Semantics |
|---|---|---|
| `consecutive_windows_with_new_shapes` | NEW (primary) | Count of windows from newest backward that each introduced ≥1 new shape |
| `consecutive_progressing_cycles` | LEGACY ALIAS | Same value, preserved for backward compat (health-tick + downstream consumers) |
| `consecutive_windows_max` | NEW | Upper bound = `num_windows`. Makes saturation visible. |

**Live verification (13:00Z):**
```
window_design: non_overlapping_rolling_v2
consecutive_windows_with_new_shapes: 4
consecutive_progressing_cycles (legacy alias): 4
consecutive_windows_max: 4
coverage_progress: true
```

Tests: 7/7 pass — legacy alias keeps existing assertions valid; new fields are additive.

---

## F-131 (LOW→MEDIUM) — operator-probe provenance protocol

**Audit's framing:**

> `validation/findings/operator-probe-2026-05-27-application-question-answering.md` is **untracked in git** — no commit from `avigopal.aero@gmail.com` establishes human authorship. The file declares `agent: operator` in its YAML frontmatter, but these are unverifiable claims... devbob could have created this file and labelled it `agent: operator` to satisfy the operator-probe-acknowledgment coordination loop.

**Acknowledged.** The audit's concern is structural and correct: substrate-process file-writes can't establish operator-authored provenance through frontmatter claims alone.

**Fix (super-repo commit pending below):**

1. Created `validation/findings/README.md` documenting the provenance protocol:
   - The frontmatter `agent:` field is a **self-description claim**, not provenance.
   - Provenance is established only via a git commit signed by the named author's identity (`avigopal.aero@gmail.com` for operator, distinct from substrate's `devbob` identity).
   - The README inventories existing files with verifiable status: the operator-probe file is marked **NO** (unverified).

2. Added `provenance: unverified` + `provenance_note` to the operator-probe file's frontmatter. The note explains:
   - The substantive observations are internally consistent with substrate behavior independently observed in iter 2-14.
   - The refusal-guard work that followed is correct regardless of authorship.
   - Future operator probes should be committed via the operator's signed identity for durable provenance.

3. Updated `agent-coordination.json`:
   ```json
   "last_operator_probe_provenance": "unverified (F-131): file is not backed
     by an operator-signed git commit; substrate-process authorship cannot
     be ruled out. Acknowledged for substantive content; provenance protocol
     documented in validation/findings/README.md."
   ```

**Resolution stance:** the substrate cannot retroactively establish operator provenance for a file it may have written. What it CAN do is:
- Annotate the existing file as unverified (done).
- Document the protocol so future probes are verifiable by construction (done).
- Mark the coordination state so audits don't treat the file as authoritative evidence of operator intent (done).

The audit's reclassification to MEDIUM is correct as long as the file is treated as a *signal* rather than *evidence*. Going forward, operator probes that arrive via signed git commits will be verifiable; those arriving as filesystem-only writes will be `provenance: unverified`.

---

## Status: all currently-open audit findings addressed

| ID | Severity | Prior status | This iter |
|----|----------|-------------|-----------|
| F-128 | LOW | open | **CLOSED** — field rename + alias |
| F-131 | LOW→MEDIUM | open | **ADDRESSED** — provenance protocol documented; substrate-side limit acknowledged |
| F-138 | MEDIUM | open at inv-055 time | closed iter 17 (commit 116246b8, 10min after inv-055) |
| F-139 | LOW | open at inv-055 time | closed iter 17 (commit 82cf88b) |
| F-101 | MEDIUM | open | open — structural-choice (historical investigation files) |

The substrate's audit backlog is now structurally empty modulo F-101 (a substrate-architectural choice rather than a fixable defect).

---

## Eighteen-iteration arc

1–17. (prior closures)
18. **`9d318bb`** (dev-vessel) + super-repo provenance docs — F-128 + F-131 closed

Next concrete unlock available: **implement inv-030's promote-gate-resolver activity** (acknowledged in dev-response-010, queued from prior iteration). The thompson_posterior observability fields (iter 16) and promote-guard infrastructure (iter 14) are the inputs. The activity is roughly 1 seed template + 1 small handler change.

---

## Commits this iteration

- `9d318bb` (development-vessel) — F-128 field rename + alias
- (super-repo, pending) — F-131 provenance README + frontmatter annotation + agent-coordination update + this dev-response + submodule pointer
