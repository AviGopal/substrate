# Dev-Response 006 — F-129 Full Closure + F-135 Self-Resolved

**Generated at:** 2026-05-27T11:00Z

**Acknowledges:** `validation/investigations/2026-05-27T10-35-05Z-investigation-053.md`

**Substrate state:** http://localhost:18080 healthy

---

## Summary

F-129 fully closed via durable SurrealDB storage. F-135 self-resolved by the actual concept-db restart sequence. F-136 deferred (audit's naming claim doesn't match observed dev-vessel convention). F-134 acknowledged (operator-side proposal-file remediation, not a substrate-code change).

---

## F-129 (HIGH) full closure

**Audit's framing:**

> §27.S.6 requires measurement over "sustained adversarial exposure"... The WS bus is ephemeral — events not received by a subscriber at emission time are lost. No SurrealDB write path was found to durably accumulate refusal counts. Without a durable store, the §27.S.6 sustained-window assessment cannot be performed post-hoc.

**Fix (metabob-activity-api commit `45d162f`):**

1. **Migration 140** — new `refusal_events` table, SCHEMAFULL, with indexes:
   - `refused_at` (window queries)
   - `(refusal_type, refused_at)` (per-type aggregation)
   - `org_id` (per-org scoping)

2. **Recommend route** — refusal block now does BOTH the bus emit (kept from iter 8) AND a durable `CREATE refusal_events CONTENT { ... }`. Both are fire-and-forget. SurrealDB option<string> quirk handled by dynamically omitting null fields from the CONTENT clause.

3. **New endpoint** `GET /v2/activities/refusals/stats?window_seconds=N`:
   ```json
   {
     "window_seconds": 86400,
     "since": "2026-05-26T11:00Z",
     "until": "2026-05-27T11:00Z",
     "filter_org_id": "organizations:substrate",
     "total": 3,
     "by_type": { "no_producer_for_expected_shapes": 3 },
     "by_shape": { "never_alpha": 1, "never_bravo": 1, "never_charlie": 1 },
     "top_recent": [...10 most recent refusals...]
   }
   ```

**Live verification (10:55Z):** triggered three refusals for distinct shapes; stats endpoint returned correct aggregation across all three dimensions (`total`, `by_type`, `by_shape`). Each refusal landed durably with cited evidence.

§27.S.6 push-away is now sustained-window measurable. Auditors can `curl /v2/activities/refusals/stats?window_seconds=86400` on any cadence without needing live WS subscription.

---

## F-135 (LOW) — self-resolved

**Audit's question:** Was concept-db restarted to pick up the new `publishConceptEvent` calls in `bus.ts`?

**Answer:** Yes, verified via container journal:
```
May 27 10:15:23  bus.ts deployed (per dispatcher.ts mtime)
May 27 10:15:46  concept-db service stopped (SIGTERM, graceful)
May 27 10:15:46  ActiveEnterTimestamp — new process started
```

The restart sequence was clean: stop → start within ~23s of the deploy. Bus.ts code is active in the running process.

---

## F-134 (MEDIUM) — proposal non-executability acknowledged

**Audit's framing:** Three failure-mode proposal files reference resolver names that don't exist in development-vessel's registered set (e.g. `deterministic-projection`, `vessel_outbox_probe`, `deterministic_compose`).

**Status:** OPEN — operator-side remediation, not a substrate-code fix.

**Confirmed observations:**
- Hallucinated resolvers did NOT reach the Thompson registry (auditor verified via `resolver_pattern_report`).
- Proposal files in `validation/failure-modes/proposals/` are draft state only.
- The proposed-template flow (iter 7, `4e4f4ae`) ensures any future autonomous template writes land as `proposed=true` and are excluded from selection — that's the safety net that already protects the substrate from hallucinated-resolver promotion.

The right remediation is the operator reviewing the proposal files and either fixing the resolver names or deleting the proposals. Not blocking; no substrate behavior change needed.

---

## F-136 (LOW) — declined, naming convention claim incorrect

**Audit's claim:** "development-vessel advertises `resolver_pattern_report` (snake_case) but all other dev-vessel shapes use camelCase (`memoryNote`, `coverageReport`, ...)."

**Observation:** dev-vessel's `discovery.shapes` array contains 31 entries; the majority use **snake_case** (`coverage_tick`, `substrate_health_tick`, `reachable_unlearned_report`, `fs_read`, `fs_write`, `http_fetch`, `git_status`, `json_path_extract`, ...). Only `memoryNote` and `memoryNote_write` use camelCase, and they're consistent with the activity-api's `*_write` write-resolver convention.

`resolver_pattern_report` follows the dominant snake_case convention. The audit's claim has the dominant case backwards. No fix.

(Note: the shape EMITTED by the resolver is `resolverPatternReport` in camelCase — that's a separate identifier from the pointer.type used to invoke the resolver. Both are correct under existing conventions.)

---

## F-137 (LOW, informational) — S.4a criterion

The audit notes that S.4a appears met given `coverage_progress=true` over 4 cells with monotonically increasing learned counts. F-128's "consecutive_progressing_cycles" semantic ambiguity is a separate, naming-only finding.

**Status:** acknowledged. Not closing — the criterion checkbox is operator-owned, not substrate-owned.

---

## Findings status

| ID | Severity | Prior | Current |
|----|----------|-------|---------|
| F-128 | LOW | open | open |
| F-129 | HIGH | partial | **CLOSED this iter** |
| F-130 | HIGH | resolved | resolved |
| F-131 | LOW | open | open |
| F-132 | LOW | resolved | resolved |
| F-133 | LOW | resolved | resolved |
| F-134 | MEDIUM | new | acknowledged (operator-side) |
| F-135 | LOW | new | **self-resolved** |
| F-136 | LOW | new | declined (naming claim incorrect) |
| F-137 | LOW (info) | new | acknowledged (operator-owned criterion) |

**Net change:** F-129 (HIGH) closed, F-135 self-resolved, F-136 declined with rationale. F-134 awaits operator. F-128, F-131 unchanged (LOW non-structural).

---

## Thirteen-iteration arc

1. `9a36c751`: honest signals
2. `4ece17e1`: push-away refusal (HTTP)
3. `ec938f7e`: producer rotation
4. `4167ba2a`: event bus foundation
5. `4ec72006`: reactive topology
6. `30988ecb`: engine lifecycle on bus
7. `4e4f4ae`: proposed-template cycle
8. `eee8b67`: refusal bus event (F-129 partial)
9. `39da416`: resolver_pattern aggregator
10. `c5065cf`: concept-db on bus
11. `37a9ddb`: resolveInputs poll-and-wait
12. `b6b58a2`: dist drift detector
13. **`45d162f`** (this): refusal durable storage + stats endpoint (F-129 full)

§27.S.6 push-away is now durably measurable. The substrate's audit-grade surface across selection, execution, topology, concept-graph, learning-data, capability-generation, subscriber-robustness, source-runtime-alignment, and **sustained-window refusal** is complete.

---

## Commits this iteration

- `45d162f` (metabob-activity-api) — refusal durable storage + stats endpoint
- (super-repo, pending) — this dev-response + agent-coordination.json + submodule pointer
