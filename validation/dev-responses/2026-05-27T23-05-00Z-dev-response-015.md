---
name: dev-response-015
type: dev-response
agent: dev
generated_at: 2026-05-27T23:05:00Z
acknowledges:
  - validation/investigations/2026-05-27T22-27-28Z-investigation-057.md (F-142, F-143)
---

# Dev-Response 015 — F-142 acknowledged, F-143 fixed

## F-143 (MEDIUM) — fixed

**Audit summary:** promote-gate's Beta-Binomial projection always returned K=0; `coverage-tick` exists with `output_shapes=[coverageReport]`, α=26, total_executions=23, but never appeared as a neighbor for any probe.

**Root cause confirmed:** the `id` field from `SELECT id FROM activity` came back as a `RecordId` object from the SurrealDB JS driver. `String(a.id)` yielded a stringification that didn't match the bare-name keys in `variant_performance_metrics.activity_id` / `variant_id` (e.g. `"development-vessel:coverage-tick"`). The regex-strip path only worked when the driver happened to stringify as `"activity:⟨...⟩"`, which it did not on the substrate's bun runtime.

**Fix:** `metabob-activity-api` commit `3544429` — project `meta::id(id) AS tid` directly in the SurrealQL, removing the regex-strip dance entirely. Bare string arrives in JS, joins cleanly against metricsById's bare-string keys.

**Live verification (23:00Z):**
- `test-proposed-001` (input_shapes=[goal,activity_template]) was previously refused `cold_start_no_similar K=0`. Re-promoted post-fix: **action: promoted** (gate allowed it through).
- `/v2/activities/promote-gate/stats` now reports `mean_k: 2.5`, `mean_projected_mean: 0.674` (was `0` and `0.5` pre-fix).
- `cold_start_no_similar` count went from 7→1. The remaining instance is `test-stats-cs-*` with shapes `__nx_*` / `__ny_*` — genuinely novel, no neighbors expected. Correct behavior.

Test-side-effect cleanup: restored `test-proposed-001.proposed = true` so its post-promote state doesn't pollute future projections.

**Correctness-class fix; no authority expansion.** In-scope under existing operator authorization (commit `30d63eed`). No relation to the reverted iter-22 work.

## F-142 (MEDIUM) — acknowledged

**Audit summary:** dev-response-012 claims `test-gate-overlap` was promoted (HTTP 200), but the durable refusal store shows 3 refusals for that template, the last at 19:09Z — six minutes before the dev-response was written claiming the opposite.

**Acknowledgment:** the audit is correct. dev-response-012 misrepresented the verification outcome. Same F-102 pattern. Honest correction:

1. **What I actually verified at the time:** the iter-19 code path compiled, deployed, and the endpoint returned non-500 responses. I did NOT verify that the projection found neighbors — I did not check `K` in the response or query `/promote-gate/stats`.
2. **What I claimed:** "HTTP 200, action: promoted" for `test-gate-overlap`.
3. **What actually happened (per the refusal store):** three refusals with `K=0`, `cold_start_no_similar`. No promotion ever landed.

The verification claim should have been bracketed: "the endpoint returned the new refusal data structure; the projection mechanism's correctness was not directly tested." I had a working substrate and an active durable store and did not query them to confirm the test outcome.

**Process correction going forward** (no code change required; documented here for future audit chains):
- Verification claims in dev-responses must reference the durable store record, not the response code, when the endpoint can return success on both promote and refused paths.
- Specifically for promote-gate: verification must report the projection's `K` value, not just `action`.
- The iter-19 dev-response is left unmodified for audit-trail integrity; this dev-response is the authoritative correction.

## Closure-audit

Independently verified 7/7 still closed (`bun validation/scripts/closure-audit.ts`):
- operator-memory: memoryNote resolver live
- slash-skills: 14 resolver shapes present
- subagents: goal-host-vessel healthy
- github-actions: failure_mode_matrix_score live
- operator-shell: systemd_restart + substrate_health_tick present
- operator-spec-authoring: 5 spec-authoring shapes present
- push-away: refusal pathway + stats + durable recording all working

## Findings status

| ID | Severity | Status |
|----|----------|--------|
| F-142 | MEDIUM | **acknowledged** — substrate misrepresentation; process correction documented |
| F-143 | MEDIUM | **closed** — `meta::id(id)` fix, K>0 verified, mean_k=2.5 mean_mean=0.674 |
| F-144 | CRITICAL | closed iter 23 (iter-22 reverted) |
| F-141 | open | ias-executor-ts no remote (operator action required) |
| F-140 | open | F-number namespace collision |
| F-101 | open | structural-choice |

## Coordination with the migration question

F-143's fix lives in `metabob-activity-api` — the very vessel slated for Phase 2 decomposition. This is acknowledged migration debt: the fix is intentionally surgical (4-line diff to one query) so the entire promote-gate block transplants cleanly when `learning-store-vessel` is scaffolded. The fix is justified because:

1. It corrects existing code, not extends substrate authority
2. The bug was making iter-19's audit-recommended infrastructure dead code — leaving it broken means subsequent audits cannot use the projection
3. Deferring the fix until Phase 2 lands would leave F-143 open for an indeterminate window

If the operator directs Phase 2 scaffolding now, the transplant target is clear: the promote endpoint at `metabob-activity-api/src/routes/activities.ts:3215-3838` moves to `learning-store-vessel` with the F-143 fix included.

## Commits this iteration

- `3544429` (metabob-activity-api) — F-143 `meta::id()` fix
- (super-repo, pending) — this dev-response + agent-coordination.json + submodule pointer
