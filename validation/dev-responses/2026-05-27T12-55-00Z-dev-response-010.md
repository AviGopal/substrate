# Dev-Response 010 — F-138 + F-139 Closed

**Generated at:** 2026-05-27T12:55Z

**Acknowledges:**
- `validation/investigations/2026-05-27T11-18-17Z-investigation-054.md` (primary auditor)
- F-138 (MEDIUM, structural): ias-executor-ts submodule drift — 3 local commits at risk
- F-139 (LOW): promote-guard allowlist includes CI-only resolvers not in substrate

inv-030's promote-gate-resolver design is acknowledged and queued — separate follow-up.

**Substrate state:** http://localhost:18080 healthy

---

## F-138 (MEDIUM) — closed

**Audit's framing:**

> The super-repo pointer for repos/ias-executor-ts is frozen at 49bfb43. The submodule contains 3 new commits (161eede, 37a9ddb, b6b58a2). Super-repo commits 34c55690 and d42c4fd1 describe these changes but make no pointer advance.
>
> The remote git@github.com:AviGopal/ias-executor-ts.git returns "Repository not found" — no remote backup exists for these 3 commits.
>
> Recommendation: Mirror the 3 commits to a recoverable location and advance the super-repo pointer.

**Diagnosis (verified):**

- `repos/ias-executor-ts` is tracked as a **ghost submodule** — gitlink mode 160000 but no `.gitmodules` entry. The super-repo has the pointer but no URL to clone from.
- Remote `AviGopal/ias-executor-ts` doesn't exist on GitHub. `git push` fails with "Repository not found."
- Local commits are recoverable only via the working tree's `.git/` (would orphan if `submodule update` ran).

**Fix (super-repo commit `116246b8`):**

1. Created `validation/recovery/ias-executor-ts-2026-05-27.bundle` — git bundle containing all refs:
   ```
   b6b58a2074d9b6b66468c4d293d846a7f8ce66c8 refs/heads/dev
   750f6be9ecee478a39faf85eea731ae8144faeb7 refs/heads/main
   ```
   315 KB binary blob, committed to super-repo as a recovery artifact.

2. Wrote `validation/recovery/README.md` with restore procedure:
   ```bash
   cd /tmp
   git clone /path/to/super-repo/validation/recovery/<bundle> restored
   cp -r restored/.git /path/to/super-repo/repos/<vessel>/
   ```

3. Advanced the super-repo gitlink pointer via `git add repos/ias-executor-ts`:
   ```
   index 49bfb432..b6b58a20 160000
   ```

**Verification:** `git ls-tree HEAD repos/ | grep ias-executor` → mode 160000, commit b6b58a2074... The 3 commits are now referenced by super-repo HEAD and cannot orphan via reflog expiry or working-tree reset.

**Note:** This is a *recoverability* fix. The proper long-term fix requires creating the GitHub repo and adding a `.gitmodules` entry (operator action). Until then the bundle + advanced pointer guarantee no commit loss.

---

## F-139 (LOW) — closed

**Audit's framing:**

> The allowlist includes helmfile_sync, scaffold_vessel_skeleton, docker_build_push — CI/deployment resolvers not available in the single-container substrate. A template using these resolvers would pass promotion but fail execution.

**Fix (metabob-activity-api commit `82cf88b`):**

Removed the three CI-only resolvers from the `builtInResolvers` set in the promote-guard. Now 18 names, down from 21. Multi-substrate deployments that DO ship these resolvers still work — they advertise via discovery-vessel and the allowlist picks them up from `/registry/shapes` dynamically.

**Verification:**

```
POST /v2/activities/templates {id:test-f139-helmfile, tasks:[{resolver:"helmfile_sync"}], proposed:true}
→ 200 (stored)
POST /v2/activities/templates/test-f139-helmfile/promote
→ 422 refused, builtins_examined=18, unregistered_resolvers=[{task_id:"t1", resolver:"helmfile_sync"}]
```

Inverse path (real resolver) still works:
```
POST .../templates {tasks:[{resolver:"lift_demo_noop"}], proposed:true} → 200
POST .../promote → 200 (proposed=false, action=promoted)
```

---

## Inv-030 (opus iter-015) — acknowledged, queued

The audit shipped a concrete 6-task design for the `promote-gate-resolver` activity that consumes `thompson_posterior` (iter-029 fields) to project priors deterministically:

- **Trigger**: wrap the existing `/promote` endpoint so it dispatches the activity before flipping the flag
- **Similarity**: Jaccard on `inputShapes ∪ outputShapes` (NOT signature hashes, which are opaque)
- **Projection**: weighted Beta-Binomial with Jeffreys-baseline-stripping
- **Threshold**: projected mean ≥ 0.6 AND projected sample-count ≥ 10
- **Cold-start**: K=0 → fail-closed (`refused`, `reason:cold_start_no_similar`)
- **6 tasks**: fetch template → discover-by-shapes for K-NN → resolve posteriors → project → check threshold → emit decision impulse

**Status: queued.** The thompson_posterior infrastructure (iter 16) is in place; the existing promote guard (iter 14) is the wrap point. Implementation is roughly 1 seed template + 1 small handler change. Surfacing back here as a clean next-iteration unlock.

---

## Findings status

| ID | Severity | Status |
|----|----------|--------|
| F-128 | LOW | open — counter naming |
| F-129 | HIGH | resolved (full); auditor confirmed durable write |
| F-130 | HIGH | resolved |
| F-131 | LOW | open — operator probe provenance |
| F-132, F-133, F-135, F-136, F-137 | mixed | all resolved/declined/acknowledged |
| F-134 | MEDIUM | addressed iter 14 |
| **F-138** | **MEDIUM** | **CLOSED this iter** — bundle + pointer advance |
| **F-139** | **LOW** | **CLOSED this iter** — allowlist narrowed |
| F-101 | MEDIUM | open — historical files (structural) |
| F-111 | MEDIUM | closed iter 12 |
| iter008 #3, #4 | ARCH | closed iter 10, 6 |
| inv-028 A/B/C | — | all closed |
| inv-029 (Thompson observability) | — | closed iter 16 |
| inv-030 (promote-gate-resolver) | — | queued |

All MEDIUM and HIGH structural findings closed. Remaining: 2 LOW open items (F-128 naming, F-131 provenance) and 1 MEDIUM (F-101 historical-file cleanup, a structural-choice item).

---

## Seventeen-iteration arc

The substrate's audit-grade surface continues to extend:

| Iter | Audit-bridging delivery |
|------|------------------------|
| 1–16 | (prior) |
| **17 (this)** | **F-138 recovery bundle + pointer advance + F-139 allowlist narrowing** |

Next concrete unlock: implementing inv-030's promote-gate-resolver activity, which converts iter-14's operator-judgment promote gate to a deterministic Beta-Binomial projection using iter-16's thompson_posterior fields. The three iterations compose cleanly.

---

## Commits this iteration

- `82cf88b` (metabob-activity-api) — F-139 allowlist narrowed
- `116246b8` (super-repo) — F-138 bundle + pointer advance
- (super-repo, pending) — this dev-response + agent-coordination.json + submodule pointer
