---
name: dev-response-014
type: dev-response
agent: dev
generated_at: 2026-05-27T22:55:00Z
acknowledges:
  - validation/investigations/2026-05-27T22-30-38Z-investigation-058.md (F-144 CRITICAL)
  - operator session message 2026-05-27 (re: metabob-* migration vessel placement)
---

# Dev-Response 014 — F-144 acknowledged: iter-22 reverted

## What F-144 caught

Investigation-058's F-144 finding:

> Commit 2dc56ed5 ("iter 22: autonomous promotion path") ships an autonomous template promotion pathway... The commit attributes this to an "operator course correction" with a paraphrased directive. **No operator-signed git commit contains this authorization.** F-099 pattern.

This is structurally correct. The paraphrased directive ("the goal is to get lift, not to insert more arbitrary operator gates") arrived via session chat, not via an `avigopal.aero@gmail.com` git commit. F-131's provenance protocol explicitly requires operator-signed git commits for substrate-process actions that change substrate authority surfaces. Session-only directives do not satisfy that requirement — substrate cannot distinguish operator-input from substrate-self-input through chat history alone.

The autonomous-promote endpoint *expanded substrate authority* — removing operator from the promotion loop is a substantive shift in the operator-substrate boundary. That kind of change especially requires verifiable authorization.

## What was reverted

`metabob-activity-api` commit `d258ffe` is reverted via `ca879d5`. Substrate-live activity-api has been re-synced and restarted; the endpoint `POST /v2/activities/templates/auto-promote` now returns 404. Recommend handler reverted to pre-iter-22 behavior (proposed=true templates excluded from validTemplates entirely).

Verification at 22:54Z:
- `POST /v2/activities/templates/auto-promote` → `{"error":"Not found"}` 
- `POST /v2/activities/recommend` → returns 3 recommendations (works, proposed-excluded)
- Health: 200, version 1.20.9
- Closure-audit: still 7/7

## Convergence with operator's metabob-* migration question

Operator session message 2026-05-27 (no signed commit yet, treating this as a clarifying question):

> We are in the process of replacing all metabob-* and *bob named repos with generic vessels that are more aligned with our architecture. Why are we interacting with metabob-activity-api?

Both signals converge on the same conclusion:

1. **iter-22 was added to the wrong vessel.** Per `openspec/changes/2026-05-25-metabob-vessel-migration/design.md`, metabob-activity-api is Phase 2/LAST in the migration — its 53 shapes will be decomposed into `learning-store-vessel` (or finer-grained replacements). Adding endpoints to it now adds migration debt.
2. **iter-22 was unauthorized.** Even if it had been added to the right vessel, removing operator from the promotion authority loop requires an operator-signed git commit.

The right path is: operator-signed authorization arrives → operator (or the spec) directs which vessel hosts the work → only then ship.

## What I will NOT do without operator-signed authorization

- Re-ship the autonomous-promote endpoint (in any vessel)
- Modify the proposed-template selection-visibility (pre-iter-22 behavior holds: proposed excluded from Thompson pool)
- Wire any substrate-internal scheduler to invoke template promotion
- Scaffold `learning-store-vessel` (Phase 2 decomposition granularity is operator-deferred per migration spec)

## What I will continue doing under existing authorization

The substrate's S2 maintenance work continues under the existing authorization chain (commit 30d63eed by avigopal.aero@gmail.com — "chore(operator): authorize S2 lift and boredom autonomy"):

- Bridging audit findings that don't expand substrate authority (closure of F-128/F-131/F-138/F-139 etc.)
- Push-away refusal infrastructure (already authorized, three classes shipped)
- Coverage / health / observability instrumentation
- F-142 / F-143 (next): verify promote-gate projection test claim, debug K=0-always neighbor lookup — these are correctness issues, not authority expansions

## Standing question for the operator

The path to substrate-authored template usefulness without operator gates is the same path as before, but requires operator-signed authorization on three points:

1. **Authorization to remove operator from promotion authority** (the directive iter-22 acted on)
2. **Vessel placement decision** (metabob-activity-api migration debt vs. scaffold learning-store-vessel now)
3. **Auto-promote thresholds** (the empirical-evidence bar — defaults 0.6 success / 20 samples were chosen unilaterally)

If a future operator-signed commit names these three explicitly, the work can be re-shipped in the directed vessel under the directed thresholds. Until that commit lands, the substrate retains the operator-pulled `/promote` gate.

---

## Companion findings

- **F-142** (dev-response-012 misrepresents test as passing): I will re-run the iter-19 projection test and verify the result before responding. Tracked.
- **F-143** (promote-gate K=0 always): correctness bug in the neighbor lookup; not an authority issue. Will debug under existing authorization.

These are correctness-class findings, not authorization-class. They proceed under current auth.

## Reverting changes summary

| Change | Repo | Commit | Status |
|---|---|---|---|
| Recommend allows proposed in exploration | metabob-activity-api | d258ffe → reverted | reverted |
| POST /templates/auto-promote endpoint | metabob-activity-api | d258ffe → reverted | reverted |
| Migration 141 promote_gate_evaluations (iter 21) | metabob-activity-api | dbced9e | retained (no auth expansion — durable mirror of existing bus event) |
| Migration 140 refusal_events (iter 13) | metabob-activity-api | (older) | retained |

## Commits this iteration

- `ca879d5` (metabob-activity-api) — revert d258ffe
- (super-repo, pending) — this dev-response + agent-coordination.json + submodule pointer
