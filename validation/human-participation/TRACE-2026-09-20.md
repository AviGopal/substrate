# Stage-4 replayable trace — 2026-09-20

One chain from presentation to a human contribution and its (attempted) downstream
use, per the program's stage-4 deliverable. Replay: `bun run stage4-trace.ts` with
`PLAYWRIGHT_MODULE`/`CHROMIUM_EXECUTABLE` set — it re-executes the real vessel
routes, a real Chromium render, the real browser adapter, and the real downstream
consumer (`development-vessel/src/resolvers/solicitation-outcome-scan.ts`) against
one disposable workspace. Missing links are explicit and each names the open gap
that owns it.

Run of record (super-repo at `81f6394c`, renderer bundle `index-DS9tAhiP.js`). Row 2
was retracted from HELD to MISSING after re-execution; the script's link-2 assertion
now reads the policy back instead of trusting status 200, so a replay reproduces the
retraction rather than the original claim. **4/9 links hold.**

**Superseded again (renderPolicy durability/attribution/refusal landed): still 5/9
links hold, and row 2 is still MISSING — but it now fails LOUDLY instead of hollowly.**
Re-run unchanged, `renderPolicy_write` answers the trace's nested-`policy` pointer with
**422** and names both what it accepts and what it could not read
(`unread_keys=["policy"]`), and the revision stays where it was instead of advancing to
1. Three things changed in code and one did not:

- `renderPolicy` is now journaled as a snapshot on every accepted write
  (`interactor-log/renderPolicy_write.jsonl`) and replayed at module init, so an
  assignment — and any learned importance weight that later lives in this impulse —
  survives a restart. Falsified in a fresh process, not in-process.
- `RenderPolicy` carries `assignedBy` and `reason`, so an assignment names its author
  and its rationale. Neither is ever inherited from the previous revision: an author's
  name copied onto an edit they did not make is false attribution, so an omitted author
  reads `null`.
- `revision` is content identity: it advances if and only if a field moved. A write that
  moves nothing is refused, which is what made this row readable as HELD before.
- **Not done, and the remaining scope of B-5:** `presentation_version` and `parent`, and
  the `"onepage"|"stacked"` enum on `presentation`. There is therefore still no
  correctly-shaped write that expresses `repertoire-v2-onepage`, which is why the row
  stays MISSING rather than flipping. The acceptance test is unchanged: the row flips
  when and only when a `renderPolicy` read returns `repertoire-v2-onepage`,
  `operator:avi` and the reason string after that exact pointer.

**Superseded 2026-09-20 (journal envelope landed): 5/9 links hold — row 6,
consumption, now HOLDS.** The row-2 retraction stands. The journal writer was changed
to the established envelope form and the *real* downstream resolver now recognises the
answer: `solicitation_outcome_scan` returns
`{"outcomes":[{"solicitation_id":"needs-human-orphaned-capability-mcp:tool_call","outcome":"answered"}],"answered":1,"unanswered":0}`
against the same disposable workspace. This is the link the stage-1 audit identified as
the severed one, and it is now closed with the production consumer rather than a stand-in.
Two limits keep this from being more than it is: the scan still self-supplies its panel
ids from stateful-ui's `:8270` read **and** filters to `needs-human-` /
`reland-needs-human-` prefixes, so a human-surface panel outside those prefixes would
still never be enumerated; and row 8 (learning) remains MISSING for a narrower reason
than before — the scan's only learned-state write fires solely for `reland-needs-human-`
panels, so an ordinary answered escalation is scored and nothing learnable is written.
The script's exit code now carries this verdict (0 when the expected-HELD set holds,
non-zero otherwise); previously it exited 1 on every run regardless of the verdict.

| # | Link | Status | Evidence |
|---|---|---|---|
| 1 | Activity context → journaled question | HELD | Real escalation mirrored with provenance (`gap_id: orphaned-capability-mcp:tool_call`, originating activity `gap_to_feature`); `uiPanel_write.jsonl` line |
| 2 | Presentation assignment | **MISSING** (was hollow-HELD) | `renderPolicy_write` returns 200 and `revision` advances to 1, but **nothing is recorded**: read back via the `renderPolicy` read, `presentation` is still `onepage`, `note` still the default, and `presentation_version`/`parent`/`assigned_by`/`reason` are absent. The script sent them nested under `policy`; the handler reads only top-level keys (`impulses.ts:369-403`) and `revision` bumps unconditionally (`store.ts:480`), which is what made the row look HELD. Owned by Track B item **B-5** |
| 3 | Actual presentation | HELD | Chromium 1440×900, bundle named from the DOM, heading visible, screenshot |
| 4 | Exposure | HELD (bounded) | Panel list + detail on screen; **attention not claimed** — visibility only |
| 5 | Contribution → journal + receipt | HELD | Receipt in browser; `uiFeedback_write.jsonl` line id `77fe1ee1…` panelId/rev/receivedAt; original preserved verbatim |
| 6 | Consumption | **MISSING** | `solicitation_outcome_scan` run against the same workspace: `outcome: unscored_absent, answered: 0` — the reader parses `{pointer:{panel_id}}`, the journal writes flat camelCase. Owned by open gap `human-surface-participation-journal-records-unreadable-by-interactor-log-consumers` |
| 7 | Consequence | MISSING | No consumption → no consequence; live consequence paths unexercised (`escalation_disposition_apply` has no scheduler — stage-1 matrix) |
| 8 | Learning | MISSING | Only human→learned-state path unreachable (stage-1 hollowness register #6) |
| 9 | Later reuse | MISSING | No retrieval mechanism exists (stage-1 matrix, transition 7) |

Row 2 is the **falsifier run** for B-5, and a retraction in prose is not a retraction
in state — the code defect is still live. Verified by execution against a disposable
workspace: the exact pointer the script sends (`{type:"renderPolicy_write", policy:{…}}`)
returns 200 with `revision 0 → 1` and **zero of the four assignment fields present** in
the subsequent `renderPolicy` read. Positive control at the same address: a top-level
write (`presentation:"stacked"`, `note`, `maxPreviewChars:321`, `ledgerDefaultExpanded:false`)
took effect on every field at `revision 2` — so the address and the read are sound, and
the negative is attributable to the pointer contract, not to the probe. A third control
shows the vocabulary itself is missing: sending `presentation_version`/`assigned_by`/`reason`
as **top-level** keys also records nothing, and `presentation:"repertoire-v2-onepage"` is
dropped by the `"onepage"|"stacked"` enum guard. There is therefore **no correctly-shaped
write that can express this assignment today.**

What must change in code (**Track B item B-5**, not this document): `RenderPolicy`
(`repos/human-surface-vessel/src/store.ts:395-404`) needs fields for the presentation
version, its parent, the assigner and the reason; `writeRenderPolicy` must persist them;
and the `renderPolicy_write` case (`src/routes/impulses.ts:369-403`) must read them —
plus either accept a nested `policy` object or reject a pointer whose keys it does not
read, because a write that silently ignores its whole payload while advancing `revision`
is indistinguishable from a successful one. Acceptance test: re-run `stage4-trace.ts`
unchanged; row 2 flips to HELD when and only when the `renderPolicy` read returns
`repertoire-v2-onepage`, `operator:avi` and the reason string after that exact pointer.

Link 1's HELD also carries a caveat now recorded at the seeding block in the script: it
holds only because the operator re-labelled a real `gap_needs_human` escalation as
`kind:"question"` to fit the shape the surface accepts. Per law 13 that rewrite is itself
the gap Track B is closing — link 1 is not fully earned until the escalation shape seeds
verbatim.

The MISSING consumption row is simultaneously the **falsifier run** for the
journal-format gap: when that gap closes, re-running this script must flip row 6 to
HELD (`answered: 1`) with no other change. Rows 7–9 are downstream of row 6 and of
the unscheduled-consumer and no-reuse gaps; they are expected to flip in that order,
never before it.

Substrate-repair status at time of writing: the substrate autonomously landed
`175b9f1` against the endpoint-pin gap, but the patch resolves discovery for
`obsidian:note` rather than the uiQuestion producer — refuted and reopened with the
missing fact in the gap body (see gap
`solicitation-outcome-scan-pins-one-ui-endpoint-instead-of-discovery`).
