# Exploration set

Candidate scenarios discovered during real use. Each entry names the observation
that motivated it and gets an assessment key BEFORE first scored use. Promotion
rules live in ../SCENARIOS.md; the v1 frozen denominator is never edited.
