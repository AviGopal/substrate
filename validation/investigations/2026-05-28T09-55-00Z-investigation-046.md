---
investigation_id: 2026-05-28T09-55-00Z-investigation-046
generated_at: 2026-05-28T09:55:00Z
investigator_session_id: claude-opus-4-7-audit-iter-031
prior_investigation_id: 2026-05-28T09-45-00Z-investigation-045
loop_iteration: 31
framing: "system fixes itself" is recursive — the meta-question must itself eventually live inside the substrate
substrate_target: http://localhost:18080
---

# Investigation 046 — The meta-framing is itself a substrate-target: L0→L4 observability stack

## The escalation

The operator's "we must always consider how the system would need to fix itself" framing — repeated four times this session — has now been escalated to: **"this itself is something we need to be able to do within the system."** The meta-reasoning ("which breaches are substrate-fixable?") must eventually be a substrate capability, not an audit-role capability.

Today the meta-question lives outside the substrate:
- Audit role (this investigator session) classifies findings by remediation-class
- Investigation files in `validation/investigations/` are the output medium
- Operator's eyes close the loop by reading and triaging

The substrate doesn't:
- Classify its own breaches by `substrate_authorable` vs `operator_required`
- Maintain a catalog of its own structural-floor primitives
- Observe whether its own corrective attempts actually corrected the breach
- Propose new observability primitives based on operator-fix patterns

## The observability stack

| Layer | What's observed | Status today | Substrate primitives needed |
|-------|----------------|--------------|------------------------------|
| L0 — behavior | Execution outcomes (α/β posteriors) | Live | None — exists |
| L1 — invariant-breach | Disciplines/contracts being violated | Mostly absent | `invariantBreach` shape (iter-045; subagent #25) |
| L2 — fix-outcome | Did corrective activity correct the breach? | Absent | V_X authoring step (iter-040 §3) |
| L3 — fix-success-pattern | Across breaches, which corrective shapes worked? | Absent — lives in audit prose | Meta-audit activity, substrate-authorable once L1+L2 ship |
| L4 — meta-proposal | What observability primitives does the pattern suggest? | Absent — lives in audit prose | `metaInvariantProposal` shape + operator-queue resolver |

Each layer is operator-floor until its predecessor ships. The path is sequential.

## L3 sketch (substrate-authorable once L1+L2 ship)

```
id: meta-audit-self-fixability
subscription: cron daily OR lifecycle:invariant_breach (debounced)
input_shapes: [invariantBreach, activityExecutionTrace, validation_result]
output_shapes: [metaInvariantProposal, selfFixabilityReport]

task 1  fetch_recent_breaches        impulse-resolve     last 7d of invariantBreach
task 2  fetch_corrective_attempts    impulse-resolve     executionTrace where input includes breach
task 3  fetch_v_x_verdicts           impulse-resolve     validation_result per corrective
task 4  classify_remediation_outcome llm                 success/failed/skipped per breach
task 5  extract_operator_fix_patterns llm                what breach-classes get operator commits
task 6  emit_meta_proposals          write               metaInvariantProposal for operator queue
```

All resolvers exist per `repos/ias-executor-ts/src/hosts/goal-host.ts:598-673`. Lifecycle-subscription pattern exists per `repos/ias-executor-ts/src/templates/lifecycle/audit-test-report.json:13-18`. LLM-tier resolvers exist.

**Blockers are upstream**: L1 (subagent #25 in flight), L2 (iter-040 — ~50 LOC across 3 files), `metaInvariantProposal` shape, operator-queue resolver.

## The deepest recursion

This very analysis — "L3 belongs in the substrate" — is itself an L4 proposal that I (audit) am making outside the substrate. The framing the operator just articulated CANNOT YET be made by the substrate, because:

- L1 doesn't observe breaches across the full taxonomy
- L2 doesn't verify whether corrections corrected
- L3 doesn't yet reason over L2's verdicts
- L4 needs all three to fire

**At step L4, the audit role becomes optional.** The operator reads `metaInvariantProposal` impulses from the substrate instead of investigation files from me. That's the structural endgame.

## The path from S2 to S3 (CLAUDE.md IAL §27.S.6)

CLAUDE.md describes S3 as "emergent and operator-measured by active push-away — substrate gates refusing operator interventions with cited evidence (`interventionRefused` impulses)." Today's audit findings are operator-pushed; the substrate doesn't refuse anything because it doesn't yet reason about its own gaps.

L4 maturity is the precondition for `interventionRefused` to be authentic rather than performative. The substrate must first know what it can and cannot fix BEFORE it can defensibly refuse an operator-pushed intervention as "I have a better approach."

**Therefore L1→L4 stack is the structural path from S2 (substrate-authored, operator-reviewed) to S3 (substrate-resistant, operator-adversarial).** Not a parallel concern.

## Sequencing recommendation

Following iter-041 prediction-protocol discipline:

1. **L1 — `invariantBreach`** (subagent #25 returns soon). Operator ratifies the shape; substrate begins emitting via hand-authored detectors for each existing finding.
2. **L2 — V_X authoring** (iter-040 §3 ready; 50 LOC; pre-ratified). Ship same window as L1.
3. **Watch for ≥3 invariantBreach types with ≥1 corrective attempt each.** Without this data, L3's LLM-tier reasoning has no signal.
4. **L3 — meta-audit activity authored.** First operator-author; later substrate-author once `meta-audit-of-meta-audit` pattern matures.
5. **L4 — `metaInvariantProposal` shape + operator-queue.** Last; depends on L3 emitting real proposals.

Each step has a measurable gate. iter-041 P1-P6 predictions extend naturally:

- P7: first `invariantBreach` emitted by a hand-authored detector (24h after L1 ships)
- P8: first corrective that includes V_X self-verification (48h after L2 ships)
- P9: first `metaInvariantProposal` emitted by L3 (1 week after L1+L2 ship)
- P10: first substrate-emitted `interventionRefused` citing a `metaInvariantProposal` (S3 indicator)

## Audit's posture

This is a framing observation. No new finding, no new code. The cadence reminders ("we must always consider how the system would need to fix itself") now have a structural reply:

> **The meta-question is itself a substrate-target. The path is L0→L4 sequential observability layers. Today the substrate is at L0; L1 is subagent #25's current scope. Each subsequent layer's possibility depends on its predecessor's existence. The audit role is the L3+L4 stand-in until those layers ship.**

When subagent #25 returns with the `invariantBreach` design, this iter-046 layering should inform the operator's ratification: the abstraction is not just "another impulse shape" — it's the L1 prerequisite for everything above.

## Loop bookkeeping

- iter-046 is a framing note; no new data
- Subagent #25 (invariantBreach design) in flight
- Findings 10+11 in `openspec/changes/2026-05-23-substrate-explicit-vessels/findings/audit.md`
- Monitors `bmzpcld51` + `bj4wvnnk7` armed
- Next observation: subagent #25 return, OR any commit introducing an `invariantBreach`-shaped abstraction, OR first selection of `gap-closing:fm-43-cascade-attribution-error-1779960003142` (Finding 11 falsification)
