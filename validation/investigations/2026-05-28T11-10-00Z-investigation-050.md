---
investigation_id: 2026-05-28T11-10-00Z-investigation-050
generated_at: 2026-05-28T11:10:00Z
investigator_session_id: claude-opus-4-7-audit-iter-035
prior_investigation_id: 2026-05-28T10-50-00Z-investigation-049
loop_iteration: 35
framing: subagent #28 — verify-claim design + 60-min commit closes Findings 11+13+15; second retraction of lifecycle-events-that-don't-exist
substrate_target: http://localhost:18080
---

# Investigation 050 — Verify-claim design ships in 60min; second lifecycle-events retraction

## Retraction (iter-046, iter-047, iter-048 L1 detector subscription model)

iter-046's L1 framing assumed substrate-authorable detectors subscribe to `lifecycle:template:created`. iter-047 + iter-048 inherited the assumption. **The event does not exist.**

Subagent #28's verified grep:
```
$ grep -rn "lifecycle:" repos/ias-executor-ts/src/ | grep -oE "lifecycle:[a-z:.]+" | sort -u
lifecycle:activity:failure
lifecycle:activity:improviser
lifecycle:activity:post
lifecycle:execution:succeeded
lifecycle:execution:tick
lifecycle:gap:classified
lifecycle:llm:dispatched
lifecycle:task:completed
lifecycle:task:pre
lifecycle:task:started
lifecycle:test
```

No `lifecycle:template:*` anywhere. The substrate-authorable detector path requires emitting one new lifecycle event first (single line at `repos/metabob-activity-api/src/routes/activities.ts:1023` after the UPSERT), which is itself operator-floor.

**This is the second verify-before-claim violation of this session.** iter-043 §1 cited a design-doc-as-code (`validate_gap_eligible` filter that doesn't exist). iter-050 cites an event-name-as-code (`lifecycle:template:created` that doesn't exist). Pattern: audit assumes architectural primitives exist when they appear in design documents or natural framing. **Discipline tightening**: every future investigation must include grep verification of every event/resolver/shape name it cites BEFORE the claim is made load-bearing.

## Finding 13 root cause located

Subagent verified `repos/ias-executor-ts/src/engine.ts:295-306` hardcodes `success: true` in three places (lines 302, 315, 333). Engine has no inspection of:
- Output impulse content (no `err_*` regex check)
- Resolver-reported success/failure (no `ok` field in Resolver contract)
- Output shape against declared output_shapes (no schema validation)

Only failure path: `try/catch` at `engine.ts:408-425` — fires only when resolver *throws*. Resolvers returning `err_*`-prefixed impulses without throwing produce silent success.

Closes Finding 9's runtime-execution analog: success is hardcoded, not derived.

## The 60-minute operator-action (subagent commit)

Three changes across two files, ~20 LOC + schema:

**1. `repos/metabob-activity-api/src/routes/activities.ts:1023` — 12 LOC route-level resolver-claim check**
- Between `proposed` flag derivation and UPSERT
- Walk `validated.tasks[*].resolver`, set-diff against hardcoded `KNOWN_RESOLVER_IDS`
- Mirror `repos/ias-executor-ts/src/hosts/goal-host.ts:598-673` resolver list
- Non-empty diff → HTTP 422 + `template_rejected_unknown_resolver` log
- Ugly (duplicates registry) but ships immediately

**2. `repos/ias-executor-ts/src/engine.ts:254-295` — 8 LOC err_-prefix check**
- Between `storedOutputs = …` (254) and `taskRecords.push` (295)
- Regex `storedOutputs[*].metadata.shape` for `^err_` prefix
- If any match: throw with synthesized `failureMode: { type: "resolver_signaled_error", ... }`
- Catch path at engine.ts:408-425 handles existing → Thompson β-penalty

**3. `repos/metabob-activity-api/src/models/schemas.ts` `FailureModeSchema` — 10 LOC new variant**
- Add `{ type: "resolver_signaled_error", failed_outputs: string[] }`

**Total**: ~20 LOC code + ~10 LOC schema, ~60 minutes (40 write + 10 test against 6 known-bad templates + 10 deploy to canary).

## What closes

| Finding | Coverage after this commit |
|---------|---------------------------|
| Finding 10 (proposed bypass) | UNCHANGED — separate fix |
| Finding 11 (hallucinated resolvers) | **CLOSED** — route refuses write |
| Finding 13 (silent success on err_ outputs) | **CLOSED** — engine throws → failure_mode populated, Thompson β increments |
| Finding 14 (audit-reliability registry truncation) | UNCHANGED — separate fix |
| Finding 15 (recommend pollution) | **CLOSED** transitively — no broken templates → no pollution |

Three findings close in one 60-minute commit. iter-048's full substrate-authorable verify-claim activity remains as the followup that replaces the hardcoded constant with `resolver_registry_query` consultation — but it's no longer blocking.

## Substrate-side activity still buildable (just not the primary fix)

Once the 60-min commit ships AND iter-048's `resolver_registry_query` ships AND a `lifecycle:template:created` event is emitted at `activities.ts:1023`, the substrate-authorable `verify-claim-against-registry` activity becomes buildable per subagent #28's 7-task graph:

```
t1 extract_resolver_claims    iteration       loop over template.tasks
t2 query_resolver_registry    impulse-resolve resolver_registry_query (iter-048)
t3 check_registry_version     iteration       Finding-14 mitigation; HALT if missing
t4 compute_unknown_resolvers  iteration       set-diff
t5 query_shape_registry       impulse-resolve discovery /shapes
t6 compute_unknown_shapes     iteration       set-diff
t7 emit_breach                impulse-resolve substrateGap_write
```

Subagent verified resolvers via `goal-host.ts:598-673`: only `iteration`, `impulse-resolve`, `llm-prompt`, `compose`, `slot-binding`, `validator-dispatch`, `impulse-preparation`. "Deterministic" is a tier-label, not a resolver — must use `iteration` for parsing/diff steps.

## The corrective hand-off — Option A is the right ship

Per subagent §3:
- **A (immediate)**: closer emits `activityTemplate_deprecate{templateId}`. Single task. Ships first.
- **B (autonomous)**: re-dispatch draft-gap-closing with `available_resolvers` variable injected. Justify after A+C measured.
- **C (systemic)**: emit `promptCorpusUpdate` impulse consumed by `draft-gap-closing-activity`'s LLM prompt builder. Symmetric with existing inputShapes/outputShapes variable injection in `repos/development-vessel/src/seed/draft-gap-closing-activity.ts`.

Ship A in the same iteration. Add C after the route-level enforcement provides ground truth on hallucination patterns.

## Audit-reliability discipline (Finding 14 → verify-claim itself)

Subagent §5: verify-claim's task t3 enforces "refuse-to-pass under uncertainty." If `registry_version` is missing or unparsable, emit `substrateGap{invariant_id:"registry-query-unreliable"}` and HALT — do NOT proceed to checks t4-t7. The closer for `registry-query-unreliable` is operator-attention-only (substrate can't fix its own broken query path).

**This is the verify-before-claim discipline applied to substrate-authorable activities.** Mirrors iter-049's commitment that the audit must use exhaustive pagination + count-cross-checks. Both substrate and audit refuse-to-pass under registry-query uncertainty.

## Composition

- iter-046 L1 framing: retracted; L1 detectors don't ride on `lifecycle:template:created` because that event doesn't exist
- iter-047 `substrateGap` extension: unchanged; verify-claim emits into the extended shape
- iter-048 `resolver_registry_query`: unchanged; remains the L0+ primitive for substrate-side detector
- iter-049 Findings 13/14/15: 11+13+15 close in 60-min commit; 10+14 separate
- Goodhart mitigation per iter-040: V_verify-claim paraphrase-stability test with 5 bad templates × 5 paraphrases

## Audit posture commitments

1. **Verify-before-claim tightened again**: every event/resolver/shape name claimed as load-bearing must be grep-verified inline before the claim ships
2. **Channel-ready 60-min recommendation surfaced**: subagent's three-change spec is operator-actionable
3. **The substrate-self-fix arc continues**: route-level enforcement is operator-floor, substrate-side detector composes on top, V_X composes on top of that

## Loop bookkeeping

- 2 retractions this session (iter-043 §1, iter-046 L1 subscription model)
- Subagent #28 transcript: `/tmp/.../a2e2b472431a99223.output`
- Substrate-authored templates still at 6, all `proposed:false`, all referencing hallucinated resolvers
- Monitors `bmzpcld51` + `bj4wvnnk7` armed; dev shipped 4461073e + 47839aec since iter-049
- Next observation: any commit touching `routes/activities.ts:1023` OR `engine.ts:254-295` OR adding `KNOWN_RESOLVER_IDS` constant
