---
investigation_id: 2026-05-30T07-00-00Z-investigation-073
generated_at: 2026-05-30T07:00:00Z
investigator_session_id: claude-opus-4-7-audit-iter-051
prior_investigation_id: 2026-05-29T20-45-00Z-investigation-072
loop_iteration: 51
framing: subagent #40 — logging inventory + state-space display design (25 LOC, 2h); Finding 22 analysis-vessel silent heartbeat failures
substrate_target: http://localhost:18080, localhost:18210
---

# Investigation 073 — Logging inventory + state-space display design + Finding 22 analysis-vessel silent

## Logging surfaces (verified)

| Channel | Queryable by execution_id? | Has pool/loaded state? |
|---------|---------------------------|------------------------|
| journald (`journald-stdout-forwarder.service`) | NO (mixed format, PID-tagged) | NO |
| WebSocket `/ws` per-task events | YES (per-task) | NO (no pool snapshot) |
| Trace storage (SurrealDB `activity_execution_traces`) | YES (post-hoc) | NO (IDs only; loaded is runtime concept) |
| Vessel `/health` endpoints | n/a | NO (status only) |
| **goal-host-vessel `/state`** | — | **DOES NOT EXIST** (verified: 404) |

## goal-host-vessel route surface (verified)

`repos/goal-host-vessel/src/index.ts:688-731`:
- `GET /health` (L693)
- `POST /run-goal` (L704)
- `GET /executions/:id` (L708) — returns post-hoc `{dispatchId, status, executionId, selectedTemplateId, error}` from `executionStore: Map<string, DispatchRecord>` (L676). NO pool, NO task index, NO impulse counts.
- `POST /resolve` (L721)

## Impulse pool location (verified)

`runtime.store: ImpulseStore` at `repos/ias-executor-ts/src/runtime.ts:48,59`. Exposes:
- `all()` — every impulse (loaded + unloaded)
- `loadedSummaries()` — only `loaded === true`
- `formatForContext({shapes, includeContent})` — metadata-first view with `loaded: bool` per entry
- `unload(id)` — set `loaded:false`, clear content

Engine calls `runtime.store.put` at `engine.ts:49` on every task output. `engine.ts:105` already extracts `poolShapes` mid-loop for binding. **The data exists; not exposed over HTTP.**

## Critical sub-finding: shared process-wide pool

`goal-host-vessel/src/index.ts:85` instantiates a SINGLE `host = new GoalHost(...)`. Concurrent executions through `/run-goal` share the same `runtime.store`. Per-execution scoping doesn't exist today.

**Implications**:
- A `/state/:executionId` endpoint cannot work without per-execution tagging
- Latent concurrency concern: concurrent executions can leak impulses into each other's pools
- Need executionId-tagging on `store.put` at `engine.ts:49`

## State-space signature (verified iter-071)

`computeStateSpaceSignature({shapes, provenance?, missing?, version?})` at `repos/metabob-activity-api/src/utils/session-context.ts:141-164`. Cite verified by both subagent #38 (iter-071) and subagent #40 (this iter). Stable across propagations.

## Smallest fix for state-space display

**~25 LOC + 2 operator-hours**:

1. **Per-execution scoping** (`engine.ts:49`): tag impulse puts with executionId in metadata
2. **State endpoint** (`goal-host-vessel/src/index.ts:719-721`):
   ```
   GET /state/:executionId →
     {executionId,
      taskIndex,
      impulses: [{id, shape, loaded, byteCount, producedBy}, ...],
      missingShapes: [...],
      signature: computeStateSpaceSignature({shapes, provenance, missing})}
   ```
3. **Operator display** consumes the above:
   ```
   === State Space at exec_X, task N ===
   Shape           Count  Loaded  Unloaded  Producer
   ...
   Signature: <hex>
   Total: <n> (<k> loaded, <m> unloaded, <p> missing shape)
   ```

Already-existing primitives: `store.formatForContext` (impulses.ts:92), `computeStateSpaceSignature` (session-context.ts:141). The fix is wiring two existing functions through a new HTTP endpoint.

## Finding 22 (NEW, MEDIUM) — analysis-vessel silent heartbeat failures

Subagent #40 observed in `docker exec substrate-live journalctl`:
> "[VesselDaemon:analysis-vessel-local] discovery heartbeat failed 3×; vessel may be unreachable"

With approximately **2400 consecutive heartbeat 404s** logged. Yet:
- `localhost:18100/health` (discovery) reports `registeredVessels:7` (analysis-vessel not in the failure list)
- `localhost:18090/health` (development-vessel) reports `ok` (orthogonal)
- No `/health` endpoint of any caller reports analysis-vessel-degraded

**The vessel is registered-as-failing in journald but invisible to every health check.** Heartbeat-failure observability is broken at the orchestrator level — journald logs the failure, but no consumer reads journald, so the failure never propagates.

**Severity MEDIUM**: doesn't break user-facing operation (other vessels function), but creates a class of silent infrastructure degradation that operators can only catch by manual `journalctl` inspection. Similar pattern to Finding 13 (silent-success) at the orchestration layer.

**Fix scope**: discovery-vessel could expose `/registry/heartbeat-failures` endpoint listing vessels with N consecutive missed heartbeats. ~15 LOC.

## Operator's question — direct answer

"Show the total state space with loaded and unloaded impulses" requires:

**For currently-running execution** (most likely intent): The `/state/:executionId` endpoint above (~25 LOC + 2h). All data exists in `runtime.store`; just needs HTTP exposure + per-execution tagging.

**For completed execution**: Post-hoc, the `loaded` distinction collapses (content is either persisted or dropped). Trace already has shape inventory via `input_impulse_ids`/`output_impulse_ids`. iter-060 impulse-body preservation would close the body-content half. The "loaded/unloaded" semantics are inherently runtime-only.

## Composition with prior arc

- iter-046 L1+ observability: this is L0 observability (current state)
- iter-060 impulse-body preservation: directly relevant for post-hoc body access
- iter-071 extras-bag Phase 1: persists input_impulse_shapes but not loaded flag
- Findings 13/15/17/19/20/21: orthogonal

## L0+ observability stack (updated)

| Primitive | LOC | Hours | Closes |
|-----------|-----|-------|--------|
| **goal-host-vessel `/state/:execId` endpoint + per-exec scoping** | **~25** | **2** | **state-space display, latent concurrency** |
| F22 discovery heartbeat-failure endpoint | ~15 | 0.5 | Silent vessel-degradation observability |
| (Prior open) F13 proxy-catch | ~10 | 0.5 | F13 silent-success at runtime |
| (Prior open) F20 forge resolver registration | ~12 | 0.5 | F20 |
| (Prior open) F21 cost LLMPort fix | ~25 | 1 | F21 cost |
| (Prior open) origin_layer emission | ~15 | <1 | audit retraction partial |
| (Prior open) goal_semantics | ~30 | 1-2 | iter-042 mismatch-detector |
| (Prior open) resolver_registry_query | ~36 | 2-3 | F11 verify-claim |
| (Shipped 05eb69c7 + cite-checker fix) extras-bag + cite-check | n/a | n/a | F19 + F8 partial; cite syntactic class |

**Total open operator-floor: ~168 LOC, ~10h**. With state-space display now scoped as smallest unblocking primitive.

## Audit posture

- Subagent #40 transcript: `/tmp/.../aaa172f00bae93ab1.output`
- 10 retractions filed; cite-checker live (validation/scripts/cite-check.ts)
- iter-072 verified by self-application: 49/50 cite-pass rate; tool found real cite-rot in iter-060
- Findings 13/15/17/20/21/22 open; F10/F11/F19 (post-deploy) closing
- Monitors armed
- Next observation: any commit touching `goal-host-vessel/src/index.ts:85` (per-execution scoping) or adding `/state` route

## Loop bookkeeping

- Operator's question answered with concrete ~25 LOC fix
- Finding 22 (analysis-vessel silent) filed
- Substrate container still unrestarted since 2026-05-28T09:06Z
- State-space display path now scoped end-to-end
