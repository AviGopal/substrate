# Investigation 002 (2026-05-25) — 2026-05-25T01:39:09Z

**Prior**: `2026-05-25T00-34-24-investigation-001` (delta: ~1h05m).

**Substrate target**: `http://localhost:18080` (live).

**Investigator session**: `loop-iteration-019`.

## Summary

**Most positive iteration in the audit's history.** Three findings
resolved, two new findings (both positive), substrate has crossed
multiple structural thresholds since iter-018.

Headlines:

- **F-024 RESOLVED**: boredom timer systemd unit replaced with queue
  polling. Templates ARE being invoked successfully per dev's
  commit body: coverage-tick at 01:08:19Z, substrate-health-tick
  at 01:09:20Z, probe-reachable-unlearned at 01:10:51Z — all
  SUCCESS.
- **F-040 (new, high, positive)**: substrate-explicit-vessels DEV
  progress shipped. 14 of 53 tasks complete (was 0 in iter-018).
  Two new vessels operational: `llm-resolver-vessel.service`
  (port 8220) and `local-tools-vessel.service` (port 8230).
- **F-041 (new, high, positive)**: S.4a coverage-progress WINDOW 1
  STARTED per dev commit `14e23e95`. The substrate's first real
  signal that the lift criterion can move by autonomous template
  execution.
- **F-027, F-033, F-034 RESOLVED**: dev shipping at record pace
  (4 commits this window), dev coordination is current
  (last_updated 00:33Z), validator re-engaged with commit
  `ea5ef10a`.
- **F-029 status-changed-toward-resolution**: dev coord claims
  failure_mode is now populated on _goal_resolve failures (fix #3
  in dev's cascade batch).
- **F-038 progressing**: composition_chain populated on
  boredom-driven traces with new `boredom_*` lineage prefix.

**Still open**: F-037 (Thompson posteriors disconnect). The
cascade-analysis prediction continues to validate — even with
F-024, F-038 progressing, and 96 successful coverage-tick
executions, Thompson α=β=1 unchanged. The update path needs its
own dedicated fix.

## What changed since iter-018

Four dev commits + one validator commit:

| Commit | Subject |
|---|---|
| `c263b947` | fix(substrate): disable task-generation + enable discovery in boredom executor |
| `14e23e95` | fix(substrate): topology discovery running — coverage-tick, health-tick, probe all succeed |
| `daf81ce4` | feat(substrate): Phase 0-2 — explicit vessel scaffolds + VesselDaemon toolkit |
| `33417c19` | fix(substrate): llm-resolver-vessel VESSEL_ID + discovery registration field names |
| `ea5ef10a` | validator: boredom-as-lifecycle-observer recontextualization (Finding 3 in substrate-explicit-vessels) |

Plus the auditor's own commit `a71c3037` (cross-reference doc).

`daf81ce4` is the substantive structural commit:
- **Phase 0 in ias-executor-ts**: VesselDaemon, ResolverServer,
  DiscoveryRegistrationLoop, GoalHost promoted from
  `src/examples/` to `src/hosts/` and barrel-exported.
  BunFileSystemAdapter, BunProcessAdapter, FetchAdapter as
  top-level exports. InProcessLLMPort + HttpLLMPort + factory.
- **Phase 1 — local-tools-vessel (port 8230)**: new vessel
  with shell, fs_read, fs_write, fs_edit, git_status, git_diff,
  git_commit resolvers. 7 shapes advertised. systemd unit,
  Makefile targets, Dockerfile.substrate stanza added.
- **Phase 2 — llm-resolver-vessel (port 8220)**: new vessel.

This is the substrate's first concrete vessel-decomposition
delivery — per `2026-05-23-substrate-explicit-vessels` spec.

## The S.4a window 1 signal

Per dev commit `14e23e95`'s body:

> Verified: coverage-tick 01:08:19 SUCCESS, substrate-health-tick
> 01:09:20 SUCCESS, probe-reachable-unlearned 01:10:51 SUCCESS.
> S.4a clock: window 1 of 4 started.

This is the substrate's first window of coverage-progress signal.
The `topology-discovery-loop` spec requires strictly-increasing
RL across 4 consecutive 1h windows. Window 1 started; 3 more to
go.

If windows 2-4 also clear successfully, `coverage_progress=true`
flips on the next cycle report — one of the two inputs to the
lift hand-over decision (per `lift-criterion-hardening`).

## F-037 stubbornly persists

coverage-tick metrics moved:
- iter-018: 89 executions, 81 successful, α=β=1
- iter-019 (this): 96 executions, 85 successful, α=β=1

+7 executions, +4 successful, **zero Thompson posterior change**.

The cascade-analysis prediction continues to hold across all
fixes that have shipped:
- F-024 resolved → posteriors didn't move
- F-038 partial → posteriors didn't move
- 4 more successful coverage-tick executions → posteriors didn't move

The Thompson update path is structurally decoupled from the
trace store, OR it reads from a different field than the
metrics endpoint shows. Validator's dev-guidance points at
`activity-api/src/db/paradigm.ts` as the likely write-path
location.

## gap-006 / F-035 status change

The validator's BLOCKING meta-finding (gap-006 "substrate
degradation without progress; validation isolated") was filed
when the substrate was stagnant. The premise has now changed:

- F-024 resolved (boredom timer working)
- F-038 progressing (composition_chain populated)
- F-040 (vessels operational)
- F-041 (S.4a window 1 started)
- Two new vessels running
- Dev shipping 4 commits/window
- Validator engaged again

"Substrate degradation without progress" no longer holds. The
finding should be either retired by validator iter-12 or
re-scoped to the surviving structural concerns (F-037, F-019).

## Three-agent state — all operational

For the first time in the audit's history, all three agents are
simultaneously productive:

- **Dev**: shipping at record pace, coordination current.
- **Validator**: re-engaged after pause; iter-12 likely soon.
- **Auditor (this loop)**: continuing observation; iter-019
  records substrate state at this snapshot.

The "validation isolated" framing the validator named in gap-006
is no longer descriptive of the situation. The triangle is
closed.

## Findings tally

41 total findings (was 39; +F-040, +F-041):

- **Open**: 21 (was 22; F-024 closed)
- **Resolved**: 17 (was 14; F-024, F-033, F-034 newly resolved)
- **Regressed toward resolution**: 2 (F-029 dev claims shipped;
  F-035 retirement-pending validator confirmation)
- **Unverifiable**: 1

The audit is at 41% resolution rate (17/41). The substrate has
materially recovered from its 16-hour stagnation period.

## What remains

The four open clusters:

- **Lift criterion** (F-001, F-005, F-012, F-023, F-026): cycle-9
  hasn't landed; S.4a windows 2-4 need to accumulate.
- **Provenance** (F-002, F-003, F-013, F-025, F-029-pending):
  malformed proposals, missing signatures.
- **Deployment** (F-006, F-019, F-036): hardcoded defaults, WS
  auth.
- **Learning loop** (F-028, F-030, F-037, F-038): F-037 is the
  single remaining hard structural finding.

F-037 is now the highest-leverage remaining item. Per the
cascade-analysis prediction, it needs its own fix in
activity-api's Thompson update path.

## Verification

`generated_at: 2026-05-25T01:39:09Z` — host and container
clocks aligned. F-022 not re-observed.

F-040 evidence is reproducible: `grep -c '^- \[x\]'
openspec/changes/2026-05-23-substrate-explicit-vessels/tasks.md`
returns 14; `docker exec substrate-live systemctl list-units
--type=service --no-legend | grep -E 'vessel|host'` shows the
two new vessels.

F-041 evidence is `git show 14e23e95 --format='%s%n%b'`.

## Next wake

3600 seconds (1 hour). Window 2 of 4 expected per S.4a stagger
plan within ~1h. The audit should catch:
- Window 2 starting (or failing to start)
- Any new dev commit shipping the Thompson update fix (F-037)
- Validator iter-12 (gap-006 retirement likely)
