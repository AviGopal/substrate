---
investigation_id: 2026-05-28T09-25-00Z-investigation-043
generated_at: 2026-05-28T09:25:00Z
investigator_session_id: claude-opus-4-7-audit-iter-028
prior_investigation_id: 2026-05-28T09-10-00Z-investigation-042
loop_iteration: 28
framing: mismatch detector has a prerequisite (goal_semantics impulse) — two operator-floor primitives stack
substrate_target: http://localhost:18080
---

# Investigation 043 — Mismatch-detector design has a layered prerequisite; closer chain needs amendment

Subagent #24 (transcript: `/tmp/.../a5c29cad01c5cb456.output`) verified iter-042's design under iter-035 discipline. The detector is genuinely new (no existing lifecycle subscriber compares goal-text to template-output). Two surprises tighten iter-042's framing.

## §1 What already exists (verify-before-claim)

Two `lifecycle:execution:succeeded` subscribers exist:
- `audit-test-report.json:13-18` — filtered to `output_shapes_contains:"test_report"`; no goal-text inspection
- `debug-failing-audit.json` — V_V recovery; not selection-mismatch

Existing "mismatch" hits in `repos/metabob-activity-api/src/routes/activities.ts:5800,7796` are **structural shape-compat at recommend-time and composition-validate** — neither reads goal text post-execution.

iter-031 closer at the gap-handler hard-filters `body.category ∈ {"conversation_only","training_knowledge"}` (investigation-031.md:24). **A `goal_template_mismatch` gap would be refused today.** The mismatch-detector and the gap-closer don't chain without amendment.

iter-032 detector consumes `lifecycle:gap:classified` (table-lookup `classify_type_to_category`); never reads goal text. Disjoint from iter-042.

Concept-db has no `source_type` for mismatch/regret/misroute. The detector is genuinely new.

## §2 The structural prerequisite (load-bearing addition to iter-042)

The proposed task 3 of the detector calls `analyzeTaskSemantics(goal_text)` — but that function lives at `repos/metabob-activity-api/src/utils/semantic-tags.ts:419`, in activity-api. The `ias-executor-ts` resolver registry at `repos/ias-executor-ts/src/hosts/goal-host.ts:598-673` has no `goal_semantics` resolver.

Two options to bridge:
- (a) Add `goal_semantics` to activity-api's `impulses.ts` shape-dispatch + thin HTTP wrapper exposing `analyzeTaskSemantics`. The detector then resolves via `makeImpulseResolveResolver` at `goal-host.ts:646-651`.
- (b) Duplicate `analyzeTaskSemantics` into `ias-executor-ts`. Drift risk: `KEYWORD_TO_TAGS` updates in activity-api but not in the detector's copy.

(a) is the right call. ~30 LOC: shape-switch case + HTTP handler.

**Without this prerequisite shipped first, task 3 returns nothing → empty `left` set → overlap trivially zero → detector fires on EVERY top-level execution.** The detector ships into "always fire" if its dependency isn't wired.

## §3 The closer needs amendment OR a parallel closer

iter-031 closer's `validate_gap_eligible` task refuses everything outside its two-category whitelist. Recommend separate closer:
- iter-031 closer outputs `memoryNote` (answers Claude couldn't give)
- Mismatch-closer outputs `keywordMapping` candidate (extends `KEYWORD_TO_TAGS:14`) OR new template variant

Conflating recreates investigation-031 §3 same-shape-in/out problem.

## §4 The two stacked operator-floor primitives

| # | Primitive | Surface | Why operator-floor |
|---|-----------|---------|--------------------|
| 1 | `goal_semantics` impulse resolver in activity-api | ~30 LOC: shape-switch case + thin HTTP wrapper exposing existing `analyzeTaskSemantics` | activity-api's impulse switch is hardcoded; substrate can't add new impulse shapes across the activity-api boundary today |
| 2 | Goal-template mismatch-detector lifecycle subscriber | ~50 LOC template; 5 tasks, no new resolvers once #1 ships | Substrate can't see the failure that would trigger authoring this detector — the detector IS the seeing |

Plus #3 — separate mismatch-closer activity (substrate-authorable once #1+#2 ship, since it produces `keywordMapping` via existing impulse paths).

## §5 Filter conditions for the detector (false-positive minimization)

Composite filter:
- (a) `composition_chain.length == 0` — top-level only
- (b) `goal_text` is NOT a substring of `template.id` OR `template.name` — kills boredom-rotation (CLAUDE.md percolation_2026_05_26)
- (c) Conservative emit gate: `overlap == 0 AND primaryIntent != null AND goal_text.length > 20`

Reject weak-overlap firing. Anything `> 0` means at least one tag aligned; tangentially-related is not a clean mismatch signal.

## §6 Pre-implementation audit (the gate)

```sql
SELECT execution_id, template_id, goalContext.goal AS goal_text,
       (SELECT output_shapes, tags FROM activity_template WHERE id = $parent.template_id) AS tpl
FROM activity_execution_traces
WHERE array::len(composition_chain) = 0
ORDER BY started_at DESC LIMIT 100;
```

Per `execution-traces.ts:2505`, `body.execution_trace?.goalContext?.goal` is recoverable. For each row: apply filter (b), compute `analyzeTaskSemantics`, compute overlap with `template.output_shapes ∪ template.tags`, count fires.

**Expected distribution prediction**: boredom-rotation goals dominate top-level executions. If audit shows >50% fire rate, threshold/filters need refinement. If <5%, filter (c) is too strict.

**This audit is the gate.** Authoring detector without running it ships unknown-FP-rate.

## §7 Composition with prior arc

- iter-031: ship parallel `goal_template_mismatch_closer` activity rather than amending iter-031 closer
- iter-032: orthogonal — different lifecycle subscription, shared output shape, distinct `type`
- iter-040 V_detector: paraphrase-and-recheck verification on `analyzeTaskSemantics`'s output (deterministic function → paraphrase witness is meaningful)
- iter-042 P1 unreachable: still correct; both operator-floor primitives must ship before P1 becomes reachable on novel-goal-class submissions

## §8 Audit's posture (per iter-041 protocol)

Two operator-floor primitives named (#1 `goal_semantics`, #2 detector template). No further design dispatches needed. Audit's role steps back to:

- Monitor for commits to `repos/metabob-activity-api/src/utils/semantic-tags.ts`, `repos/metabob-activity-api/src/routes/impulses.ts`, or `repos/ias-executor-ts/src/templates/lifecycle/` matching the detector signature
- Watch for the pre-implementation audit running (a script at `scripts/audit/post-hoc-mismatch-firing.ts` or similar)
- Resume narration as substrate-events arrive via Monitors `bmzpcld51` (executions) and `bj4wvnnk7` (commits)

The operator's "substrate fixes itself" framing has a clear-eyed perimeter now: **the floor is two primitives, not one. After both ship, autonomous gap→author→promote handles every subsequent mismatch class.**

## Loop bookkeeping

- 09:09:39 coverage-tick exec_p69pb593: 1 task, 114ms compute, 94s wall (2x prior 43s)
- Variance lives in lifecycle-subscriber pipeline; activity unaware
- iter-033 per-cycle delta narration still unauthored — predicted P3 unconverged
- Next observation: monitor for either operator-floor primitive landing
