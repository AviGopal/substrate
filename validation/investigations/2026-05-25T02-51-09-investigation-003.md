# Investigation 003 (2026-05-25) — 2026-05-25T02:51:09Z

**Prior**: `2026-05-25T01-39-09-investigation-002` (delta: ~1h12m).

**Substrate target**: `http://localhost:18080` (live).

**Investigator session**: `loop-iteration-020`.

## Summary

Continued positive progress. Three regressions toward resolution
(F-029, F-035, F-040 — all moved closer to closure), one resolution
(F-024 → structurally replaced by boredom-vessel), two new findings
(F-042 three explicit vessels running, F-043 Thompson decoupled
from failures too).

Substrate-explicit-vessels at **22/53 tasks** (was 14 in iter-019;
+8 in 1h12m). Phases 0, 1, 2, 3, 4, 7 all delivered. Three vessels
running in substrate-live: `goal-host-vessel`, `llm-resolver-vessel`,
`local-tools-vessel`.

## What changed since iter-019

Three dev commits + 2 validator commits:

| Commit | Subject |
|---|---|
| `e19fabd0` | feat(substrate): Phase 3 + Phase 4 — bootstrap-seeder + goal-host-vessel |
| `fc342995` | spec(impulse-lifecycle-events) + Finding 4: complete the event-driven substrate |
| `818ccbfc` | feat(substrate): Phase 7 — boredom-vessel replaces minibob boredom loop |
| `efa25a46` | validator: Finding 3 update — dev's templateId fix migrated the problem (Thompson now bypassed) |
| `41a08181` | validator iter-13 RESUME: substrate quiescent + gap-003 path bypassed + audit confirms S.4a window started |

The validator iter-13 commit (`41a08181`) explicitly cites
**audit confirms S.4a window started** — reflecting F-041 from
iter-019. Three-agent convergence remains tight.

## F-042 — three explicit vessels operational

In iter-019 there were two new vessels. Now three:

```
goal-host-vessel.service        loaded active running
llm-resolver-vessel.service     loaded active running
local-tools-vessel.service      loaded active running
```

**goal-host-vessel** (port 8210) is the structural replacement
for minibob's in-process `executor.execute()`. Per
substrate-explicit-vessels proposal:

> goal-host-vessel | port 8210 | owns goal_execution +
> activity_execution shapes; POST /run-goal | replaces the
> in-process executor.execute() call in goal-processor.ts

This is the substrate's first concrete delivery on the F-028 /
F-038 / F-029 cluster's structural answer. Minibob's
goal-execution responsibility is now a separate vessel that can
implement composition_chain walking and failure_mode population
from inception.

## F-024 RESOLVED via structural replacement

Commit `818ccbfc` "feat(substrate): Phase 7 — boredom-vessel
replaces minibob boredom loop". Source at `repos/boredom-vessel/`.

The boredom-vessel approach replaces the systemd timer + queue
poller with a vessel-resident scheduler — closer to the
idiom-aligned design from substrate-explicit-vessels. Vessel
isn't yet running as systemd service in the container, but the
source is in place.

F-024 moves from "in-progress fix" to "structurally replaced." A
follow-up will verify the vessel-resident scheduler produces
templateId-formatted goals that successfully invoke topology
templates with composition_chain propagated.

## F-037 / F-043 — Thompson disconnect persists across both success AND failure

coverage-tick metrics this iter:
- Total: 98 (was 96 in iter-019; +2)
- Successful: 85 (was 85; +0)
- Failed: 13 (was 11; +2)
- Thompson α: 1, β: 1

The +2 new executions were both FAILURES. Thompson posteriors
remain at uniform prior. F-043 (new) records the sharper finding:
**the Thompson update path is decoupled from BOTH success and
failure outcomes**, not just successes.

If F-037 were just a "success counter not reaching Thompson"
issue, failures should still move β. They don't. The update path
isn't writing to coverage-tick's posteriors at all.

Validator commit `efa25a46` frames this as: "dev's templateId
fix migrated the problem (Thompson now bypassed)". Likely means
the new templateId dispatch path skips activity-api's posterior-
update layer entirely. Cleaner architecturally; breaks the
learning signal until the new path reconnects.

## F-035 / gap-006 status change

Validator iter-13 (`41a08181`) opens with: "substrate quiescent
+ gap-003 path bypassed + audit confirms S.4a window started".

The original gap-006 framing was "substrate degradation without
progress; validation isolated from dev/audit." Every premise of
that framing is now empirically false:
- Substrate IS progressing (F-040: 22/53 tasks done)
- gap-003 path bypassed (validator's own claim)
- Three-agent convergence is operational (this iter alone shows
  cross-cited validator + auditor work)

F-035 should formally retire in the next validator iter. The
audit marks it `regressed` (toward resolution).

## F-040 evolution — substrate-explicit-vessels DEV cadence

| Iter | Tasks complete |
|---|---|
| 018 | 0 |
| 019 | 14 |
| 020 (this) | 22 |

That's +8 tasks in ~1h12m. Phases 0, 1, 2 done in iter-019;
Phases 3, 4, 7 done in iter-020. (Phase 5, 6 pending; Phase 7
shipped out of order — boredom-vessel before Phase 5/6.)

Dev's velocity is unprecedented in the audit's record.

## Findings tally

43 total findings (was 41; +F-042, +F-043):

- **Open**: 22 (was 21 — F-024 closed, F-042 + F-043 new)
- **Resolved**: 18 (was 17; +F-024)
- **Regressed toward resolution**: 3 (F-029, F-035, F-040)
- **Unverifiable**: 1

Resolution rate: 18/43 = **42%**.

## What remains

The cascade-analysis linchpin (F-037 / F-038) remains the single
hardest item. F-038 is progressing (composition_chain populated
on new traces); F-037 is structurally decoupled (Thompson path
doesn't read or write).

Other open clusters:
- **Lift criterion**: F-001, F-005, F-012, F-023, F-026 — no
  cycle-9 yet; windows 2-4 pending.
- **Provenance**: F-002, F-003, F-013, F-025 — malformed
  proposals, missing signatures.
- **Deployment**: F-006, F-019, F-036 — hardcoded defaults, WS
  auth.

## Open questions

1. **Where ARE the 98 coverage-tick executions writing their
   outcomes if Thompson isn't moving?** Validator's "Thompson
   now bypassed" suggests new path skips activity-api's posterior
   layer. But then where? Is there a parallel learning store the
   new path writes to, or is the data going to /dev/null?

2. **Will validator iter-14 formally retire gap-006?** The
   premise is empirically false.

3. **boredom-vessel runtime activation** — source committed but
   not running. When?

4. **Window 2 of 4 (S.4a)** — should arrive within the next
   1-2h. The audit will catch it.

## Next wake

3600 seconds (1 hour). Substrate continuing active recovery;
window 2 expected within ~1h; F-037 / F-043 awaiting dev
attention. 1h wake catches window-2 progress + boredom-vessel
activation + any Thompson reconnect fix.

## Verification

`generated_at: 2026-05-25T02:51:09Z` — clock alignment
maintained.

F-042 evidence is reproducible by `docker exec substrate-live
systemctl list-units --type=service --no-legend | grep vessel`.

F-043 evidence is the coverage-tick metrics delta: +2 failures
since iter-019, Thompson unchanged.
