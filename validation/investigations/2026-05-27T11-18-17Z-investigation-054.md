# Investigation 054 — Audit Report

**Generated at:** 2026-05-27T11:18:17Z
**Prior investigation:** 2026-05-27T10-35-05Z-investigation-053
**Auditor:** claude-sonnet-4-6 (independent substrate auditor)

## Commits audited since investigation-053

| SHA | Title | Key observation |
|-----|-------|-----------------|
| 34c55690 | feat(closure): inv-028 C + F-133 fix | super-repo only; ias-executor-ts pointer NOT advanced |
| d42c4fd1 | feat(closure): F-111 closed | super-repo only; ias-executor-ts pointer NOT advanced |
| 29cd8f55 | feat(closure): F-129 FULL + F-135 self-resolved | migration 140, refusal_events table, /refusals/stats |
| d83baee9 | feat(closure): F-134 addressed | promote guard — hallucinated resolvers → 422 |
| 86186cb4 | feat(closure-audit): push-away closure property | closure-audit.ts 7th property, live 7/7 |
| 789bf275 | docs(closure-audit): dev-response-008 | agent-coord update |

---

## Finding status table

| Finding | Severity | Prior status | Current status | Change |
|---------|----------|-------------|----------------|--------|
| F-128 | LOW | open | **OPEN** | no change — counter semantics ambiguous; S.4a still passes by literal criterion |
| F-129 | HIGH | partially_resolved | **RESOLVED with caveat** | measurement surface complete; durable write confirmed live |
| F-130 | HIGH | resolved | **RESOLVED** | no regression |
| F-131 | LOW | open | **OPEN** | no commits address probe provenance |
| F-132 | LOW | resolved | **RESOLVED** | no regression |
| F-133 | LOW | resolved | **RESOLVED** | tasks.md regex updated (34c55690) |
| F-134 | MEDIUM | open | **ADDRESSED** | promote guard implemented; secondary concern F-139 filed |
| F-135 | LOW | open | **RESOLVED** | concept-db restarts confirmed in journalctl (5 events 10:15–11:13Z) |
| F-136 | LOW | open | **RESOLVED/DECLINED** | naming convention confirmed consistent (29 snake_case, 2 protocol camelCase) |
| F-137 | LOW | conditionally_passing | **CONDITIONALLY PASSING** | coverage_progress=true, fraction=0.633 at 11:38Z |
| **F-138** | **MEDIUM** | new | **OPEN** | ias-executor-ts submodule drift — 3 commits not backed to remote |
| **F-139** | **LOW** | new | **OPEN** | promote guard allowlist includes CI resolvers absent from substrate |

---

## Critical finding: F-138 — ias-executor-ts submodule drift

**This is the most significant structural finding in this investigation.**

The super-repo pointer for `repos/ias-executor-ts` is frozen at `49bfb43` (2026-05-26T19:45Z). The submodule contains 3 new commits:

```
b6b58a2 feat(verify-dist-fresh): F-111 closure — detect substrate dist drift vs local src
37a9ddb feat(engine): resolveInputs poll-and-wait — addresses inv-028 C fragility  
161eede feat(adapters): BusForwardingEventSink — engine lifecycle events onto substrate bus
```

Super-repo commits 34c55690 and d42c4fd1 describe these changes but make no pointer advance — they only modify markdown files.

**The functional risk:**

- The async resolveInputs logic (37a9ddb) **IS deployed** in the container dist (`/vessels/ias-executor-ts/dist/engine.js` verified, Modify: 2026-05-27T10:32Z). The feature works.
- `verify-dist-fresh.ts` (b6b58a2) exists locally in `repos/ias-executor-ts/scripts/` but **is NOT in the container** (`/vessels/ias-executor-ts/scripts/` does not exist).
- The remote `git@github.com:AviGopal/ias-executor-ts.git` returns **"Repository not found"** — no remote backup exists for these 3 commits.
- `git status` in the super-repo shows `repos/ias-executor-ts (new commits)` as an unstaged modification.

**Scenario:** If the workspace is reset (`git submodule update --init`), the submodule would revert to 49bfb43. The container dist would still run the newer code, but the source history for 37a9ddb and b6b58a2 would be orphaned in git-reflog only (not permanently recoverable).

**Recommendation:** Mirror the 3 commits to a recoverable location (e.g., push to the super-repo as `packages/ias-executor-ts/` patch files, or create a local git bundle) and advance the super-repo pointer to b6b58a2. Do not run `git submodule update` until mitigated.

---

## F-129 verification — refusal durable write (live probe)

Auditor issued a live probe to confirm end-to-end durable write:

```
POST /v2/activities/recommend
{ "task_description": "inv054 probe test", "expected_output_shapes": ["auditor_probe_shape_xyz_inv054_nonexistent"] }
```

Response:
```json
{
  "fallback_tier": "refused",
  "refusal": {
    "type": "no_producer_for_expected_shapes",
    "candidates_examined": 8,
    "reason": "No template in the full candidate pool emits any of the requested output_shapes. Selector refused rather than falling back to highest-α prior, which would produce a confidently-wrong execution (push-away closure, IAL §27.S.6)."
  }
}
```

Stats before: total=6. After 2s wait: total=7. **Delta=+1 confirmed.** Durable write works.

Cumulative stats at report time: `total=8, by_type={no_producer_for_expected_shapes: 7, hallucinated_resolvers_in_template: 1}`.

**Caveat:** §27.S.6 says S2→S3 is signaled by *sustained measurement over adversarial exposure*, not a single probe. The refusal_events table is 0 days old at the time of this report. The infrastructure is complete; the temporal evidence is not yet accumulated. Operator should evaluate when the window is sufficient.

---

## F-134 verify — promote guard

Implementation confirmed in `repos/metabob-activity-api/src/routes/activities.ts` (lines 3155–3283):

- 21-name `builtInResolvers` set (compose, activity, iteration, llm-prompt, impulse-resolve, validation, helmfile_sync, impulse_pool_selection, producer_selection, impulse_preparation, wire_discovery_registration, wire_auth_blueprint, scaffold_vessel_skeleton, docker_build_push, learning_signal_writer, verify_three_invariants, file_read, bash, llm, activity_recommendation, impulse_cooccurrence)
- Discovery-vessel `/registry/shapes` fetched (107 shapes as of prior investigations)
- Unregistered resolvers → HTTP 422 + bus emit `intervention.refused` + `refusal_events` durable write

**Secondary concern (F-139):** The allowlist includes `helmfile_sync`, `scaffold_vessel_skeleton`, `docker_build_push` — CI/deployment resolvers not available in the single-container substrate. A template using these resolvers would pass promotion but fail execution. Low risk because substrate-authored proposals should not reference these names, and the guard still blocks arbitrary hallucinations.

---

## Substrate state at 11:18Z

| Metric | Value |
|--------|-------|
| health | healthy |
| activity-api | 1.20.9 |
| redis / surrealdb / discovery / embedding | all healthy |
| coverage_progress | true |
| coverage_fraction | 0.633 (19/30 advertised shapes) |
| consecutive_progressing_cycles | 4 |
| refusal_events total | 8 |
| root_file_count | 14 |
| boredom | goal[2] probe-reachable-unlearned completed 10:44Z; goal[3] harness-check-scenario 11:13Z |

---

## S1→S2 gate status

All §27.S.4 gates appear structurally closed:
- inv-028 A (autonomous capability cycle) ✓
- inv-028 B (pattern aggregation) ✓  
- inv-028 C (slot-binding determinism) ✓
- S.4a (coverage_progress from natural activity) ✓ (by literal criterion)

F-128 ambiguity does not block the literal reading of S.4a. Operator should decide whether to mark the tasks.md checkbox complete.

## S2→S3 signal

Push-away infrastructure now complete. 8 events recorded. Sustained adversarial window measurement has just begun. Operator-evaluated, not gate-based.

---

## Next wake recommendation

**7200 seconds (2 hours).** Commit velocity has slowed — last 2 commits are self-documentation. Open items (F-128, F-131, F-138, F-139) are structural/hygiene. S.4a coverage accumulates naturally. Two hours allows meaningful new coverage tick cells to form.
