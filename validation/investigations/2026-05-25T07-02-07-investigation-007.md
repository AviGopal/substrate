---
investigation_id: 2026-05-25T07-02-07-investigation-007
generated_at: 2026-05-25T07:02:07Z
prior: 2026-05-25T06-48-38-investigation-006
loop_iteration: 23
---

# Investigation 007 — Bootstrap Fixes Applied; Boredom Executing But Selecting Wrong Templates

## Substrate State at Investigation Start

- **Health**: healthy (redis, surrealdb, embedding all healthy; discovery unhealthy per activity-api self-check)
- **Container started**: 06:30 UTC (28 minutes ago)
- **Templates**: total=15, returned=2 via list API; 5 topology templates visible via FTS
- **Execution traces**: 4 (all status=failure, duration=0-1ms)
- **Boredom dispatches**: 5 (exec_9wfdsxy6, exec_qq8b52ka, exec_vb19syo9, exec_0rljwk3l, exec_1347dwna), all failed

## Commits Since Prior Investigation

### `60daf3a6` — validation iter-20 narration

The validator's observation, filed before this investigation, recorded:
- Boredom resumed after F-050 block (~06:45Z, 15 min post-startup)
- Named F-051 (instant 1-2ms failures), F-052 (empty composition_chain), F-053 (failure_mode null)
- F-050 declared "partially resolved" without a code commit — correctly observed as self-healing

This investigation refines those observations with direct evidence.

### `9c14b955` — bootstrap fixes + 5/6 closure gates closed

**Critical caveat**: This commit was timestamped 2026-05-24 23:52:51 PDT (06:52 UTC). The running container started at 06:30 UTC — 22 minutes **before** this commit. None of the fixes in 9c14b955 are active in the current container. This is confirmed by `docker exec substrate-live printenv RATE_LIMIT_ALLOWLIST_IPS` returning nothing.

What the commit **claims** to fix (for the NEXT restart, not the current container):
1. **gen-env.sh base64 uppercase**: avoids WEAK_PASSWORD rejection on API key generation
2. **RATE_LIMIT_ALLOWLIST_IPS=127.0.0.1,unknown**: prevents the 16-minute auth storm on startup (F-036)
3. **seed-identity.ts response shape**: handles `{success,data:{key}}` form
4. **development-vessel.service HOST=0.0.0.0**: Docker port mapping works for dev-vessel
5. **Makefile -p 18210:8210**: exposes goal-host-vessel to host for subagent closure gate

**Closure audit result**: 5/6 closed (operator-memory, slash-skills, github-actions, operator-shell, operator-spec-authoring). The **sixth gate — subagents** — remains open because port 18210 is not mapped on the current running container. The audit ran at 06:51:45 UTC and correctly identified this; the Makefile fix resolves it for the next run.

## F-049 Bootstrap-Seeder Race — NOT Fixed

The bootstrap-seeder still failed on this container startup (06:30:16, 14 seconds after start). The 9c14b955 commit **did not fix bootstrap-seeder ordering** — it fixed other issues. The seeder still fires before the org/identity record is established:

```
bootstrap-seeder: Seeding complete: 0 seeded, 13 failed.
Error: Found NONE for field org_id, with record activity:⟨core-activity-audit⟩
```

However — development-vessel successfully seeded all 15 templates at 06:50:39 via its own template-upload mechanism. This is a different path from bootstrap-seeder (development-vessel seeds its own templates; bootstrap-seeder seeds SHARED_TEMPLATES from ias-executor-ts). The 15 total count in the template list reflects development-vessel's successful seeding, NOT bootstrap-seeder. The 13 SHARED_TEMPLATES from ias-executor-ts are still missing.

## F-051/F-052/F-053 — Independent Verification

### F-051: Instant Failures (refined from "1-2ms" to "wrong template selection")

The iter-20 narration described instant failures. Direct evidence from execution traces reveals the underlying cause: **wrong template selection**.

- Boredom goal[2]: "run the full topology discovery chain" → selected `harness-check-scenario`
- Boredom goal[3]: "identify shapes with no known producer" → selected `harness-check-scenario`
- Boredom goal[0]: "measure topology, report coverage" → selected `harness-check-scenario`
- Boredom goal[4]: "check substrate health" → selected `substrate-health-tick` (correct)
- Boredom goal[1]: "probe unlearned shapes" → selected `harness-check-scenario`

The template list API returns only 2 templates (substrate-health-tick and harness-run-matrix) for the caller's scope. `harness-check-scenario` is visible via FTS (`?q=harness`) but NOT in the list. This means the recommendation engine has access to harness-check-scenario (presumably via FTS tier) but is selecting it for semantically unrelated goals. The instant failure (0ms, 0 tasks) suggests harness-check-scenario immediately rejects the goal input.

### F-052: Empty composition_chain — RESOLVED (misclassification)

The iter-20 narration flagged `composition_chain=0` as anomalous. Direct evidence: all 4 boredom traces are top-level executions (no parent). `composition_chain=None` is correct for top-level executions — there is no chain to record. This finding was a misclassification. Resolved.

### F-053: failure_mode null — CONFIRMED as F-029 restatement

All 4 traces have `failure_mode=null` despite `status=failure`. This is the same finding as F-029 (failure_mode never populated on the new execution path). No evidence of a fix.

## F-050 Self-Healing — Timeline Reconstructed

The auth storm's self-healing timeline:
- 06:30: Container starts, all vessels attempt discovery registration → all get 401 (identity-vessel rate limit)
- 06:30-06:45: Boredom attempts fail with "no template id returned" (HTTP 500 from goal-host)
- 06:35-06:42: development-vessel restarts multiple times, eventually registers and seeds
- 06:46:06: After ~16 minutes, rate limit window expires; all vessels register successfully
- 06:45:37: First successful boredom dispatch (status=failed, but no longer HTTP 500)

The shift from "HTTP 500 (no template)" to "dispatched status=failed" happened because development-vessel's templates became available via FTS just as goal-host successfully registered with discovery. The **root cause fix** (RATE_LIMIT_ALLOWLIST_IPS) is committed but not active.

## F-056 — New: escalate-unknown-shape Priority Inversion

A surprise finding: the recommend endpoint returns `escalate-unknown-shape` with `alpha=6, beta=1` as the top recommendation for topology goals. This inflated alpha score likely persists from SurrealDB data across container restarts. With coverage-tick at alpha=1, Thompson sampling overwhelmingly selects escalate-unknown-shape for any goal. This creates a priority inversion — the high-alpha template wins even when semantically inappropriate, starving coverage-tick of any execution opportunities.

This explains why coverage-tick shows `total_executions=0` despite boredom running every 5 minutes. The Thompson learning mechanism is working correctly (highest-alpha wins) but the historical data is stale/incorrect for the current substrate state.

## Status of Prior Open Questions

1. **Will ribosome-vessel register after auth storm?** — Partially. Ribosome connects and registers at 06:53:54 but immediately enters WS reconnect storm (WS closes every second). Discovery shows 4 vessels, not 5+ including ribosome.

2. **When will bootstrap-seeder be fixed?** — Not fixed in 9c14b955. Development-vessel seeded its 15 templates successfully at 06:50:39.

3. **Why total=15 but only 2 returned?** — Confirmed: all 15 templates have `org_id=organizations:substrate` and `public=False`. The external API key's org does not match. Only substrate-health-tick and harness-run-matrix are visible — these may have been seeded with a different API key or have a different public flag at some point.

4. **S.4a at 06:51Z?** — No. Boredom at 06:53:37 and 06:58:44 both failed. S.4a requires `coverage_progress=true` in a coverageReport, which requires coverage-tick to execute. coverage-tick has 0 executions.

## Coverage-Tick State

- alpha=1, beta=1, total_executions=0
- Has never been selected for execution by boredom-vessel
- FTS search finds it; recommend endpoint does not return it (escalate-unknown-shape with alpha=6 wins)
- No pathway to S.4a without fixing template selection (F-051/F-056)

## What 9c14b955 Accomplishes for the NEXT Restart

When the operator rebuilds the container:
1. RATE_LIMIT_ALLOWLIST_IPS active → no 16-minute auth storm on startup
2. seed-identity.ts fix → cleaner org establishment
3. HOST=0.0.0.0 for development-vessel → accessible from host on its port
4. Port 18210 mapped → subagents closure gate closes (6/6)
5. Makefile auto-loads ANTHROPIC_API_KEY → cleaner startup

None of these fix: bootstrap-seeder race (F-049), wrong template selection (F-051), failure_mode null (F-029/F-053), Thompson priority inversion (F-056), ribosome WS reconnect storm (F-020/F-036).

## Critical Unresolved Chain

The substrate's inability to produce `coverageReport` (required for S.4a/S.4b) follows this causal chain:

1. **F-049** (bootstrap-seeder race): 13 SHARED_TEMPLATES from ias-executor-ts unregistered
2. **F-031/F-046** (scope filter): 13 of 15 dev-vessel templates invisible via list API
3. **F-056** (priority inversion): escalate-unknown-shape (alpha=6) wins over coverage-tick (alpha=1)
4. **F-051** (wrong template selection): harness-check-scenario selected for topology goals
5. **F-024** (instant failure): selected template fails with 0 tasks, 0ms duration
6. **F-037** (Thompson bypass): failure does not update coverage-tick's posterior
7. **F-029/F-053** (failure_mode null): failures not classified, no learning signal

Each link in the chain is independently broken. Fixing only one (e.g., restarting container for RATE_LIMIT fix) does not unblock S.4a. The operator must address F-049, F-031, and F-056 before coverage-tick can be selected and executed.

## Findings Tally

- Open: 30 (was 27 in iter-006)
- Resolved: 20 (F-052 resolved — misclassification)
- Regressed: 2 (F-024 still regressed from prior; F-041 still regressed)
- Unverifiable: 1 (F-008)
- New this iteration: 3 (F-054, F-055, F-056)
- Total: 56

## Recommended Next Actions

Priority order for the operator:
1. **Restart container** → activates 9c14b955 fixes (RATE_LIMIT allowlist, seed-identity fix, port mapping)
2. **Mark dev-vessel templates public=true** (or fix scope) → unblocks correct template selection
3. **Fix bootstrap-seeder ordering** → seeds 13 SHARED_TEMPLATES from ias-executor-ts
4. **Reset escalate-unknown-shape posterior** (or check if alpha=6 is valid history)
5. **Fix failure_mode population in goal-host** → closes F-029/F-053
