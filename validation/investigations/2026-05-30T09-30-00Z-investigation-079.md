---
investigation_id: 2026-05-30T09-30-00Z-investigation-079
generated_at: 2026-05-30T09:30:00Z
investigator_session_id: claude-opus-4-7-audit-iter-056
prior_investigation_id: 2026-05-30T09-00-00Z-investigation-078
loop_iteration: 56
framing: subagent #47 F17 detector concrete design — in-process activity-api module, keywordMappingProposal closer, ~90 LOC; substrate-side closure via iter-038 promoter
substrate_target: repos/metabob-activity-api/src/learners/goal-template-mismatch.ts (new)
---

# Investigation 079 — F17 detector concrete design; in-process bus subscriber; keywordMappingProposal closer; substrate-side closure via iter-038

## Subagent #47 verified findings

**`lifecycle:execution:succeeded` payload at `engine.ts:395-412` (verified)**:
```ts
data: {
  executionId, templateId, templateName, status,
  durationMs, costUsd, outputShapes, outputImpulseIds,
  taskCount, parentDepth, compositionChain,
  ...(options.goalContext ? { goalContext: options.goalContext } : {}),
}
```

`goalContext.goal` (engine.ts:26) IS preserved. Populated by `goal-host.ts:747`, `examples/goal-host.ts:558`, `resolvers/activity.ts:98`. F17 IS bus-subscriber-buildable.

**`analyzeTaskSemantics` location verified**: `repos/metabob-activity-api/src/utils/semantic-tags.ts:419-443`. Returns `{tagPrefixes, impliedShapes, primaryIntent, allIntents}` from `KEYWORD_TO_TAGS` map at line 14 (90+ lines of keyword→tag-prefix mappings).

**Critical architectural choice**: `analyzeTaskSemantics` lives in activity-api SAME PROCESS as the bus broadcaster. In-process module decisively wins over activity-template approach:
- Template approach: new `goalSemanticAnalysis` resolver + per-firing RPC
- In-process: direct library call, zero RPC

## Final design (subagent #47's recommendation)

**Placement**: in-process bus-subscriber module at `repos/metabob-activity-api/src/learners/goal-template-mismatch.ts`

**Subscription**: `lifecycle:execution:succeeded` via the bus (the new `WebSocketBroadcaster.subscribe()` hook from iter-078)

**Filter gates** (iter-042 §3 re-verified):
- (a) `data.parentDepth === 0` — top-level only (verified clean at engine.ts:408)
- (b) `!template.id.includes(goalTextNormalized) && !template.name.includes(goalTextNormalized)` — kills boredom goal[N]→matching-template rotation
- (c) `goalText.length > 20 && primaryIntent != null && overlap === 0` — gates on substance + classified intent + zero tag overlap

**Action on fire**:
- Emit `keywordMappingProposal` impulse (new shape) with `{goalText, extractedTokens, templateThatRanInstead, suggestedTagPrefixes}`
- Emit `substrateGap` record per iter-047 framing

**Closer correction strategy: extend KEYWORD_TO_TAGS via autonomous-promoter graduation**

Per iter-066 (substrate-correction = autonomous-VARIANTING). Options evaluated:
| Option | Verdict |
|--------|---------|
| (a) Author replacement template | Substrate doesn't know operator-class template shape per iter-054 |
| (b) Emit keywordMapping candidate | **WIN** — structural minimum |
| (c) interventionRequest | Defers to human |
| (d) Log-only | Defers entirely |

**(b) wins**: structurally minimal — the mismatch IS that goal's keywords don't map to any template's tags. Emit proposal capturing the keyword→tag-prefix candidate. After 3 corroborating samples (iter-038 promotion threshold), autonomous-promoter graduates by extending `KEYWORD_TO_TAGS` via a config-write resolver. Thompson re-routes future identical goals correctly without any new template authored.

**Composability win**: closer composes with existing machinery (iter-038 autonomous-promoter, Thompson re-ranking) rather than introducing a new authoring path.

## Authoring path: (i) operator-authors once

(i) Operator authors detector module → ships. Substrate graduates proposals via iter-038 chain on the closer side. (ii) and (iii) substrate-authored detector paths deferred until empirical demonstration.

The substrate-side closure is real: after ~3 keywordMappingProposal samples, autonomous-promoter graduates without operator intervention. **F17 closes substrate-side once the detector module ships.**

## Composition with prior arc

| Prior | This iter |
|-------|-----------|
| iter-042 design (subagent #22) | RESCOPED: in-process module instead of activity template |
| iter-046 retraction (lifecycle:template:created doesn't exist) | UNCHANGED: F17 uses lifecycle:execution:succeeded which DOES exist |
| iter-066 autonomous-VARIANTING | COMPOSED: closer extends KEYWORD_TO_TAGS, not authors templates |
| iter-077 bus-shipped | COMPOSED: detector subscribes to bus directly (Option A) |
| iter-078 live-observer | INDEPENDENT: F17 doesn't depend on observer's frame Map |
| iter-038 autonomous-promoter | COMPOSED: graduates keywordMappingProposal at ≥3 samples |

## Updated L0+ stack (post-iter-079)

| Item | LOC | Hours | Closes |
|------|-----|-------|--------|
| Live-execution-observer (iter-078) | ~168 | 3-4 | L0 live-state |
| **F17 detector module (iter-079)** | **~90** | **~2** | **F17 routing pollution** |
| F13 proxy-catch | ~10 | 0.5 | F13 silent-success runtime |
| F20 forge resolver registration | ~12 | 0.5 | F20 |
| F21 cost LLMPort fix | ~25 | 1 | F21 cost |
| F23 engine-local Set | ~8 | 0.5 | F23 cross-execution pool |
| origin_layer emission | ~15 | <1 | audit retraction class partial |
| resolver_registry_query | ~36 | 2-3 | F11 verify-claim |

**Total operator-floor: ~364 LOC, ~13h** across 8 primitives. Adds F17 detector (~90 LOC) vs iter-078's ~384.

## Notable: F17 substrate-side closure

After this detector ships, F17 closes via the substrate's existing autonomous-promoter chain:
1. Detector accumulates `keywordMappingProposal` impulses on each operator-goal-misroute
2. iter-038 autonomous-promoter monitors with min_samples=3 threshold (per commit 17ec6803)
3. At 3 samples, promoter graduates the mapping
4. `KEYWORD_TO_TAGS` extended; Thompson re-routes future identical goals

**Substrate-side closure means**: dev ships operator-floor detector once (~90 LOC, ~2h). Substrate autonomously extends keyword mappings forever after. The operator is in the engine and the detector authoring; the substrate inhabits the closer-and-routing layer.

## Audit posture

- Subagent #47 transcript: `/tmp/.../a4089bf09070b07fb.output`
- F17 detector design ready (~90 LOC)
- Substrate-side closure path via iter-038 promoter (no new authoring path needed)
- 10 retractions filed; cite-checker stable
- Findings 13/15/20/21/23 open; F17 closeable via this design + iter-038
- Monitors armed
- Next observation: any commit adding `learners/goal-template-mismatch.ts` OR `keywordMappingProposal` impulse shape

## Loop bookkeeping

- iter-079 design ready for dev implementation
- 4 of 8 audit findings now have concrete substrate-side closure paths (F19 done, F17 designed, F22 closed, F23 + observer scoped)
- Operator-floor LOC stack: ~364 LOC across remaining primitives
