---
investigation_id: 2026-05-28T10-05-00Z-investigation-047
generated_at: 2026-05-28T10:05:00Z
investigator_session_id: claude-opus-4-7-audit-iter-032
prior_investigation_id: 2026-05-28T09-55-00Z-investigation-046
loop_iteration: 32
framing: subagent #25 returned — abstraction collapses to substrateGap; iter-043 §1 retracted; Finding 10 corrective is bootstrap-stuck
substrate_target: http://localhost:18080
---

# Investigation 047 — Subagent #25 returns: collapse to substrateGap; retract iter-043 §1; Finding 10 needs operator bootstrap

## Retraction (iter-043 §1)

iter-043 §1 cited: "iter-031 closer hard-filters `body.category ∈ {conversation_only, training_knowledge}` (investigation-031.md:24)" as a code-fact load-bearing the framing that mismatch-detector gaps would be refused.

**This was design-document-as-code.** Subagent #25's exhaustive grep:

```
grep -rn "validate_gap_eligible" repos/  →  0 hits
ls repos/development-vessel/src/seed/    →  17 templates; no *-gap-closer
```

The filter is in `investigation-031.md` (design), not in shipped code. There is no shipped closer at all. iter-043's structural-blocker reasoning was wrong: the closer doesn't refuse anything because the closer doesn't exist.

This is exactly the verify-before-claim violation iter-035 established the discipline against. The audit failed its own rule. Acknowledged; updating future investigations to grep shipped code, not design docs, for code-facts.

**Knock-on effects:**
- iter-043 §4 ("Closer chain compatibility") — invalid; no closer exists to chain with
- iter-045 §"proposed-discipline enforcer" — sketch is still substrate-authorable in principle, but no closer chain to wire into
- iter-046 L1+L2 sequencing — still correct in shape, but L1 ships as `substrateGap` extension, not new shape

## Decision: collapse to substrateGap

Per subagent #25 §1 + final commit:

**Schema delta** (`repos/development-vessel/src/resolvers/substrate-gap.ts:45-65`):
- Extend `SubstrateGapCategory` enum: add `"invariant_violation"` (operator-curated `invariant_id` rides in `classification_metadata`)
- Add optional `severity?: "low"|"medium"|"high"`
- Add optional `remediation_class?: "substrate_authorable"|"operator_required"|"advisory_only"`

**≤30 LOC. No new shape.** All field needs except severity+remediation_class fit inside the existing `classification_metadata: Record<string,unknown>` slot.

Goodhart mitigation: skip `recommended_fix_shape`. Detectors don't hint at fixes — closers derive fix from `invariant_id` lookup.

## The load-bearing structural change: closer fork

Subagent #25 §4: the change isn't the schema, it's **forking the closer into three remediation-class-specific families**:

| Closer | Consumes | Produces |
|--------|----------|----------|
| `close-knowledge-gap` | `category ∈ {conversation_only, training_knowledge}` | `memoryNote` (iter-031 original design) |
| `close-invariant-breach-substrate-authorable` | `category="invariant_violation"` ∧ `remediation_class="substrate_authorable"` | `activityTemplate` (the substrate-authored corrective) |
| `close-invariant-breach-escalate` | `category="invariant_violation"` ∧ `remediation_class="operator_required"` | `escalationReport` (operator-readable, no substrate-author attempt) |

This is a fork, not an amendment. The contract differs by output shape; conflating recreates the iter-031 §3 same-shape-in/out problem.

## Bootstrap-stuck: Finding 10's corrective

Subagent #25 §6 surfaced a finding the audit missed: **the substrate cannot autonomously author its own `proposed:true` enforcer**.

Reasoning: the corrective for Finding 10 (a substrate-authored template-update activity that subscribes to `lifecycle:template:created` and emits `activityTemplate_update` setting `proposed:true`) — if substrate-authored, would itself land `proposed:false` per Finding 10. The enforcer wouldn't be selection-visible. The boredom-cycle wouldn't pick it. It would never execute. **Self-referential dead-loop.**

Therefore: **Finding 10's first corrective MUST be operator-authored.** Subsequent breaches (Finding 11a, iter-042) compose normally once the first corrective is in place and selectable.

This refines iter-045's framing. The substrate's structural floor isn't just invariant-observability — it's **the first corrective of the proposed-discipline invariant specifically**. Once that ships, the abstraction unlocks downstream.

## Per-finding routing through the abstraction

| Finding | invariant_id | remediation_class | Status |
|---------|-------------|-------------------|--------|
| Finding 10 (proposed bypass) | `iter-028-a/proposed-on-substrate-authored` | `substrate_authorable` (but bootstrap-stuck) | **operator-authored bootstrap required** |
| Finding 11a (resolver hallucination, pre-check) | `resolver-must-exist-in-registry` | `substrate_authorable` (needs `resolver_registry_query` impulse — operator-floor) | latent operator-floor |
| Finding 11b (resolver hallucination, post-feedback) | n/a (Thompson β already handles) | `advisory_only` | no detector needed |
| Finding 8 #1-3 (signature wire-bridge) | `phase-24/signature-on-success-traces` | `operator_required` | escalate-class corrective |
| iter-042 (mismatch-detect) | `operator-goal-routes-to-overlapping-output-shapes` | `substrate_authorable` (after `goal_semantics` ships) | latent operator-floor |

**Of 5 findings, only 1 is end-to-end substrate-authorable today.** Three have latent operator-floor dependencies. One is bootstrap-stuck.

## The cheapest experiment (subagent #25 §7)

**Finding 10, ~1.5 operator-hours, end-to-end:**

1. Amend `substrate-gap.ts:45` — 2 fields + 1 enum value (~10 min)
2. Hand-author `discipline-violation-detector` seed template — 5 tasks (~45 min)
3. Hand-author `close-invariant-breach-substrate-authorable` closer template — 3 tasks (~30 min)
4. Seed via `bun run cli seed-templates` (~5 min)
5. Submit synthetic violation (fake substrate-authored template with `proposed:false`); expect closer to flip `proposed:true` within one boredom cycle (~5 min)

Measurement: (a) breach impulse appears within 30s, (b) closer dispatches, (c) `proposed:true` set on fixture within next boredom tick.

**This experiment bootstraps the abstraction AND closes Finding 10 as side-effect.** If it passes, the next finding (11a or iter-042) requires only +invariant_id-enum + +detector — no schema or closer changes. Per-additional-invariant cost drops to ~45 min.

## Composition with iter-040+042+043+045+046

- iter-040 V_X: each detector ships paired with paraphrase-stability tests; without V_detector the junk-drawer risk materializes immediately
- iter-042 mismatch-detect: confirmed as `substrateGap{category:"invariant_violation", invariant_id:"operator-goal-routes-to-overlapping-output-shapes"}`. Operator-floor unchanged (`goal_semantics` still needed); routing unified.
- iter-043 operator-floor: both items (goal_semantics + mismatch-detector) survive. The unification reduces per-additional-invariant operator cost, not the bootstrap cost.
- iter-045: pattern claim ("invariant-observability is the floor") survives subagent. Specific abstraction sketch (new `invariantBreach` shape) refuted. Replacement: extend `substrateGap`.
- iter-046 L1→L4 layering: L1's design is "extend substrateGap + fork closer + ship Finding-10 bootstrap"; L2 (V_X authoring) unchanged.

## Audit's posture

Three new posture commitments:

1. **Verify-before-claim discipline tightened.** Code-facts must cite shipped code (grep results inline) not design documents. iter-043 violated this; iter-047+ won't.
2. **Bootstrap-stuck class identified.** When a substrate-authorable corrective requires the very invariant it would establish to function, the first instance is operator-floor. Future findings should self-classify for this.
3. **Channel-ready recommendation surfaced.** The 1.5h Finding-10 experiment is operator-actionable. Audit names it once and stops pushing.

## Loop bookkeeping

- iter-043 §1 retracted; iter-046 L1 layer-design updated
- Findings 10+11+12 remain in `openspec/changes/2026-05-23-substrate-explicit-vessels/findings/audit.md` (Finding 12 = 4-parallel-executions-converged-on-same-template; LOW, observation)
- Subagent #25 transcript: `/tmp/.../ae143b679a56c862c.output`
- Monitors `bmzpcld51` + `bj4wvnnk7` armed
- Next observation: any commit touching `substrate-gap.ts:45-65` (the proposed schema delta) OR seed template authored matching `discipline-violation-detector` or `close-invariant-breach-*`
