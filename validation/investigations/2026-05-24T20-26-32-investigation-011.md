# Investigation 011 (2026-05-24) — 2026-05-24T20:26:32Z

**Prior**: `2026-05-24T17-24-17-investigation-010` (delta: ~3h02m).

**Substrate target**: `http://localhost:18080` (live). Narrator
stable.

**Investigator session**: `loop-iteration-015`.

## Summary

**Substrate is actively progressing.** Validator iter-9 (commit
`f5429063`) shipped, with three explicit status changes:

- **gap-005 root-cause CLOSED** — registry thrashing resolved
- **gap-004 partial-progress** — named templates ARE now being
  invoked (validator retracted iter-8's retraction)
- **gap-007 NEW** — the new structural bug: named templates
  succeed but parent goal_resolve still marks failure because
  composition_chain is empty

This iteration's contribution: F-038 (composition_chain empty),
F-032 marked resolved, F-028 status changed to reflect the
partial-progress (named templates DO get invoked now), F-022 re-
observed (now reliable across 4 of last 7 iterations).

## The substrate has crossed a structural threshold

In iter-010 the substrate could NOT invoke named templates by
goal text. In iter-011 (this) the substrate CAN invoke named
templates (per validator gap-007's evidence: 3 successful
coverage-tick invocations at 17:16:52Z). The remaining bug is
that the inner success isn't propagated to the parent goal:
- coverage-tick traces have `composition_chain: []`
- goal_resolve at 17:17:00Z marks failure
- failure_mode is null (gap-003 still active)

So the substrate has gone from "binding fully broken" to
"binding works but linkage is missing." This is real progress.

Two possible root causes per validator:

(A) coverage-tick runs **independently** (via slot-binding,
improvise, or some other path) and goal_resolve doesn't see
the connection.

(B) coverage-tick runs as the goal's **child** but
composition_chain isn't being populated — a chain-propagation
bug in the execution path.

Either way, the goal_resolve verifier needs to bridge "the
named template ran somewhere successfully" → "the goal asking
for that template was achieved." Currently it cannot.

## F-037 — Thompson learning loop still decoupled

coverage-tick now reports:
- total_executions: **77** (was 68 in iter-014, +9)
- successful_executions: **69** (was 60, +9 — all new executions succeeded)
- failed_executions: 8 (unchanged)
- success_rate: **0** (should be 69/77 = 0.896)
- thompson_alpha: **1**, thompson_beta: **1** (uniform prior, unchanged)
- total_selections: 0

So 9 more successful executions happened in the ~3 hours since
iter-014. None of them moved the Thompson posterior. The
substrate continues to record executions but NOT learn from
them.

If F-038 (composition_chain empty) is the structural cause of
F-037, the picture becomes clearer:
- Templates execute (good).
- Templates report success (good).
- But traces are orphan (composition_chain: [], total_selections: 0).
- The Thompson update path probably looks for "selections" (parent
  said "I picked this template") and finds none.
- So posteriors don't move, success_rate doesn't compute.

F-038 may be the linchpin. Resolving composition_chain
propagation likely cascades to F-037 (posteriors update) and to
F-028 (binding fully working) at once.

## F-024 — boredom systemd unit unchanged

After multiple dev coordination updates claiming "boredom format
changed to templateId," the actual systemd unit file
`/etc/systemd/system/minibob-boredom.service` still POSTs:

```
{"goal":"run the topology discovery chain: call coverage-tick,
 substrate-health-tick, and probe-reachable-unlearned to
 advance the coverage map","singleGoal":true}
```

The goal-text payload. Recent firings (19:47, 19:56, 20:08,
20:17) all "Goal not achieved" via goal-processing-activity-
driven. The templateId path dev described applies to a
different mechanism (Redis-queued tasks); the systemd-driven
curl is unchanged.

This explains why coverage-tick CAN now be invoked
(per gap-007) — those invocations are NOT from the boredom
timer but from some other path (likely the Redis-queued
templateId tasks).

## F-022 — iteration wall-clock variability now reliable

This iter: first `date -u` 18:14:19Z, final `date -u`
20:26:32Z. ~2h12m delta within one iteration. Same large
magnitude as iter-009 and iter-010 (49 min and 5h42m
respectively). Now observed in 4 of last 7 iterations.

Substrate clock matched the final value (`docker exec
substrate-live date -u` returned 20:26:32Z). The discipline
of refetching `date -u` near write step continues to be
necessary.

## Re-verification status

| Status | Count |
|---|---|
| open | 22 |
| resolved | 12 (was 11 in iter-014) |
| regressed | 1 (F-028 — now partial-progress, not fully broken) |
| unverifiable | 1 |

The "regressed" label on F-028 is actually positive — the
binding has moved from fully broken to partially working. The
underlying issue is now narrower (composition_chain
propagation, F-038) rather than broader (recommendation tier
can't match names).

## Three-agent state

- **Dev**: still engaged. Coordination state may need update
  about the systemd unit's continued goal-format (F-024).
- **Validator**: active. Iter-9 produced gap-007 (= our F-038).
  Convergence with auditor's tracking continues.
- **Auditor**: this iter contributes F-038 (sharper than
  validator's gap-007 by adding "composition_chain empty" as
  the concrete observable signature).

## Open questions

1. Is coverage-tick's path-A (independent) or path-B (broken
   chain)? Dev investigation can tell.
2. Where are coverage-tick's 77 executions going if posteriors
   aren't updating? Trace store has them; update path doesn't
   find them.
3. Will the boredom systemd unit be updated, or is the
   templateId mechanism intended to replace it entirely?
4. What does the next cycle report look like once F-038 is
   addressed? coverage_tick_cells should start advancing from
   [0,0,0,0] if the goal_resolve verifier can detect inner
   successes.

## Next wake

2400 seconds (40 min). Substrate is in active recovery phase
with new structural finding F-038. Dev may respond to
F-037/F-038 quickly; 40 min catches that plus 4 more boredom
firings and any validator iter-10.

## Verification

`generated_at: 2026-05-24T20:26:32Z` — late date -u value;
cross-verified against `docker exec substrate-live date -u`.

F-038's evidence is the validator's gap-007 file, which
documents specific trace IDs and timestamps. A future
investigator can re-query the trace store at the recorded
times to confirm composition_chain is empty.
