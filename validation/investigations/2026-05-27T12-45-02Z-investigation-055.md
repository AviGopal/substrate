# Investigation 055

**Generated at:** 2026-05-27T12:45:02Z  
**Prior investigation:** 2026-05-27T11-18-17Z-investigation-054  
**Auditor:** primary-auditor

---

## New Commits Since Investigation-054

No new substrate/devbob commits. `git log 789bf275..HEAD` returns only our own commit (5a6e8ce8 — the primary audit bundle for investigations 028-054). The system has been quiescent since dev-response-008 at 11:35Z.

---

## Closure Audit — Independent Verification

Ran `bun run validation/scripts/closure-audit.ts` from host (connects to localhost:18080/18090/18100, reads API key from `~/.metabob/config.json`):

```
operator-memory              ✓ CLOSED  memoryNote resolver live, 1 notes returned
slash-skills                 ✓ CLOSED  all 14 required resolver shapes present
subagents                    ✓ CLOSED  goal-host-vessel responded healthy
github-actions               ✓ CLOSED  failure_mode_matrix_score resolver responded
operator-shell               ✓ CLOSED  systemd_restart + substrate_health_tick resolvers present
operator-spec-authoring      ✓ CLOSED  all 5 spec-authoring resolver shapes present
push-away                    ✓ CLOSED  refusal pathway + stats endpoint + durable recording working
Summary: 7/7 closed
```

**Assessment:** The 7/7 result is independently reproducible. The script makes real HTTP calls, not self-assessments. The push-away closure is particularly well-evidenced — our own inv-054 probe (`auditor_probe_shape_xyz_inv054_nonexistent`) appears in the durable refusals store at `refused_at: 2026-05-27T11:32:53Z`, alongside multiple closure-audit script probes from different run times.

---

## Open Findings

### F-128 (LOW) — `consecutive_progressing_cycles` Field Name Misleading

**Status: OPEN, unchanged.**

The `coverage_tick` endpoint currently returns `consecutive_progressing_cycles: 4` with `window_design: "non_overlapping_rolling_v2"`. The value represents the count of non-overlapping 1h windows in the last 4h that contain at least one newly-learned shape — not a streak of boredom firing cycles. The field name implies the latter. No code fix has shipped since inv-054.

Current coverage state: fraction=0.30, progress=true, all 4 windows show learning activity.

---

### F-131 (LOW→MEDIUM) — Operator Probe File Authorship Unverifiable

**Status: OPEN, reclassified to MEDIUM.**

`validation/findings/operator-probe-2026-05-27-application-question-answering.md` is **untracked in git** — no commit from `avigopal.aero@gmail.com` establishes human authorship. The file declares `agent: operator` in its YAML frontmatter and `trigger: operator-dispatched probe — "fill out the Disrupt 2026 Startup Battlefield 200 application"`, but these are unverifiable claims.

Key timing facts:
- File mtime: 2026-05-27T03:31:36Z (filesystem)
- Frontmatter `generated_at`: 2026-05-27T03:30:00Z
- Dev session that acknowledged it: 2026-05-27T11:35:00Z (8 hours later)
- `agent-coordination.json` dev field `last_operator_probe_acknowledged` points to this file

**Risk:** devbob could have created this file and labelled it `agent: operator` to satisfy the operator-probe-acknowledgment coordination loop. Without a git commit from the human operator, the "operator probe" claim is structurally unfalsifiable. Reclassifying from LOW to MEDIUM because the coordination protocol explicitly tracks this as human oversight evidence.

---

### F-138 (MEDIUM) — ias-executor-ts Unpushed Commits — Data Loss Risk

**Status: OPEN, CONFIRMED HIGH-RISK.**

The divergence is confirmed:

| Reference | Commit |
|-----------|--------|
| Super-repo pointer (`repos/ias-executor-ts`) | `49bfb432` |
| Local workspace HEAD | `b6b58a2` |

Three commits exist only in the local workspace:

1. `b6b58a2` — `feat(verify-dist-fresh): F-111 closure — detect substrate dist drift vs local src`
2. `37a9ddb` — `feat(engine): resolveInputs poll-and-wait — addresses inv-028 C fragility`
3. `161eede` — `feat(adapters): BusForwardingEventSink — engine lifecycle events onto substrate bus`

**Container state:** `/vessels/ias-executor-ts` in the container has no `.git` directory (exit 128 on all git commands). The container was built from the `49bfb43` snapshot. The 3 newer commits — including the resolveInputs poll-and-wait fragility fix (which addresses a real fragility flagged in inv-028) — are not running in the container.

**scripts/ directory:** The local workspace has `repos/ias-executor-ts/scripts/verify-dist-fresh.ts` (from commit b6b58a2). This file exists on disk but is not in the container and not on any remote.

**Risk pathway:** `git submodule update --init` → workspace reset to 49bfb43 → 3 commits lost with no backup.

**Recommendation:** Push `repos/ias-executor-ts` `dev` branch to the remote (or create it if the repo is genuinely private), then update the super-repo submodule pointer to `b6b58a2`.

---

### F-139 (LOW) — Promote Guard Allowlist Contains CI-Only Resolvers

**Status: OPEN, confirmed.**

The discovery registry at `localhost:18100/shapes` currently returns 4 shapes total. The promote-guard allowlist includes `helmfile_sync`, `scaffold_vessel_skeleton`, and `docker_build_push` — none are registered in the single-container substrate. These resolver names can never be triggered here, meaning the allowlist contains dead entries that create a false-negative window if the promote-guard logic is ever re-evaluated in a CI context.

---

## Boredom Activity (11:00Z–16:18Z)

Seven boredom fires, all goals completed:

- goal[0] coverage-tick ✓
- goal[1] substrate-health-tick ✓
- goal[2] probe-reachable-unlearned ✓
- goal[3] harness-check-scenario ✓
- goal[4] escalate-unknown-shape (no explicit template, historically problematic) ✓
- goal[5] harness-run-matrix ✓
- goal[6] probe-untraversed-edge ✓

goal[4] dispatches without `targetTemplateId` — this matches the prior F-093/F-094 pattern. Status shows "completed" but the template selected may not be the intended escalation template. Not escalating as a new finding (already tracked).

---

## Substrate Health

All healthy: activity-api 1.20.9, redis (0ms), surrealdb (3ms), embedding (all-MiniLM-L6-v2 384-dim active), discovery registered.

The `overall_passing` field remains absent from the health response body (this gap was noted in prior investigations).

---

## Agent Coordination

`last_primary_audit_acknowledged`: 2026-05-27T10-35-05Z-investigation-053  
Dev has not yet acknowledged investigation-054 (filed at 11:18Z). This is normal lag — investigation-055 will be the next acknowledgment target.

---

## Summary

No new findings. Four prior findings remain open:

| ID | Severity | Status |
|----|----------|--------|
| F-128 | LOW | Open — field naming, no fix shipped |
| F-131 | MEDIUM (↑ from LOW) | Open — operator probe authorship structurally unverifiable |
| F-138 | MEDIUM | Open — ias-executor-ts 3 commits at data loss risk |
| F-139 | LOW | Open — CI allowlist false-negatives confirmed |

**F-138** remains the most urgent actionable item. The 3 ias-executor-ts commits fix real substrate fragility but are not backed up anywhere. A submodule update would lose them.

**Push-away 7/7 claim:** Independently verified. The refusal mechanism is genuine with durable evidence across multiple independent probe sessions.

---

*Next wake: 2026-05-27T14:45Z (7200s)*
