---
investigation_id: 2026-05-25T10-53-00Z-investigation-019
generated_at: 2026-05-25T10:53:00Z
investigator_session_id: claude-opus-4-7-audit-iter-003
prior_investigation_id: 2026-05-25T10-32-00Z-investigation-018
loop_iteration: 3 (cron-fired tick)
framing: deploy-vs-source drift sweep
substrate_target: http://localhost:18080
---

# Investigation 019 — 20-minute tick: port-fix sticks; F-069 fix unshipped; analysis-vessel committed but not running

Cron-fired audit tick at 10:52Z. ~21 min since iter-018. Boredom cycles ran (F-036 grew from 91→102 cumulative, +11 in window). Two dev commits landed since iter-018; this iteration checks whether they reached running state.

## Verifications from iter-018 carry-over

- **Phantom on host:18260 — still dead.** `ss -tlnp` shows the proxy container listening; `GET http://localhost:18260/health` returns real concept-db (`service:"concept-db",version:"0.1.0"`), not the synthetic phantom response. Port mapping fix sticks.
- **concept-db still empty.** `GET /concepts/search?source_type=vessel_construction_pattern` returns `{"concepts":[],"count":0}`. The `record<concept>` schema-mismatch from iter-018 Finding A (corrected) has not been addressed by dev. F-036 cascade hypothesis (empty concept-db → empty relatedConcepts → synthesis refusal) continues to hold; F-036 stays 10/0 in the last 100 traces, 102 cumulative failures, every one with `duration_ms < 20` and `failure_mode=null`.

## Finding F — F-069 submodule fix landed but is not deployed

Commit `cb4dd022 fix(submodule): bump metabob-activity-api to c1364e4 (F-069 Thompson posterior reset)` is in the super-repo git log but `GET /health` reports activity-api version `1.20.9` — same version observed in iter-015 and iter-017. The submodule pointer moved; the running binary did not.

Current Thompson state vs. expected:

| Template | n | α | expected α (≈n+1) | ratio |
|----------|---|-----|------------------|-------|
| validator-dispatch | 735 | 390 | 736 | 0.53 |
| slot-binding | 100 | 57 | 101 | 0.56 |
| create-shape-provider-goal | 102 | 1 (β=58) | β≈103 | 0.56 (β) |

The ratio is steady ~0.55 across three templates of different sizes. iter-015 reported ~0.27. So *something* improved — possibly an earlier intermediate fix — but the ratio is not 1.0, and the most recent `cb4dd022` submodule bump has not deployed yet.

**Operator-actionable:** rebuild and restart the substrate-live image to pick up the activity-api submodule, OR rerun the in-container `sync-activity-api` (if such a target exists; Makefile has `sync-*` for vessels but I don't see one for activity-api specifically — the activity-api may need a full image rebuild).

## Finding G — analysis-vessel committed but not running

Commit `06bd8c04 feat(analysis-vessel): Phase 1 migration — stateless VesselDaemon replaces metabob-analysis-api`. Makefile (modified in this window) now declares `ANALYSIS_VESSEL_SRC`, `sync-analysis-vessel`, `restart-analysis-vessel`, `logs-analysis-vessel`, and references `analysis-vessel.service` in the `status-live` and `health-live` targets. Discovery registry: `totalVessels: 7, totalShapes: 106` — unchanged from iter-015. `docker exec substrate-live systemctl is-active analysis-vessel.service` returns `inactive` with `Unit analysis-vessel.service could not be found.`

The source migration shipped; the deployment didn't. The Makefile assumes the service unit exists in the container, but the substrate-live image predates the commit. Next `make run-live` (after rebuild) should fix this; until then, the Makefile's new analysis-vessel targets will fail.

**This pattern matches Finding F.** Two dev commits in the last 30 min, neither reached running state in the substrate. The deploy-step is a missing link between commit-time and observability-time.

**Suggested activity:** a `deployment-drift-tick` that compares (a) submodule pointers in HEAD with (b) versions advertised by `/health` endpoints of each vessel and (c) systemd-unit existence inside the substrate. Emits `deploymentDriftReport` when the three diverge. Runs every 10 minutes alongside boredom. Closes the silent gap where commits land but observers (audit, validator) continue measuring the pre-commit binary.

## Delta sweep observations

- New trace activity: F-036 +11 in 21 min (~31/h rate). All `failure_mode=null`.
- No new `coverage-tick` or `substrate-health-tick` traces in this window — but the 100-trace ring may have just cycled past them.
- No new validator-dispatch failure pattern observed.

## Addendum (11:22Z, cron tick #4) — partial deploy: container recreated, F-069 still not shipped

Substrate-live was recreated at ~10:58Z (new container ID `8205d8876c8e`, previously `7c4dca957ce5`). Findings:

- **Port mapping for 18260 is now native** (`docker port substrate-live` confirms `8260/tcp -> 0.0.0.0:18260`). The iter-018 Makefile edit propagated cleanly through recreate. The sidecar `port-proxy-18260` is no longer running — clean state, no leftover proxy. **Closes the iter-018 operator action item on port-mapping.**
- **F-069 fix STILL not deployed.** activity-api `/health` reports `1.20.9` again. The container was rebuilt but the activity-api submodule was not bumped in the image — submodule pointer in super-repo HEAD moved (`cb4dd022`) but the image build did not pick it up. Strengthens Finding F: container-recreate is necessary but not sufficient for deploy; submodule-sync must happen first.
- **analysis-vessel deploy partial.** Service is now `active` (Finding G's prior state was `inactive`). But `[DiscoveryRegistrationLoop] register failed: 401` on startup, then heartbeats 404 indefinitely (failure #23 and counting). Either `METABOB_API_KEY` from `/etc/substrate/env` isn't being read by analysis-vessel, or discovery-vessel doesn't recognize the key for this vessel id. Dev's territory, not audit's — flagging only.
- **No new boredom-fired learning.** create-shape-provider-goal stays 0/11 (was 0/11). concept-db still empty. Schema mismatch from iter-018 Finding A remains the structural blocker.

The cron-tick rhythm is exposing a pattern: dev commits at high cadence (one every ~5-15 min), but deploy lag is hours. The `deployment-drift-tick` activity I sketched in Finding G is the canonical surface for closing the gap. Without it, audit and validator are perpetually measuring 1-3 commits behind HEAD.

## Loop bookkeeping

- iter-018's two operator-actionable items: (a) `kill 836244` — done that cycle; (b) `record<concept>` schema fix — still outstanding.
- This iteration's operator-actionable: (a) rebuild substrate image OR pin a deploy step that picks up submodule changes; (b) note the deploy-drift pattern for future spec work.
- Next cron fire 11:17Z.
