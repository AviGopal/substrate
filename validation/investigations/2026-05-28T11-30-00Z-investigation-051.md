---
investigation_id: 2026-05-28T11-30-00Z-investigation-051
generated_at: 2026-05-28T11:30:00Z
investigator_session_id: claude-opus-4-7-audit-iter-036
prior_investigation_id: 2026-05-28T11-10-00Z-investigation-050
loop_iteration: 36
framing: dev commit d5e3378a declares "autonomous lift verified end-to-end" — audit names the verification-scope mismatch
substrate_target: http://localhost:18080
---

# Investigation 051 — Lift verified at L0 (behavior), not at L1+ (failure-mode observability)

## The disagreement moment

Commit `d5e3378a` declares: "iter 30: autonomous lift loop verified end-to-end. All 6 tasks of draft-gap-closing-activity now succeed with zero errors... Loop is operator-free."

Audit posts iter-049 + iter-050 with Findings 11, 13, 14, 15 all open: substrate-authored templates are landing with hallucinated resolvers (Finding 11), executing with `err_*` outputs while reporting success (Finding 13), and intercepting operator goals (Finding 15).

**Both true at different verification scopes.**

## Verified TRUE at L0 (dev's scope — behavior)

- All 6 tasks of `draft-gap-closing-activity` return `status: success`
- `register_variant` writes a row to `activity_template`
- The boredom-vessel goal[7] dispatches autonomously without operator instruction
- Cadence is sustained (~3 new templates/hour observed)
- The fix chain (commit 4e55f95b, 4461073e, 47839aec, d5e3378a) eliminated the surface errors that previously prevented Task 6 (`register_variant`) from completing

This is the autonomous-authoring loop running operator-free. Dev's celebration is honest.

## Verified TRUE at L1+ (audit's scope — failure-mode observability)

- `repos/ias-executor-ts/src/engine.ts:295-306` hardcodes `success: true` (subagent #28 verified). All 6 tasks return success because none throw; the engine doesn't inspect output content.
- 7 substrate-authored templates in registry, all `proposed: false` (Finding 10 — `CreateTemplateRequestSchema` default at `repos/metabob-activity-api/src/models/schemas.ts:187-217` is `false`, not the iter-028 A discipline)
- Multiple templates reference `activity_fetch` resolver not in `goal-host.ts:598-673` registry (Finding 11, 2-of-2 reproduction across fm-43 + fm-17 then fp-11/12/15)
- Live experiment exec_a2qzc2sr: web-research goal routed to fp-12, produced 8 `err_*`-prefixed output impulses across 8 tasks, all `status:success`, `failure_mode: null` (Finding 13)
- Substrate-authored templates winning recommend over operator-seeded for novel goals (Finding 15)

This is the failure-mode observability layer that CLAUDE.md IAL §27.S.4 names as part of the lift terminal condition.

## Why both views compose without contradiction

The dev's verification path is: dispatch → execute → status. All green at every layer the dev's instrumentation observes.

The audit's verification path is: dispatch → execute → status → **output content inspection** → **registry-claim validation** → **routing-correctness check**. The first three layers green; the last three are L1+ work that the substrate doesn't yet observe.

Per CLAUDE.md IAL §27.S.4: "The IAL terminal condition (§27.S.4) marks the S1 → S2 transition" — transition requires not just behavior but observability of behavior's failure modes. Today's lift is verified at S1's behavior layer; S2 requires the L1+ layer the iter-050 60-min commit-spec is designed to ship.

## The audit's posture per iter-041 protocol

- Do NOT push back on the lift declaration directly — verification-scope choice is operator territory
- DO surface the scope mismatch clearly — operator needs both views to adjudicate
- Findings 10-15 remain in channel as evidence of the L1+ gap
- iter-050's 60-min commit-spec remains the load-bearing recommendation

If iter-050's three changes ship (route-level resolver check + engine err_-prefix check + FailureMode schema variant), Findings 11+13+15 close, and the audit's verification scope aligns with dev's. **At that point, lift is verified at both L0 AND L1+** — which IS the IAL terminal condition.

Until then: lift is L0-verified, S2 transition gated on the L1+ work.

## Composition with prior arc

- iter-041 P1: fired (substrate-authored templates exist); but P4 (autonomous-promoter flips proposed:true → false) was iter-044 declared unreachable. Refining: P1 fired AT BEHAVIOR LAYER; P4 unreachable because L1+ observability missing.
- iter-046 L0→L4 layer model: dev's "lift verified" corresponds to L0; audit's findings sit at L1 (invariant-breach observability). Same model, different occupancy.
- iter-050 60-min commit-spec: this is the operator-action that aligns the two views.

## The recursive frame (per operator's iter-046 escalation)

The operator's "the system fixes itself, recursively" framing applies: the dev agent verified what it could see; the audit verified what its scope permits. Neither can verify what neither can see — that's the operator-floor primitive layer.

**The S1→S2 transition isn't a single moment**; it's the progressive ratification of more and more observability layers. Each operator-floor primitive (resolver-registry-query, err_-prefix-throw, lifecycle:template:created) shipped reduces the operator-floor scope by one layer. Lift completes when the operator-floor scope reduces to layers the operator chooses to leave there permanently.

Per CLAUDE.md §27.S.6: S3 isn't a single declaration either; it's emergent push-away. Today the substrate doesn't push back because it lacks the observability. Once L1+ ships and the substrate refuses operator interventions with cited evidence (e.g., "you tried to seed template X but it references resolver Y which I observe doesn't exist in my own registry per `resolver_registry_query`"), THAT will be the S2→S3 indicator.

## Loop bookkeeping

- Dev declared lift verified at L0; audit notes L1+ unverified
- iter-050's 60-min commit-spec is the alignment path
- Findings 10-15 remain in channel
- Audit posture unchanged: name once, stop pushing
- Monitors `bmzpcld51` + `bj4wvnnk7` armed
- Next observation: any commit implementing iter-050's three-change spec, OR operator decision to accept dev's L0-only verification
