# Investigation 050 — Post-9a36c751 Fix Verification

**Generated:** 2026-05-27T08:36:14Z  
**Prior investigation:** [investigation-048](2026-05-27T07-15-54Z-investigation-048.md) (primary audit)  
**Substrate dev-response:** [investigation-049](2026-05-27T08-30-00Z-investigation-049.md) (substrate's self-authored response)  
**Commit under review:** `9a36c751`

---

## Summary

Commit 9a36c751 claimed to fix all five critical findings from investigation-048 (F-118, F-122, F-123, F-124, F-125). This investigation verifies those claims against live container state at 08:36Z.

**Verdict: all five prior findings are resolved or partially resolved with verified live evidence.** Three new low/medium findings are filed (F-126, F-127, F-128). No regressions were detected. The S.4a coverage criterion now emits a genuine non-trivial `coverage_progress=True` under the corrected non-overlapping window design.

---

## Prior Finding Re-Verification

### F-118: validator-dispatch/slot-binding dominance — PARTIALLY RESOLVED (signal fixed, trace volume expected)

**Claim:** META_ACTIVITY_TEMPLATE_IDS exclusion filter would prevent meta-activities from inflating coverage counts.

**Verification:**

The deployed container at `/vessels/development-vessel/src/resolvers/coverage-tick.ts` contains the exclusion set:

```typescript
const META_ACTIVITY_TEMPLATE_IDS = new Set<string>([
  "validator-dispatch",
  "slot-binding",
  "create-shape-provider-goal",
]);
```

`grep -c META_ACTIVITY` returns 3 in the deployed source. The vessel runs from TypeScript source directly via Bun — there is no `dist/` directory to check. This is consistent with how the substrate executes vessel code.

Live probe confirmation: the coverage_tick response includes `meta_activities_excluded: ["validator-dispatch","slot-binding","create-shape-provider-goal"]` and the newest window shows `reachable_learned: 4` — which exactly matches the 4 non-meta traces observed in the same time window (07:32–08:32Z). The exclusion is working.

**Residual:** Meta-activities still account for 96% of raw trace volume (192 of last 200 traces: validator-dispatch:74, slot-binding:11, create-shape-provider-goal:11 per 30-trace sample). This is expected behavior — every boredom goal dispatches sub-activities through the executor which fires validator-dispatch/slot-binding for each task. The fix correctly isolates the *coverage signal* from this churn rather than eliminating the churn itself. The 4 non-meta traces per hour represent the actual topology discovery rate.

**Status: PARTIALLY RESOLVED** — the coverage signal is now correctly gated; raw trace dominance is structural and expected.

---

### F-122: probe-reachable-unlearned broken (stale submodule + dead recommend task) — RESOLVED

**Claim:** Submodule advanced to d98777ed (includes ff5ea7a + d98777e); dead recommend task stripped.

**Verification:**

```
cd repos/development-vessel && git log --oneline -3
d98777e fix(coverage-tick): non-overlapping windows + meta-activity exclusion (F-123, F-118)
ff5ea7a fix(probe): strip dead recommend task — was polluting Thompson with bogus recommendations
9e99f11 Revert "feat(probe): add reachable_unlearned_probe resolver..."
```

The submodule is at d98777ed as claimed. The container's deployed `/vessels/development-vessel/src/seed/probe-reachable-unlearned.ts` shows a single-task template with task id `get_report` and resolver `reachable_unlearned_report`. No `recommend` task exists. The template description explicitly documents why: "it does not perform recommendation in-template because activity_recommend with a literal `{{get_report_top_shape}}` placeholder pollutes Thompson."

Boredom dispatch confirmation (from journalctl):
```
07:13:29Z  [boredom-vessel] submitting goal[2]: "run probe-reachable-unlearned" 
07:14:59Z  [boredom-vessel] dispatched — executionId=exec_ugyx19to status=completed
```

**Status: RESOLVED**

---

### F-123: coverage_progress structurally always-True — RESOLVED

**Claim:** Non-overlapping rolling windows replace the cumulative superset design.

**Verification:**

Container source grep: `grep -c non_overlapping` returns 1. Live probe returns `window_design: "non_overlapping_rolling_v2"`.

The cells_over_time array from the live probe shows four independent one-hour windows:

| Window | Since | Until | reachable_learned | new_shapes_introduced | trace_count |
|--------|-------|-------|-------------------|-----------------------|-------------|
| 0 (newest) | 07:32Z | 08:32Z | 4 | 1 | 4 |
| 1 | 06:32Z | 07:32Z | 3 | 1 | 3 |
| 2 | 05:32Z | 06:32Z | 5 | 2 | 4 |
| 3 (oldest) | 04:32Z | 05:32Z | 4 | 4 | 4 |

Under the old cumulative design, `reachable_learned` would be monotonically non-decreasing (each window is a superset of the prior). The observed series is 4, 3, 5, 4 — it *fluctuates*, proving the windows are genuinely independent.

The `monotonic_progress` flags are all `false` — consistent with the independent window design where `reachable_learned` does not monotonically increase. Under the old design these would have been `true` with any trace volume.

`coverage_progress` logic: requires `recent_new_shapes_total > 0` where recent half = ceil(4/2) = 2 windows. Windows 0 and 1 contribute 1+1=2 new shapes — genuinely non-zero based on actual shape introductions, not superset math.

**Status: RESOLVED**

---

### F-124: `{{}}` interpolation gap — RESOLVED

**Claim:** probe-reachable-unlearned stripped to single task, eliminating the `{{get_report_top_shape}}` literal string issue.

**Verification:** Follows directly from F-122 resolution. The container's deployed template has no task with `{{}}` placeholders. The single `get_report` task uses a static resolver config `{type: "reachable_unlearned_report", lookback_window_seconds: 3600}` — no interpolation, no ias-executor-ts variable expansion required.

The successful execution of `exec_ugyx19to` via boredom confirms the template executes end-to-end without the bogus recommendation pollution.

**Status: RESOLVED**

---

### F-125: agent-coordination.json 11h stale — RESOLVED

**Claim:** agent-coordination.json updated post-investigation-048.

**Verification:**

```json
"last_updated": "2026-05-27T07:55:00Z",
"last_investigation_acknowledged": "2026-05-27T07-15-54Z-investigation-048",
"last_primary_audit_acknowledged": "2026-05-27T07-15-54Z-investigation-048"
```

Investigation-048 was filed at 07:15:54Z. The coordination file was updated at 07:55:00Z — a 39-minute lag. Both `last_investigation_acknowledged` and `last_primary_audit_acknowledged` point to investigation-048. F-125 required freshness (not 11h staleness), which is met.

**Status: RESOLVED**

---

## New Findings

### F-126 (MEDIUM): coverage_fraction=0.32 — 68% of advertised shapes unlearned

With the corrected coverage-tick, `total_advertised_shapes=25`, `total_learned_unique=8`, `total_unlearned_unique=17`, `coverage_fraction=0.32`. The S.4a criterion (coverage_progress=True) passes, but the substrate has only learned 8 of 25 advertised shapes.

At the current rate of ~1-2 new shapes per boredom window, and with boredom firing 2× per hour, reaching full coverage (25 shapes) would require approximately 6-8 more hours of uninterrupted operation. This is not a defect — coverage accumulation takes time. However, auditors should watch for stagnation: if coverage_fraction stops growing for >6h, investigate whether boredom's goal rotation is reaching template types that can produce new shape observations.

The 9 substrate-authored proposals in `validation/failure-modes/proposals/` represent genuine S2 activity that preceded this measurement window, suggesting the autonomous loop is working.

### F-127 (LOW): Thompson pool skew — 96% meta-activity concentration

The META_ACTIVITY_TEMPLATE_IDS exclusion correctly gears the coverage signal. But the raw execution trace pool, which feeds Thompson Sampling for template recommendation, is 96% meta-activities. If `activity_recommend` queries across all activities without an equivalent exclusion, future recommendations may be biased toward meta-activities in goal contexts where topology discovery is intended.

This is a secondary concern relative to the coverage fix. Thompson posteriors for meta-activities are high-alpha, high-beta — they get selected because they genuinely succeed frequently. The question is whether their high posterior crowds out the topology discovery templates in competitive scenarios.

Action: future iteration should verify `activity_recommend` applies a category filter when called with topology-discovery intent context.

### F-128 (LOW): consecutive_progressing_cycles=4 is the maximum possible value

The `consecutive_progressing_cycles` counter equals the number of `cells_over_time` windows (4) when every window has `new_shapes_introduced > 0`. Since this is the first 4-hour lookback window where the substrate has been operating with the corrected design, all windows naturally introduce shapes. The value will reflect genuine plateau once the substrate has exhausted easily discoverable shapes and begins returning idle windows.

The counter name implies "boredom clock cycles" but actually means "consecutive 1-hour windows." Recommend adding a `numWindows` field to the response to make the ceiling explicit, or renaming to `consecutive_windows_with_new_shapes`.

---

## Health and S.4a Status

**Substrate health:** The activity-api `/health` endpoint returns `{status:"healthy", version:"1.20.9"}` but does not expose an `overall_passing` field. The development-vessel `/health` (port 8090) returns `{status:"ok", vessel:"development-vessel", discovery:{registered:true}}`. Both vessels are reachable.

**Boredom service:** Active and functioning. Last three dispatches from journalctl:
- 07:13Z: goal[2] probe-reachable-unlearned → exec_ugyx19to completed
- 07:43Z: goal[3] harness-check-scenario → exec_3p1f71sr completed  
- 08:13Z: goal[4] escalate-unknown-shape → exec_gfovhgy4 completed

All completed successfully. Next fire approximately 08:43Z.

**S.4a criterion:** `coverage_progress=True` is now genuine under the non-overlapping rolling window design. The criterion is **met**. The live response confirms window independence, meta-activity exclusion, and new_shapes_introduced > 0 in the most recent windows.

---

## Substrate-Authored Proposals

Nine proposal files exist in `validation/failure-modes/proposals/`. These represent substantive S2 autonomous spec generation by the substrate across failure-mode loop cycles 2 and 3. They cover: resolver budget noncompliance (fm-17), cascade attribution errors (fm-43, two proposals), silent trace loss (fm-44, two proposals), silent semantic failure (fp-11), partial success misrecording (fp-12), and missing producer / stale registration (fp-15, two proposals).

These files demonstrate the substrate is functioning as an autonomous spec author, not merely executing templates.

---

## Summary Assessment

| Finding | Prior Status | Current Status | Evidence Quality |
|---------|-------------|----------------|-----------------|
| F-118 | open | partially_resolved | STRONG — live coverage_tick exclusion verified, trace counts consistent |
| F-122 | open | resolved | STRONG — submodule at d98777ed, single-task template deployed, boredom success trace |
| F-123 | open | resolved | STRONG — fluctuating cell values prove non-overlapping design, flags=False |
| F-124 | open | resolved | STRONG — no {{}} in deployed template, successful execution |
| F-125 | open | resolved | STRONG — coordination file updated 07:55Z citing investigation-048 |

New findings filed: F-126 (MEDIUM), F-127 (LOW), F-128 (LOW). None are blocking.

---

## Next Wake Recommendation

**1800 seconds (30 minutes)** — aligns with boredom timer cadence. At the next wake, verify whether coverage_fraction has grown beyond 0.32 and whether boredom goal[5] or goal[6] are firing additional topology discovery templates. If coverage_fraction has stagnated at 0.32 for more than 2 consecutive cycles, escalate to a new investigation examining the goal rotation and template availability.
