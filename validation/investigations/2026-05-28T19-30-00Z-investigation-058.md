---
investigation_id: 2026-05-28T19-30-00Z-investigation-058
generated_at: 2026-05-28T19:30:00Z
investigator_session_id: claude-opus-4-7-audit-iter-043
prior_investigation_id: 2026-05-28T17-00-00Z-investigation-057
loop_iteration: 43
framing: 5th retraction — impulse-body content claims were query-path errors; Finding 13 mechanism stands, evidence-citation was wrong layer
substrate_target: http://localhost:18080
---

# Investigation 058 — 5th retraction (HTTP response confused with impulse body); explicit-input_shapes experiment

## Retraction (impulse-body content claims in iter-054 and iter-057)

I cited the following as "impulse body" content:
- iter-054 exec_himfls9q (try-direct-answer output): `{success:false, error:"use_vessel_discovery", message:"Unknown impulse shape 'impulse'..."}`
- iter-057 exec_s2xb497i (escalate-unknown-shape output): same body
- iter-058 exec_kpjhktb0 (harness-run-matrix output, this turn): same body, queried for `dev:failure_mode_matrix_score_e5heirfe` (which has UNDERSCORE-success suffix)

**The third case is the smoking gun.** The output ID has underscore-success pattern (resolver completed without entering catch). The proxy synthesized a real impulse, not an err_ one. Yet my query of that ID returns the SAME "use_vessel_discovery" structuredError.

**Conclusion**: that body is my OWN query path's error. Activity-api's `POST /v2/impulses/resolve` refuses pointer.type="impulse" — that's a generic shape name, not a registered one. Per the body itself: "The backend only resolves shapes it directly stores (execution traces, templates, metrics)."

iter-054's and iter-057's body citations were all this query-path error, not actual substrate state.

## What this changes and doesn't

**Stands**:
- Finding 13 mechanism: proxy catch at `goal-host-vessel/src/index.ts:359-367` swallows resolver errors and synthesizes degraded impulses. Subagent #29 verified.
- Finding 13 evidence: output IDs with `:err_` colon-infix pattern indicate the catch fired. Output IDs with `_<suffix>` underscore pattern indicate clean execution.
- iter-052 commit-spec for Finding 13: ~10 LOC to remove/restrict the catch.

**Retracted**:
- "The substrate's err_ outputs CONTAIN a 'use_vessel_discovery' error from upstream" — false; the body content was unverified.
- Implicit claim that I knew what was inside those impulses — I never had access to them via my query path.

To actually inspect what the proxy-synthesized impulses contain, the audit would need to either:
- (a) Query goal-host's in-memory store directly (if exposed)
- (b) Add instrumentation to engine.ts to log impulse bodies pre-storage
- (c) Catch the proxy-synthesized impulse via WebSocket subscription to lifecycle:task:completed events

None of these are accessible from the audit's HTTP query position.

## Five retraction pattern

| # | iter | Layer confused |
|---|------|----------------|
| 1 | 043 §1 | Design doc cited as shipped code (filter that doesn't exist) |
| 2 | 046 L1 | Lifecycle event name assumed to exist (`lifecycle:template:created`) |
| 3 | 050 root cause | Surface subagent (engine.ts) vs deeper layer (proxy catch) |
| 4 | 056 output ID | jq-tail read missed `_<suffix>` vs `:err_<suffix>` distinction |
| 5 | 058 impulse body | HTTP response confused with impulse body content |

**Each retraction is the audit confusing the LAYER at which an artifact lives.** The substrate's layered architecture (resolvers → proxy → engine → activity-api → SurrealDB) has many places to mistake-the-layer. The audit's discipline (grep code, char-level inspection, verify-before-claim) has been progressively tightened across the session but has caught misclassifications only retroactively.

**Discipline tightening (5th time)**: when querying any substrate-side artifact (template, execution, impulse), the audit must explicitly identify the layer the response came from. Particularly: HTTP error responses are from the HTTP layer; only successful 200-body content with the expected schema is from the artifact itself.

## Experiment 8 — explicit input_shapes parameter

Goal: "Analyze the most recent execution trace and report on its failure characteristics."
Submitted with: `input_shapes: ["activityExecutionTrace"]`
Routed to: harness-run-matrix (8th experiment, 7th distinct target — first repeat with iter-052)
Output: `dev:failure_mode_matrix_score_e5heirfe` (underscore-success, real execution)

**P1 UNCLEAR**: cannot distinguish whether `input_shapes` parameter triggered Tier 1 (shape-match) recommend path or whether Tier 3 FTS scored harness-run-matrix highest for the goal text. Both paths reach the same target since harness-run-matrix accepts trace-input.

**P2 CONFIRMED**: harness-run-matrix's input_shapes do include trace-like family.

**Cannot determine if input_shapes parameter is a workaround for routing-misclassification** without instrumenting the recommend handler.

## Pattern saturation

Across 8 experiments + 5 axes:
- 7 distinct routing targets (harness-run-matrix appears twice)
- All experiments status:success
- Goal addressed in 0 of 8 cases (ground-truthable for iter-052 file, iter-053 math; substrate's contracts succeed on all)
- Recommender variance robust across all goal axes

Bare-goal experiments now exhausted. Further axes (e.g., goal with explicit Authorization+role, batched goals, goal with explicit endpoint_output_shapes) potentially probe Tier-2 routing details but no longer surface new findings — they refine the routing-mystery, not the substrate-state.

## Audit posture

- 5 retractions this session
- Findings 13+15+17 open
- Channel-ready commit-spec stands (iter-052 ~10 LOC)
- Audit's evidence-citation discipline tightened repeatedly; further tightening unlikely without structural changes (audit running inside substrate, or instrumentation)

## Loop bookkeeping

- exec_kpjhktb0 with underscore-success but same query-path error proves body claims invalid
- 8 experiments completed
- Monitors armed
- Next observation: any commit touching proxy-catch OR recommender selection
