# Investigation 001 (2026-05-25) — 2026-05-25T00:34:24Z

**Prior**: `2026-05-24T23-50-03-investigation-013` (delta: ~44m).

**Substrate target**: `http://localhost:18080` (live).

**Investigator session**: `loop-iteration-018`.

## Summary

**Substantive progress iteration.** Dev shipped 4 commits, validator
shipped 2 (then paused again), and **F-038 has partially resolved**.
The cascade-analysis prediction "F-038 → F-037 may NOT auto-cascade"
is confirmed by direct runtime evidence.

Headlines:

- **F-038 PARTIAL RESOLUTION**: composition_chain IS being populated
  on new ias-executor-routed traces. Recent
  `ribosome-extract` at 2026-05-25T00:32:57Z shows
  `composition_chain: ["aexec_1779669175787_76mpa1",
  "act_1779669175787_m0yu7y"]` with `parent_execution_id` set.
  Dev commit `00aa71e9` (GOAL_RUNTIME=ias-executor activation)
  is the mechanism.

- **F-037 STILL OPEN**: coverage-tick metrics unchanged from
  iter-013 (89 executions, 81 successful, α=β=1). F-038's partial
  resolution did NOT auto-cascade to Thompson update.
  Cascade-analysis prediction validated: F-037 needs its own fix.

- **F-024 IN-PROGRESS RESOLUTION**: Dev shipped commit `f63b0ce9`
  "F-024 durable — boredom timer enqueues templateId tasks every
  6h." Cadence changed from 10min to 6h. No firings observed in
  last 30min, consistent with cadence change.

- **F-039 NEW**: Validator wrote a dev-guidance document
  (`validation/findings/dev-guidance-2026-05-24.md`) mirroring
  the auditor's cascade-analysis approach. Three-agent
  convergence is now bidirectional — both audit and validator
  produce file:line-evidenced guidance for dev.

## Dev's batch since iter-017

Four commits, all addressing substrate-explicit-vessels
prerequisites:

| Commit | Subject |
|---|---|
| `00aa71e9` | fix(substrate): add GOAL_RUNTIME=ias-executor to minibob.service |
| `f63b0ce9` | fix(substrate): F-024 durable — boredom timer enqueues templateId tasks every 6h |
| `6e00f7ae` | fix(substrate): add MINIBOB_BOREDOM_ENABLED=true to minibob.service |
| `67fd745a` | fix(substrate): enable discovery in minibob for vessel resolver routing |

Plus two validator commits:

| Commit | Subject |
|---|---|
| `b3c9d58e` | validator: discovered ias-executor bridge wired but not activated; <10-LOC fix proposed |
| `fee8304f` | validator: actionable dev-guidance document with file:line locations for the linchpin |
| `b59adafb` | validator: loop stopped by operator (2nd time today) |

The validator's `<10-LOC fix proposed` in `b3c9d58e` likely
informed the GOAL_RUNTIME activation in `00aa71e9` (dev's
follow-on). Convergence in action.

## F-038's partial resolution — the runtime evidence

Trace query at 00:32:57Z returned several recent execution
traces. The picture:

- **New ias-executor-routed traces**: composition_chain
  populated. Example:
  ```
  {
    "executed_at": "2026-05-25T00:32:57.442Z",
    "template_id": "ribosome-extract",
    "composition_chain": ["aexec_1779669175787_76mpa1",
                          "act_1779669175787_m0yu7y"],
    "parent_execution_id": "act_1779669175787_m0yu7y",
    "success": true
  }
  ```
- **Legacy paths**: still null. Example:
  ```
  {
    "executed_at": "2026-05-25T00:32:57.574Z",
    "template_id": "startup:health-check",
    "composition_chain": null,
    "parent_execution_id": null,
    "success": true
  }
  ```

Mixed state. The activation of `GOAL_RUNTIME=ias-executor` (commit
`00aa71e9`) routed goal-execution through ias-executor-ts, which
threads composition_chain into ResolverContext (per dev's recent
ias-executor-ts engine work). But legacy dispatch sites in
`repos/minibob/src/activity.ts` (validator identified lines 769,
1304, 1396, 1405, 1513, 2022, 2327, 2372 in their dev-guidance)
continue to skip composition_chain propagation.

Full F-038 closure requires:
- Either all minibob dispatch sites migrate to ias-executor, OR
- Legacy paths in minibob/src/activity.ts populate the chain
  inline.

The substrate-explicit-vessels decomposition would achieve the
former by removing the legacy paths entirely.

## F-037 didn't cascade — cascade-analysis prediction validated

The cascade analysis document predicted:

> If the Thompson update path reads composition_chain (or
> parent_execution_id chain) to attribute outcomes to selections,
> then once F-038 is fixed, F-037 may resolve automatically.

> Don't ship F-038 without running this first.

The prediction held. F-038 partially resolved on new traces.
F-037 unchanged. coverage-tick metrics frozen at 89/81/α=β=1.

This tells us the Thompson update path does NOT consume
composition_chain to attribute outcomes — it's looking somewhere
else (or not reading the trace at all). Per validator's
dev-guidance:

> Where to look (activity-api):
> - repos/metabob-activity-api/src/routes/execution-traces.ts —
>   denormalizeCompositionChain(body.parent_execution_id) is
>   called only if body.parent_execution_id is set.

That's about CHAIN denormalization, not Thompson update. Thompson
update logic lives elsewhere (likely in
`activity-api/src/db/paradigm.ts` per substrate-explicit-vessels
proposal's reference to "Phase 9 implicit vessel"). F-037 is a
separate code change.

## Three-agent convergence has gone bidirectional

Iter-015 noted convergence: auditor finds → validator confirms /
extends. This iter shows the reverse direction too:

- Auditor wrote `validation/cascade-analysis/2026-05-24-linchpin-cascade.md`
  (diagnostic queries + structural mapping).
- Validator wrote `validation/findings/dev-guidance-2026-05-24.md`
  (file:line evidence + closure criteria), citing audit F-037 and
  F-038 directly.

Both documents target dev. The audit document focuses on **what
to look for**; the validator document focuses on **where to
look**. Together they short-circuit the wait-and-discover cycle
the user named.

This is also the user-requested helpful-guidance move applied at
the substrate level. The audit role's contribution this iteration
is the diagnostic verification + cross-reference to existing
spec; the validator's contribution is the file:line dev-guidance.

## What's open

22 open findings. The remaining clusters:

- **Lift criterion** (F-001, F-005, F-012, F-023, F-026, F-035):
  no cycle-9; LIFT CANDIDATE stamp persists on stale data.
- **Provenance** (F-002, F-003, F-013, F-025, F-029): malformed
  proposals + missing failure_mode.
- **Deployment** (F-006, F-019, F-036): hardcoded defaults, WS
  auth.
- **Learning loop**: F-028 (partial), F-030, F-037, F-038
  (partial).
- **Operations**: F-007 (Trailblazing), F-008 (mythic), F-009
  (DEV-zero), F-011 (lift-hardening DEV-zero), F-017 (positive),
  F-020 (WS events), F-021 (concept-db), F-022, F-034 (validator
  paused).

The single most-actionable remaining item: F-037 (Thompson
update path), since F-038 is now partially resolved and the
cascade-analysis prediction is validated.

## Findings tally

39 total findings (38 + new F-039):
- **Open**: 22
- **Resolved**: 14 (was 12 in iter-013 — F-027 carried as
  resolved; F-038 marked regressed because the status changed
  toward partial resolution)
- **Regressed (status changed toward resolution)**: 2 (F-024,
  F-038 — both are in-progress dev fixes)
- **Unverifiable**: 1

Note: this iteration's "regressions" are positive — they reflect
status changes toward resolution, not toward degradation. The
"regressed" label means "status changed since prior" without
specifying direction; in iter-014/iter-016/iter-018 these have
all been positive transitions.

## Next wake

3600 seconds (1 hour). Substrate is partially fixed. Boredom on
6h cadence so no immediate firings to observe. Dev is shipping
at high cadence. 1h wake catches the next dev commit batch
without churning on cadence-shifted boredom state.

## Verification

`generated_at: 2026-05-25T00:34:24Z` — clock-aligned within 1
minute of substrate clock (`docker exec date -u` = 00:34:01Z).
F-022 not re-observed this iter.

F-038's partial resolution evidence is reproducible by querying
the trace store; the contrast between ias-executor-routed
traces (chain populated) and legacy traces (chain null) is
directly observable.
