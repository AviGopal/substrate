---
investigation_id: 2026-05-28T13-10-00Z-investigation-056
generated_at: 2026-05-28T13:10:00Z
investigator_session_id: claude-opus-4-7-audit-iter-041
prior_investigation_id: 2026-05-28T12-40-00Z-investigation-055
loop_iteration: 41
framing: 4th retraction — overclaim about exec_6wjor10p; substrate-aligned goal experiment 5 reveals routing variance + selective Finding 13 trigger
substrate_target: http://localhost:18080
---

# Investigation 056 — 4th retraction (exec_6wjor10p reads); substrate-aligned goal also misroutes

## Retraction (claim that "all templates produce err_ outputs")

I claimed exec_6wjor10p's outputs were err_*-prefixed. **Wrong.** Subagent #29 verified the distinguishing structure:
- Success: `random.id("dev:${shape}")` → `dev:<shape>_<8char>` (underscore-suffix)
- Error: `random.id("dev:${shape}:err")` → `dev:<shape>:err_<8char>` (colon-err-infix)

exec_6wjor10p outputs (literal): `dev:fs_read_7oeiawub`, `dev:fs_read_ctmhnlty`, `dev:llm_completion_dispatch_7je9ak5z`, `dev:json_path_extract_9hr4a9a5`, `dev:fs_write_v5c0mspf`, `dev:activity_create_variant_rmnifb8o`.

**Underscore-suffix pattern. Genuinely successful. Dev's iter-31 claim verified.**

This is the **4th retraction this session**. Pattern across the four:
| # | Retraction | Root cause |
|---|-----------|-----------|
| 1 | iter-043 §1 hard-filter | Cited design doc as code |
| 2 | iter-046 L1 subscription | Cited lifecycle event that doesn't exist |
| 3 | iter-050 root cause layer | Surface subagent missed deeper layer |
| 4 | iter-056 exec_6wjor10p outputs | Misread output ID pattern without char-level inspection |

**Audit discipline tightening (4th time)**: when inspecting trace outputs for the err_-pattern, MUST distinguish `_<suffix>` from `:err_<suffix>` character-by-character. jq-tail patterns are insufficient.

## Experiment 5 — substrate-aligned goal

Goal: "probe reachable unlearned shapes in the registry" (verbatim paraphrase of `probe-reachable-unlearned`'s description, designed to maximize FTS match).

Predictions:
- P1: classifier likely emits something
- P2: routed to `probe-reachable-unlearned` via FTS substring match
- P3: status:success
- P4: routing-mismatch isolated to operator-class goals if P2 fires

Actual:
- Dispatched: `probe-untraversed-edge` (NOT probe-reachable-unlearned)
- All 4 tasks status:success
- All 4 outputs: `dev:fs_read:err_q3g2o6y2`, `dev:json_path_extract:err_5e557kup`, `dev:json_path_extract:err_vi3ydq4i`, `dev:activity_recommend:err_jtrhlhtl`
- All 4 colon-err-infix pattern. All failed silently.

P2 REFUTED. Even a goal text containing the exact words of `probe-reachable-unlearned` routes to `probe-untraversed-edge` instead. **Recommender's FTS scoring doesn't favor name substring matches** — or favors something else more strongly. The exact mechanism is unclear; subagent investigation needed if this matters.

P5 distinguishing test outcome: substrate-aligned goal ALSO misroutes (to sibling probe template) AND ALSO triggers Finding 13 silent-success. **Misroute is not isolated to operator-class goals — substrate-aligned goals fall into it too.**

## Finding 13 still open, refined

The proxy catch at `goal-host-vessel/src/index.ts:359-367` swallows errors **selectively** — when resolvers actually hit a failure path. exec_6wjor10p's resolvers (fs_read on existing files, llm_completion_dispatch on a valid endpoint, json_path_extract on parseable input) all completed without entering the catch. exec_dnneol8r's resolvers all hit failure paths and got swallowed.

Refined claim: Finding 13 triggers on every execution where ≥1 resolver returns an error structuredError or throws. Not on every execution. iter-049's original framing was correct for fp-12's case; my just-revised "ALL templates" claim was wrong.

**Finding 13 still requires the proxy-catch fix per iter-052's commit-spec.** Substrate cannot distinguish "all-tasks-succeeded" from "all-tasks-failed-silently" without that fix.

## What experiment 5 actually shows

| Layer | Status |
|-------|--------|
| Substrate-authored templates with valid resolvers (post-commit a5e549f9) | Genuinely execute (exec_6wjor10p verified) |
| Goal routing for substrate-aligned text | Still misroutes (probe-untraversed-edge selected over probe-reachable-unlearned for goal containing "probe reachable unlearned") |
| Runtime silent-success on resolver-error path | Still active (exec_dnneol8r 4-of-4 err_ outputs) |
| Finding 13 fix urgency | Unchanged — proxy catch is load-bearing |

## Pattern: 5 experiments, 5 distinct outcomes

| # | Goal | Routed to | Success type |
|---|------|-----------|--------------|
| 49 | web-research | fp-12 | err_ (pre-prompt-fix, hallucinated resolvers) |
| 52 | file-creation | harness-run-matrix | underscore-success but file not created |
| 53 | math (Fibonacci) | probe-reachable-unlearned | underscore-success but no answer |
| 54 | security-audit (classifier-HIT) | try-direct-answer | err_ (structuredError swallowed) |
| 56 | substrate-aligned (probe variant) | probe-untraversed-edge | err_ (resolver failures) |

Five distinct routing targets across five attempts. **No routing-correctness signal across the experiments**: substrate routes operator goals to whatever it routes to, executes whatever the chosen template does, reports success regardless of whether the goal was addressed or the runtime hit errors.

The audit's prediction-protocol now confirms: ANY goal submitted by the operator gets a dispatched template, a success report, and an output impulse. The success report is meaningful only when output ID has `_<suffix>` (genuinely ran); meaningless when it has `:err_<suffix>` (silently failed). Operator cannot distinguish without parsing impulse IDs character-by-character.

## Audit posture

- 4 retractions this session — discipline tightening (4th time)
- Findings 10+11 verified closed (iter-055)
- Findings 13+15+17 open, Finding 13 still load-bearing
- Bare-goal experiments now genuinely zero marginal value — pattern fully characterized
- Next-most-informative work: subagent investigation of WHY the recommender variance produces 5 distinct targets, OR observation of Finding 13 closure

## Loop bookkeeping

- 5 experiments completed; routing variance fully observed
- exec_dnneol8r confirms Finding 13 still active
- Substrate continues firing boredom rotation (multiple execs since experiment)
- Monitors armed
- Next observation: any commit touching proxy-catch at `goal-host-vessel/src/index.ts:359-367` OR recommender selection logic
