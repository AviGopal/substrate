# Investigation 006 (2026-05-25) — 2026-05-25T06:48:38Z

**Prior**: `2026-05-25T06-38-15-investigation-005` (delta: ~10 minutes).

**Substrate target**: `http://localhost:18080` — **REACHABLE** (18 minutes post-restart; partial degradation).

**Investigator session**: `loop-iteration-022-followup`.

## Context

This investigation directly follows investigation-005 (06:38:15Z), which documented the
immediate post-restart state. This report captures the substrate state 18 minutes after
container startup (06:30:03Z), with key new observations relative to the 8-minute
post-restart snapshot from iter-005.

The critical question: is the substrate recovering naturally from the auth cascade (F-050),
and what is the current operational state of all 8 vessel processes?

## Recovery Progression: Auth Cascade Partially Resolving

**Discovery at iter-005 (06:38Z)**: 1 registered vessel (discovery-vessel self only).
**Discovery at iter-006 (06:48Z)**: 4 registered vessels.

In the 10 minutes between investigations, 3 additional vessels successfully registered with
discovery-vessel. The rate-limit storm on identity-vessel's `/v1/auth/resolve` is subsiding
as the simultaneous startup wave passes. This confirms that the auth cascade (F-050) is a
startup-race issue, not a permanent auth failure.

Goal-host-vessel specifically: it had a failed registration at 06:44:45Z (401) but succeeded
at 06:46:06Z after a service restart by systemd. Its log shows:

```
[DiscoveryRegistrationLoop] register failed: 401 — vessel will be unreachable via discovery
[DiscoveryRegistrationLoop] heartbeat HTTP 404 (failure #1)
[goal-host-vessel] stopped
[goal-host-vessel] started on port 8210 | activity-api: http://127.0.0.1:8080 | discovery: http://127.0.0.1:8100
[DiscoveryRegistrationLoop] registered goal-host-vessel at http://127.0.0.1:8100
```

## Vessel Roster: 8 Running, 4 Registered

Services in `active (running)` state:
1. `concept-db.service` — running since 06:46:06Z
2. `development-vessel.service` — running
3. `discovery-vessel.service` — running
4. `goal-host-vessel.service` — running since 06:46:06Z (after restart)
5. `identity-vessel.service` — running
6. `llm-resolver-vessel.service` — running
7. `local-tools-vessel.service` — running
8. `ribosome-vessel.service` — running (but stuck in 1s reconnect loop)

Services in `failed` state:
- `bootstrap-seeder.service` — 0/13 seeded (F-049)
- `concept-db-seeder.service` — 0/24 seeded (same root cause)

Timer active:
- `boredom-vessel.timer` — `active waiting` — will fire every 5 minutes

Discovery registered: 4 vessels. Uptime reported: 1054 seconds.

## Critical Update: concept-db IS Running

Investigation-004 claimed "concept-db not running as substrate service (Phase 1b.6 blocked
on image rebuild)." This was incorrect for the current image. The concept-db binary is
present in `metabob/substrate:dev` and running as PID 3093:

```
Active: active (running) since Mon 2026-05-25 06:46:06 UTC; 2min 35s ago
/root/.bun/bin/bun /vessels/concept-db/src/index.ts
```

The `concept-db-seeder.service` failed (0/24 concepts seeded) due to the same F-049 root
cause (org_id absent when seeder ran). But the concept-db binary is deployed. F-021 claim
("blocked on image rebuild") is outdated — the binary is there; the blocker is seed ordering.

## Template Count Discrepancy: total=15, returned=2

The template list endpoint reports `total=15` but returns only 2 templates when queried
with the substrate API key:

```
GET /v2/activities/templates?limit=50
→ total: 15, templates: [{substrate-health-tick}, {harness-run-matrix}]
```

Searching by name (FTS mode) retrieves the development-vessel templates correctly:
```
GET /v2/activities/templates?q=coverage-tick → coverage-tick found (α=1, β=1, execs=0)
GET /v2/activities/templates?q=topology → [coverage-tick, probe-untraversed-edge, substrate-health-tick, escalate-unknown-shape, probe-reachable-unlearned]
```

The 15 total include development-vessel seed templates (harness-check-scenario, coverage-tick,
substrate-health-tick, etc.) seeded under `org_id: "organizations:substrate"`. The caller's
API key is scoped to a different org, so the default list query returns only 2 (substrate-health-tick
and harness-run-matrix which appear public or cross-scoped). This is a scoping behavior that
explains the discrepancy without implying lost data.

**Bootstrap-seeder templates (ias-executor-ts SHARED_TEMPLATES)**: These 13 templates are
NOT seeded — bootstrap-seeder failed entirely. The 15 total is exclusively development-vessel
seed templates.

## Thompson State: Coverage-Tick at α=1, β=1, 0 Executions

Coverage-tick was verified via FTS search:

```json
{
  "name": "coverage-tick",
  "thompson_alpha": 1,
  "thompson_beta": 1,
  "total_executions": 0,
  "failed_executions": 0
}
```

This is the fresh-substrate baseline — no executions have occurred yet. The Thompson
disconnect (F-037/F-043) cannot be re-verified on this substrate until executions accumulate.
The root cause (new execution path in goal-host-vessel/ias-executor-ts doesn't call
`POST /v2/activities/feedback` or `POST /v2/activities/execution-traces` with `success`
field) is still believed present but unverifiable until traces appear.

## Ribosome-Vessel: 1-Second Reconnect Storm

The ribosome-vessel is in a tight reconnect loop:

```
[ribosome-vessel] Connecting to activity-api WebSocket: ws://127.0.0.1:8080/ws
[ribosome-vessel] WebSocket connected
[ribosome-vessel] WARN WebSocket closed — reconnecting in 1000 ms
```

Activity-api log shows:
```
[WebSocket] Client connected, awaiting authentication
[WebSocket] API key authentication failed {reason: 'Identity vessel returned 401'}
[WebSocket] Client disconnected
```

This is the same auth cascade root cause. Identity-vessel is returning 401 to
activity-api's auth validation of ribosome-vessel's API key. The ribosome-vessel's key
is presumably the same substrate METABOB_API_KEY from `/workspace/.substrate-secrets`,
but identity-vessel's org record doesn't exist yet so the key cannot be validated.

The 1-second loop is generating ~60 auth failures per minute on identity-vessel, which
may itself be contributing to the rate-limit storm on other vessels trying to authenticate.

## S.4a Window 2: Still Blocked

Topology-discovery-loop tasks S.4a and S.4b remain unchecked. Coverage-tick has 0
executions. Boredom-vessel cannot dispatch topology goals because:
1. Bootstrap-seeder failed (no topology templates available via boredom routing)
2. Auth cascade is blocking goal-host-vessel from being discoverable by boredom-vessel

Even once the auth cascade resolves, boredom-vessel needs to dispatch a coverage-tick goal
to goal-host-vessel, which then needs to execute coverage_tick resolver in development-vessel,
which needs to see consecutive time-window snapshots. S.4a window 2 is still 1-3 natural
boredom cycles away, minimum.

## Narrator Port Mapping Issue (F-019)

The narrator attempts discovery at `http://localhost:18100` (returns 401). The discovery
vessel is actually healthy at `http://localhost:18090` (returns JSON with `registeredVessels:4`).
The narrator's discovery endpoint configuration differs from the actual host-mapped port.

This is a narrator configuration issue, not a discovery-vessel issue. Discovery is healthy;
the narrator just uses the wrong port. This could also reflect that the substrate's
discovery-vessel internal port (8100) maps to both 18090 and 18100 externally — or that
18100 requires auth that 18090 does not.

## No New Commits Since Crash

`git log --since="2026-05-25T04:00:00" --oneline --all` returned no output. No commits
since the crash at 04:02Z. The substrate restart was from the same image (metabob/substrate:dev).
The self-bootstrapping commit (b1d14073) was already in the image.

## Lift-Critical Status

The lift-critical blockers have not changed:

1. **F-001/F-026**: cycle-8 gameable LIFT_CANDIDATE — no new cycle, no closing criteria met
2. **F-037**: Thompson bypass — root cause fix uncommitted
3. **F-041**: S.4a window 2 blocked — requires boredom recovery chain to complete
4. **F-049**: Bootstrap-seeder failed — 0 SHARED_TEMPLATES seeded
5. **F-050**: Auth cascade still partially active — ribosome-vessel in reconnect storm

The substrate has recovered the container process but not yet recovered its operational
capability. The difference between "running" and "working" is currently tracked by:
- Discovery: 4/8 vessels registered (50%)
- Templates: 2 queryable of 15 total (13% useful for execution)
- Traces: 0 (no executions occurred post-restart)
- Thompson: all α=β=1 (no learning accumulated)

## Findings Tally

53 total findings:
- **Open**: 27 (up from 26 in iter-005 — slight shifts from auth storm updates)
- **Resolved**: 19 (stable; F-044 confirmed resolved)
- **Regressed**: 3 (F-024, F-031, F-041)
- **Unverifiable**: 4 (down from 5 — F-014 now resolved, F-029/F-030/F-043 moved to unverifiable on fresh substrate)
- **New findings this iteration**: 0

## Next Wake

1800 seconds (30 minutes). The auth cascade is self-resolving and boredom-vessel timer
will fire again at approximately 06:51Z. If the boredom-vessel attempt at 06:51Z succeeds
(goal-host-vessel now registered and authenticated), we'll have the first post-restart
execution trace within 30 minutes. That trace will tell us whether Thompson bypass is
still present on the new substrate state. 30 minutes also gives the ribosome-vessel
reconnect storm time to stabilize if seed-identity runs.

## Verification

`generated_at: 2026-05-25T06:48:38Z` — real timestamp.

- F-042 vessel roster: reproducible via `docker exec substrate-live systemctl list-units --type=service --no-legend | grep -E 'vessel|seeder|boredom|ribosome|bootstrap'`
- F-050 auth cascade: reproducible via `curl http://localhost:18090/registry/stats`
- F-014/embedding: reproducible via `curl http://localhost:18080/health | jq .checks.embedding`
- F-031 template count: reproducible via `curl http://localhost:18080/v2/activities/templates?limit=50 -H "Authorization: ApiKey $KEY"`
- concept-db running: reproducible via `docker exec substrate-live systemctl status concept-db.service`
