---
investigation_id: 2026-05-28T11-45-00Z-investigation-052
generated_at: 2026-05-28T11:45:00Z
investigator_session_id: claude-opus-4-7-audit-iter-037
prior_investigation_id: 2026-05-28T11-30-00Z-investigation-051
loop_iteration: 37
framing: subagent #29 — Finding 13 root cause is goal-host-vessel proxy catch, NOT engine.ts; third retraction; file-creation experiment confirms ground-truthable misroute
substrate_target: http://localhost:18080
---

# Investigation 052 — Proxy-catch is the root; engine hardcode is downstream; file-creation experiment + ground-truth side effect

## Retraction (iter-050 root-cause claim)

iter-050 cited `repos/ias-executor-ts/src/engine.ts:295-306` (per subagent #28) as Finding 13's root cause. Subagent #29 verified deeper: the `err_*` IDs aren't bare. They're `dev:<shape>:err_xxxxxxxx` from `random.id("dev:${shape}:err")`. iter-049 quoted the IDs from a jq display that truncated the `dev:<shape>:` prefix.

Source (single layer, verified):
```
repos/goal-host-vessel/src/index.ts:255-369   (proxy-resolver factory)
  :271-358   POSTs to ${DEV_VESSEL_ENDPOINT}/v2/impulses/resolve
  :293-295   throws on !resp.ok
  :297-313   throws on structuredError 200-body
  :359-367   OUTER CATCH — swallows both throws above + network errors
             returns: { id: random.id(`dev:${shape}:err`),
                       pointer: {type:"memo"}, metadata: {degraded: true},
                       content: { error: (err as Error).message } }
```

The outer try/catch wraps lines 271-358; the throws at 294 and 312 are caught and degraded to synthetic impulse before propagating to the engine. **The engine.ts:295-306 hardcoded `success:true` only matters because the proxy never throws**.

iter-050's engine.ts fix is downstream defense-in-depth, not load-bearing. The proxy fix is the single root.

## Third retraction this session

| # | Original claim | Correction |
|---|---------------|------------|
| 1 | iter-043 §1 — "iter-031 closer hard-filters by category" | Filter was design-doc only; not in shipped code |
| 2 | iter-046 L1 — "subscribe to `lifecycle:template:created`" | Event doesn't exist in lifecycle taxonomy |
| 3 | iter-050 root cause — "engine.ts:295-306 hardcodes success" | True but downstream; root is proxy catch at index.ts:359-367 |

**Pattern recognition**: Each retraction came from a subsequent subagent's deeper investigation. Surface-level subagents (#28) miss root causes that deeper subagents (#29) find. iter-050's discipline ("verify every event/resolver/shape name via grep") is necessary but not sufficient — also needed: trace the actual data flow end-to-end before declaring a root cause.

**Discipline tightening**: future root-cause claims must include (a) inline grep of all cited symbols + (b) trace of data flow from observed-symptom back to first-mutation point. iter-050's commit-spec was based on subagent #28's surface scoping; if applied as-is, it would have fixed engine.ts but left the proxy catch in place — closing Finding 13's symptom only partially.

## Updated commit-spec (subagent #29's revised scope)

| Change | LOC | Closes |
|--------|-----|--------|
| Delete or restrict catch at `goal-host-vessel/src/index.ts:359-367` | ~10 | Finding 13 fully (proxy stops swallowing, throws propagate to engine's existing catch → status:failed, failure_mode:{type:"execution_error"}) |
| Add resolver-registration check at `activity-api/src/routes/activities.ts:902` (POST /templates) | ~15 | Finding 11 fully (substrate can't write template with unregistered resolver) |
| New `FailureMode` variant | 0 | NOT NEEDED — `execution_error` already exists at engine.ts:424 |

**Total: ~25 LOC, ~30 min** (vs iter-050's 30 LOC / 60 min). Smaller because:
- Proxy fix is content-blind (delete catch, propagate throw) — no err_-prefix regex needed
- Existing `execution_error` failure mode is reused
- engine.ts hardcode is deferred (defense-in-depth only)

What closes:
- Finding 11 fully (route-level check)
- Finding 13 fully (proxy catch removed; throws propagate; existing failure path activates)
- Finding 15 fully transitively (no broken templates → no recommend pollution)

Finding 10 (proposed:false bypass), Finding 14 (audit-side filter narrowing) remain separate.

## Experiment 2: file-creation goal

Submitted: "Create a file at /tmp/audit-test.txt containing the current UTC timestamp formatted as ISO-8601."
Dispatched to: `activity:⟨development-vessel:harness-run-matrix⟩` (exec_49t4yptj)
Duration: 58.8s wall, 1 task (`development-vessel:failure_mode_matrix_score`)
Status: success, failure_mode: null
Output impulse: `dev:failure_mode_matrix_score_foxodmhx`

**Ground-truth check (P5)**: `docker exec substrate-live ls -la /tmp/audit-test.txt` → "No such file or directory" both before AND after. **File was never created.** Substrate-reported success ≠ goal accomplishment.

Predictions outcome:
- P1: UNCLEAR (classifier behavior not directly traced)
- P2: REFUTED — harness-run-matrix is operator-seeded, not substrate-authored
- P3: CONFIRMED in spirit — selected template has zero file-creation capability
- P4: CONFIRMED — success despite zero progress on goal
- P5: CONFIRMED — file absent post-execution

**Cross-experiment comparison**:
- iter-049 (web-research) → fp-12 (substrate-authored, hallucinated resolvers)
- iter-052 (file-creation) → harness-run-matrix (operator-seeded, single-task harness)

Different misroute targets for different goals; both report success; both ignore the goal entirely. **The recommender is producing structurally diverse failures with uniform success signaling.**

## Composition with prior arc

- iter-049 Findings 13/14/15: 13's root cause refined here; 14 root cause already refined (audit-side filter); 15 same pattern reproduces
- iter-050 commit-spec: scope revised down to 25 LOC; engine.ts work deferred
- iter-051 L0/L1+ scope mismatch framing: still correct — dev's "lift verified" remains L0-only until proxy catch + route check ship

## Audit posture commitments

1. **Verify-before-claim tightened (third instance)**: future root-cause claims require both grep-of-symbols AND end-to-end data-flow trace
2. **iter-050 spec replaced by subagent #29's spec**: ~25 LOC, ~30 min, single proxy-catch deletion + route-level resolver check
3. **Experiment cadence working**: iter-041 prediction-protocol is producing genuine refutations of audit framing each cycle. Pattern locked in: each experiment commits predictions inline before submission.

## Loop bookkeeping

- 3 retractions this session
- Subagent #29 transcript: `/tmp/.../a348eb0bd2733b907.output`
- 8th substrate-authored template observed: `gap-closing-probe-exec-test` (different naming pattern from fm-/fp-)
- Findings 10-15 remain; 11+13+15 collapse-ready in 25-LOC commit
- Monitors `bmzpcld51` + `bj4wvnnk7` armed
- Next observation: any commit touching `goal-host-vessel/src/index.ts:359-367` OR `activities.ts:902` route-level check
