---
investigation_id: 2026-05-29T20-30-00Z-investigation-071
generated_at: 2026-05-29T20:30:00Z
investigator_session_id: claude-opus-4-7-audit-iter-049
prior_investigation_id: 2026-05-29T20-15-00Z-investigation-070
loop_iteration: 49
framing: subagent #38 — extras-bag Phase 1 ~20 LOC closes 4 findings simultaneously; iter-037 line-number citation was wrong (10th retraction)
substrate_target: http://localhost:18080
---

# Investigation 071 — Extras-bag Phase 1 unifies F8/F19/F21-tokens/iter-060; iter-037 signature claim refuted

## 10th retraction

iter-037 claimed engine had signature "in scope at engine.ts:84/537" for Finding 8 framing. Subagent #38 verified: `grep state_space_signature` across `repos/ias-executor-ts/src/` returns **zero hits**. iter-037's line numbers don't exist (file is 617 LOC). The audit's cite was wrong.

**Actual signature production location**: `repos/deployment/vessels/minibob/src/mcp.ts:897-916` (legacy minibob, not in active substrate flow) AND server-side at `repos/metabob-activity-api/src/routes/execution-traces.ts:2387-2397` via `computeStateSpaceSignature(body.input_impulse_shapes)` — already implemented and ready, gated only on receiving `input_impulse_shapes` in the body.

The implication is positive: Finding 8 fix is CHEAPER than estimated because activity-api has the derivation already. Engine just needs to thread `input_impulse_shapes` through (which it has at `engine.ts:338` per `lifecycle:task:completed` event `inputShapes: declaredInputShapeNames`).

## Subagent #38 decision: Extras-bag Phase 1

**Single architectural primitive ~15-20 LOC closes 4 findings**:

```
ontology.ts:153-167:
  ExecutionTrace.metadata?: Record<string, unknown>       // +1 LOC
  ExecutionTaskRecord.metadata?: Record<string, unknown>  // +1 LOC

engine.ts:352-366:
  metadata: {
    input_impulse_shapes: [...new Set(allDeclaredInputShapes)],
    failure_mode_raw: trace.failureMode
  }                                                       // +5 LOC

adapters/activity-api-trace-sink.ts:82-149:
  input_impulse_shapes: trace.metadata?.input_impulse_shapes,
  metadata: trace.metadata                                // +6 LOC

schemas.ts:972-996:
  input_impulse_shapes: z.array(z.string()).optional(),
  metadata: z.record(z.any()).optional()                  // +2 LOC
```

**Simultaneously closes**:
- F8 input_impulse_shapes (extras carries it; sink ships it; route reads it)
- F8 state_space_signature (server-side derivation at execution-traces.ts:2387-2397 already exists)
- F19 failure_mode_raw (subsumes subagent #37's separate template; raw lives in extras)
- iter-060 impulse-body embedding (via `taskRecord.metadata`)
- F21 tokens (once LLMPort fix lands, engine writes to `taskRecord.metadata.tokens`)

## Bonus finding: schema-route divergence

Subagent #38 + #37 both flagged: route at `execution-traces.ts:1618` reads `body.input_impulse_shapes` but schema at `schemas.ts:972-996` doesn't declare it. Latent bug — works only because route reads raw body, not Zod-parsed output. Phase 1 fix incidentally regularizes this.

## Updated L0+ stack (~148 LOC)

| Primitive | LOC | Closes |
|-----------|-----|--------|
| Extras-bag Phase 1 (4-in-1 unified) | ~20 | F8 + F19 + iter-060 + F21-tokens |
| F13 proxy-catch | ~10 | F13 silent-success |
| F20 forge resolver registration | ~12 | F20 |
| F21 cost LLMPort fix | ~25 | F21 cost |
| `resolver_registry_query` | ~36 | F11 verify-claim |
| `origin_layer` emission | ~15 | audit retraction partial |
| `goal_semantics` | ~30 | iter-042 mismatch-detector |

**Total: ~148 LOC** across 7 distinct fixes. Refines iter-070's ~228 LOC estimate via the unified extras-bag primitive.

## Audit retraction count: 10 instances

| # | iter | Layer/claim confused |
|---|------|----------------------|
| 1-9 | (see iter-070 §"Audit retraction pattern") | |
| 10 | 071 | iter-037 line-number citation (`engine.ts:84/537` doesn't exist; signature not computed in engine) |

Pattern continues: audit cites code-fact without verification at cite-time. iter-037 was earlier in session; the cite has been propagating through all iter-060/070 framing of "engine has signature in scope."

**Discipline tightening (10th time)**: when an investigation re-uses a prior investigation's cite (e.g., "per iter-037, engine has X"), re-grep the cite to verify it remains accurate. Cumulative cite-rot is real.

## Composition with prior arc

- iter-037 F8 framing: cite was wrong but conclusion (signature missing from trace) was correct
- iter-060 4-axis bottleneck: 3 of 4 axes correctly attributed in iter-070; F21 cost moved to LLMPort fix in iter-069
- iter-068 F19: subsumed into extras-bag Phase 1
- iter-069 F21: cost gap separate (LLMPort), tokens portion absorbs into extras-bag
- iter-070 ~228 LOC: revised down to ~148 LOC via unified primitive

## Audit posture

- 10 retractions filed
- Extras-bag Phase 1 is the load-bearing channel-ready recommendation
- Findings 13/15/17/19/20/21 open
- Findings 10/11 closed verified (iter-055)
- Next observation: any commit touching `repos/ias-executor-ts/src/ontology.ts:153-167` (extras-bag) OR `repos/metabob-activity-api/src/models/schemas.ts:972-996` (schema extension)

## Loop bookkeeping

- Subagent #38 transcript: `/tmp/.../afadc3157bdf00c3e.output`
- Cumulative audit retractions: 10
- L0+ stack estimate: ~148 LOC, ~5h operator-floor
- Monitors armed
