# Investigation 009 — 2026-05-25T08:52:35Z

**Loop iteration:** 25  
**Prior:** `2026-05-25T08-43-08-investigation-008`  
**Trigger:** SUBSTRATE_DOWN → SUBSTRATE_UP + NEW_COMMITS (e53bae09)

---

## Headline Summary

Container was restarted. `e53bae09` ("feat(goal-host): register development-vessel proxy resolvers at startup") was the commit that landed. This is a **major fix** — goal-host-vessel now fetches shapes from development-vessel at startup and registers 30 proxy resolvers. The direct consequence: **topology templates are now being selected and executed for the first time** (coverage-tick: execs=3, was 0 in iter-008). However, all topology template executions are failing (success_rate=0). S.4a is closer but not yet achieved.

---

## Substrate State

- Container uptime: ~6 minutes at investigation time (started 08:46:51Z)
- Substrate version: 1.20.9  
- Discovery: 5 vessels, 28 shapes, all healthy  
- Embedding: healthy (all-MiniLM-L6-v2, 384-dim)

---

## 9c14b955 Fixes Active?

**YES** — bootstrap-seeder succeeded 13/13 in 3 seconds (vs immediate failure on prior container start at 06:30Z). The `Requires=` ordering fix is in effect. Container was rebuilt after 06:52Z commit.

---

## Port 18210 Status

**STILL NOT MAPPED.** Docker port bindings: 8080→18080, 8090→18090, 8100→18100. Port 8210 (goal-host-vessel) remains unmapped. Subagents gate (F-055) still open. Internal health confirms vessel is running: `{"status":"healthy","vesselId":"goal-host-vessel","shapes":["goal_execution","activity_execution"]}`.

---

## Bootstrap-Seeder

**SUCCESS — 13/13 seeded in 3 seconds.** No race condition. Log shows completion at 08:46:54Z vs container start at 08:46:51Z.

---

## e53bae09 Impact — Critical

Goal-host-vessel log at startup (both restarts):
```
[goal-host-vessel] registered 30 development-vessel proxy resolvers
```

This registers HTTP proxy resolvers for each development-vessel shape under both bare name (`coverage_tick`) and qualified name (`development-vessel:coverage_tick`). Prior to this commit, coverage-tick tasks dispatched to resolver `development-vessel:coverage_tick` which had no registration in goal-host — causing immediate task failure with task_count=0.

**Result:** Topology templates now have actual execution counts:
- `coverage-tick`: execs=3 (was 0), alpha=1, beta=2, **success_rate=0**
- `substrate-health-tick`: execs=3 (was 0), alpha=1, beta=2, success_rate=0  
- `escalate-unknown-shape`: execs=2 (was 0), alpha=1, beta=2, success_rate=0
- `harness-check-scenario`: execs=5 (was 0), alpha=1, beta=2, success_rate=0

---

## Thompson Preservation

**YES — volume preserved.** All prior posteriors intact:
- `validator-dispatch`: alpha=2, beta=1, execs=207 ✓
- `slot-binding`: alpha=2, beta=1, execs=29 ✓  
- `evolve-activity-self-contained`: alpha=2, beta=1, execs=1 ✓

---

## S.4a Status

S.1[x], S.2[x], S.3[x], **S.4a[ ]**, S.4b[ ]

Coverage-tick IS being executed now (execs=3). But all 3 executions failed. S.4a requires a successful `coverageReport` with `coverage_progress=true`. The proxy resolver is registered but the underlying development-vessel `coverage_tick` resolver is returning failure.

**Blocking question:** why does coverage-tick fail despite the proxy resolver being registered? Possible causes:
1. Development-vessel `coverage_tick` resolver errors internally
2. Input shapes (activity_metrics, goal, trace) not available in execution context
3. Development-vessel seeder failed to initialize the resolver's internal state

---

## New Findings

### F-059: development-vessel template seeder fails all 15 templates on restart

Both container starts: all 15 bootstrap templates fail with "Unable to connect. Is the computer able to access the url?". The seeder bun subprocess fires before activity-api is accessible OR targets a wrong endpoint. Templates survive from DB volume so this is low-impact today, but a fresh DB would have no dev-vessel templates.

### F-060: Topology templates executing but all failing (the new S.4a blocker)

Coverage-tick now gets selected and dispatched (proxy resolver registered), but all task executions fail. Must check development-vessel logs during the next boredom cycle to diagnose the resolver error.

---

## Resolutions This Iteration

- **F-030** (Thompson α/β flat) — RESOLVED: topology templates now updating
- **F-037** (Thompson disconnect) — RESOLVED  
- **F-042** (4 vessels registered) — RESOLVED: now 5 vessels, 28 shapes
- **F-043** (Thompson posteriors flat) — RESOLVED
- **F-049** (bootstrap-seeder race condition) — RESOLVED
- **F-051** (topology templates never selected) — RESOLVED
- **F-054** (9c14b955 not active) — RESOLVED
- **F-058** (Thompson not updating) — RESOLVED

---

## Findings Tally

| Status | Count |
|--------|-------|
| Open | 26 |
| Resolved | 34 |
| Unverifiable | 1 |
| **Total** | **61** |
| New this iteration | 2 |

---

## Key Open Items

1. **F-060** (HIGH): Why do coverage-tick/substrate-health-tick task executions fail? Check dev-vessel logs during next boredom cycle.
2. **F-041** (HIGH): S.4a still blocked — need one successful coverageReport.
3. **F-059** (MEDIUM): dev-vessel seeder fails 15/15 templates on restart.
4. **F-019** (HIGH): Discovery /resolve 401s persist.
5. **F-055** (MEDIUM): Port 18210 not mapped — subagents gate still open.

---

## Wake Recommendation

**1800 seconds (30 min)** — wait for several boredom cycles to accumulate additional coverage-tick executions. The next iteration should check dev-vessel resolver logs directly to diagnose F-060.
