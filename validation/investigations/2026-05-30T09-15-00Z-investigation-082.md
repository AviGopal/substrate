---
investigation_id: 2026-05-30T09-15-00Z-investigation-082
generated_at: 2026-05-30T09:15:00Z
investigator_session_id: claude-opus-4-7-audit-iter-082
prior_investigation_id: 2026-05-30T09-06-00Z-investigation-081
loop_iteration: 82
framing: subagent — reverse-engineer draft-spec-from-gap (substrate authoring openspec proposals + tasks), locate writes, audit operator-floor scope, identify gating, compose with iter-080/iter-081 pipelines; NO code
substrate_target: repos/development-vessel/src/seed/draft-spec-from-gap.ts + fs-write resolver + substrate bind-mount config
---

# Investigation 082 — draft-spec-from-gap: substrate authors openspec (research/design pass; NO code)

## §1 — Seed file located + task graph

Path: `repos/development-vessel/src/seed/draft-spec-from-gap.ts` (237 lines). Exported from `src/seed/index.ts:20,40,67` and bundled in `SEED_TEMPLATES`. Verified by `grep -n "DRAFT_SPEC_FROM_GAP" src/seed/index.ts`.

**Template shape** (`draft-spec-from-gap.ts:71-86`):
```
id:           "development-vessel:draft-spec-from-gap"
inputShapes:  ["substrateGapBatch", "openspecExemplar"]
outputShapes: ["specProposal"]
tags:         ["lift.autonomous.loop", "substrate.authored.openspec", "spec.drafting"]
variables:    [gap_class, fix_priors_query, max_gaps, date]
```

**9-task graph** (`draft-spec-from-gap.ts:108-235`):
1. `read_gaps` — `http_fetch GET http://127.0.0.1:8260/concepts/search?source_type=substrate_gap&query={{gap_class}}` (concept-db).
2. `read_priors` — same endpoint with `fix_priors_query` (5 concepts).
3. `read_exemplar_proposal` — `fs_read openspec/changes/2026-05-22-failure-mode-autonomous-loop/proposal.md`.
4. `read_exemplar_tasks` — fs_read of the paired `tasks.md`.
5. `draft_via_llm` — `llm_completion_dispatch` to `anthropic/claude-haiku-4-5-20251001` with `PROMPT_TEMPLATE` (lines 3-69) demanding `{ slug, proposal_md, tasks_md }` JSON.
6-8. Three `json_path_extract` tasks pulling `slug`, `proposal_md`, `tasks_md` from `{{draft_via_llm_text}}`.
9-10. Two `fs_write` tasks to `openspec/changes/{{date}}-substrate-authored-{{extract_slug_value}}/{proposal.md,tasks.md}` with `createDirs: true`.

The LLM prompt enforces structural discipline (lines 41-67): proposal sections (Motivation/Proposal/Out of Scope/Success Criteria/References), tasks DEV-N checkboxes, mandatory two-concept citation, `INSUFFICIENT_EVIDENCE` slug on failure to find ≥2 cited ids.

## §2 — Where does it write? Bind-mount fact-check

`docker inspect substrate-live --format '{{json .Mounts}}'` returns:

```
Source: /home/avi/documents/work/exp-repo/metabob-devbob       Destination: same      Mode: ro RW: false
Source: /home/avi/documents/work/exp-repo/metabob-devbob/scripts/substrate/workspace  Destination: /workspace  RW: true
```

The repo root is **read-only**. Verified: `docker exec substrate-live touch /home/avi/documents/work/.../test-write-probe` → `Read-only file system`. `touch /workspace/test-write-probe` → succeeds.

Systemd unit `/etc/systemd/system/development-vessel.service` sets `Environment=WORKSPACE_ROOT=/workspace` (verified via `systemctl show development-vessel -p Environment`).

`fs-write.ts:12-18` `assertInWorkspace()` resolves the path through `resolve(path)` (so `openspec/changes/foo/proposal.md` resolves against the vessel's cwd `/vessels/development-vessel`, NOT `/workspace`). Then `relative(workspaceRoot, abs)` is checked for `..` prefix. **A relative path like `openspec/changes/2026-05-30-substrate-authored-foo/proposal.md` would resolve to `/vessels/development-vessel/openspec/...` and fail the workspace check unless the vessel runs with cwd inside `/workspace`** — but `WorkingDirectory=/vessels/development-vessel` per the unit file.

So unmodified, the writes either (a) fail at `assertInWorkspace`, or (b) hit `/vessels/development-vessel/openspec/...` which is INSIDE the read-only bind mount and would error at the `Bun.write` syscall. Verified empirically: `docker exec substrate-live find /workspace -path "*openspec*"` returns **nothing**. No substrate-authored openspec dir exists at `/workspace/openspec/changes/`. Host-side `ls openspec/changes/ | grep substrate-authored` also returns **nothing**.

**Conclusion:** `draft-spec-from-gap` is seeded but has not yet successfully written anything to disk. The template is plumbed, but the path-resolution path against `/workspace` is broken (or untriggered).

## §3 — Recursive escalation: templates → specs

`draft-gap-closing-activity.ts` (commit `06bd39ea`, iter 39) authors **activity templates** as JSON to `/workspace/proposals/` — a structured artifact validated by template schema and gated by Thompson promotion (`min_samples=3`, commit `17ec6803`).

`draft-spec-from-gap.ts` (iter 45) authors **openspec proposals + tasks** as freeform markdown. There is no schema validation. There is no promotion gate. The artifact is intended for operator review per the description (line 78): *"Operator reviews the resulting directory; this template does not commit."*

This is a category jump:
- Templates: bounded JSON, machine-checked, Thompson-graduated.
- Specs: unbounded prose, human-checked, no graduation mechanism.

## §4 — Gating audit

The `WRITE_ALLOWLIST` env var (`fs-write.ts:28-42`) IS the spec's chosen scoping mechanism (option B in `openspec/changes/2026-05-30-draft-spec-from-gap-template/proposal.md:85-100`). **It is not set in the substrate** (`systemctl show development-vessel -p Environment` → no `WRITE_ALLOWLIST`). So `assertInAllowlist` returns at line 30 (`if (!raw) return`) — no enforcement. The spec's own §6 success criterion ("substrate writes a real openspec/changes/2026-05-30-substrate-authored-<slug>/ directory") has not been verified — and given the bind-mount fact in §2, it can't be without either widening WORKSPACE_ROOT to the repo or changing the write target to `/workspace/openspec-drafts/`.

**Operator review IS the gate** per template description. There is no autonomous-promoter analog for specs. No `proposed=true` flag, no Thompson posterior, no `min_samples`.

## §5 — Composition with iter-080 audit-ingestion

iter-080 bridge (not yet shipped) emits `substrateGap` from audit findings. iter-082 (`drain-pending-substrate-gaps.ts:46-113`) currently dispatches **`development-vessel:draft-gap-closing-activity`** (variant drafter), NOT `draft-spec-from-gap`. Verified: `drain-pending-substrate-gaps.ts:100` — `targetTemplateId: "development-vessel:draft-gap-closing-activity"`. Boredom goal[10] (`repos/boredom-vessel/src/index.ts:76`) wires drain, which routes to variant-drafter.

**Therefore `draft-spec-from-gap` is currently orphaned by the autonomous loop.** It must be invoked manually via `/run-goal` with explicit `targetTemplateId`. The pipeline (audit → substrateGap → drain → drafter) ends at `draft-gap-closing-activity`, not at the spec drafter. The closure (audit prose → substrate-authored spec) is not yet end-to-end automated despite the template existing.

This is consistent with iter-045 commit-message language ("Unlock C templates") — these are **unlocked capabilities**, not yet activated paths.

## §6 — Workspace writability (re-verified)

`docker exec substrate-live touch /workspace/test-write-probe` → succeeded. `/workspace` IS RW from inside the substrate. Substrate CAN write freely under `/workspace/**`. Repo root cannot be written. Operator-floor process-boundary IS holding for the canonical repo tree.

But the writes specified by `draft-spec-from-gap` use the path `openspec/changes/...` (relative). Under `WorkingDirectory=/vessels/development-vessel` (RO-mounted), those paths resolve to read-only locations and would fail. Unless a downstream commit retargets the path (e.g., prefixes with `/workspace/`), this template cannot succeed. **The blocking constraint is path-resolution semantics, not policy.**

## §7 — Composition with iter-081 concept-ingestion

iter-081 recommended ingesting `openspec/changes/**` into concept-db (option c). If `draft-spec-from-gap` ever succeeds in writing to `/workspace/openspec/...` AND iter-081's pipeline ingests that subtree, the loop becomes:

```
substrateGap → draft-spec-from-gap → /workspace/openspec/.../proposal.md
   ↓ (iter-081 ingest)
concept-db: source_type=substrate_authored_proposal
   ↓ (read_priors_query in next draft-spec-from-gap run)
LLM cites substrate-authored prior as "fix_priors" → drafts new spec
```

This is a closed self-justifying loop. The only break in current architecture: (1) bind-mount RO on canonical openspec/ — operator must physically copy `/workspace/openspec/...` to repo openspec/; (2) iter-081 not shipped; (3) absence of operator-merge step (no `git add/commit` happens autonomously). Three operator gates remain.

## §8 — The single observation

**Commit in writing.** `draft-spec-from-gap` is the substrate's first attempt to author governance artifacts (openspec proposals — prose that describes future architectural changes), not just execution artifacts (templates — JSON the substrate itself runs). It is currently SEEDED but UNREACHABLE in three independent ways: (a) the boredom→drain loop routes to the template-variant drafter, not the spec drafter, leaving spec-drafting accessible only via manual `/run-goal`; (b) the `fs_write` path resolution against `WorkingDirectory=/vessels/development-vessel` (RO mount) would fail at syscall before any spec lands on disk; (c) the `WRITE_ALLOWLIST` enforcement designed to bound substrate authoring is not set, so policy enforcement is absent — only physical path resolution and bind-mount RO are holding the operator-floor.

The substrate has **declared intent** to author its own openspec changes (template registered, prompt designed, exemplars cited, spec proposal #2026-05-30-draft-spec-from-gap-template specifies the pathway) but has **not yet executed** it. The operator-floor framing remains intact today — but only by accident of cwd path resolution, not by design. The intended design (WRITE_ALLOWLIST=openspec/changes/) is policy-soft: a single env-var change would let substrate writes land in the repo's actual openspec/ tree IF the bind-mount were RW. The bind-mount being RO is therefore the load-bearing operator-floor for spec authoring — and the only one. If the operator widens the bind-mount to RW (likely necessary for substrate to write commits, ship vessel code, or otherwise close the S2→S3 gap), spec-authoring becomes immediately live and unbounded. The hardest substrate-self-authoring boundary is one bind-mount flag away from collapse, and the policy intended to replace it (WRITE_ALLOWLIST) is currently unset.

**Load-bearing recommendation:** before iter-080 audit-ingestion and iter-081 concept-ingestion compose into a closed loop, the operator must (1) set `WRITE_ALLOWLIST=openspec/changes/` in substrate systemd env regardless of bind-mount state, (2) decide whether `draft-spec-from-gap` should ever be wired to boredom (currently the only spec-drafting trigger is manual — keeping it so is a defensible operator-floor decision), and (3) treat any RW widening of the repo bind-mount as a multi-step ratification, not a convenience change. The substrate is structurally ready to author its own architecture; the operator-floor today is a single mount-mode flag.
