---
investigation_id: 2026-05-28T10-50-00Z-investigation-049
generated_at: 2026-05-28T10:50:00Z
investigator_session_id: claude-opus-4-7-audit-iter-034
prior_investigation_id: 2026-05-28T10-25-00Z-investigation-048
loop_iteration: 34
framing: iter-041 P1-experiment iteration 2 — web-research goal exposes 3 critical findings + audit-reliability meta-failure
substrate_target: http://localhost:18080
---

# Investigation 049 — Web-research goal experiment exposes runaway autonomous-authoring + audit-reliability floor

## Experiment summary

**Goal**: "Summarize the practical state of homomorphic encryption usability as of mid-2026."
**Dispatched to**: `gap-closing:fp-12-partial-success-recorded-as-total-1779960500760`
**Execution**: exec_a2qzc2sr, 378s wall, 8 tasks all `status:success`, `failure_mode:null`
**Output impulses**: all 8 prefixed `err_*` (literal: `err_7l4buvym`, `err_jndvdiri`, etc.)

## iter-041 prediction outcomes

| # | Prediction | Outcome |
|---|-----------|---------|
| P1 | Empty primaryIntent | UNCLEAR (semantic-tags.ts lacks crypto keywords; routing happened anyway) |
| P2 | Tier 3 FTS fires | REFUTED — fp-12 picked over harness-run-matrix despite identical defaults |
| P3 | Substrate-internal template | CONFIRMED (different mechanism than predicted) |
| P4 | Wrong shape produced | CONFIRMED — `unutilizedOutputReport` vs expected research summary |
| P5 | No mismatch signal | CONFIRMED — failure_mode:null, status:success |

All 5 expectations refuted (semantic intent uncaptured, wrong output shape, zero web-search resolvers, outputs are err_* markers, 6m18s wall hiding silent-retry chain).

## Three critical findings filed to channel

**Finding 13 (CRITICAL)** — silent success across error-marked impulses. All 8 task statuses `success`, all 8 outputs `err_*`-prefixed. Substrate has zero signal. Thompson α grows for a template that produced nothing but errors.

**Finding 14 (CRITICAL — audit-reliability)** — registry list endpoint silently truncated. Prior queries reported "2 substrate-authored templates" when 6 existed. Finding 12 ("4-burst produced 0 templates") was hallucinated by the audit because the verification path itself hallucinated empty results. **The audit's epistemological floor matches the substrate's.**

**Finding 15 (HIGH)** — substrate-authored templates winning recommend. fp-12 has zero shape/tag overlap with the goal, default α=β=1, yet selected over operator-seeded alternatives. Routing-mystery unresolved.

## The runaway pattern (combining Findings 10/11/13/15)

Each draft-gap-closing cycle produces a template that:

1. Lands `proposed:false` (Finding 10) — selection-visible immediately
2. Contains hallucinated resolvers (Finding 11) — execution generates err_* impulses
3. Reports `status:success` regardless (Finding 13) — Thompson α increments
4. Wins recommend over operator-seeded templates (Finding 15) — operator goals route here

**Therefore**: as substrate-authored templates accumulate (~2/hour observed), they progressively monopolize recommend for novel operator goals, with silent-error execution and no negative feedback. The dev agent's "first substrate-authored template registration" celebration (commit 4e55f95b) is the start of a runaway.

iter-048's `resolver_registry_query` primitive blocks the INPUT side (catches hallucinated resolvers pre-write). Findings 13+15 are OUTPUT/SELECTION side issues that need separate fixes:

- **Finding 13 fix**: task-level validation that output_impulse_ids don't start with `err_` before status:success can be set. Cheap (~5 LOC in ias-executor-ts engine). Operator-floor.
- **Finding 15 fix**: requires understanding the routing-mystery first. Defer until reproduced under instrumentation.

## Audit-reliability meta (Finding 14)

The audit cited "2 templates exist" multiple times based on identical-format queries. Today's query returned 6. Either pagination, cache, or filter-pattern bug. **Until rooted out, every audit query that returns a count is suspect.**

Mitigation for future audit queries:
1. Always include OFFSET pagination explicitly + iterate to exhaustion
2. Verify count against `SELECT count() FROM activity_template` directly via root-token (if available)
3. Cross-check with discovery-vessel's view of the same data

This is the audit's analog of Finding 11: an internal claim (template count) not verified against an authoritative registry source. The iter-048 `resolver_registry_query` pattern applies recursively: the audit needs registry-query confidence the same way the substrate does.

## Composition with prior arc + retraction

**Finding 12 retracted.** The 09:28 4-burst DID produce templates (fp-11, fp-12, fp-15). My prior claim "0 new templates" was wrong; Finding 12 hallucinated based on truncated registry response.

iter-046 L0+ framing strengthened: `resolver_registry_query` is L0+ for resolver validity AT TEMPLATE-AUTHOR TIME. The audit needs the L0+ equivalent for itself: **reliable registry pagination**. This isn't a substrate-fixable issue (it's in activity-api), but it's the same pattern at a different scope.

iter-041 prediction-protocol working as designed: predictions committed before submission, refuted by data, new findings extracted from refutation.

## Audit posture commitments

1. **Finding 14 forces a discipline change**: every future audit count-claim must include pagination evidence (count via SELECT-COUNT OR exhaustive iteration). Inline literal proofs.
2. **Findings 13 + 15 added to operator-floor bundle**. iter-048's 5h estimate undercounts; output-side fixes need separate work.
3. **The runaway is real and accelerating**. Each hour adds ~2 selection-visible silent-error templates.

## Loop bookkeeping

- 3 new findings filed; 1 prior finding retracted (Finding 12)
- iter-046 framing strengthened (L0+ enabler pattern recursive to audit)
- Monitors `bmzpcld51` + `bj4wvnnk7` armed
- Next observation: any commit touching task-status-vs-err_-output validation, OR Finding-14 root cause (registry pagination), OR Finding-15 routing diagnostics
