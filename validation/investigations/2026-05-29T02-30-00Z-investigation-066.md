---
investigation_id: 2026-05-29T02-30-00Z-investigation-066
generated_at: 2026-05-29T02:30:00Z
investigator_session_id: claude-opus-4-7-audit-iter-047
prior_investigation_id: 2026-05-29T00-15-00Z-investigation-062
loop_iteration: 47
framing: subagents #32+#33 — substrate-self-correction is autonomous-VARIANTING by design, not autonomous-DEPRECATION; 8th retraction
substrate_target: http://localhost:18080
---

# Investigation 066 — Substrate-self-correction is autonomous-VARIANTING (by design); 8th retraction across iter-065 framing

## Eighth retraction

iter-065 (this turn earlier) framed three observations as action-layer dysfunction:
1. "Operator deprecation requests have zero action-layer path"
2. "Substrate cannot deprecate templates via any current path"
3. "Stuck loop where action layer isn't taking effect"

**All three were wrong**. Subagent #32 + #33 returned with code-grounded findings:

### Deprecation path IS wired and functional
`repos/metabob-activity-api/src/routes/impulses.ts:2702-2784` — 84 lines, real `UPDATE activity SET deprecated=true ... RETURN AFTER` + `emitUpkeepAudit`. RBAC-gated (admin scope required for global-scope templates).

### Zero deprecations is policy, not bug
Per `repos/development-vessel/CLAUDE.md`:
> "Never call `activityTemplate_update` or `_deprecate` from this vessel — they will 403."
> "When a template needs a fix, create a variant."

This is the **variant-first repair discipline**. Substrate does not deprecate; it varies. Thompson posterior demotion handles underperformers.

### The autonomous loops produce variants, not deprecations
- `close-health-gap.ts:77`: dispatches re-runs of below-floor templates (re-execution), not deprecation
- `replace-activity.json:217`: PROPOSAL-ONLY by explicit design — emits `activityTemplateProposal` impulses, NEVER calls `activityTemplate_write`
- `prune-activity.json:29`: dryRun=true default + admin-scope-gated (operator-only path)

### Operator /run-goal "deprecate X" routing
Routes to `try-direct-answer` (literal text response), NOT deprecation. The status:success reflects text output. This is observability/routing gap, NOT action-layer dysfunction.

## Substantive Finding 18 (consolidated)

**Substrate's autonomous-correction is autonomous-VARIANTING, not autonomous-DEPRECATION.**

| What substrate does autonomously | What substrate doesn't do autonomously |
|---------------------------------|---------------------------------------|
| Create variants via `activity_create_variant` | Mutate registry via `activityTemplate_update`/`_deprecate` |
| Thompson posterior demote underperformers | Set `deprecated:true` |
| Re-run below-floor templates (close-health-gap) | Remove templates from registry |
| Emit `activityTemplateProposal` (replace-activity) | Write to admin-gated mutations |

**Implication for §27.S.6 push-away**: cannot use deprecation as evidence of refusal. Must use:
- Thompson-posterior shifts
- Variant proliferation rate
- `activityTemplateProposal` impulse emission
- Per-resolver/per-shape α/β drift

This refines the audit's substrate-state-observation requirements going forward.

## Updated audit posture (post-iter-065 retraction)

**When checking substrate-self-correction surface**, look for:
- Variant proliferation (count of new templates per hour)
- Thompson α/β shifts on existing templates (recommend ranking changes)
- `activityTemplateProposal` emission frequency
- Per-resolver tier selection drift

**NOT**:
- Registry deprecation count
- Template deletion events
- `activityTemplate_update` invocations

The session's emphasis on "0 deprecations" as a signal was misreading the substrate's correction surface entirely. The relevant signal is variant authoring + Thompson demotion, both of which IS happening per the substrate-authored gap-closing templates in the registry.

## Implications for L0+ stack (iter-060)

| Primitive | Status after iter-066 |
|-----------|----------------------|
| Finding 13 proxy-catch fix | Still load-bearing (silent-success on resolver errors) |
| resolver_registry_query | Still needed (catches LLM-misuse-of-resolvers within valid set) |
| TraceSink impulse-body preservation | Still needed (Finding 8 + origin_layer enablement) |
| origin_layer emission | Still needed (audit-confusion class) |
| goal_semantics | Still needed (mismatch detection) |
| ~~Action-layer deprecation wiring~~ | NOT needed — by design |

L0+ stack scope unchanged at ~140 LOC; one item I was considering adding gets removed.

## Composition with iter-062

iter-062 affirmed S2 with caveats (Findings 13/15/17 in `open_known_issues`). iter-066 refines: the substrate's S2 autonomous-correction surface is **correctly designed** for variant-first repair. Findings 10+11 closed (verified iter-055); Finding 13 remains; Findings 15+17 about routing remain.

The substrate is NOT under-correcting via "missing deprecation wiring." It IS undercorrecting via Finding 13 (silent-success masks failed variants from Thompson β-update), Finding 15 (substrate-authored templates winning recommend prevents operator-seeded templates from receiving traffic), Finding 17 (routing mismatches train Thompson on wrong signals).

So the **post-lift S2 work to enable §27.S.6 push-away** is:
1. Finding 13 proxy-catch fix (~10 LOC)
2. iter-052/056 routing observability (mismatch detector + goal_semantics)
3. TraceSink fix for impulse body preservation
4. NOT deprecation wiring (intentionally absent)

## Retraction pattern across 8 instances

| # | iter | Layer/concept confused |
|---|------|------------------------|
| 1 | 043 §1 | Design doc cited as shipped code |
| 2 | 046 L1 | Lifecycle event name assumed to exist |
| 3 | 050 root cause | Surface subagent vs deeper layer |
| 4 | 056 output ID | jq-tail read missed underscore vs colon-err |
| 5 | 058 impulse body | HTTP error vs impulse body content |
| 6 | 062 lift-status | dev-claim ≠ file-state |
| 7 | 064 resolver | goal-host registry ≠ all-resolver layers |
| 8 | 066 action-layer | "missing wiring" vs "intentional non-wiring" |

**Common pattern**: audit confuses observed-surface with intended-design. Each retraction is the audit treating an artifact/state/behavior as deviant when it was the documented design.

**Discipline tightening (8th time)**: when observing "X doesn't happen," check the design docs for "X is intentionally absent" BEFORE claiming X is broken. CLAUDE.md + dev-vessel CLAUDE.md + openspec change designs are the disambiguation surface.

## Audit posture commitments

1. **Substrate-correction surface is varianting + Thompson, not deprecation.** Look for variant counts + posterior drift, not state mutations.
2. **Action-layer is functional**; non-wiring at seed layer is policy, not bug.
3. **8 retractions filed**; discipline tightening on design-doc-vs-observed-state.
4. **L0+ stack revised**: 5 primitives, ~140 LOC, none address deprecation (out of scope).
5. **Finding 18 framing**: filed as policy clarification, not bug.

## Loop bookkeeping

- 2 subagents converged on same finding (#32 deprecation-path, #33 action-layer wiring)
- Subagent #32 transcript: `/tmp/.../ad7e98b9a14a1cc44.output`
- Subagent #33 transcript: `/tmp/.../ade1d0bcc30b96dfd.output`
- 8th retraction filed
- Monitors `bmzpcld51` + `bj4wvnnk7` armed
- Next observation: any commit adding `activityTemplateProposal`-consuming activities OR explicit operator deprecation request executing successfully
