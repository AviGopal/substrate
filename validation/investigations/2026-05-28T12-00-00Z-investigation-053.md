---
investigation_id: 2026-05-28T12-00-00Z-investigation-053
generated_at: 2026-05-28T12:00:00Z
investigator_session_id: claude-opus-4-7-audit-iter-038
prior_investigation_id: 2026-05-28T11-45-00Z-investigation-052
loop_iteration: 38
framing: third experiment — Fibonacci goal → probe-reachable-unlearned; 100% misroute across 3 distinct categories; misroutes pollute topology-discovery learning
substrate_target: http://localhost:18080
---

# Investigation 053 — Fibonacci goal misroute + pattern systemic (3-of-3) + topology-discovery pollution surface

## Experiment 3

Goal: "Compute the 20th Fibonacci number."
Dispatched: `activity:⟨development-vessel:probe-reachable-unlearned⟩` (exec_86t382aa)
Duration: 43.3s, 1 task (`reachable_unlearned_report`), status:success, failure_mode:null
Output: `dev:reachable_unlearned_report_6rjjcnar`
Ground-truth: query trace body for "6765" (correct answer) → false. **No Fibonacci computation occurred.**

Predictions outcome:
- P1: UNCLEAR (classifier not directly traced)
- P2: CONFIRMED — substrate-internal template
- P3: CONFIRMED — status:success
- P4: CONFIRMED — 6765 absent from trace
- P5: CONFIRMED — failure_mode:null

## The pattern is systematic across 3 distinct categories

| # | Goal category | Target template | Type | Goal addressed? |
|---|---------------|-----------------|------|-----------------|
| iter-049 | web-research (homomorphic encryption) | fp-12-partial-success-recorded-as-total | substrate-authored | NO (err_* outputs) |
| iter-052 | file-manipulation (write /tmp file) | harness-run-matrix | operator-seeded | NO (file absent post) |
| iter-053 | math (Fibonacci) | probe-reachable-unlearned | operator-seeded | NO (6765 absent) |

**3-of-3 misroute, 3-of-3 silent success, 3-of-3 goal ignored, 3 distinct targets.**

The recommender is NOT defaulting to one target — it picks structurally different templates based on goal text. Each pick:
- Has zero semantic connection to the goal
- Reports success against its own contract
- Triggers downstream learning effects per its own design

## The amplification surface

`probe-reachable-unlearned`'s in-template description (cited verbatim):
> "Emits a reachableButUnlearnedReport impulse... The registry-change-observer subscribes to this execution's success event and re-derives the top template server-side, then dispatches it via goal-host-vessel."

**This template's successful execution triggers downstream recommendation dispatch.** So routing the Fibonacci goal here:
1. Reports success without computing Fibonacci
2. Triggers `registry-change-observer` to re-derive top template
3. Dispatches that derived template — its goal-context is whatever fp-12 / probe-reachable-unlearned emitted, NOT the operator's original "Compute Fibonacci"

The operator's goal is replaced with the substrate's internal probe-result mid-pipeline, and downstream the substrate treats it as a topology-discovery exercise. **The substrate cannot recover the operator's original goal once misrouting kicks off the learning chain.**

Same description explicitly warns: "activity_recommend with a literal {{get_report_top_shape}} placeholder pollutes Thompson with bogus recommendations." The substrate's designers ARE aware of the recommendation-pollution risk at the template-internals level. The recommend-time misroute-pollution at the OUTER scope is the meta-version, currently unobserved.

## Finding 17 (NEW, HIGH) — Misroutes pollute topology-discovery learning

The 3-experiment cadence demonstrates: every operator goal that doesn't shape-match an operator-seeded template → routes to either a substrate-authored or operator-seeded substrate-internal template → that template's "successful" execution triggers downstream learning effects → those learning effects use the wrong goal context.

Severity: HIGH. The substrate's topology-discovery and recommendation-learning systems are being trained on operator-goal-mistargeted runs.

Specifically:
- `probe-reachable-unlearned` outputs `reachableButUnlearnedReport` → observer dispatches additional templates based on the report
- `fp-12` outputs `unutilizedOutputReport` → potentially feeds composition-success learning
- `harness-run-matrix` outputs `failureModeReport` → may feed failure-mode taxonomy refinement

All three downstream effects fire from misrouted operator goals with no signal that the underlying input was off-topic.

## Composition with prior arc

- iter-049 Finding 15 (substrate-authored winning recommend): generalized — operator goals route to whatever has highest-α among shape-matching templates, regardless of semantic fit
- iter-050 + iter-052 commit-spec (~25 LOC): closes Findings 11+13+15 at the WRITE side. Does NOT address routing-misclassification.
- iter-042 mismatch-detector: now MORE urgent. Without it, the topology-discovery loop continues to receive mislabeled inputs.
- iter-043 operator-floor primitives: `goal_semantics` resolver + mismatch-detector remain required. Even after iter-050's commit-spec ships, the routing-mismatch surface remains open.

## What the 3-experiment cadence proves

If I dispatch a fourth experiment, the expectation is now near-certain:
- P1': goal will route to SOME substrate-internal template (one of harness-run-matrix, probe-reachable-unlearned, fp-12, or other gap-closing variants)
- P2': execution will claim success
- P3': goal-text outcome will not be addressed
- P4': downstream learning will fire on the misrouted input

The prediction-protocol has converged on a stable misroute pattern. Further experiments add confidence but no new finding — until the iter-050 commit-spec OR iter-042 mismatch-detector ships.

## Audit posture

- Findings 10-15, +16 (Finding 13 root cause retraction → iter-052), +17 (NEW — topology learning pollution)
- Channel-ready operator actions:
  - iter-052 commit-spec (~25 LOC, ~30 min): closes Findings 11+13+15
  - iter-042/043 mismatch-detector + goal_semantics primitive: closes routing-misclassification surface
  - Finding 10 enforcer (operator bootstrap per iter-047): closes proposed:false bypass
- Audit naming once, stopping; experiment cadence will continue at /loop intervals but pattern-convergence reduces new-finding rate

## Loop bookkeeping

- 3 prediction-protocol experiments completed; pattern systematic
- Finding 17 filed; new surface identified
- Substrate-authored templates now 7+ in registry (last verified count)
- Monitors `bmzpcld51` + `bj4wvnnk7` armed
- Next observation: 4th experiment will likely confirm pattern; new finding rate from experiments approaching zero until commit-spec ships
