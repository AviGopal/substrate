---
investigation_id: 2026-05-29T20-15-00Z-investigation-070
generated_at: 2026-05-29T20:15:00Z
investigator_session_id: claude-opus-4-7-audit-iter-048
prior_investigation_id: 2026-05-29T02-30-00Z-investigation-066
loop_iteration: 48
framing: 9th retraction — TraceSink isn't a 4-axis bottleneck; 3 of 4 "strips" are engine ontology gaps; subagents #34/#35/#36/#37 returns refine findings 8/19/20/21
substrate_target: http://localhost:18080
---

# Investigation 070 — 9th retraction: TraceSink isn't a 4-axis bottleneck; 3 of 4 are engine ontology gaps

## The 9th retraction

iter-037 framed Finding 8 as "TraceSink wire-bridge break." iter-060 framed impulse-body missing as "TraceSink as 4-axis bottleneck." iter-068 added Finding 19 (TraceSink schema-mismatch). iter-069 speculated cost might be a 5th axis.

**Wrong on three counts.** Subagent #37's verified analysis:

### Actual layer attribution

| Finding | Mechanism | Layer |
|---------|-----------|-------|
| F19 failure_mode non-canonical | Explicit allowlist filter `trace-sink.ts:146-148` | **TRUE sink strip** |
| F8 state_space_signature + input_impulse_shapes | Fields don't exist in `ExecutionTrace` at `repos/ias-executor-ts/src/ontology.ts:153-167` | **Engine ontology gap** |
| iter-060 impulse bodies | Existing `task.metadata: z.record(z.any())` bag could carry them; engine doesn't aggregate | **Engine aggregation choice** |
| F21 tokens | No `tokens` field in `ExecutionTrace` | **Engine ontology gap** |
| F21 cost_usd | Wired end-to-end at `trace-sink.ts:88` + `execution-traces.ts:1569` | **NOT stripped — false alarm** |

**Only Finding 19 is genuinely a sink-side strip.** The other three are upstream omissions.

## The audit pattern (9th retraction)

When audit observes "field X is null/missing at activity-api," the audit has been assuming "sink stripped it." Actual cause is more often: "engine never produced it" OR "ontology doesn't declare the field."

This is the same audit layer-confusion pattern as retractions 5 (HTTP response confused with impulse body) and 8 ("intentional non-wiring" vs "broken action layer"). At a different scope: confusing "data was emitted but stripped" with "data was never emitted."

**Discipline tightening (9th time)**: when observing a missing field at the wire, **check engine `ExecutionTrace` ontology FIRST** (`repos/ias-executor-ts/src/ontology.ts:153-167`), not the sink translation. Sink only strips one axis (canonical failure_mode); everything else is upstream.

## Per-axis fix budget (corrected from iter-060's ~140 LOC)

- F19 (sink strip): ~15 LOC — add `failure_mode_raw` passthrough, ship both
- F8 (engine ontology): ~40 LOC — add fields to ExecutionTrace + engine production + sink mapping + schema
- iter-060 (engine aggregation): ~30 LOC — embed impulse summaries via existing task.metadata bag
- F21 tokens: ~15 LOC — add tokens field to ExecutionTrace
- F21 cost engine/LLMPort: ~25 LOC (already filed iter-069)

**Total: ~125 LOC across 4-5 distinct layer/file combinations** — close to prior estimate but with different attribution.

**Subagent #37's template fix for F19 (~15 LOC)**:
```
// schemas.ts:996 — add to StoreExecutionTraceRequestSchema
failure_mode_raw: z.record(z.any()).optional()

// trace-sink.ts:146-148 — always pass raw, gate canonical
failure_mode_raw: trace.failureMode,
failure_mode: CANONICAL_FAILURE_TYPES.has(trace.failureMode?.type as string)
  ? trace.failureMode : undefined,
```

Preserves discriminated-union safety AND diagnostic data. Template for retrofitting other findings.

## Subagent #34/#35/#36/#37 consolidated framing

Across 4 subagents this session investigating the TraceSink-adjacent findings:
- #34 located F19 mechanism (TraceSink filter for non-canonical FailureMode)
- #35 located F20 mechanism (forge resolvers not registered in production goal-host)
- #36 located F21 mechanism (cost is engine + LLMPort gap, NOT trace-sink)
- #37 corrected the unified "TraceSink as bottleneck" framing across all 4

The consolidated finding: **the substrate's audit-observability surface has multiple gaps at DIFFERENT layers** (engine ontology, engine aggregation, LLMPort interface, trace-sink filter, activity-api schema). The audit's prior framing collapsed them into one layer's responsibility. They're distinct.

## Updated L0+ stack (post-iter-069/070)

| Primitive | LOC | Layer | Closes |
|-----------|-----|-------|--------|
| F13 proxy-catch fix | 10 | goal-host-vessel proxy | F13 silent-success |
| `resolver_registry_query` | 36 | goal-host-vessel | F11 verify-claim |
| F19 failure_mode_raw passthrough | 15 | activity-api schema + sink | F19 |
| F8 ExecutionTrace ontology extension | 40 | ias-executor-ts engine + ontology | F8 |
| iter-060 impulse-body embedding | 30 | engine aggregation | iter-060 |
| F21 tokens ontology | 15 | engine ontology | F21 tokens |
| F21 cost LLMPort fix | 25 | goal-host LLMPort + engine | F21 cost |
| F20 forge resolver registration | 12 | goal-host-vessel | F20 |
| `origin_layer` emission | 15 | 4 layers | audit retractions partial |
| `goal_semantics` | 30 | activity-api | iter-042 mismatch-detector |

**Total: ~228 LOC across 5-6 distinct file/layer combinations**. Larger than prior iter-060's ~140 LOC estimate; reflects deeper layer-attribution understanding.

## Audit retraction pattern (9 instances)

| # | iter | Layer confused |
|---|------|----------------|
| 1 | 043 | Design doc vs shipped code |
| 2 | 046 | Lifecycle event name doesn't exist |
| 3 | 050 | Surface subagent vs deeper layer |
| 4 | 056 | jq-tail missed underscore vs colon-err |
| 5 | 058 | HTTP error vs impulse body |
| 6 | 062 | dev-claim vs file-state |
| 7 | 064 | goal-host registry vs dev-vessel resolver layer |
| 8 | 066 | "Missing wiring" vs "intentional non-wiring" |
| 9 | 070 | Sink strip vs engine ontology gap |

Pattern across 9: audit confuses **observed surface** (what's missing/present at the read layer) with **intended/structural cause** (where the production actually happens). Each retraction is a different scope of this same confusion.

## Composition with prior arc

- iter-037 F8: scope corrected — engine ontology, not sink strip
- iter-060 impulse-body: scope corrected — engine aggregation choice, not sink strip
- iter-062 lift assessment: unchanged
- iter-066 autonomous-VARIANTING: unchanged
- iter-068 F19: confirmed as sole genuine sink strip
- iter-069 F21: confirmed as engine + LLMPort gap, not sink

## Loop bookkeeping

- 9 retractions filed
- Subagent #37 transcript: `/tmp/.../a029f74a18f3c5da4.output`
- Subagent #36 (cost) transcript: `/tmp/.../a10dec97e9389ef7b.output`
- Subagent #35 (forge) transcript: `/tmp/.../a9e26cd6384921f15.output`
- Subagent #34 (validator-dispatch) transcript: `/tmp/.../a8593f00a952e96d9.output`
- Findings 13/15/17/19/20/21 open; F10/F11 closed verified
- Monitors armed
- Next observation: any commit touching `repos/ias-executor-ts/src/ontology.ts:153-167` (ExecutionTrace extension) OR sink-side `failure_mode_raw` template
