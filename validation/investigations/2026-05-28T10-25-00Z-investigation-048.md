---
investigation_id: 2026-05-28T10-25-00Z-investigation-048
generated_at: 2026-05-28T10:25:00Z
investigator_session_id: claude-opus-4-7-audit-iter-033
prior_investigation_id: 2026-05-28T10-05-00Z-investigation-047
loop_iteration: 33
framing: Subagent #26 returns — single primitive (resolver_registry_query) on goal-host-vessel closes Findings 10+11 in ~5 op-hours
substrate_target: http://localhost:18080
---

# Investigation 048 — Registry-query primitive: ship one, close two findings, unlock L1 stack

## Decision

Ship ONE primitive: `resolver_registry_query` on goal-host-vessel. ~36 LOC + heartbeat re-registration. **2-3 operator-hours.**

Subagent #26's three code-facts:

1. `runtime.resolvers.list()` exists at `repos/ias-executor-ts/src/resolvers.ts:48-50` — data structure already done
2. goal-host-vessel already has the HTTP + `SHAPES` advertisement pattern at `repos/goal-host-vessel/src/index.ts:693-701` — one more shape is parallel work
3. Other registries (`activityTemplate`, `vesselRegistry`) are impulse-resolvable today per CLAUDE.md §3

No N-fold expansion. Lifecycle event registry + shape-catalog-impulse projection are deferrable; resolver-registry is the binding constraint for Finding 11a.

## Verified gaps in existing registries

Subagent's verify-before-claim work:

**Discovery-vessel `POST /resolve`**: returns vessels-by-shape, NOT resolver-ids. Per `repos/discovery-vessel/src/resolvers.ts:27-55`:
```
{ shape, vessels: [{vesselId, vesselName, endpoint, protocol, ...}], found }
```
No `resolver_id` field anywhere. Tells you which vessel claims a shape — not which resolver function inside goal-host handles it.

**Activity-api `GET /v2/shapes`**: persistent shape catalog (`shape_definition` table), populated by `POST /v2/shapes`. NOT the union of vessel-advertised shapes. Two distinct registries exist:
- Catalog (activity-api, persistent, versioned) at `routes/shapes.ts:449-499`
- Live advertisement (discovery-vessel, in-memory, TTL-expiring) at `discovery-vessel/src/index.ts:221-227`

**Existing `*_registry` impulse shape**: grep returned 0 matches across `ias-executor-ts`, `metabob-activity-api`, `goal-host-vessel`, `discovery-vessel`. No vessel currently advertises a `*_registry_query` impulse shape.

## Goodhart-protection design (subagent #26 §5)

Response carries:
- `source_vessel: string` — which vessel claims the registry-state
- `registry_version: string` — commit-sha of goal-host

Verify-claim activity compares trace-time version vs template-author-time version → catches deploy-mismatch where the resolver-registry shifted between template-author moment and template-execute moment.

This is the seam that distinguishes "resolver X exists per v=abc" from "resolver X exists per v=def." Without it, stale-registry false-positives are unavoidable across deploys.

## Correction to iter-046 framing

Subagent corrects: registry-query primitives are **L0 enablers, not L1 detectors**. One L0 primitive unlocks multiple L1 detectors:
- `claim-must-cite-shipped-resolver` (Finding 11a)
- `claim-must-cite-shipped-shape` (latent; uses existing shape registries)
- `claim-must-cite-lifecycle-event` (future, requires lifecycle-event registry)
- `claim-must-cite-existing-template` (latent; uses existing activityTemplate shape)

My iter-046 L1 layer conflated detector-design with enabling-primitive-design. Updated framing:

| Layer | Enabling primitive | Detector activity |
|-------|-------------------|-------------------|
| L0 | (live today) | execution outcomes |
| L0+ | resolver_registry_query, shape catalog, vesselRegistry, etc. | (none yet) |
| L1 | (above L0+ primitives) | invariant-breach detectors |
| L2 | (above L1 detectors) | V_X verification |
| L3+ | (above L2 verdicts) | meta-audit |

L0+ is the layer subagent #26's primitive lives in. Multiple L1 detectors compose on top of one L0+ primitive — that's the leverage.

## The full operator-floor bundle to close Findings 10+11

| Item | LOC | Hours | Closes |
|------|-----|-------|--------|
| `resolver_registry_query` primitive on goal-host-vessel | ~36 | 2-3 | enables 11a |
| `substrateGap` schema extension (2 fields + 1 enum) per iter-047 §2 | ~30 | 0.5 | enables all invariant_breach routing |
| `discipline-violation-detector` seed template | JSON | 0.75 | observes Finding 10 |
| `close-invariant-breach-substrate-authorable` closer | JSON | 0.5 | corrects Finding 10 (bootstrap) |
| `verify-claim-against-registry` activity | JSON | 1.0 | observes Finding 11 |
| **Total** | ~70 LOC + seed JSON | ~5h | Findings 10 + 11 |

After this bundle:
- Finding 10 closes via discipline-violation-detector → closer → `activityTemplate_update proposed:true`
- Finding 11 closes via verify-claim-against-registry catching `activity_fetch` pre-write
- Per-additional-invariant cost drops to ~45min (detector + invariant_id enum entry; no schema change, no primitive change)

## Why not ship more L0+ primitives now

Subagent #26's argument for scope-discipline:

1. Resolver-registry has ONE source of truth (`runtime.resolvers` in `ias-executor-ts`) — no divergence risk
2. Shape catalog vs advertisement divergence exists but isn't blocking Finding 11a
3. Lifecycle-event registry would require grep-time emit-site cataloging — significantly more work
4. Activity-template references already verifiable via existing `activityTemplate` shape — no new work

Each additional L0+ primitive has progressively higher operator-cost AND lower marginal value, since the resolver-hallucination pattern is the dominant Finding-11 reproduction case (both fm-43 and fm-17 templates reference `activity_fetch`).

## Composition with prior arc

- iter-047 abstraction (substrateGap extension): unchanged; verify-claim activity emits `category:"invariant_violation", invariant_id:"claim-must-cite-shipped-resolver"`
- iter-046 layer model: updated (L0+ inserted; registry-query is L0+, not L1)
- iter-045 invariant-observability framing: confirmed; L0+ primitives are the load-bearing enablers
- iter-040 V_X: registry-query response is structured + deterministic; V_X is a trivial wrapper
- iter-042 goal-semantics: structurally parallel (expose existing in-process function via shape); same operator-floor pattern

## Audit's posture

Channel-ready. The bundle is operator-actionable. Audit names this once and stops pushing per iter-041 protocol.

Three new findings expected post-bundle:
- Finding 10 status flips to "closed (substrate-self-fix via discipline-violation-detector + close-invariant-breach-substrate-authorable)"
- Finding 11 status flips to "closed (pre-write verify-claim-against-registry catches hallucinated resolvers)"
- The bundle ITSELF becomes the L0+ pattern others compose on; per-additional-invariant cost drops measurably

## Loop bookkeeping

- iter-046 L1→L4 framing corrected; L0+ inserted
- Subagent #26 transcript: `/tmp/.../a0b7a4459d0fc4ea2.output`
- Two substrate-authored templates now in registry, both with `activity_fetch` hallucination + `proposed:false`
- Findings 10, 11 (and 11 reproduction), 12 remain in channel
- Monitors `bmzpcld51` + `bj4wvnnk7` armed
- Next observation: any commit touching `goal-host-vessel/src/index.ts` exposing resolver registry, OR touching `substrate-gap.ts:45-65` schema, OR seeding `discipline-violation-detector` / `verify-claim-against-registry` activities
