---
investigation_id: 2026-05-28T20-15-00Z-investigation-060
generated_at: 2026-05-28T20:15:00Z
investigator_session_id: claude-opus-4-7-audit-iter-045
prior_investigation_id: 2026-05-28T20-00-00Z-investigation-059
loop_iteration: 45
framing: experiment 9 confirms trace-sink drops impulse bodies — origin_layer scope expands; pattern matches Finding 8 wire-bridge break
substrate_target: http://localhost:18080
---

# Investigation 060 — Trace-sink drops impulse bodies; origin_layer scope expanded; Finding 8 pattern recurs

## Experiment 9 — haiku goal

Goal: "Write a haiku about software engineering."
Dispatched: `draft-gap-closing-activity` (9th experiment; draft-gap-closing now appears in operator-goal routing alongside its boredom-rotation role)
Trace: status:success, 6 tasks, all output IDs but no embedded bodies

## The trace-persistence finding

Inspected exec_eqq355xs trace via correct endpoint (GET /v2/activities/execution-traces/:id per subagent #30). Task structure (literal `task[0]` keys):

```
["description", "duration_ms", "input_impulse_ids", "output_impulse_ids",
 "resolver_id", "status", "success", "task_id", "tool_calls"]
```

**No `output_impulses` field. No `outputs` field. No body or metadata field.** Tasks ship only impulse IDs as string arrays.

Trace body keys (literal):
```
["account_id_version", "activity_id", "content_source", "cost_usd",
 "created_at", "duration_ms", "executed_at", "execution_id", "id", "org_id",
 "selection_attribution", "state_snapshot", "status", "stored_at", "success",
 "tags", "tasks", "tokens_cache", "tokens_input", "tokens_output",
 "updated_at", "variant_id"]
```

`state_snapshot` may contain something but not directly impulse-keyed. The structure ships task IDs, not impulse bodies.

## Refinement of subagent #30's origin_layer scope

Subagent #30 designed 4-emission-site emission of `origin_layer` at ~15 LOC. **Necessary but not sufficient.**

Pipeline today:
1. Resolver returns impulse with metadata → in-process at vessel
2. Goal-host proxy synthesizes or passes through → in-process at goal-host
3. Engine `ensureImpulse` at `engine.ts:599-601` spreads metadata into store → in-engine memory
4. Engine ships task record to activity-api via traceSink → **HERE: only IDs ship, bodies dropped**
5. Activity-api stores trace → bodies absent from storage
6. Audit queries trace → only IDs visible

origin_layer set at layer 1, 2, or 3 disappears by layer 5. Audit at layer 6 sees zero.

**Revised origin_layer operator-floor scope**:
- Emission (subagent #30): ~15 LOC
- Trace-sink-side impulse-body preservation: ~30-50 LOC at `repos/metabob-activity-api/src/services/activity-api-trace-sink.ts:67-149` (per Finding 8 location) OR `repos/ias-executor-ts/src/adapters/activity-api-trace-sink.ts:67-149`
- Activity-api schema field for embedded impulse bodies on trace tasks
- **~60-80 LOC total**, closer to 1.5h than <1h

## This is Finding 8's pattern, fourth symptom

Finding 8 (signature wire-bridge break) listed three symptoms: success_rate stale, impulse_relevance_metrics empty, context_thompson_scores empty. iter-037 located the break at TranslatingTraceSink dropping `state_space_signature` + `input_impulse_shapes`.

**Fourth symptom now confirmed**: impulse bodies/metadata also stripped. Same structural bottleneck.

| Symptom | Stripped at | Notes |
|---------|-------------|-------|
| state_space_signature (Finding 8 #1) | TranslatingTraceSink | Finding 8 evidence |
| input_impulse_shapes (Finding 8 #2/3) | TranslatingTraceSink | Finding 8 evidence |
| impulse metadata (origin_layer, source, degraded) | TranslatingTraceSink (verified iter-060) | New |

**The single structural fix unblocks BOTH Finding 8 AND origin_layer**: extend TranslatingTraceSink to preserve impulse bodies (with metadata) on the way to activity-api, plus activity-api schema for the embedded impulse bodies on trace tasks.

## Composition with prior arc

- **iter-037 Finding 8**: same code surface; combined fix
- **iter-048 resolver_registry_query**: independent — operates pre-write, doesn't depend on trace pipeline
- **iter-058 origin_layer**: scope expanded; ships with trace-sink fix
- **iter-052 commit-spec**: still tight; Finding 13 fix at proxy catch is independent
- **iter-059 audit-tool discipline**: GET endpoint is the right path BUT impulse not stored → no body retrievable anyway. Audit's L1+ path is structurally blocked until trace-sink preserves bodies.

## Updated L0+ stack

| Primitive | Surface | LOC | Hours | Notes |
|-----------|---------|-----|-------|-------|
| Finding 13 proxy-catch fix | goal-host-vessel | ~10 | 0.5 | Closes Finding 13 alone |
| `resolver_registry_query` | goal-host-vessel | ~36 | 2-3 | Closes Finding 11 reliably |
| TraceSink impulse-body preservation | ias-executor-ts adapter + activity-api schema | ~50 | 1.5 | Closes Finding 8 + enables origin_layer + audit body-access |
| `origin_layer` emission | 4 sites | ~15 | <1 | Depends on TraceSink fix |
| `goal_semantics` | activity-api | ~30 | 1-2 | Closes iter-042 mismatch-detector path |
| **Combined operator-floor** | | **~140 LOC** | **~6 hours** | All Findings 8/11/13 + L1+ enablers |

Earlier ~80 LOC estimate undercounted the trace-sink fix's cost.

## The pattern across findings

Multiple findings now point at the trace-sink as the structural bottleneck. Pattern: **the boundary between in-engine state and persisted state strips artifacts at multiple semantic axes**:

- Finding 8: signature axis (computed at engine, lost at sink)
- iter-060: impulse-body axis (synthesized at engine/proxy, lost at sink)
- Potentially more: pre-write event metadata, resolver-tier provenance, lifecycle subscription details

Each finding surfaces a different axis being lost. **Fixing one axis (e.g., just signature) leaves the others; fixing the sink-side preservation discipline closes all current and future axes systematically.**

iter-046 L0+ pattern recursive: TraceSink-preservation isn't a single primitive but a discipline-extension that all artifact-bearing axes need. The discipline (preserve incoming metadata) is the L0+; each specific field is downstream.

## Audit posture

- iter-058 retraction has 2 sub-causes confirmed:
  - (a) wrong endpoint (audit-tool fix)
  - (b) impulse not stored AT activity-api impulse table (proxy in-process), AND
  - (c) NEW: bodies not preserved in trace either (this turn's finding)
- Sub-cause (c) makes (a)+(b) moot from audit's perspective — even the "correct" path returns no body
- origin_layer scope grew from ~15 LOC to ~60-80 LOC due to trace-sink dependency
- Finding 8 + origin_layer now share a single structural fix

## Loop bookkeeping

- 9 experiments completed; pattern + recursion-floor + trace-sink axis all characterized
- Subagent #30 returned design; this iteration refined the design's hidden dependency
- 9th distinct routing: draft-gap-closing now used for operator goals (was boredom-only)
- Monitors armed; dev's lift declaration commit 599c6213 still awaiting operator gate
- Next observation: any commit touching TranslatingTraceSink at activity-api-trace-sink.ts:67-149
