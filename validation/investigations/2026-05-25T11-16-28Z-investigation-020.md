# Investigation 020 — Post-Restart Audit

**Generated:** 2026-05-25T11:16:28Z  
**Loop iteration:** 35  
**Prior:** investigation-019 (2026-05-25T10:38:48Z)  
**Substrate:** http://localhost:18080 — HEALTHY (up 17 min post-restart)

---

## Summary

The substrate restarted between investigation-019 and investigation-020. This restart activated two significant consequences: (1) the Thompson posterior reset bug (F-081) materialized, destroying 569+ executions of accumulated validator-dispatch learning; (2) the analysis-vessel deployment (commits 06bd8c04 + c696acff) is live but its discovery registration fails with 401, making it unreachable.

S.4a is confirmed **FAILED** — the window closes in 34 minutes and the structural block (flat reachable_learned=17/23) was never resolved.

---

## 1. Substrate State

Container `substrate-live` restarted at **2026-05-25T10:59:01Z** (17 minutes before this investigation). Image includes the analysis-vessel commits but NOT the posterior fix (c1364e4). Health is healthy.

Bootstrap-seeder ran at 10:59:12Z: 13/13 templates seeded. UPSERT CONTENT wiped all Thompson posteriors.

---

## 2. S.4a Final Verdict: FAILED

**Window:** 07:50:27Z → 11:50:27Z (34 min remaining at investigation time)

**Criterion:** 4 consecutive 1h windows with strictly-increasing `reachable_learned`

**Evidence against PASS:**
- Windows 1 (07:50Z) and 2 (09:40Z): `reachable_learned=17/23` — flat, not increasing
- Window 3 (expected ~11:30Z): substrate was down at ~10:57-10:59Z during the restart transition; even if coverage-tick fires before 11:50Z, the structural block (no executions of the 6 zero-exec templates) cannot produce new learned shapes
- Post-restart traces (19 total): 0 coverage-tick executions; Thompson monoculture continues (validator-dispatch α=85 dominates all routing)

**Verdict: FAILED** — insufficient consecutive windows with strictly-increasing `reachable_learned`. Compounded by substrate down event and posterior reset.

The window had been structurally decided before the restart (investigation-019 verdict: "S.4a WILL FAIL"). The restart changed nothing about the structural outcome.

---

## 3. Posterior Reset Bug: MATERIALIZED (F-083 NEW)

**Warning from investigation-019 (F-081):** "Do NOT restart with current image — bootstrap-seeder CONTENT wipe will destroy posteriors."

**Outcome:** The restart deployed the new image (analysis-vessel commits) but did NOT include the c1364e4 posterior fix. Bootstrap-seeder ran the old UPSERT CONTENT code.

| Template | Prior α (019) | Prior n (019) | Now α (020) | Now n (020) | Lost |
|---|---|---|---|---|---|
| validator-dispatch | 300 | 651 | 85 | 82 | 569 execs |
| slot-binding | 44 | 89 | 13 | 12 | 77 execs |
| create-shape-provider-goal | 1 | 91 (failures) | 1 (β=13) | 12 | reset |
| coverage-tick | 2 | 4 | 1 | 0 | 4 execs |

**Confirmed by:** `docker exec substrate-live grep -n 'UPSERT activity' /vessels/activity-api/src/routes/activities.ts` → lines 1200 and 5268 still show `CONTENT` not `SET`. The c1364e4 fix is absent from the running container.

---

## 4. Analysis-Vessel Migration Audit (F-084, F-085 NEW)

**Commits:** 06bd8c04 (analysis-vessel Phase 1), c696acff (cpg-inference-ts Docker fix)

**Structural compliance:** YES — `repos/analysis-vessel/src/index.ts` correctly uses `VesselDaemon` from `@avigopal/ias-executor-ts`, imports `@metabob/cpg-inference`, advertises the 6 required shapes (problem_detection, error_log, source_code, code_quality, code_annotation, cpg_query_result), runs on port 8250, uses `DiscoveryRegistrationLoop`. Matches the spec in `openspec/changes/2026-05-25-metabob-vessel-migration/design.md`.

**Operational status:** FAILING — discovery registration returns 401 at startup, then all heartbeats hit 404 (vessel never registered, no slot in registry).

```
10:59:01 [DiscoveryRegistrationLoop] register failed: 401 — vessel will be unreachable via discovery
11:00:01 – 11:16:01: heartbeat HTTP 404 (failure #1–#17)
```

**Discovery registry:** 7 vessels, 106 shapes — unchanged. analysis-vessel contributes 0 shapes because it's not registered.

**Acceptance gates (from spec):**

| Gate | Status |
|---|---|
| Registers in discovery with 6 shapes | FAIL |
| POST /v2/impulses/resolve returns problem_detection | UNTESTABLE |
| POST /v2/impulses/resolve returns source_code | UNTESTABLE |
| GET /health returns discovery.registered=true | FAIL |
| systemd unit added | PASS |

**Root cause:** `METABOB_API_KEY` likely missing from `scripts/substrate/units/analysis-vessel.service` — the 401 on registration is an auth failure, not a discovery-vessel routing issue. Fix: add the env var and `systemctl restart analysis-vessel.service`.

---

## 5. Thompson State Post-Restart

The monoculture is rebuilding from scratch:
- validator-dispatch: α=85, β=1, n=82 (was α=300, n=651) — dominant but weakened
- slot-binding: α=13, β=1, n=12 (was α=44, n=89)
- create-shape-provider-goal: α=1, β=13 (13 failures in 17 min post-restart; structural bug drives rapid β accumulation)
- 7 templates at α=1, β=1, n=0 (reset to base; includes coverage-tick)

Without the posterior fix, the next restart will wipe this again.

---

## 6. Routine Checks

**Hardcoded URLs:** None found in identity-vessel/src or discovery-vessel/src.  
**Stray PNGs at root:** 0.  
**Cycle reports:** Most recent cycle-6.json (2026-05-23). No new cycles.  
**Proposals:** 4 proposals, all from 2026-05-22. No new proposals.

---

## Open Findings Summary

| ID | Category | Severity | Status |
|---|---|---|---|
| F-083 (NEW) | regression | critical | posterior reset materialized |
| F-084 (NEW) | regression | high | analysis-vessel not registered in discovery |
| F-085 (NEW) | validation | medium | analysis-vessel partial spec compliance |
| F-079 | provenance | high | c1364e4 fix not in running image |
| F-074 | lift | high | S.4a window closes in 34 min |
| F-075 | lift | high | reachable_learned flat, S.4a FAILED |
| F-076 | provenance | medium | coverage-tick posteriors reset |
| F-036 | validation | high | create-shape-provider-goal parent-fail pattern persists |
| F-053 | validation | medium | failure_mode=null on failures |

**Tally:** 11 open, 0 resolved, 2 new regressions materialized.

---

## Operator Actions Needed

1. **Fix analysis-vessel auth** — add `METABOB_API_KEY` to `scripts/substrate/units/analysis-vessel.service`, then `docker exec substrate-live systemctl restart analysis-vessel.service`. This is a one-line fix to complete the Phase 1 migration.

2. **Deploy posterior fix** — verify `repos/metabob-activity-api` is at c1364e4, rebuild image (`make -C scripts/substrate build`), restart. Without this, every restart resets all Thompson posteriors.

3. **Bootstrap unlearned shapes** — manually trigger one of: `branch-health`, `release-change`, `ship-change`, `add-resolver-to-vessel`, `propagate-judgment`, `scaffold-new-vessel`, `run-sensitivity-probe`, `audit-test-report`, `probe-untraversed-edge` to increase `reachable_learned` past 17. Required for S.4b or the next lift criterion.

---

## Next Wake

**Delay:** 1800s (warm cache)  
**Wake at:** ~12:46Z  
**Verify:** S.4a window closed + final verdict, analysis-vessel registration fix, Thompson recovery progress.
