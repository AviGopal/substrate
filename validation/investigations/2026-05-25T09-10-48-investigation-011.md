---
investigation_id: 2026-05-25T09-10-48-investigation-011
loop_iteration: 27
generated_at: 2026-05-25T09:10:48Z
prior: 2026-05-25T08-58-34-investigation-010
---

# Investigation 011 — Thompson Confirmed; First Topology Success; S.4a Still Blocked

## Context

Two major commits arrived since investigation-010:
- `1702cad7`: Validator iter-21 narration claiming F-037/F-043 (Thompson flat) RESOLVED, plus F-051/F-052/F-054
- `156f1c30`: goal-host-vessel now registers `activity_recommendation` built-in resolver at startup

Container was restarted mid-investigation (~09:03:36Z). Bootstrap-seeder re-ran and confirmed 13/13 templates seeded.

## Critical Audit: F-037/F-043 — Thompson Posteriors

**VERDICT: CONFIRMED RESOLVED (F-037, F-043, F-062 all closed)**

The normalizeActivityId fix IS in the running container. Evidence:
1. `docker exec substrate-live grep -n normalizeActivityId /workspace/repos/metabob-activity-api/src/lib/posterior-update.ts` → **line 437** present. Bun runs TypeScript directly from source (`/workspace`) — no separate compile step. Fix is live.
2. `GET /v2/activities/templates/activity:⟨validator-dispatch⟩` → `α=2, β=1, execs=285, rate=1.0`. A posterior stuck at the initial value would show α=1. α=2 with 285 executions is definitive.
3. `GET /v2/activities/templates/activity:⟨slot-binding⟩` → `α=2, β=1, execs=39, rate=1.0`. Same pattern.

These were the "100+ executions flat" templates from prior investigations. They are no longer flat.

Prior investigation (010) caveat that "the fix might not be in the running image" is resolved. Bun's interpreted execution model means the source and the running code are the same file.

## Major New Event: First Topology Goal Success

**Finding: F-061 PARTIALLY RESOLVED**

Boredom-vessel goal[0] "measure the substrate topology and report coverage progress" completed successfully at 09:09:24Z:
```
09:05:37  [boredom-vessel] submitting goal[0]: "measure the substrate topology..."
09:09:24  [boredom-vessel] dispatched — executionId=exec_eoil758o status=completed
```
Duration: **3 minutes 47 seconds**. Prior behavior: 300s timeout or SIGTERM.

The `activity_recommendation` resolver is operational — confirmed in startup logs:
```
[goal-host-vessel] registered built-in resolver: activity_recommendation
[goal-host-vessel] registered 30 development-vessel proxy resolvers
```

**However: Wrong template selected.** exec_eoil758o used `activity:⟨replace-activity⟩` (α=2, β=1, 1 prior exec success) NOT `development-vessel:coverage-tick` (α=1, β=2, 3 prior exec failures). Thompson correctly chose the template with better posterior — but that template is semantically wrong for "measure topology". This is **new finding F-056** (semantic mismatch in template selection for topology goals).

## Coverage-Tick Status: Still 0 Successes

```
coverage-tick: α=1, β=2, execs=3, success_rate=0
```
Unchanged from iter-010. No new executions. S.4a remains blocked.

The feedback cycle is adverse: coverage-tick fails → β increases → Thompson deprioritizes it → boredom selects other templates → coverage-tick never gets a chance to succeed. This is a cold-start failure mode requiring intervention or forced execution.

## Validator Discrepancy: create-shape-provider-goal

Validator iter-21 claimed: "create-shape-provider-goal: 33 execs, 24% success, α~2"

Live substrate shows: `execs=35, success_rate=0, α=1, β=2, avg_duration_ms=0.19`

The 0.19ms average means these are essentially **instant failures** — same class as F-051 (proxy resolver not registered). The 21 bare-form traces (`create-shape-provider-goal`) all fail in 3ms. The 3 full-form traces (`activity:⟨create-shape-provider-goal⟩`) fail in ~89s. Neither path is succeeding.

This discrepancy with the validator's 24% success claim is significant. The validator may have queried at a different substrate state, or the metrics were from templates that no longer exist in this DB. Filed as F-050.

## Activity_recommendation Resolver

Confirmed live. Goal-host-vessel logs it at startup, confirmed on both restarts. The resolver:
- Forwards to `POST /v2/activities/recommend`
- Passes goal/targetShape from execution variables
- Returns activity recommendations by Thompson posterior

It's working — the goal succeeded. The semantic mismatch problem (replace-activity chosen for topology) is a downstream issue with how the recommendation is used, not a resolver registration problem.

## Boredom Loop Pattern

| Goal | Status | Pattern |
|---|---|---|
| goal[0]: measure topology | COMPLETED (09:09:24, 3m47s) | Selected replace-activity |
| goal[1]: probe unlearned shapes | FAILED in 1s | Instant failure |
| goal[2]: run full topology chain | SIGTERM at 18s | systemd unit kill |
| goal[3]: identify missing shapes | SIGTERM at 24s | systemd unit kill |
| goal[4]: check substrate health | "unreachable" | goal-host was restarting |

Goals [2] and [3] are being killed by SIGTERM within 20-24 seconds despite `TimeoutStartSec=300`. This suggests a different systemd limit (possibly `RuntimeMaxSec` or the boredom-vessel.service oneshot mode has a separate kill timer). Filed as F-057 (persistent).

## Boredom Trace Visibility

**F-047 RESOLVED.** Validator iter-21 claimed boredom traces were invisible. exec_eoil758o (the topology goal) IS visible as the most recent trace in `/v2/activities/execution-traces`. Boredom traces appear under the selected template's activity_id. The validator's claim was either incorrect or condition-specific to that measurement window.

## Template Count Anomaly (F-023, F-046)

`GET /v2/activities/templates?limit=200` → `total=28, returned=2`. Bootstrap-seeder seeded 13/13. Direct ID lookups work (coverage-tick, validator-dispatch, etc. all queryable). This appears to be a Redis cache or FTS index artifact — the default-query path returns only 2 templates even though 28 exist. Impact: FTS-based discovery is severely degraded.

## S.4a Window Status

**BLOCKED.** Requires coverage-tick to succeed with `coverage_progress=true`.

- No new cycle files since cycle-8 (2026-05-23)
- lift-status.json still `phase=S1`
- coverage-tick: 0 successes, β=2 posterior
- substrate-health-tick: same state (α=1, β=2, execs=3, rate=0)

The execution loop IS running (validator-dispatch accumulating, slot-binding accumulating), but the topology-measurement templates aren't succeeding, so S.4a window never arrives.

## Findings Tally

| Status | Count |
|---|---|
| Resolved this iteration | 5 (F-037, F-043, F-062, F-047, F-045) |
| Partially resolved | 1 (F-061) |
| New findings | 3 (F-050, F-056, and F-047 reclassification) |
| Open | 26 |
| Unverifiable | 1 (F-008) |
| **Total** | **66** |

## Next Steps for Development Agent

1. **Coverage-tick cold-start**: Thompson posterior is stuck at β=2. Either (a) force a coverage-tick execution via `docker exec substrate-live curl localhost:8210/run-goal` with the coverage-tick template directly, or (b) reset the posterior via DB intervention, or (c) fix the root cause of why coverage-tick fails.

2. **create-shape-provider-goal instant failures**: 35 execs at 3ms suggests the proxy resolver path still doesn't work for this template. What resolver does create-shape-provider-goal use in its first task?

3. **Goals [2],[3] SIGTERM**: Why are boredom goals [2] and [3] killed at 18-24s when TimeoutStartSec=300? Is there a `RuntimeMaxSec` or a rapid successive-start limit in the boredom timer unit?

4. **Template selection semantic mismatch**: The activity_recommendation resolver picks by Thompson posterior but the goal description semantics aren't factored in. "measure topology" should prefer coverage-tick but gets replace-activity because the prior is better. This is a Thompson cold-start problem — templates that fail accumulate β, get deprioritized, never recover without intervention.

## Verification Commands

```bash
# Thompson status
curl -H "Authorization: ApiKey $APIKEY" "http://localhost:18080/v2/activities/templates/activity:⟨validator-dispatch⟩" | python3 -c "import json,sys; d=json.load(sys.stdin); m=d.get('metrics',{}); print(f'α={m[\"thompson_alpha\"]} β={m[\"thompson_beta\"]} execs={m[\"total_executions\"]}')"

# Coverage-tick
curl -H "Authorization: ApiKey $APIKEY" "http://localhost:18080/v2/activities/templates/activity:⟨development-vessel:coverage-tick⟩" | python3 -c "import json,sys; d=json.load(sys.stdin); m=d.get('metrics',{}); print(f'α={m[\"thompson_alpha\"]} β={m[\"thompson_beta\"]} execs={m[\"total_executions\"]} rate={m[\"success_rate\"]}')"

# Boredom status
docker exec substrate-live journalctl -u boredom-vessel.service --no-pager -n 10

# Topology goal success check
curl -H "Authorization: ApiKey $APIKEY" "http://localhost:18080/v2/activities/execution-traces?limit=5" | python3 -c "import json,sys; [print(f'  {t[\"activity_id\"]} ok={t[\"success\"]} dur={t[\"duration_ms\"]}ms') for t in json.load(sys.stdin)['executions'][:5]]"
```
