# Investigation 063 (2026-05-28) — 2026-05-28T02:35:00Z

**Prior**: `2026-05-28T02-22-57Z-investigation-062` (delta: ~12 min).

**Substrate target**: `http://localhost:18080` — **HEALTHY**.

**Trigger**: Monitor notification — `f18bb246 iter 29: LLM dispatch path — fix shape + model mismatches; trace engine gap`.

---

## Summary

One commit since investigation-062. **F-148 (HIGH)**: The substrate discovered that NO LLM calls have been happening across the entire substrate. All 53 sub-execution traces from `exec_x19p0558` (goal[7]) show `tokens_in=0/tokens_out=0`. Three bugs fixed (shape name mismatch, model prefix, wrong shape in dev-vessel resolver). Direct llm-resolver-vessel calls now work. However, a **remaining gap**: the activity execution engine still produces 0 tokens — the `llm-prompt` resolver is invoked but doesn't reach `llm.generate()`. Every prior "successful" draft-gap-closing-activity execution was hollow. The lift loop stage 2 (LLM synthesis) has never functioned.

---

## F-148 (HIGH): LLM Path Silently Non-Functional Across Entire Substrate

The substrate traced `exec_x19p0558` (goal[7] fire at 23:48Z 2026-05-27) in detail and found all 53 sub-execution traces with `tokens_in=0/tokens_out=0`. Root cause: three layered bugs.

**Bug 1 — Shape name mismatch (llm-resolver-vessel)**

```
Before: advertised "llmCompletion" (camelCase) in discovery registration
        handler registered under "llm_completion" (snake_case)
After:  advertise both; handler now matches either form
```

Any discovery client querying `llmCompletion` got 404 from the handler. Every activity that dispatched the LLM resolver via discovery was silently failing to reach the LLM.

**Bug 2 — Model name prefix**

```
Callers sent: "anthropic/claude-haiku-4-5-20251001"  (provider-prefixed)
SDK expects:  "claude-haiku-4-5-20251001"             (bare name)
Fix: strip leading "anthropic/" before Anthropic SDK call
```

**Bug 3 — dev-vessel wrong shape query**

`llm_completion_dispatch` resolver in dev-vessel was querying the wrong shape name. Fixed to try both `llmCompletion` and `llm_completion`.

**Changes shipped** (`repos/llm-resolver-vessel/src/index.ts` + `repos/development-vessel` pointer advance):

```diff
+ const rawModel = body.model ?? DEFAULT_MODEL;
+ const model = rawModel.startsWith("anthropic/") ? rawModel.slice("anthropic/".length) : rawModel;
```

**Live verified by substrate**: Direct POST to llm-resolver-vessel returns real token counts. The endpoint itself is now functional.

### The remaining engine gap

The commit message explicitly states the remaining gap:

> "Activity execution path still produces 0 tokens. Goal-host is wired with HttpLLMPort (startup log 'llm: vessel(...)') but llm-resolver-vessel sees no traffic from execution paths. The engine `llm-prompt` resolver is invoked but apparently doesn't reach llm.generate(). Needs deeper engine-level trace next iter."

The three fixes are necessary but not sufficient. The ias-executor-ts engine's `llm-prompt` resolver code path has a gap between invocation and the actual `llm.generate()` call. This is the load-bearing node for template synthesis.

### Implications for prior audit records

Every previous execution that claimed success via an LLM-backed activity was completing without LLM contribution:

- `exec_zaq88543` — goal[7] fire 1, "success, no variant" — now explained: LLM never called
- `exec_x19p0558` — goal[7] fire 2, "success, no variant" — same; 53 sub-traces, all tokens=0
- All other activities across the substrate with `llm-prompt` tasks — executing with no LLM output

The "lift loop mechanically complete" declaration in inv-060/inv-061 was premature. Stage 2 (LLM synthesis) was silently non-functional throughout.

---

## Revised Lift Loop Status

| Stage | Status |
|---|---|
| 1. boredom goal[7] dispatch | LIVE |
| 2. LLM template synthesis | **PARTIALLY FIXED** — 3 bugs addressed, engine-level gap remains |
| 3. activity_create_variant | LIVE (will work once LLM outputs something) |
| 4–8. exploration/promote | LIVE |

The substrate has identified the next debugging target: engine.ts's handling of the `llm-prompt` resolver invocation. Iter-30 likely imminent.

---

## F-146: Still Critical, No Change

```bash
git show 4d0c305e --format='%H %ae %s'
# → 4d0c305e devbob@metabob.local — not operator-signed
```

No new operator commit. Auto-promote remains running correctly (considered=6, promoted=0 — correct, no thresholds crossed).

---

## Prior Findings Status

| Finding | Status |
|---|---|
| F-148 | **HIGH NEW** — LLM path silently broken; tokens_in=0 across all traces; 3 bugs fixed, engine gap remains |
| F-147 | LOW OPEN — 16 PNG files at root |
| F-146 | CRITICAL OPEN — no operator-signed autonomous promotion authorization |
| F-145 | OPEN — not actioned |
| F-144 | RE-OPENED — subsumed by F-146 |
| F-143 | CLOSED |
| F-142 | ACKNOWLEDGED |
| F-141 | OPEN — ias-executor-ts GitHub 404 |
| F-140 | ADDRESSED |
| F-101 | OPEN |

---

## Next Wake

1800 seconds (30 min). Substrate actively debugging engine-level LLM gap — iter-30 likely imminent. 30min to catch the engine fix.

## Verification

`generated_at: 2026-05-28T02:35:00Z` — `date -u`

F-148 evidence: `git show f18bb246 --stat` + commit message

F-146 author: `git show 4d0c305e --format='%H %ae %s'`

Closure-audit: `bun validation/scripts/closure-audit.ts`
