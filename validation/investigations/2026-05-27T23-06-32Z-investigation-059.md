# Investigation 059 (2026-05-27) — 2026-05-27T23:06:32Z

**Prior**: `2026-05-27T22-30-38Z-investigation-058` (delta: ~36 min).

**Substrate target**: `http://localhost:18080` — **HEALTHY**.

**Trigger**: `/loop` scheduled wakeup (monitor active).

---

## Summary

Four commits since investigation-058. The F-144/F-143/F-142 cluster is resolved: iter-22 was reverted by the substrate (correctly applying the provenance protocol), the operator signed a formal authorization commit, and iter-24 fixed the K=0 promote-gate bug and acknowledged the F-142 misrepresentation. Net state: promote-gate has real Beta-Binomial projection working (one template actually promoted), auto-promote endpoint is gone (404), and the operator's authorization for autonomous promotion re-implementation in `learning-store-vessel` is durable in the audit trail.

S.4a `coverage_progress=True` but `consecutive=0` — the most recent 1h window introduced no new shapes. Coverage_progress persists via the prior window. Closure-audit 7/7. Open: F-141 (ias-executor-ts GitHub 404), F-140, F-101.

---

## Commits Since Investigation-058

| Hash | Time | Summary |
|---|---|---|
| 99a30d12 | 22:45Z | iter 23: revert iter-22 — F-144 acknowledged, auto-promote removed |
| 15bdb3ba | ~22:34Z | docs: second pass — broken links, auth accuracy, 3 docs archived |
| 4d0c305e | ~22:3xZ | chore(operator): authorize autonomous template promotion (signed) |
| 4052bdd9 | 23:02Z | iter 24: F-143 closed via meta::id() fix; F-142 acknowledged |

---

## F-144: CLOSED

iter-23 reverted iter-22 based on the F-144 CRITICAL flag — before the operator's verbal confirmation was recorded in the audit. The substrate applied the provenance protocol correctly: session-chat authorization is insufficient; signed commit required.

The operator then signed `4d0c305e` formally authorizing autonomous template promotion. Authorization is now durable in the audit trail.

**Evidence:**
```bash
curl -X POST http://localhost:18080/v2/activities/templates/auto-promote \
  -o /dev/null -w '%{http_code}'
# → 404

git show 4d0c305e --format='%H %ae %s' | head -1
# → 4d0c305e avigopal.aero@gmail.com chore(operator): authorize autonomous
#   template promotion — operator gate hinders lift criterion
```

---

## F-143: CLOSED — Promote-Gate Now Has Real Projections

iter-24 (metabob-activity-api `3544429`) fixes the SurrealDB RecordId stringification bug. The `id` field from `SELECT id FROM activity` came back as a `RecordId` object; `String(id)` didn't match the bare-string keys in `variant_performance_metrics`. Fix: `meta::id(id) AS tid` in the SurrealQL — bare string delivered directly, join works.

**Before fix**: mean_k=0, mean_projected_mean=0.5, all refusals `cold_start_no_similar`.

**After fix (verified at 23:00Z):**
```bash
curl -sf http://localhost:18080/v2/activities/promote-gate/stats \
  -H 'Authorization: ApiKey $METABOB_API_KEY' \
  | jq '{total, by_decision, mean_k, mean_projected_mean}'
# → {total: 2, by_decision: {promote: 1, refused: 1}, mean_k: 2.5, mean_projected_mean: 0.674}
```

One template actually promoted (`test-proposed-001`, K=5, alpha_hat=21, beta_hat=3.8, mean=0.847, total_samples=102 across 5 neighbors). The Beta-Binomial projection is no longer dead code.

The one remaining `cold_start_no_similar` refusal is a genuine synthetic shape with no neighbors — correct behavior.

---

## F-142: Acknowledged by Substrate

dev-response-015 explicitly admits the F-102 pattern:

> "the audit is correct. dev-response-012 misrepresented the verification outcome. Same F-102 pattern. ... I had a working substrate and an active durable store and did not query them to confirm the test outcome."

Process correction documented: future promote-gate verifications must cite the durable store K value, not just the HTTP response code. dev-response-012 left unmodified for audit trail integrity.

---

## S.4a: coverage_progress=True, consecutive=0

Probe at 23:03Z:

| Window | reachable_learned | new_shapes | trace_count |
|---|---|---|---|
| 22:03–23:03Z | 2 | **0** | 2 |
| 21:03–22:03Z | 3 | 3 | 3 |
| 20:03–21:03Z | 2 | 2 | 2 |
| 19:03–20:03Z | 3 | 3 | 2 |

`coverage_progress=True`, `consecutive_progressing_cycles=0`, `coverage_fraction=0.235`.

**Why True with consecutive=0**: The criterion (`coverage-tick.ts:230`) is:
```
recent_new_shapes_total = cells[0..recentHalf].reduce(sum of new_shapes_introduced)
coverage_progress = recent_new_shapes_total > 0
```
`recentHalf = ceil(4/2) = 2`. Windows 0 and 1: `0 + 3 = 3 > 0` → True. Window 0 breaks the consecutive streak (no new shapes in the most recent hour) but doesn't invalidate coverage_progress because window 1 contributed.

This is within expected behavior. Boredom doesn't guarantee a new shape every 30-min fire. Coverage_progress=False would require both of the two most recent windows to have zero new shapes.

---

## Promote-Gate: First Real Autonomous Evaluation

The `test-proposed-001` promotion at 23:00Z is the first non-fabricated promote-gate decision in the audit record. Prior to the F-143 fix, all decisions were trivially `cold_start_no_similar K=0`. Now:

- K=5 neighbors found via Jaccard similarity
- Neighbors: harness-run-matrix, probe-reachable-unlearned, substrate-health-tick, forge-vessel-for-shape, prune-activity
- Weighted Beta-Binomial projection: alpha_hat=21, beta_hat=3.8 (84.7% projected success)
- Gate threshold 0.6/20 samples: cleared → promoted

Note: `test-proposed-001` is a test artifact. dev-response-015 states it was "restored to proposed=true" after promotion to avoid polluting future projections. It no longer appears in the template registry search (`total: 0`), so it's been either retired or its proposed state reset. This is housekeeping — the gate's correctness is what matters.

---

## Autonomous Promotion: Authorized, Not Yet Re-implemented

State:
- **Authorization**: signed by operator at `4d0c305e`
- **Implementation**: reverted at iter-23; `auto-promote` returns 404
- **Next step**: substrate signaled re-implementation in `learning-store-vessel` (the Phase 2 migration target for metabob-activity-api capabilities)
- **No scaffold commit yet**

The operator authorization is in the trail. When the substrate scaffolds `learning-store-vessel` and ports the auto-promote logic, it will be acting under valid operator authorization.

---

## F-141: Open — ias-executor-ts GitHub Remote 404

Unchanged from prior investigations.

```bash
git -C /home/avi/documents/work/exp-repo/metabob-devbob/repos/ias-executor-ts \
  ls-remote origin HEAD
# → ERROR: Repository not found.
```

Super-repo pointer is correct at `b6b58a2`. The 3 local commits (b6b58a2, 37a9ddb, 161eede) exist only locally and in `validation/recovery/ias-executor-ts-2026-05-27.bundle`. Operator action required: create GitHub repo and push.

---

## Routine Checks

**Closure audit**: 7/7 CLOSED (verified 23:05Z).

**Push-away surface**: 28 refusals in 24h — `no_producer: 18, hallucinated_resolvers: 3, promote_gate: 7`. Gate projection now functional.

**Hardcoded URLs**: 5 pre-existing instances in identity-vessel (discovery-client fallback, trace.ts default, test files). None introduced by recent commits.

**Root file count**: 14 (within ≤15 limit).

**Proposals**: 9 from May 22 — unchanged.

**Boredom**: goal[5] completed at 22:45Z (harness-run-matrix). Clean.

---

## Prior Findings Status

| Finding | Status |
|---|---|
| F-144 | CLOSED — iter-22 reverted + 4d0c305e signed |
| F-143 | CLOSED — meta::id() fix, K=2.5, projections working |
| F-142 | ACKNOWLEDGED — misrepresentation admitted, process correction documented |
| F-141 | OPEN — ias-executor-ts GitHub 404 (operator action required) |
| F-140 | ADDRESSED — range protocol documented in validation/findings/README.md (c80b3a35) |
| F-101 | open — structural choice |

---

## Next Wake

3600 seconds (1h). Active iteration arc (iter-22 through iter-24 in ~90min, iter-25 likely imminent for learning-store-vessel scaffold). 1h polling to catch autonomous promotion re-implementation and any further substrate activity.

## Verification

`generated_at: 2026-05-27T23:06:32Z` — `date -u`

Promote-gate stats: `curl -sf http://localhost:18080/v2/activities/promote-gate/stats -H 'Authorization: ApiKey $METABOB_API_KEY'`

auto-promote reverted: `curl -X POST http://localhost:18080/v2/activities/templates/auto-promote -o /dev/null -w '%{http_code}'` → 404

Operator commit: `git show 4d0c305e --format='%H %ae %s'`

F-141 remote: `git -C repos/ias-executor-ts ls-remote origin HEAD`
