---
investigation_id: 2026-05-30T11-00-00Z-investigation-083
generated_at: 2026-05-30T11:00:00Z
investigator_session_id: claude-opus-4-7-audit-iter-083
prior_investigation_id: 2026-05-30T09-15-00Z-investigation-082
loop_iteration: 83
framing: subagent — execution forensics on exec_151uni4h + exec_ftzw0hvl (draft-spec-from-gap); identify dispatcher, LLM output state, write outcomes, composition with iter-080 audit-ingestion + drain template; NO code
substrate_target: repos/development-vessel/src/seed/draft-spec-from-gap.ts (no edits) + activity-api trace store
---

# Investigation 083 — draft-spec-from-gap execution forensics (research; NO code)

## §1 — Static cite reconfirm

`repos/development-vessel/src/seed/draft-spec-from-gap.ts:108-235` defines **10 tasks**, not 9 as iter-082 stated. Verified by `grep -nE '^\s+id: "' draft-spec-from-gap.ts:111-211`:

```
111:      id: "read_gaps"            resolver: http_fetch
122:      id: "read_priors"          resolver: http_fetch
136:      id: "read_exemplar_proposal" resolver: fs_read
149:      id: "read_exemplar_tasks"  resolver: fs_read
159:      id: "draft_via_llm"        resolver: llm_completion_dispatch
174:      id: "extract_slug"         resolver: json_path_extract
187:      id: "extract_proposal_md"  resolver: json_path_extract
198:      id: "extract_tasks_md"     resolver: json_path_extract
~210:     id: "write_proposal"       resolver: fs_write
~225:     id: "write_tasks"          resolver: fs_write
```

iter-082 §1 says "9-task graph"; the live trace tasks-array length confirms **10** (trace JSON `(.tasks | length) = 10` in both executions).

## §2 — exec_151uni4h forensics

`GET /v2/activities/execution-traces/exec_151uni4h` returns:

```json
{"execution_id":"exec_151uni4h","activity_id":"activity:⟨development-vessel:draft-spec-from-gap⟩",
 "status":"success","success":true,"duration_ms":512881,"executed_at":"2026-05-30T09:15:10.323Z",
 "cost_usd":0,"tokens_input":0,"tokens_output":0,"output_impulse_shapes":[
   "http_fetch","fs_read","llm_completion_dispatch","json_path_extract","fs_write"]}
```

No `parent_execution_id` field. No `goalContext` field. **Root execution.** All 10 tasks `success:true`. Per-task output_impulse_ids:

| task | resolver | output_impulse_id | duration_ms |
|---|---|---|---|
| read_gaps | http_fetch | `dev:http_fetch_7j9vqa3k` | 7 |
| read_priors | http_fetch | `dev:http_fetch_z89q7pum` | 211 |
| read_exemplar_proposal | fs_read | `dev:fs_read:err_g4yzggr9` | 11 |
| read_exemplar_tasks | fs_read | `dev:fs_read:err_2mnfv6pl` | 14 |
| draft_via_llm | llm_completion_dispatch | `dev:llm_completion_dispatch_jc18f8b2` | 21239 |
| extract_slug | json_path_extract | `dev:json_path_extract_2zid199g` | 9 |
| extract_proposal_md | json_path_extract | `dev:json_path_extract_120jr7sq` | 9 |
| extract_tasks_md | json_path_extract | `dev:json_path_extract_023hdh0v` | 14 |
| write_proposal | fs_write | `dev:fs_write:err_zyt7eahq` | 9 |
| write_tasks | fs_write | `dev:fs_write:err_foafjh1c` | 12 |

**Six of ten output IDs carry `:err_<suffix>`** — both fs_reads, both fs_writes. The :err_ pattern is the proxy-catch silent-success: resolver threw, but the catch wrote a tombstone impulse with prefix `:err_` and bubbled `success:true` to the trace (per audit's iter-058/iter-077 prior finding). **trace-level `status:"success"` is meaningless for this template**; the actual content tasks (2 fs_reads + 2 fs_writes = 4 of 10) silently failed.

`input_impulse_ids: []` on every task — slot-binding never wired the task graph; each task self-resolves config without consuming pool impulses.

`state_snapshot.output_state.filesCreated: []`. `filesModified: []`. **Confirms: zero files were written to disk.**

Duration 512,881 ms ≈ 8.5 min — virtually all in `draft_via_llm` (21.2s real) plus the wall-clock gap before trace insertion at 09:15:10 (execution started ~09:06:37 per log timeline).

## §3 — exec_ftzw0hvl forensics

Same pattern. `executed_at:"2026-05-30T09:21:06.556Z"`, `duration_ms:496542`. Tasks again 10, all `success:true`. Per-task IDs:

| task | output_impulse_id |
|---|---|
| read_exemplar_proposal | `dev:fs_read:err_fpq8vbnw` |
| read_exemplar_tasks | `dev:fs_read:err_calxgmin` |
| draft_via_llm | `dev:llm_completion_dispatch_h3z1mt3v` (21336 ms) |
| write_proposal | `dev:fs_write:err_vaeqhmin` |
| write_tasks | `dev:fs_write:err_oq0uwtjf` |

Identical failure topology: 4× `:err_` on the four content-bearing tasks, http_fetch + json_path_extract succeed. `filesCreated:[]`. Zero writes.

## §4 — LLM output inspection: gap confirmed

`GET /v2/impulses/dev:llm_completion_dispatch_jc18f8b2` returns:

```json
{"error":"Impulse not found","impulse_id":"dev:llm_completion_dispatch_jc18f8b2"}
```

This matches iter-058 finding: dispatch-resolver-synthesized impulses are NOT persisted to activity-api's `impulse_resolutions` table. The drafted JSON bundle exists only in-process during the execution; it is unrecoverable post-hoc. The LLM content is gone — neither the `proposal_md` nor `tasks_md` body can be inspected from outside the runtime.

This is itself a **substrate observability gap**: substrate authored content (the LLM's actual draft) has zero persistence outside the wall-clock window of the in-process resolver chain.

## §5 — Dispatcher: backward chaining via shape-provider-goal

Journald shows the dispatch chain at 09:06:30–09:06:37:

```
09:05:33  exec_cmkv6jmi  create-shape-provider-goal  success (α=737)
09:05:41  exec_dco4blen  create-shape-provider-goal  success (α=738)
09:05:43  exec_e8e62cdx  create-shape-provider-goal  success (α=739)
09:05:48  exec_jw4r2v02  create-shape-provider-goal  success (α=740)
09:06:24  exec_n6v4ktym  create-shape-provider-goal  success (α=741)
09:06:29  exec_jy1gtxau  create-shape-provider-goal  success (α=742)
09:06:30  exec_tr19exis  create-shape-provider-goal  success (α=743)
09:06:36  concept-db BM25: query "Substrate authors openspec proposal.md + tasks.md from substrateGap impulses via draft-spec-from-gap"
09:06:37  GET /v2/activities/templates/development-vessel:draft-spec-from-gap → 200
09:15:10  exec_151uni4h  draft-spec-from-gap  success (after 512s wall)
```

`create-shape-provider-goal` has fired **600+ times** (total_executions=608 at 09:06:30). It is the binding-layer's backward-chaining mechanism: when the pool needs a shape with no producer, it spawns a sub-goal whose `outputShapes` request that shape and looks up a producer template.

The BM25 query text — "Substrate authors openspec proposal.md + tasks.md from substrateGap impulses via draft-spec-from-gap" — is the **template description** (matches `draft-spec-from-gap.ts:74-79` near-verbatim). This is concept-db being queried with the template's description as the search string, which is how `create-shape-provider-goal` looks for prior context before dispatching.

**Neither boredom nor operator triggered these two executions.** They were triggered by the binding layer's recursive shape resolution requesting `specProposal`. The grandparent goal that needed `specProposal` is not visible in the trace metadata (no `goalContext`, no `parent_execution_id` on the root); the chain has been severed at the trace boundary.

## §6 — drain → templates, not specs (iter-082 framing correction)

`repos/development-vessel/src/seed/drain-pending-substrate-gaps.ts:100`:

```typescript
targetTemplateId: "development-vessel:draft-gap-closing-activity",
```

Confirmed: drain dispatches to `draft-gap-closing-activity` (template-author), **not** `draft-spec-from-gap` (spec-author). iter-080's audit-ingestion-bridge design (substrateGap → drain → template-draft) is architecturally separate from the spec-authoring path.

If audit findings are to drive spec-authoring (not just template-authoring), there is no existing pull mechanism. `draft-spec-from-gap` fires only when something *upstream* explicitly demands a `specProposal` shape via `create-shape-provider-goal`.

## §7 — draft-spec-from-gap's natural input source

`draft-spec-from-gap.ts:80` declares `inputShapes: ["substrateGapBatch", "openspecExemplar"]`, but tasks `read_gaps` (line 113) and `read_priors` (line 127) self-fetch via `http_fetch` against concept-db `http://127.0.0.1:8260/concepts/search?source_type=substrate_gap&...`. Tasks `read_exemplar_proposal/tasks` (lines 142,152) read hardcoded paths `/workspace/openspec/changes/2026-05-22-failure-mode-autonomous-loop/{proposal,tasks}.md` via `fs_read`.

`/workspace` is the **RW bind mount** (per iter-082 §2). But `/workspace/openspec/changes/...` does not exist:

```
$ docker exec substrate-live find /workspace -path '*openspec*'    # (iter-082)
(empty)
```

The fs_reads point at paths that **don't exist on the writable mount** — they exist only on the read-only repo-root mount at `/home/avi/documents/work/exp-repo/metabob-devbob/openspec/changes/2026-05-22-failure-mode-autonomous-loop/`. This is why both fs_read tasks return `:err_` impulses: the path doesn't resolve under `/workspace`, and `assertInWorkspace` rejects the absolute path even if the file existed elsewhere.

The LLM thus drafted from gaps + priors (concept-db hits succeeded) but **without the structural exemplar**. The fs_write tasks then failed by the same path-resolution mechanism: relative `openspec/changes/{{date}}-substrate-authored-{{slug}}/proposal.md` resolves against vessel cwd `/vessels/development-vessel` (RO), not `/workspace`.

**The natural input source is concept-db query**, dispatched by the binding layer when something wants `specProposal`. No other mechanism feeds this template; there is no input-shape subscription, no boredom rotation slot, no operator goal in the trace.

## §8 — The single observation

**exec_151uni4h and exec_ftzw0hvl were dispatched not by boredom, not by operator, but by the binding layer's `create-shape-provider-goal` recursion (α=743 at dispatch time) — some upstream goal demanded a `specProposal` shape and the only template that produces it is `development-vessel:draft-spec-from-gap`. Both executions ran the LLM resolver to completion (21s each), generated draft JSON that was never persisted (LLM output impulse 404 in activity-api), and silently failed every fs_read and fs_write task via the proxy-catch `:err_` pattern. Trace status reads `success:true`; filesCreated reads `[]`; the substrate has now logged a 1.0 success_rate (α=2, β=1) for `draft-spec-from-gap` on the back of two ghost executions that produced nothing on disk and lost their LLM-drafted content the moment the resolver process exited. The substrate's "spec-authoring capability" is at present a Thompson posterior with no artifacts behind it — and the binding layer is calling it again on every shape-provider-goal that asks for `specProposal`.** Commit in writing.
