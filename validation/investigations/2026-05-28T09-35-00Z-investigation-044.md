---
investigation_id: 2026-05-28T09-35-00Z-investigation-044
generated_at: 2026-05-28T09:35:00Z
investigator_session_id: claude-opus-4-7-audit-iter-029
prior_investigation_id: 2026-05-28T09-25-00Z-investigation-043
loop_iteration: 29
framing: P1 fired (autonomous non-test template authored); iter-042 wrong about "unreachable"; gate bypass + resolver-validity uncertain
substrate_target: http://localhost:18080
---

# Investigation 044 — P1 fired via draft-gap-closing-activity; new findings 10+11

## P1 status: FIRED

exec_dwo03h3d (09:15:44 → 09:20:30, 286s wall) ran development-vessel's `draft-gap-closing-activity` and **authored a substantial non-test template autonomously**.

Inlined literal data:

```
id: activity:⟨gap-closing:fm-43-cascade-attribution-error-1779960003142⟩
created_at: 2026-05-28T09:20:03.169444013Z
org_id: organizations:substrate
name: "Cascade Attribution Disambiguation Activity"
description: "Identifies execution chains where a downstream task is marked
  as cascading from an upstream task, but the upstream task's outputs are
  valid by independent witness..."
proposed: false
deprecated: false
tasks: [7 tasks — activity_fetch, fs_read, activity_fetch, fs_read, fs_edit,
        llm_completion_dispatch, fs_write]
tag_prefixes: [fp, fp.recovery, learning, learning.attribution,
               learning.attribution.cascade,
               learning.attribution.cascade.validation, ...]
```

Authored by exec_dwo03h3d's last task `register_variant` (resolver `activity_create_variant`, dur 88ms, success=true), output impulse `dev:activity_create_variant_5s7x2f99`.

The 13.2s LLM dispatch (task 3) is where the template content was synthesized.

## Correcting iter-042's framing

iter-042 said: "P1 currently UNREACHABLE without operator intervention." Wrong. **Two distinct authoring paths exist:**

| Path | Trigger | Status |
|------|---------|--------|
| (a) Operator-submitted novel-goal-class → mismatch-detector → iter-031 closer → make-activity | Operator goal text that doesn't match any tag | Blocked; needs the two operator-floor primitives from iter-043 |
| (b) Substrate-self-observed failure-mode gap → `draft-gap-closing-activity` → `activity_create_variant` | Boredom-rotation lands a gap-closing goal | **LIVE — fired today** |

iter-042 conflated them. The mismatch-detector covers path (a)'s blindspot; path (b) was already implemented in `repos/development-vessel/src/seed/draft-gap-closing-activity.ts`. The substrate's failure-mode-loop bootstrap (CLAUDE.md memory percolation_2026_05_22) authored the path-(b) machinery; it just took N cycles to actually emit a non-stub template.

The audit's "structural floor" framing in the prior turn IS still correct — **for path (a)**. Both operator-floor primitives from iter-043 (the `goal_semantics` resolver + the mismatch-detector subscriber) remain needed for novel-goal-class submissions.

## Finding 10 (HIGH) — proposed=true discipline bypassed

iter-028 A (commit 87ab8d18) established the discipline: substrate-authored templates land `proposed: true` (registry-visible, selection-invisible) until promote-gate ratifies. The newly-authored template landed `proposed: false`.

Audit query result (inlined):
```json
{
  "id": "activity:⟨gap-closing:fm-43-cascade-attribution-error-1779960003142⟩",
  "proposed": false,
  "created_at": "2026-05-28T09:20:03.169444013Z",
  "total_executions": 0
}
```

This means the template is **selection-visible immediately**. Recommender can pick it for goals shape-matching `(activityExecutionTrace, impulseRelevance)` → `(attributionAuditReport, thompson_posterior_correction)`. No gate ratifies before selection.

Root cause hypothesis: the `activity_create_variant` resolver path either:
- (i) Hardcodes `proposed: false` rather than honoring the schema default of `proposed: true` (per `repos/metabob-activity-api/src/models/schemas.ts:187-217` `CreateTemplateRequestSchema` — `proposed: z.boolean().default(false)`. **DEFAULT IS false, not true.** The schema itself doesn't enforce iter-028 A.)
- (ii) The discipline was enforced only at a different write path (e.g., `make-activity`'s extract_template), not at `activity_create_variant`.

This is Finding 6 (iter-028 A shipped before C) made concrete: the autonomous-authoring path bypasses the gate it was supposed to require.

Severity: HIGH. The autonomous-promoter (iter-038, commit 93a6cb42) is now irrelevant for templates authored via this path — they're auto-selectable without any ratification.

## Finding 11 (MEDIUM) — resolver-validity uncertain in authored template

The template uses resolvers:
- `activity_fetch` — does this exist in `goal-host.ts:598-673`? Subagent #24's audit listed the resolver registry; `activity_fetch` was not among them. Likely hallucinated by the LLM.
- `fs_read`, `fs_edit`, `fs_write` — exist in registry. But the configs reference filesystem paths the substrate doesn't have (e.g., t-4's `validation_method: "downstream_consumption"` isn't a filesystem operation; t-5's `reclassification_rule` is a config string, not executable logic).
- `llm_completion_dispatch` — exists.

The template's quality is **plausibly structurally invalid at execution time**. If the recommender selects it, dispatch will fail at task 1 (`activity_fetch` not registered) or task 4 (`fs_read` won't perform "downstream consumption" validation — that's not what `fs_read` does).

This is the iter-040 V_X gap made concrete: **the substrate authored X without authoring V_X**. iter-040 §1 noted `make-activity` ends at `extract_template` with no companion-validator step. Same gap here. Without V_X, the template's plausibility is its own warrant — no behavioral verification before selection-eligibility.

## Audit's posture (iter-041 prediction protocol update)

| Prediction | Status |
|-----------|--------|
| P1: first non-test substrate-authored template lands within 24h | **FIRED** at 09:20:03 (15h after iter-041 baseline) |
| P2: gap-closer dispatched on a substrateGap | Unchanged — no substrateGap observed yet |
| P3: per-cycle delta narration | Unconverged |
| P4: autonomous-promoter flips a `proposed:true` row | Unreachable — templates land `proposed:false` (Finding 10) |
| P5: signature wire-bridge | Operator-needed (named) |
| P6: audit↔state divergence | Operator-needed (named) |

P1's actual mechanism doesn't match what iter-041 had in mind (a goal-misroute observation → make-activity invocation). The substrate fired P1 via its existing seed-template path; iter-041's protocol asked the right "does it author?" question but mis-modeled the path.

P4 is now blocked structurally — auto-flipping `proposed:true → false` is moot if the autonomous-authoring path bypasses `proposed:true` entirely.

## Composition with prior arc

- iter-028 A (proposed=true default) → broken at the schema (defaults to false) AND at `activity_create_variant`. Finding 10.
- iter-038 autonomous-promoter → no-op for path (b) templates (already promoted at birth)
- iter-040 V_X — gap reaffirmed; path (b) templates land without behavioral verification
- iter-042 mismatch-detector → still needed for path (a); narrower scope than iter-042 framed
- iter-043 operator-floor primitives → still both needed for path (a); independent of P1's firing on path (b)

## What changes from here

Audit's role:
- **Watch first selection of the auto-authored template.** When recommender picks it, does dispatch succeed (Finding 11 falsified) or fail at `activity_fetch` (Finding 11 confirmed)?
- **Watch for further path-(b) authoring.** Does draft-gap-closing-activity fire again? Same cadence?
- **Don't dispatch design subagents on path (a).** iter-043's framing stands; both primitives still needed.
- **No new file edits.** Findings 10+11 are operator-actionable; named, not pushed.

## Loop bookkeeping

- iter-042 framing of "P1 unreachable" was wrong; corrected here
- Two new findings filed (10+11); Finding 10 is HIGH severity, immediate-action class
- Monitors `bmzpcld51` + `bj4wvnnk7` remain armed; cron continues
- Next observation: first selection of the auto-authored template, OR a commit touching `activity_create_variant` proposed-flag handling
- The substrate is more capable than the audit modeled; staying skeptical of "X is unreachable" claims when seed templates exist for that X
