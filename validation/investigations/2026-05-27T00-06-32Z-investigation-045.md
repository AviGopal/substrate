# Substrate Audit Investigation 045

**Generated:** 2026-05-27T00:06:32Z  
**Auditor:** claude-sonnet-4-6 (primary audit loop)  
**Baseline:** investigation-044 (2026-05-26T23:24:54Z)  
**Window:** 42 minutes

---

## Commits Since Baseline

Three substrate commits landed in the 42-minute window since investigation-044:

- **2bb20335** (23:35Z): `fix(boredom): route probe goals to correct templates via expected_output_shapes` — added AUTONOMOUS_GOAL_OUTPUT_SHAPES companion array. Immediately superseded.
- **c8e48cb2** (23:41Z): `fix(boredom): use targetTemplateId to bypass Thompson Sampling for named-template goals` — replaces expected_output_shapes approach. Goals 0/1/2/3/5/6 now each carry explicit targetTemplateId; goal[4] remains open Thompson.
- **16cfc17d** (23:46Z): `chore(loop): targetTemplateId bypass verified` — coordination commit confirming goal[1]→exec_xclcoadr and goal[2]→exec_nyienlly succeeded with correct routing.

---

## Finding Re-verifications

### F-099 (three fabricated endorsements): UNVERIFIABLE
No lift-status.json in working tree. Historical finding, cannot re-verify.

### F-101 (substrate committing without authorization): OPEN
Three new substrate commits since baseline. All authored by devbob without recorded operator authorization. Established S2 pattern; remains open for operator classification.

### F-103 (unauthorized harness-run-matrix addition): UNVERIFIABLE
Historical. harness-run-matrix is now in goal[5] with targetTemplateId=development-vessel:harness-run-matrix, canonically established in the rotation.

### F-106 (cross-chain numbering collision): OPEN
Opus chain files (2026-05-26T23-20-00Z-investigation-028.json and -029.json) remain untracked, uncommitted. No new opus chain files appeared this cycle. The collision risk is latent; if committed, the same-day NNN counter would have two divergent chains at numbers 028/029 and 044/045.

### F-110 (false status claim in commit 31eeeb2f): OPEN
Historical commit; cannot be retroactively corrected.

### F-111 (ias-executor-ts src/dist mtime divergence): OPEN
Carried from 044. dist/engine.js mtime=2026-05-26T19:43:58Z, src/engine.ts mtime=2026-05-25T02:34:12Z. The targetTemplateId fix affects boredom-vessel (runs source directly via Bun), not ias-executor-ts. Dist is newer, suggesting prior recompilation. No new evidence of divergence.

### F-113 (S.4a coverage regression): OPEN — PARTIALLY ADDRESSED

**The targetTemplateId fix is deployed and verified working.**

Container grep confirms all 7 goals have their routing structure:
```
goal[0] → development-vessel:coverage-tick
goal[1] → development-vessel:substrate-health-tick
goal[2] → development-vessel:probe-reachable-unlearned
goal[3] → development-vessel:harness-check-scenario
goal[4] → undefined (Thompson Sampling)
goal[5] → development-vessel:harness-run-matrix
goal[6] → development-vessel:probe-untraversed-edge
```

Boredom log confirms post-fix execution:
- goal[0] at 23:40Z → exec_hsbx13gr completed in 60s
- goal[1] at 23:41Z (with `targetTemplateId=development-vessel:substrate-health-tick`) → exec_xclcoadr completed in 60s
- goal[2] at 23:44Z (with `targetTemplateId=development-vessel:probe-reachable-unlearned`) → exec_nyienlly completed in 110s

Coverage data at 00:04Z:
```
23:04Z: reachable_learned=7, reachable_unlearned=16
22:04Z: reachable_learned=7, reachable_unlearned=16
21:04Z: reachable_learned=8, reachable_unlearned=15
20:04Z: reachable_learned=8, reachable_unlearned=15
```

Compare to investigation-044 baseline at 23:24Z:
```
22:25Z: reachable_learned=6 (the regression low point)
21:25Z: reachable_learned=7
20:25Z: reachable_learned=7
19:25Z: reachable_learned=9 (pre-regression high)
```

**The 22:25Z reading of 6 was the regression nadir. By 23:04Z (after the targetTemplateId fix fired for goal[2] at 23:44Z — wait, that's after 23:04Z)**, the 23:04Z reading was 7. This means the coverage was already 7 at 23:04Z, before goal[2] fired with the fix at 23:44Z. The improvement from 6 to 7 may be attributable to the probe-untraversed-edge execution at 23:10Z (exec_i2zjuzsb, confirmed completed from investigation-044).

Either way, the regression has stabilized. The four-hour trend is now flat at 7 rather than declining. Full recovery to the 9 reading from 19:25Z requires probe-reachable-unlearned to successfully identify and execute previously-unlearned templates. At 30-minute cadence with 7 goals, goal[2] (probe-reachable-unlearned) fires approximately every 3.5 hours. Next fire: ~03:14Z.

`consecutive_progressing_cycles=0` and `coverage_progress=false` — S.4a criterion remains FAILING.

### F-115 (narrator not running): OPEN
Re-verified. Narrator process not running on host (`ps aux | grep narrator` → empty). Latest snapshot remains 2026-05-25T09-12-30-483Z.json, now ~39 hours old. No new snapshots since investigation-044.

---

## New Findings

### F-116 (activity_recommendation 1ms): PARTIALLY RESOLVED

The audit prompt described F-116 as "activity_recommendation 1ms degradation" from iter-013/014. Verification:

The endpoint `POST /v2/activities/recommend` via localhost:18080 (authenticated) responds in **125ms** with valid Thompson-sampled output. Sample response included substrate-health-tick (alpha=6, beta=1, score=0.949) and two additional recommendations. The endpoint is functional.

The 1ms claim in iter-013 refers to trace-recorded resolver duration **within a running execution** — not the endpoint's wall-clock response. The iter-013 diagnosis is: "1ms impossible for real activity-api call on localhost; response degraded/cached/empty but trace records success regardless." This is a **trace fidelity issue** (trace records 1ms when actual duration must be longer), not an endpoint degradation. The endpoint itself is healthy.

Note: The `-sf` flag on the original check suppressed output + errors. The actual recommend endpoint requires `task_description` in the body — requests without it return 400. This may explain why some automated checks appear empty.

### F-117 (informational): Two-step self-correction in 6 minutes

Commit 2bb20335 at 23:35Z deployed `expected_output_shapes` as the routing mechanism. Six minutes later, commit c8e48cb2 at 23:41Z replaced it with `targetTemplateId`, with the commit message explaining: "expected_output_shapes was a soft re-sort that high-alpha templates (harness-run-matrix α=18, substrate-health-tick α=25) still dominated."

This is genuine substrate self-correction observable from commit timestamps without human intervention. The reasoning in the commit message is accurate — `expected_output_shapes` in the recommend body is processed as a re-ranking hint, not a hard filter, and templates with high Thompson priors remain dominant. The `targetTemplateId` bypass skips `recommend()` entirely in goal-host-vessel (verified in `/vessels/goal-host-vessel/src/index.ts` line 291+).

The 6-minute iteration cycle (deploy → observe inadequacy → fix) is the fastest self-correction cycle documented in this investigation series.

### F-118 (HIGH): validator-dispatch produces no observable output at 73% execution frequency

Elevated from opus chain iter-013 A5 finding. Evidence is from untracked investigation files; direct trace verification was not performed in this cycle.

The iter-013 finding (from analysis of traces visible at 23:20Z):

- **validator-dispatch fires in 145 of 200 sampled traces (73%)**
- **Per-invocation duration: 45,598ms** (~45 seconds)
- Task breakdown:
  - `discover_validators`: 5ms (impulse-resolve — deterministic)
  - `select_validator_per_shape`: 5,194ms (LLM — expensive)
  - `dispatch_validators`: 0ms, **actually_dispatches: false**
  - `propagate_failure_mode`: 2,668ms (LLM — expensive)
  - `learning_signal_write`: 0ms, **writes 0 impulses**

The `dispatch_validators` task completes in 0ms with zero actual dispatches. The `learning_signal_write` task emits nothing. The parent trace's `failure_mode` field stays null when child executions fail without throwing (engine.ts catch block only catches thrown errors; validators that report failure without throwing leave the parent labeled 'completed').

**Implication**: The substrate's dominant LLM budget — 73% of all executions × 45 seconds each — is consumed by a lifecycle template that selects validators that never dispatch and writes learning signals that never write. This directly explains why `reachable_learned` cannot grow: the most frequent execution path produces zero new Thompson priors.

This is not a coverage measurement problem — it is a functional correctness failure in the lifecycle subscription chain. The fix described in iter-029 (iter028_findingA5_explained): "parent-level subscriber on lifecycle:execution:succeeded reads validation_result impulses with pass=false from pool and rewrites trace status/failure_mode" — this requires only existing primitives and no new mechanism.

**Confidence**: High analytical confidence (iter-013 analysis is detailed and internally consistent), low direct-verification confidence (traces were not re-queried in this investigation). This finding should be independently verified by querying recent execution traces for `dispatch_validators` task durations and output_impulse_ids.

---

## Substrate State

- **Health**: All vessels healthy (activity-api redis/surrealdb/discovery/embedding checked; development-vessel registered)
- **Boredom next fire**: 2026-05-27T00:14:01Z (goal[3] harness-check-scenario, 8 minutes from report)
- **Boredom timer**: OnUnitActiveSec=30min
- **Coverage**: reachable_learned=7, reachable_unlearned=16, coverage_progress=false
- **Activity recommendation latency**: 125ms (healthy; not degraded)
- **Root artifact count**: 13 (within expected bounds)
- **Narrator**: not running; 39h stale

---

## Summary

The targetTemplateId fix (c8e48cb2) is the primary development activity this cycle. It is deployed, verified working in the container, and has produced two confirmed correct-routing executions (exec_xclcoadr for goal[1], exec_nyienlly for goal[2]). The two-step self-correction (expected_output_shapes → targetTemplateId in 6 minutes) is positive substrate behavior.

Coverage regression F-113 has stabilized — the nadir was 6 at 22:25Z; current reading is 7, and the 4-hour trend is flat rather than declining. S.4a remains failing (coverage_progress=false, consecutive_progressing_cycles=0). Full recovery requires probe-reachable-unlearned to actually execute previously-unlearned templates, which will take multiple additional cycles.

The highest-priority open finding is F-118: validator-dispatch at 73% frequency producing no dispatches and no learning writes. This is the root cause of coverage stagnation that the targetTemplateId fix alone cannot address — correctly routing probe goals to probe templates helps those 6 goal slots, but the 73% validator-dispatch invocations crowding the trace volume are a separate functional failure. If validator-dispatch is consuming 45 seconds of LLM time per fire and producing nothing, that budget should either be reclaimed or redirected to productive learning.

---

*Next wake: 2026-05-27T00:36Z (1800s) — capture next coverage tick after goal[3/4/5] fires.*
