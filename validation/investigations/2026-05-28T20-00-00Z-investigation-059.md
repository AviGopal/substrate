---
investigation_id: 2026-05-28T20-00-00Z-investigation-059
generated_at: 2026-05-28T20:00:00Z
investigator_session_id: claude-opus-4-7-audit-iter-044
prior_investigation_id: 2026-05-28T19-30-00Z-investigation-058
loop_iteration: 44
framing: subagent #30 — origin_layer is genuinely new, ~15 LOC; iter-058 retraction has 2 sub-causes (both audit-tool-side); dev declared S.4a+S.4b passing; operator gate next
substrate_target: http://localhost:18080
---

# Investigation 059 — origin_layer design (~15 LOC) + iter-058 retraction refined + dev lift declaration

## Subagent #30 decisions

**`origin_layer` is genuinely new.** Verified grep `origin_layer\|originLayer` returns 0 hits across `ias-executor-ts`, `goal-host-vessel`, `metabob-activity-api`, `development-vessel`.

**Does NOT collapse to `metadata.source`** — source is freeform across 14 distinct values (vessel-id, resolver-id, operation-name, lifecycle-name) at 11 emission sites. Source answers "who"; origin_layer answers "where in the pipeline" — orthogonal axes.

**Schema delta**: additive optional `z.enum(["resolver_returned","proxy_synthesized","engine_persisted","activity_api_stored"]).optional()` on `ImpulseMetadataObjectSchema` at `repos/metabob-activity-api/src/models/schemas.ts:339-357`. Already `.passthrough()`; no migration.

**Per-layer emission (~15 LOC total)**:
- `resolver_returned`: 8 resolvers in `ias-executor-ts/src/resolvers/*.ts` (~1 LOC each)
- `proxy_synthesized`: `goal-host-vessel/src/index.ts:355` (success path) and `:363` (degraded) — 2 LOC
- `engine_persisted`: `ias-executor-ts/src/engine.ts:599-601` `ensureImpulse` — 2 LOC, preserves incoming
- `activity_api_stored`: `metabob-activity-api/src/routes/impulses.ts:238` — 3 LOC, merges incoming

**Critical invariant**: each layer's emission must preserve incoming `origin_layer` (don't clobber). Resolver→engine→activity-api propagation maintains the earliest-set value.

## Refinement of iter-058 5th retraction (2 sub-causes)

Both confirmed in-trace this turn:

**Sub-cause A — wrong endpoint**: I hit `POST /v2/impulses/resolve` (pointer-shape-dispatcher) when I should have hit `GET /v2/impulses/:impulseId` (direct row fetch). The GET endpoint exists at `routes/impulses.ts:517` per subagent #30's grep.

**Sub-cause B — even right endpoint fails**: I tested `GET /v2/impulses/dev:failure_mode_matrix_score_e5heirfe` (underscore-success ID) and `GET /v2/impulses/dev:fs_read:err_q3g2o6y2` (err_ID). Both returned literal:
```
{"error":"Impulse not found","impulse_id":"..."}
```

**Implication**: proxy-synthesized impulses (both success and err_) are NEVER stored in activity-api's impulse table. They live in goal-host-vessel's runtime, get embedded into task records, and ship to activity-api ONLY as part of the execution trace's `task.output_impulse_ids` array (IDs only, no body).

To inspect origin_layer for any of these impulses, audit must use:
```
GET /v2/activities/execution-traces/:execId  →  body.tasks[*].output_impulses[*].metadata
```

(if execution-trace persistence preserves embedded impulse bodies — needs verification, not yet inspected).

Different access path entirely. Adding to audit-tool discipline.

## Dev lift declaration — commit 599c6213

"iter 32: S.4a + S.4b both passing — coverage_progress + overall_passing = True"

Substrate authored a new shape (`budgetComplianceReport` — related to fm-17 templates I observed in iter-049/055). Confidence at exactly 8/32 = 25% threshold. Operator gate remaining: `validation/state/lift-status.json` per CLAUDE.md §27.S.4.

**Critical finding in commit**: "activity 'proposed' field: Redis cache was masking SurrealDB updates" — this is a deeper Finding-10-adjacent layer the audit didn't catch. The `proposed:true` flag I verified in iter-055 was being shown by direct SurrealDB queries, but the autonomous-promoter's UPDATE wasn't actually persisting through the cache. Fixed via direct activity_template UPDATE + Redis flush.

## Audit's posture at the operator gate moment

Per iter-051's L0/L1+ framing + iter-058's retraction pattern, dev's lift declaration sits at L0 + L1-partial:

| Layer | Verified by dev? | Audit findings? |
|-------|------------------|----------------|
| L0 (behavior) | YES (S.4a/S.4b mechanics passing) | confirmed iter-055 |
| L1 (runtime failure observability) | NO | Finding 13 open — proxy catch at goal-host-vessel/src/index.ts:359-367 |
| L1 (routing observability) | NO | Finding 17 + 7/7 misroute pattern (iter-053/057/058) |
| L1 (recommend pollution) | partial | iter-056/058 still active |
| L0+ (audit-tool layer-attribution) | n/a | 5 retractions; origin_layer + audit endpoint-discipline |

**Findings for operator to see before ratifying `lift-status.json`**:
1. Finding 13 (~10 LOC) — runtime silent-success layer
2. Finding 17 — routing misroutes pollute topology learning
3. iter-051/058 L0/L1+ scope distinction
4. iter-046 L4-recursion floor: substrate cannot identify its own failing templates because the silent-success hides them (empirically demonstrated iter-057)

These don't refute S.4a/S.4b — they refine the framing of what "lift" carries.

## Composition with prior arc

- **iter-048 + iter-058**: `resolver_registry_query` (L0+ for registry) + `origin_layer` (L0+ for artifact attribution) compose cleanly. Together: verify-claim can distinguish "resolver X returned successfully" from "proxy synthesized after X failed".
- **iter-043 + iter-058**: `goal_semantics` is a different artifact class (goals); orthogonal to origin_layer (impulses).
- **iter-046 layer model**: origin_layer is L0+ enabler analogous to resolver_registry_query. Subagent #30 confirms L0+ classification.
- **iter-051 L0/L1+**: dev's lift is L0; audit's open findings are L1+. origin_layer ships into the L0+ layer that enables both.

## Updated L0+ stack

| Primitive | Surface | LOC | Hours | Unlocks |
|-----------|---------|-----|-------|---------|
| `resolver_registry_query` (iter-048) | goal-host-vessel | ~36 | 2-3 | Finding 11 verify-claim |
| `origin_layer` (iter-059) | 3-repo additive | ~15 | <1 | 5 audit retractions partial + Finding 13 attribution + Finding 17 routing |
| `goal_semantics` (iter-043) | activity-api | ~30 | 1-2 | iter-042 mismatch-detector |

Combined ~80 LOC. Three operator-floor primitives. Each unlocks a class of L1+ detectors.

## Audit posture

- 5 retractions; 2 sub-causes of iter-058 confirmed (both audit-tool-side)
- Subagent #30 transcript: `/tmp/.../a39260eab691cadec.output`
- Findings 13+15+17 open
- Dev declared S.4a/S.4b passing; operator gate next
- Audit's role at the gate: ensure L1+ findings visible to operator; do not adjudicate

## Loop bookkeeping

- iter-058 5th retraction has 2 confirmed sub-causes
- origin_layer design verified (~15 LOC, additive optional, no migration)
- 4 harness-run-matrix executions in ~20s observed (potential side-effect of commit 599c6213 promote-fix)
- Monitors armed
- Next observation: any commit touching ImpulseSchema or per-layer origin_layer emission OR operator ratification of lift-status.json
