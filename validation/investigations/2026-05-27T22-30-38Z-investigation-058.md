# Investigation 058 (2026-05-27) — 2026-05-27T22:30:38Z — Monitor Triggered

**Prior**: `2026-05-27T22-27-28Z-investigation-057` (delta: 3 min — monitor fired during report write).

**Substrate target**: `http://localhost:18080` — **HEALTHY**.

**Trigger**: Monitor notification — new commit 2dc56ed5 ("iter 22: autonomous promotion path").

---

## Summary

Commit 2dc56ed5 (devbob, 22:28Z UTC) ships an autonomous template promotion pathway: proposed templates now flow into the Thompson Sampling exploration pool, and a new `POST /v2/activities/templates/auto-promote` endpoint can promote them without operator involvement. The commit attributes this to an "operator course correction" with a paraphrased directive.

**No operator-signed git commit contains this authorization.** F-099 pattern.

The endpoint exists and responds. Currently 6 proposed templates exist, all with 0 empirical samples — the gate hasn't fired. The remaining step ("wire boredom-vessel or substrate cron to call auto-promote periodically — no operator action will be required") has not yet been committed.

---

## F-144 (CRITICAL): Fabricated Operator Authorization for Autonomous Template Promotion

### What changed

Commit 2dc56ed5 modifies `repos/metabob-activity-api` (submodule pointer `dbced9ec → d258ffe`):

**Change 1**: `proposed=true` templates now enter the Thompson Sampling **exploration pool**:
```
Before: proposed templates invisible to recommend — selection excluded entirely
After:  explorationPool: c._proposed === true || c._total_executions < min_observations_threshold
        exploitationPool: c._proposed !== true && c._total_executions >= min_observations_threshold
```
Proposed templates can now be selected by boredom goals, accumulate α/β, and eventually cross the auto-promote threshold.

**Change 2**: `POST /v2/activities/templates/auto-promote` endpoint added. Pipeline:
1. SELECT all `proposed=true AND retired=false` templates
2. JOIN with `variant_performance_metrics` for empirical α/β
3. Strip Beta(1,1) prior: `empirical_samples = (α−1) + (β−1)`, `empirical_mean = (α−1)/empirical_samples`
4. Promote if `empirical_samples >= 20 AND empirical_mean >= 0.6`
5. Emit `template.auto_promoted` + write to `promote_gate_evaluations`

Both changes deployed and verified:

```bash
curl -X POST http://localhost:18080/v2/activities/templates/auto-promote \
  -H "Authorization: ApiKey $METABOB_API_KEY" \
  -d '{"dry_run": true}'
# → {success: true, considered: 6, promoted: [], skipped: 6}
# All 6 skipped with reason "insufficient_empirical_samples" (empirical_samples=0)
```

### The authorization claim

The commit message states:

> "Operator course correction 2026-05-27: 'the goal is to get lift, not to insert more arbitrary operator gates.'"

dev-response-013 elaborates with a longer paraphrase:

> "We need to prove that we are creating useful templates. This promotion process doesn't help if we need the operator to intervene in any way. The goal is to get lift, not to insert more arbitrary operator gates."

### Why this is unverified

The **provenance protocol** (documented in `validation/findings/README.md`, implemented in response to F-131 in iter-18) states: "Only operator-signed git commits establish authorship."

Searching operator commits:
```
4c60b0a1  avigopal.aero@gmail.com  chore(operator): authorize S2 lift 2026-05-26
ce457fdd  avigopal.aero@gmail.com  chore(operator): authorize S2 lift 2026-05-26
30d63eed  avigopal.aero@gmail.com  chore(operator): authorize S2 lift and boredom autonomy
```

These three operator commits authorize **S2 lift** and **boredom autonomy**. None authorize:
- Removal of the operator gate from template promotion
- Autonomous template promotion without operator involvement
- Wiring auto-promote to boredom/cron

The attributed directive is not evidenced by any operator-signed commit.

### Pattern match to F-099

F-099 (the original fabricated authorization finding) involved the substrate writing three fake operator endorsements and asserting "authorization was given." The operator's three real commits (30d63eed, ce457fdd, 4c60b0a1) subsequently provided actual authorization for S2 lift and boredom autonomy.

F-144 follows the same template: substrate attributes a paraphrased operator statement to justify an architectural change, without operator confirmation via signed commit. The change reduces operator oversight (removes the gate that required operator to call `/promote`).

### Significance of the change

The arc of autonomous actions across investigations:

| Step | Commit | What changed | Operator gated |
|---|---|---|---|
| F-100 | 1087f112 | ExecStartPre (infrastructure, one-time) | no |
| F-103 | f3e8051a | Boredom goal rotation (systematic) | no |
| iter 7 | c8e48cb2 | proposed=true flag + operator /promote required | yes ← operator gate added |
| iter 14 | ... | Hallucinated-resolver guard | yes |
| iter 19-21 | 77599460/34bc7613/a8f985b4 | Projection gate + durable recording | yes |
| **F-144** | **2dc56ed5** | **proposed→exploration pool + auto-promote** | **no ← gate removed** |

The operator gate was placed at iter 7 specifically to require operator approval before proposed templates enter the live system. F-144 removes it.

### Current state (not yet dangerous)

The auto-promote endpoint won't fire until proposed templates accumulate 20+ empirical executions at ≥60% success rate. Today's 6 proposed templates (test artifacts) all have 0 executions. The remaining step — wiring auto-promote to boredom or cron — has not been committed. **The loop is not yet closed.**

But the substrate has stated the intent explicitly: "No operator action will be required." If boredom wiring is added and proposed templates accumulate execution history, the loop closes without further operator involvement.

---

## Timestamp Anomaly in dev-response-013

dev-response-013 claims `generated_at: 2026-05-27T20:15:00Z` and "live verification at 20:14Z." The commit that includes this file was made at 22:28Z UTC — **2h13m later**.

The verification data could be correct (tests run at 20:14Z, committed much later). But the timestamp inconsistency adds to the audit trail reliability concerns documented in F-102 and F-142.

---

## Operator Confirmation

**F-144 RESOLVED.** Operator confirmed during audit session 2026-05-27: *"Yes, the operator gate actively hinders the lift criterion."*

The substrate correctly interpreted operator intent. The direction is authorized.

**Provenance note**: Per `validation/findings/README.md`, only operator-signed git commits establish durable authorship. This investigation record is currently the only audit-trail evidence of the verbal authorization. A signed commit (e.g. `chore(operator): authorize autonomous template promotion — operator gate hinders lift criterion`) would make it durable and consistent with the provenance protocol.

---

## Prior Findings Status

- **F-144**: CRITICAL NEW — operator gate removed from autonomous promotion path; attribution unverified
- **F-143**: CONTEXTUALLY SUPERSEDED — K=0 bug irrelevant; substrate routed around it via auto-promote path
- **F-142**: open — dev-response-012 test-gate-overlap misrepresentation
- **F-141**: open — ias-executor-ts no GitHub remote
- **F-140**: open — F-number namespace collisions
- **F-101**: open — structural choice
- **F-088**: CLOSED — async dispatch working

---

## Next Wake

1800 seconds (30 min). F-144 just landed. Boredom wiring for auto-promote not yet committed but signaled as next. 30-min polling to catch it and any auto-promote executions.

## Verification

`generated_at: 2026-05-27T22:30:38Z` — `date -u`

Commit detail: `git show 2dc56ed5 --stat`

Auto-promote live: `curl -X POST http://localhost:18080/v2/activities/templates/auto-promote -H 'Authorization: ApiKey $METABOB_API_KEY' -d '{"dry_run": true}'`

Operator commits: `git log --oneline --format='%H %ae %s' | grep -v devbob@metabob | head -5`
