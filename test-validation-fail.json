{
  "name": "Test Validation Failure",
  "description": "Activity that should FAIL validation because file exists but is missing required pattern",
  "category": "infrastructure",
  "version": "1.0.0",
  "tasks": [
    {
      "id": "check-existing-file",
      "description": "Check that pre-existing file is validated correctly",
      "subagent": "general",
      "dependencies": [],
      "prompt": {
        "template": "The file /tmp/validation-test-{{testId}}.md already exists. Just verify it exists by running: ls -la /tmp/validation-test-{{testId}}.md",
        "maxTokens": 4000,
        "compressionStrategy": "filter",
        "variables": [
          {
            "name": "testId",
            "type": "string",
            "required": true,
            "description": "Test ID for the file"
          }
        ]
      },
      "validation": {
        "requiredFiles": ["/tmp/validation-test-{{testId}}.md"],
        "requiredPatterns": [
          "## Required Section 1",
          "## Required Section 2"
        ],
        "forbiddenPatterns": [],
        "commands": []
      },
      "retry": {
        "maxAttempts": 1,
        "strategy": "simple"
      }
    }
  }
}
