apiVersion: v1
kind: Service
metadata:
  name: {{ include "metabob-activity-api.fullname" . }}
  labels:
    {{- include "metabob-activity-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metabob-activity-api.selectorLabels" . | nindent 4 }}
