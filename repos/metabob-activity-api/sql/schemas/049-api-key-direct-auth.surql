-- ============================================================================
-- 049-api-key-direct-auth.surql — DEPRECATED (moved to identity-vessel)
-- ============================================================================
--
-- Per the architectural realignment (2026-04-28): identity-vessel owns the
-- api_keys table schema. Activity-api is a read-only consumer that resolves
-- API keys via identity-vessel's /v1/auth/resolve endpoint.
--
-- The schema definition that USED to live here is now in:
--   repos/identity-vessel/sql/migrations/00X-api-keys.surql
--
-- This file is kept as a stub (rather than deleted) so the migration runner
-- doesn't error on missing files. The runner skips empty files.
-- ============================================================================
