# VERIFY — 2026-05-21 (second pass) — §9 acceptance gates green

Loop stage: VERIFY closing the cycle on §9 (the delta change driven by
the first VERIFY pass `docs/VERIFY_2026_05_21.md`).

## Baseline

- `bun test` — **72 pass / 0 fail** across 17 test files (was 29/5)
- `bun run typecheck` — clean
- `bun run lint` — chains tsc + shape-dispatch-check; both green

## Acceptance gates

| Gate | Requirement | Status | Evidence |
|-|-|-|-|
| 9.S.1 | ≥ 13 resolver test files + no fails | ✓ | `test/resolvers/*.test.ts` count = 13; bun test 72/0 |
| 9.S.2 | `bun run lint` runs both typecheck AND shape-dispatch-check; exit 0 | ✓ | "13 advertised shapes, 13 dispatch cases, all agree" |
| 9.S.3 | README.md + CLAUDE.md present | ✓ | both files at repo root |
| 9.S.4 | `test/cli.test.ts` exercises all three CLI verbs | ✓ | 11 references to run-activity/seed-templates/call-resolver in the test file |

## What landed where (history of §9)

- **G5 (9.1)** closed by `b8e7943` (mine) — restructured config to inline
  `shapes: [...]` literal; added `scripts/check-shape-dispatch.ts`;
  chained lint script.
- **G3 + G4 (9.3, 9.4)** closed by `b755fc1` (mine) — README + CLAUDE.md.
- **G1 (9.2)** closed in parallel by user commits — 11 per-resolver
  test files added with real-shell-out + tmp-dir scratch pattern.
- **G2 (9.5)** closed in parallel by user — `test/cli.test.ts`.

The four-stage cycle (VERIFY → DEBUG → SPEC → DEV → VERIFY) is now
**closed** for the §9 delta. Every spec amendment task in §9.1-§9.5
has corresponding code/docs in the sub-repo and an acceptance test
verifying it.

## What's next per the spec

Remaining open in the parent change (`openspec/changes/2026-05-21-
development-vessel/tasks.md`):

- **§6 (operator)** — `seed-templates` against canary activity-api.
  Requires write-scope METABOB_API_KEY + operator authorization.
  NOT autonomous-loop work.
- **§7 (parity gate)** — run dev-vessel's `branch-health` activity
  against this repo, compare to `validation/scripts/verify-branch-health.ts`.
  Requires §6 to have populated activity-api first.
- **§8 (cleanup)** — remove the temporary ship-change/branch-health
  examples from `repos/ias-executor-ts/src/examples/`. Gated on
  §7 passing.
- **§S.1-S.5 (acceptance)** — the parent change's gates. S.1 maps
  to 9.S.1 + parity-passing (already green for the test side; canary
  side waits for §6+§7). S.4/S.5 (lift demo + self-application)
  are forward-looking work that follows §7.

The autonomous loop can now either:
1. Stand down (everything that doesn't need operator/canary is done).
2. Move to §S.4 — the "demo of lift" test
   (`test/lift-demo.test.ts`) which is autonomous-loop-doable since
   it uses fake activity-api capture.

Recommendation: option 2 if continuing; close-out report if pausing.

## Loop discipline anchor

Per `feedback_autonomous_loop_alternates_dev_verify.md` (updated
2026-05-21): four-stage cycle. This document closes the §9 cycle.
Next iteration's SPEC stage (if continuing) will define the
lift-demo test contract before any code lands.
