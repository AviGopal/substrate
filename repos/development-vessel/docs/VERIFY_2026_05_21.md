# VERIFY — 2026-05-21 — dev-vessel sub-repo vs openspec spec R1-R10

Loop stage: VERIFY → DEBUG (combined). Output is a punch list feeding the
next SPEC iteration of the four-stage cycle (VERIFY → DEBUG → SPEC → DEV).

## Baseline

- `bun test` — **29 pass / 0 fail** across 5 test files
- `bun run typecheck` — **clean** (0 errors)
- `bun run lint` — currently aliased to typecheck only

## What's met

| Requirement | Status | Evidence |
|-|-|-|
| R1.1-1.5 package layout | ✓ | `src/{config,cli,index,discovery-registration}.ts`, `src/{resolvers,routes,seed,templates}/` |
| R2.1-R2.13 resolvers (13) | ✓ | All 13 resolver files present; all 13 dispatch cases in `routes/impulses.ts` |
| R3.1-R3.5 bootstrap templates | ✓ | `src/seed/{ship-change,branch-health,release-change,add-resolver-to-vessel,propagate-judgment}.ts` |
| R3.6 boot-fetch-template | ✓ | `src/templates/boot-fetch-template.ts` with paired test |
| R4.1-R4.3 CLI verbs | ✓ | `cli.ts` handles `run-activity`, `seed-templates`, `call-resolver` |
| R7.3 `/health` endpoint | ✓ | Present in `src/index.ts` |
| R9.3 CASES_AND_FLOWS.md | ✓ | `docs/CASES_AND_FLOWS.md` |

## Gaps (DEBUG → punch list)

### G1 — per-resolver tests (R8.1)

Spec R8.1: "one file per resolver in R2". Currently present: 2/13.

Missing test files:
- `test/resolvers/git-add.test.ts`
- `test/resolvers/git-commit.test.ts`
- `test/resolvers/git-diff.test.ts`
- `test/resolvers/git-log.test.ts`
- `test/resolvers/fs-write.test.ts`
- `test/resolvers/fs-edit.test.ts`
- `test/resolvers/activity-fetch.test.ts`
- `test/resolvers/activity-create-variant.test.ts`
- `test/resolvers/vessel-register-passthrough.test.ts`
- `test/resolvers/code-introspect.test.ts`
- `test/resolvers/propagate-judgment.test.ts`

Severity: medium. The resolvers are integration-tested via
`test/vessel-integration.test.ts` but per-resolver coverage is the spec's
contract; without it a regression in one resolver can hide behind an
integration-test wide-net.

### G2 — CLI tests (R8.3)

Spec R8.3 — `test/cli.test.ts` exercising each CLI verb. Not present.
Verbs are exercised indirectly through integration + dry-run tests, but
stdout/stderr format and exit codes aren't pinned.

Severity: low. Verbs are thin wrappers; covered indirectly.

### G3 — README.md (R9.1)

Spec R9.1 — quickstart + topology summary. Not present.

Severity: medium. Future-developer onboarding cost without it.

### G4 — CLAUDE.md (R9.2)

Spec R9.2 — vessel-specific guidelines. Not present.

Severity: medium. Pattern across other vessel repos.

### G5 — shape-dispatch-check lint wiring (R1.5 implication)

Spec R1.5: "shape-dispatch-check script in `packages/shape-dispatch-check/`
MUST pass against this file". `bun run lint` is currently just
`tsc --noEmit`; no shape-dispatch-check invocation.

Severity: high. The invariant "every advertised shape has a dispatch
case" is the load-bearing safety net for spec R2 → routes/impulses.ts.
Without it wired, a future resolver added in one place but not the
other can ship silently.

## Notes that aren't gaps

- The advertised-shape list in `src/config.ts` and the dispatch switch
  in `src/routes/impulses.ts` are in agreement today (manually verified
  in this VERIFY pass). G5 is about preventing future drift, not fixing
  current drift.
- §6 / §7 (canary seed + parity gate) and §S.2-S.5 acceptance gates
  remain pending — they are operator-or-canary-dependent and don't
  count as gaps in the offline sub-repo state.

## Next stage: SPEC

The five gaps above are the SPEC iteration's input. Expected output of
SPEC stage: a delta openspec change (or amendment to the current change)
that:

1. Adds tasks for G1-G5 with explicit ordering.
2. Bumps tasks.md to reflect the actual completion state of §1-§5
   (some tasks marked done aren't fully done per G1).
3. Defines acceptance gates for each gap.

After SPEC: DEV (implement gaps) → VERIFY (re-run this pass; gaps closed).

## Loop discipline anchor

Per `feedback_autonomous_loop_alternates_dev_verify.md` (updated
2026-05-21): four-stage cycle. This document is the artifact of the
VERIFY + DEBUG combined iteration. The next iteration MUST be SPEC
before any code lands.
