In repos/development-vessel/src/resolvers/vessel-mitosis-cutover.ts, make every change_window maintenance-lease call take the lease under the name `cutover` (openspec 2026-09-24-resumable-landings task 3.2, spec change-window-names "Cutovers and the reconcile use their own names"). maintenanceLease_write now accepts an optional `name` (landed in maintenance-lease.ts): a named hold is stored in its own file, two different names do not exclude each other, and only the unnamed global hold still excludes everything. Today the cutover and the trace-store reconcile share one global mutex although they touch different resources, so a reconcile hold refuses/defers verified cutovers ("DEFERRED: change_window lease held by ...", env_change_window_held) and the whole verified patch is rolled back.
Change: add `name: "cutover"` to EVERY `resolveMaintenanceLeaseWrite({ type: "maintenanceLease_write", ... })` pointer in this file — the acquire at the git-aware path (`holder: \`cutover:${String(pointer.vessel_name ?? "unknown")}\``), the bounded-wait `retryAcquire`, the acquire in runGitAwareCutover (`holder: leaseHolder`), the proposal-lease acquire `pAcq`, and every `op: "release"` / `op: "renew"` call. No other behaviour changes. Note for reviewers: two cutovers for different vessels still exclude each other because both use name `cutover` with different holders; same-holder re-acquire (the self-deadlock guard) still succeeds because acquire accepts the same holder.
Falsifier: with `change_window` held under name `trace_store` by another holder, a FAVORABLE cutover acquires `cutover`, pushes, and logs no `DEFERRED: change_window lease held` / `env_change_window_held`.
