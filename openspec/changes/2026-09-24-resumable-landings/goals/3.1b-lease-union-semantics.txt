In repos/development-vessel/src/resolvers/maintenance-lease.ts, finish the lease name dimension with union semantics (openspec 2026-09-24-resumable-landings task 3.1, second half; the first half — `name` on the pointers, `namedLeasePath(name)`, and name-aware readLease/writeLease/deleteLease — is already in this file).
Changes, all in this one file:
(1) Add `async function listHolds(): Promise<Array<MaintenanceLease & { name: string | null }>>` that readdir()s dirname(leasePath()) (catch -> []), and for every file matching /^maintenance(-(.+))?\.json$/ reads it (reuse the same parse/validity checks as readLease), skips expired ones, and returns each with `name` = the captured suffix or null for maintenance.json.
(2) UNNAMED acquire (pointer.name absent): refuse ({acquired:false, held_by, expires_at, name}) if ANY hold from listHolds() is held by a different holder — today it only checks maintenance.json, so an unnamed acquire would ignore a named cutover or trace_store hold.
(3) UNNAMED release: if maintenance.json does not carry the token, look through listHolds() for a hold with that token and delete that named file (so existing callers that release without a name still release a named hold); report token_mismatch only when no file carries it.
(4) resolveMaintenanceLease WITHOUT a name: answer for the union — held:true if listHolds() is non-empty, keep the top-level holder/acquired_at/expires_at of the first hold (unnamed first) so existing readers keep working, and add `holds: listHolds().map(h => ({ name: h.name, holder: h.holder, acquired_at: h.acquired_at, expires_at: h.expires_at }))`. With a name, keep the current behaviour.
Normalise optional names with `?? null` (a prior draft failed typecheck with TS2322 on string | null | undefined).
Falsifier: with a named `cutover` hold by "a", an unnamed acquire by "b" returns acquired:false held_by "a"; an unnamed read lists the hold in `holds`; releasing that token without a name deletes maintenance-cutover.json.
