In repos/development-vessel/src/cli.ts, make `seedTemplates()` upsert a seed template whose SOURCE changed since it was last seeded, instead of skipping every template once the catalogue is populated (openspec 2026-09-24-resumable-landings task 3.3b; gap a-landing-in-a-seed-template-file-is-inert). Measured: src/seed/trace-store-reconcile.ts has carried `timeoutMs: 900_000` since commit 62e174a, but the registered row still has no timeout, so every reconcile aborted at 15 s and leaked the change_window lease — a landing in a seed file is inert on every running substrate.
Keep the reason the SEED-IF-EMPTY guard exists: re-seeding on every restart clobbered learned/evolved versions. So do NOT upsert when the registered row merely differs; upsert only when the seed SOURCE differs from what this seeder last uploaded. Changes, all in this one file:
(1) Keep the catalogue-empty check and the cold-start path exactly as they are.
(2) When the catalogue is populated, instead of `return`, load a seed ledger from `join(process.env["WORKSPACE_ROOT"] ?? "/workspace", "seed-ledger.json")` — a JSON object `{ [templateId]: sha256Hex }`. For each template in SEED_TEMPLATES compute sha256 of JSON.stringify(template) (node:crypto createHash). If the ledger file does not exist at all, write the ledger with the current hashes and upsert NOTHING (first run establishes the baseline without clobbering). Otherwise, for each template whose hash differs from its ledger entry (or has no entry), upload it through the same path the cold start uses (resolveActivityCreateVariant / the existing per-template upload in this function — reuse it, do not add a new HTTP client), log `[seed] upserted changed seed ${id} (source hash ${old?.slice(0,8)} -> ${new.slice(0,8)})`, and on success record the new hash. Write the ledger atomically (tmp + rename). Log a one-line summary `[seed] populated catalogue: ${changed} changed seed(s) upserted, ${same} unchanged skipped`.
(3) Never throw out of seedTemplates for a ledger read/write failure (warn and skip upserts).
Falsifier: with the ledger present, editing one seed file then restarting development-vessel-seed upserts exactly that template (activity-api GET by id shows the new task body) and skips the rest; a restart with no seed edit upserts nothing.
