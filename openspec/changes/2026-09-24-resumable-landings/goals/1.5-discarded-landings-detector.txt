In repos/development-vessel/src/services/gap-drain-observer.ts, add a discarded-landings detector so verified-then-discarded landings become a gap event instead of a journal grep (openspec 2026-09-24-resumable-landings task 1.5, spec parked-landing "Discarded landings are detected without an operator"). Measured 2026-09-24 04:14Z-09:23Z: 7 FAVORABLE composes refused at cutover for a held change_window lease, each rolled back and discarded, and nothing in the substrate counted them.
Changes, all in this one file:
(1) Add `private async scanDiscardedLandings(): Promise<void>`, rate-limited to once per 10 minutes via a globalThis timestamp (same pattern as `__drainLastScan`), called at the END of `handleExecutionCompleted()` wrapped in try/catch (never throws).
(2) It readdir()s `join(WORKSPACE_ROOT, "..", "proposals")` falling back to "/workspace/proposals" (use whichever exists; compose reports are written to /workspace/proposals/<gap>-compose-report.json), considers files ending `-compose-report.json` whose mtime is within the last 60 minutes, JSON.parse()s each, and counts a report as DISCARDED when `verdict === "FAVORABLE"` (or `effective_verdict`/`semantic_gate.addresses === true` with a non-empty `cutovers` array) and NO entry of `cutovers[].result` has `push_status === "pushed"` with a non-empty `new_git_sha`. Classify each by the JSON text of its cutovers: /env_change_window_held|change[_ ]window( lease)? held|lease held/i -> `lease_refused`; /deferred/i -> `deferred`; /drain|SIGTERM|draining/i -> `drain_killed`; else `other`.
(3) Build `{ shape: "discardedLandingReport", window_minutes: 60, generated_at, total, by_cause: { lease_refused, drain_killed, deferred, other }, gap_ids: [...] }`, write it atomically to `join(WORKSPACE_ROOT, "pool", "discarded-landings.json")` and append one line to `join(WORKSPACE_ROOT, "pool", "discarded-landings.jsonl")`.
(4) When total > 0, `await resolveSubstrateGapWrite({ type: "substrateGap_write", gap: { id: \`discarded-landings-${YYYY-MM-DD}\`, category: "systematic_failure", source: "substrate_detected", status: "open", summary: \`${total} verified landing(s) discarded after passing every gate in the last hour (lease_refused=…, drain_killed=…, deferred=…, other=…): ${gap_ids.slice(0,5).join(", ")}\`, detected_at: new Date().toISOString(), classification_metadata: { incident_kind: "discarded_landings", by_cause, gap_ids } } } as never)` imported dynamically from "../resolvers/substrate-gap.js" (same call shape as build-signature-detector.ts). Log `[gap-drain-observer] discarded landings: total=<n> <by_cause json>` on every scan.
Falsifier: one FAVORABLE compose refused at cutover in the last hour -> the next scan logs total=1 lease_refused=1 and gap discarded-landings-<today> is open; an hour of only pushed landings -> total=0 and no gap write.
