In repos/development-vessel/src/index.ts, make /health report the age of the oldest in-flight long-running request as `in_flight_oldest_ms` (openspec 2026-09-24-resumable-landings task 2.1, spec restart-budgets "Health reports the age of the oldest in-flight request"). A requester that owes a restart (substrate-pull-sync, already updated to read this field) defers by age instead of by a count of busy polls.
Changes, all in this one file:
(1) Beside `let readInFlight: () => number = () => 0;` and `export function publishInFlight(...)` add `let readInFlightOldestMs: () => number | null = () => null;` and `export function publishInFlightOldest(fn: () => number | null): void { readInFlightOldestMs = fn; }`.
(2) In the /health handler, directly after the line `in_flight: readInFlight(),` add `in_flight_oldest_ms: readInFlightOldestMs(),` (null when nothing is in flight). Leave the drain_ms line unchanged in this goal.
(3) Beside `let inFlightRequests = 0;` add `const inFlightStarts = new Set<{ at: number }>();` and after `publishInFlight(() => inFlightRequests);` add `publishInFlightOldest(() => { let oldest: number | null = null; for (const s of inFlightStarts) if (oldest === null || s.at < oldest) oldest = s.at; return oldest === null ? null : Date.now() - oldest; });`.
(4) In Bun.serve's fetch, declare `let startRec: { at: number } | null = null;` next to `let counted = false;`; where the handler does `counted = true; inFlightRequests++;` also do `startRec = { at: Date.now() }; inFlightStarts.add(startRec);`; in the existing `finally { if (counted) inFlightRequests--; }` also `if (startRec) inFlightStarts.delete(startRec);`.
Do not change the drain loop or the drain deadline. Falsifier: with one compose in flight for 180 s, GET /health returns in_flight:1 and in_flight_oldest_ms >= 180000; idle it returns in_flight:0 and in_flight_oldest_ms:null.
Use four small old->new string edits anchored on lines that exist TODAY (e.g. the line `let inFlightRequests = 0;`, the line `publishInFlight(() => inFlightRequests);`, `in_flight: readInFlight(),`, `inFlightRequests++;`, `if (counted) inFlightRequests--;`) — a previous draft was drift-refused because its replace_lines anchor quoted the NEW line as if it already existed.
A previous draft was rejected because it added the /health field and publishInFlightOldest but OMITTED step (4): the request handler never adds to or deletes from inFlightStarts, so the age would always be null. The diff MUST contain all of (1)-(4); step (4) is the three small edits inside Bun.serve's fetch: declare startRec beside counted, add it next to inFlightRequests++, delete it next to inFlightRequests--.
