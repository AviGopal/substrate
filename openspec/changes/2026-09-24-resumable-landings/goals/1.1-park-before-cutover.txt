In repos/development-vessel/src/resolvers/feature-compose.ts, park a verified patch before its cutover so a refused, deferred or interrupted cutover no longer discards it (openspec 2026-09-24-resumable-landings task 1.1, spec parked-landing "A verified patch is parked before its cutover"). Measured: 7 verified landings refused for a held change_window lease were each followed by `live-sync rollback` and thrown away after passing typecheck, two suite runs and the semantic gate.
Changes, all in this one file:
(1) Add and EXPORT three helpers near the top-level helpers (gap-to-feature.ts will import them in a later goal):
  - `export function parkedLandingPath(gapId: string): string` = join(process.env["PARKED_LANDINGS_DIR"] ?? "/workspace/parked-landings", `${gapId.replace(/[^A-Za-z0-9._-]/g, "_")}.json`).
  - `export async function writeParkedLanding(park: ParkedLanding): Promise<void>` — mkdir -p the dir, write `${path}.tmp` then rename (atomic, same pattern as the gap store). Never throws (catch and console.warn).
  - `export async function readParkedLanding(gapId: string): Promise<ParkedLanding | null>` and `export async function deleteParkedLanding(gapId: string): Promise<void>` (ENOENT is fine).
  - `export interface ParkedLanding { gap_id: string; compose_id: string; vessel: string; files: Array<{ path: string; content: string; base_content: string | null }>; verify: { typecheck: boolean; shape_dispatch: boolean; tests: boolean }; judge: { addresses: boolean | null; reason: string | null }; parked_at: string; reason: string }` — `path` is the vessel-relative path (the `rel` in changedRel), `content` is the verified file content from `${vBase}/${rel}`, `base_content` is the pre-edit live content captured in preLiveSync (null if absent).
(2) In the landing loop, immediately BEFORE `const cut = await resolveVesselMitosisCutover({`, when `pointer.gap?.id` is set, read each `${vBase}/${rel}` for rel in changedRel (fs_read via callTool like the surrounding code, or node:fs readFile) and `await writeParkedLanding({...})` with compose_id = `${vessel}-fc-${ts}`, verify all true (this point is only reached after tcOk && sdExit === 0 && testOk), judge from `semantic_gate` (addresses, reason), parked_at = new Date().toISOString(), reason = "pre-cutover".
(3) In the `UNDO THE LAND-TIME SYNC WHEN NOTHING LANDED` block and after it: when the cutovers `landed` (push_status === "pushed" with a new_git_sha) and pointer.gap?.id is set, `await deleteParkedLanding(pointer.gap.id)`. When not landed and a park was written, keep it and log exactly `[feature-compose] cutover did not land — verified patch parked for ${gapId}`. The live-tree undo still runs unchanged.
(4) Add `parked: boolean` to the returned featureComposeReport body — true when a park was written and no cutover pushed.
Do not change the drafter, verify or the semantic gate. Falsifier: hold change_window by hand for 3 minutes during a compose — /workspace/parked-landings/<gap>.json exists with the verified file content and the report says parked:true; a normal landing leaves no park file.
