In repos/development-vessel/src/seed/trace-store-reconcile.ts, give the reconcile's change_window lease its own name and release it before the post-swap check (openspec 2026-09-24-resumable-landings task 3.3a, spec change-window-names "Cutovers and the reconcile use their own names" and "The reconcile releases on failure and fits its work"). maintenanceLease_write accepts an optional `name`; a named hold does not exclude a different name, so a reconcile holding `trace_store` no longer refuses cutovers (which take `cutover`). services/gap-drain-observer.ts already reads the lease with name "trace_store".
Changes, all in this one file:
(1) In the `acquire_lease` task config add `name: "trace_store",` after `holder: "trace-store-reconcile",`, and update the comment "This lease is the global change_window every cutover must take." to say the lease is named trace_store so cutovers (name cutover) are not excluded by it.
(2) In the `release_lease` task config add `name: "trace_store",` after `op: "release",`.
(3) Move the whole `release_lease` task object so it comes immediately AFTER the `reconcile` task and BEFORE the `verify` task. `verify` is a dry_run read of the trace-store counters that does not need the lease, and it is the task that fails most often (forbiddenPatterns over_cap) — with release after it, every over-cap run left the lease held to its TTL.
Keep `timeoutMs: 900_000` on the reconcile task unchanged. Update the header comment block that says "if the reconcile or verify task fails mid-run, release_lease is simply never reached" to say a verify failure no longer holds the lease, and a reconcile failure leaves only the trace_store name held (which no cutover takes). Falsifier: the registered template (once the seeder upserts changed seeds) acquires with name trace_store, and a run whose verify fails leaves no maintenance-trace_store.json behind.
