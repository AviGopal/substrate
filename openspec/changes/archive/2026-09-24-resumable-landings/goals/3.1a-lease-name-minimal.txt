In repos/development-vessel/src/resolvers/maintenance-lease.ts, add an optional lease `name` (openspec 2026-09-24-resumable-landings task 3.1, first half). Small, mechanical change — about 40 lines — in this one file:
(1) Add `name?: string;` to MaintenanceLeaseReadPointer and to MaintenanceLeaseWritePointer.
(2) Add `export function namedLeasePath(name?: string): string { return name ? join(dirname(leasePath()), \`maintenance-${name.replace(/[^a-z0-9_-]/gi, "_")}.json\`) : leasePath(); }` right after leasePath().
(3) Give readLease, writeLease and deleteLease an optional second/first parameter `name?: string` and make them use `namedLeasePath(name)` instead of `leasePath()`.
(4) In resolveMaintenanceLeaseWrite: acquire reads `const existing = await readLease(pointer.name)` and, when pointer.name is set, ALSO `const global = await readLease()`; refuse (same body shape, plus `name: pointer.name ?? null`) if either is unexpired and held by a different holder; write with `writeLease({...}, pointer.name)` and add `name: pointer.name ?? null` to the success body. renew and release use `readLease(pointer.name)` / `writeLease(..., pointer.name)` / `deleteLease(pointer.name)`.
(5) In resolveMaintenanceLease: `const lease = await readLease(_pointer.name)`; when a name was given and that lease is absent or expired, fall back to `await readLease()` (an unnamed hold means everything, and services/gap-drain-observer.ts reads with names "trace_store" and "change_window" and must keep seeing the global hold). Add `name: _pointer.name ?? null` to the held body.
Callers that pass no name behave exactly as today; maintenance.json's format is unchanged.
Falsifier: acquire {holder:"a", name:"trace_store"} then {holder:"b", name:"cutover"} -> both acquired:true and two files exist; with an unnamed hold by "c", acquire {holder:"b", name:"cutover"} -> acquired:false held_by "c"; read {name:"trace_store"} during the unnamed hold -> held:true.
Typing note: a previous draft failed typecheck with TS2322 "Type string | null | undefined is not assignable to type string | null" — normalise every optional name with `pointer.name ?? null` / `name ?? undefined` at the boundary.
A previous draft was rejected because renew and release still operated only on the global lease: step (4) REQUIRES renew to use readLease(pointer.name)/writeLease(..., pointer.name) and release to use readLease(pointer.name)/deleteLease(pointer.name). All five numbered steps must be in the diff.
