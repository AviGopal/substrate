In repos/development-vessel/src/resolvers/vessel-mitosis-cutover.ts, make the early "change_window lease held — defer without rollback" check read the lease under the name cutovers take (openspec 2026-09-24-resumable-landings task 3.2b). maintenanceLease now has names: cutovers acquire name "cutover", the trace-store reconcile name "trace_store", and a read WITHOUT a name reports the union (held if ANY hold exists). This early check reads without a name, so since the union landed it refuses every cutover whenever the reconcile holds trace_store or another cutover holds its own lease — undoing the point of naming them (measured 14:53:30Z: a FAVORABLE, fully verified compose refused here and its patch discarded by the live-sync rollback). A read WITH name "cutover" reports held only when the cutover name or the unnamed global is held, which is exactly what this cutover's own acquire would be refused by. Exactly one edit (the old line occurs once in the file), nothing else:
EDIT 1 — old:
      ? await leaseMod["resolveMaintenanceLease"]({ type: "maintenance_lease", lease: "change_window" })
new:
      ? await leaseMod["resolveMaintenanceLease"]({ type: "maintenanceLease", name: "cutover", lease: "change_window" })
Falsifier: with maintenance-trace_store.json held (unexpired) by the reconcile, a FAVORABLE cutover no longer logs "change_window lease held — deferring cutover without rollback"; with an unnamed global hold it still does.
