## Phase 1: Config Loading

- [x] 1.1 Create `repos/react-renderer/src/config-loader.ts` exporting `loadRendererConfig(): Promise<RendererConfig>`. Read `~/.metabob/config.json` and `.metabob/config.json` (project root) using the same key paths as MiniBob's `config.ts` (`metabob.apiKey`, `instance.apiKey`, `discovery.endpoint`). Precedence for each field: `process.env` > project config > user config > hardcoded default. Default `discoveryEndpoint` is `"https://discovery.metabob.com"` (not the cluster-internal URL). Default `vesselEndpoint` is `http://localhost:${PORT}`. Export a `RendererConfig` interface. Verify: unit test creates a temp config file at a non-home path, passes it as override, asserts `discoveryEndpoint` and `metabobApiKey` are read correctly; env var `DISCOVERY_VESSEL_ENDPOINT` overrides the file value.

- [x] 1.2 Wire `loadRendererConfig()` into `initializeDiscovery()` in `repos/react-renderer/src/index.ts`. Replace the two direct `process.env.*` reads for `discoveryEndpoint` and `vesselEndpoint` with values from `RendererConfig`. The cluster-internal default URL must not appear anywhere in the resulting code. Verify: `bun run typecheck` passes; grep confirms `discovery-vessel.activity-system.svc.cluster.local` no longer appears in `src/index.ts`.

- [x] 1.3 Smoke-test the zero-export workflow. Unset `DISCOVERY_ENABLED`, `DISCOVERY_VESSEL_ENDPOINT`, `METABOB_API_KEY`, `VESSEL_ENDPOINT`. Ensure `~/.metabob/config.json` has `metabob.apiKey` set (standard developer setup). Run `bun run dev` in `repos/react-renderer`. After 5 seconds, `curl http://localhost:3000/health`. Verify: response field `checks.discovery.status` is `"healthy"` or `"pending"` (not `"disabled"`); server logs show `https://discovery.metabob.com` as the discovery endpoint, not the cluster-internal URL.

## Phase 2: Org-Scoped Registration

- [x] 2.1 Add `parseIdentityEndpoint(apiKey: string): string` to `src/config-loader.ts`. API keys are `mb-[base64url(payload)]-[sig]`; decode the middle segment and extract the `iss` field. Return it as the identity endpoint. Fallback to `"https://identity.metabob.com"` if parsing fails or `iss` is absent. Verify: unit test with a real-format key string asserts the result contains `"identity.metabob.com"`; unit test with a garbage string returns the fallback without throwing.

- [x] 2.2 Add `resolveOrgId(apiKey: string, identityEndpoint: string): Promise<string | undefined>` to `src/config-loader.ts`. Call `POST <identityEndpoint>/v1/keys/validate` with body `{ api_key: apiKey }`, 5-second timeout. Return `data.org_id` from a successful response. On any failure (network error, non-200, invalid key) log a warning and return `undefined`. Verify: integration test with a real API key from `~/.metabob/config.json` returns a non-empty string; test with `"mb-garbage"` key returns `undefined` and does not throw.

- [x] 2.3 Pass `orgId` to `VesselClient` in `initializeDiscovery()`. After `loadRendererConfig()`, call `resolveOrgId(config.metabobApiKey, config.identityEndpoint)`. Pass the result as `orgId` in the object passed to `new VesselClient(config)`. Confirm `packages/vessel-discovery-client/src/` already accepts `orgId` in its config type; if the field is missing, add it. Verify: `bun run typecheck` passes; server startup log shows `[Discovery] Registering with orgId: <your-org-id>`.

- [x] 2.4 Verify the registration record in the discovery backend includes `orgId`. With the server running (Tasks 1.x + 2.x complete), query the discovery backend: `curl -H "Authorization: ApiKey $METABOB_API_KEY" https://discovery.metabob.com/vessels`. Filter to `react-renderer`. Verify: the returned record includes `"orgId"` matching the `org_id` returned by `resolveOrgId()`. If the discovery-vessel `/vessels` endpoint does not exist, use `/registry/stats` or the equivalent endpoint that lists registered vessels. (manual verification — requires running server + API key)

## Phase 3: MiniBob Discovery with Org Isolation

- [ ] 3.1 Verify MiniBob discovers local react-renderer by shape. Pre-conditions: react-renderer running locally with Tasks 1.x and 2.x complete; MiniBob configured with the same `METABOB_API_KEY` from `~/.metabob/config.json`. Run `minibob --single "list all vessels that can render a ui_component"`. Verify: MiniBob logs show at least one vessel returned for shape `ui_component` with endpoint `http://localhost:3000`. The vessel ID matches the react-renderer registration.

- [ ] 3.2 Verify org isolation: a vessel registered without `orgId` or under a different org does not appear in the same query. Using `curl`, register a dummy vessel under `orgId: "other-org"` directly against the discovery backend. Re-run the MiniBob discovery query from Task 3.1. Verify: the dummy vessel does not appear in the results for the developer's own org. Stop react-renderer; re-run the query after TTL expiry (or force a cache clear). Verify: react-renderer no longer appears.

## Phase 4: Activity Drives UI Update

- [ ] 4.1 Create `repos/react-renderer/templates/render-file-tree.json`. Three sequential tasks: (1) `resolver: "bash"` runs `find {{directory}} -maxdepth 2 -type f -printf '%p\t%s\t%TY-%Tm-%Td\n' 2>/dev/null | head -100`, outputs `bash_output`; (2) `resolver: "llm"` parses bash output into `{ columns: ["path","size","modified"], data: [...] }` outputting a `ui_component` shape; (3) `resolver: "http"` with `discoveryShape: "ui_component"` POSTs to `/impulses` on the discovered vessel. Template variable: `directory` (string, required). See design.md §3 for full structure. Verify: template is valid JSON; `bun run typecheck` still passes; `GET http://localhost:3000/manifest` does not crash (vessel.json is still valid after the next task).

- [ ] 4.2 Register `render-file-tree` in `repos/react-renderer/vessel.json` under the `activities` array: `{ "id": "render-file-tree", "category": "ui", "description": "Run find on a directory and render the file list as a data-table", "templatePath": "templates/render-file-tree.json" }`. Verify: `GET http://localhost:3000/manifest` returns an `activities` array containing `render-file-tree`; `bun run typecheck` passes.

- [ ] 4.3 Manual end-to-end test: MiniBob → react-renderer → browser. Both servers running. Open `http://localhost:3000/view` in a browser (or use `wscat -c ws://localhost:3000/ws` to watch raw messages). Run `minibob --single "render the file tree for /home/avi/documents/work/exp-repo"`. Verify: within 10 seconds a `<table>` element appears in the `/view` page with at least one row containing a file path. No 5xx errors appear in the react-renderer logs.

## Phase 5: Playwright E2E Test

- [ ] 5.1 Create `repos/react-renderer/test/e2e/local-discovery.test.ts`. The test: (a) skips gracefully if `~/.metabob/config.json` is missing or lacks an API key; (b) asserts react-renderer is running on port 3000 (health check); (c) navigates to `http://localhost:3000/view` via Playwright; (d) asserts the `.empty` element is visible; (e) spawns `minibob --single "render the file tree for /tmp"` as a subprocess; (f) calls `page.waitForSelector('table', { timeout: 5000 })`; (g) asserts `page.locator('th').first()` contains text `"path"`; (h) asserts `page.locator('tbody tr').count() > 0`. Verify: `bun test test/e2e/local-discovery.test.ts` passes with both servers running and valid config; test exits with skip (not fail) when config is absent.

- [ ] 5.2 Add Playwright config to `repos/react-renderer/playwright.config.ts` (create if absent) with two projects: `unit` (default, excludes `*.e2e.test.ts`) and `local-only` (matches `*.e2e.test.ts`, requires `@local-only` tag). Verify: `bun test` (default run) does not execute `local-discovery.test.ts`; `bun test --grep @local-only` does execute it.

- [ ] 5.3 Add a "Local MiniBob Integration" section to `repos/react-renderer/docs/local-dev.md` (create file). Content: prerequisites (config file, MiniBob in PATH), three commands (start react-renderer, dispatch activity, open `/view`), and the automated test invocation. Do not modify `CLAUDE.md`. Verify: file exists; all commands in the doc are reproducible by following them verbatim.
