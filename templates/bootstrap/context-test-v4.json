{
  "name": "Context Test V4",
  "description": "Test after removing .default([])",
  "category": "infrastructure",
  "contextRequirements": [
    {
      "key": "finalTest",
      "hint": "Final test message",
      "impulseTypes": ["memo"],
      "required": true,
      "budgetRange": [100, 500]
    }
  ],
  "tasks": [
    {
      "id": "write-final",
      "subagent": "general",
      "description": "Final test",
      "dependencies": [],
      "prompt": {
        "template": "Write {{finalTest}} to /tmp/final-{{id}}.txt",
        "maxTokens": 2000,
        "compressionStrategy": "filter",
        "variables": [
          {
            "name": "id",
            "type": "string",
            "required": true,
            "description": "ID"
          }
        ]
      },
      "validation": {
        "requiredFiles": [],
        "requiredPatterns": [],
        "forbiddenPatterns": [],
        "commands": []
      },
      "retry": {
        "maxAttempts": 2,
        "strategy": "simple"
      }
    }
  ]
}
