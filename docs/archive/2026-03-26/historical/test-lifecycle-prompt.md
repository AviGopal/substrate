# Test Activity Lifecycle Tracing

Use the activity tool to execute the create-activity-self-contained template with these variables:

- activityName: "test-simple-feature"
- activityDescription: "A simple test activity"
- category: "feature"
- numTasks: "2"
- reason: "Testing lifecycle tracing to understand task 3 failure"

After execution, check if the file `activity-lifecycle-trace.log` was created and show its contents.
