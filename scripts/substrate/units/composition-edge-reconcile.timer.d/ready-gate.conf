# Boot ordering (2026-07-02): arm this timer only once the fleet readiness gate
# has run, so fleet-dependent ticks don't fire into a half-booted substrate.
# substrate-ready.service exits 0 (with a warning) on timeout — a sick vessel
# delays but never wedges timer arming.
[Unit]
After=substrate-ready.service
Wants=substrate-ready.service
