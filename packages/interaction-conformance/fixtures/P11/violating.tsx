// P11 VIOLATION: a verdict state written as a hand-authored colour. `reached` and
// `not-reached` are defined at equal visual weight in the token package; a hex
// here breaks that pairing silently.
export const badge = {
  reachedColor: '#1f9d55',
  notReachedColor: '#d64545',
  shadow: 'rgba(0, 0, 0, 0.4)',
};
