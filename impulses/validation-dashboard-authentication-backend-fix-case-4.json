{
  "testCase": "JWT Token Validation",
  "testNumber": 4,
  "description": "Test token validation by calling /auth/session with Bearer token from login. Verify token can be decoded and contains correct claims.",
  "input": {
    "endpoint": "GET /auth/session",
    "url": "http://app.metabob.local/auth/session",
    "headers": {
      "Authorization": "Bearer {TOKEN_FROM_LOGIN}"
    },
    "prerequisite": "Valid JWT token from Test Case 3 (login)"
  },
  "expectedOutput": {
    "httpStatus": 200,
    "responseBody": {
      "user_id": "string (UUID format, matches login user_id)",
      "email": "string (matches login email)",
      "org_id": "string (UUID format)",
      "role": "string (e.g., 'owner')"
    }
  },
  "passCriteria": [
    "HTTP status is 200 OK",
    "Response contains 'user_id' claim",
    "Response contains 'email' claim matching login email",
    "Response contains 'org_id' claim",
    "Response contains 'role' claim",
    "Token is successfully decoded with JWT_SECRET_KEY"
  ],
  "failureIndicators": [
    "HTTP status is 401 Unauthorized (token validation failed)",
    "Response missing expected claims",
    "Email mismatch",
    "JWT decode error (signature verification failed)",
    "ExpiredSignatureError or PyJWTError"
  ]
}
