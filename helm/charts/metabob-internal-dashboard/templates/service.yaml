apiVersion: v1
kind: Service
metadata:
  name: {{ include "metabob-internal-dashboard.fullname" . }}
  labels:
    {{- include "metabob-internal-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metabob-internal-dashboard.selectorLabels" . | nindent 4 }}
