apiVersion: v1
kind: Service
metadata:
  name: {{ include "metabob-cloud-dashboard.fullname" . }}
  labels:
    {{- include "metabob-cloud-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "metabob-cloud-dashboard.selectorLabels" . | nindent 4 }}
