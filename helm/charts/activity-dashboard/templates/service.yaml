apiVersion: v1
kind: Service
metadata:
  name: {{ include "activity-dashboard.fullname" . }}
  labels:
    {{- include "activity-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "activity-dashboard.selectorLabels" . | nindent 4 }}
